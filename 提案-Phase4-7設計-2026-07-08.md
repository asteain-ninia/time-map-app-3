# 提案 — Phase 4-7（結合: 集約のみ平坦化 + 混在自動帰属 + 派生形状）設計判断メモ（2026-07-08）

> **位置づけ**: 未確定の設計提案。現状.md §6.10 Phase 4-7（現状.md:514-516）の着手前に、論点ごとの選択肢と推奨をFable（subagent, READ-ONLY調査）がまとめたもの。仕様・実装ではない。本ファイル新規作成以外、リポジトリへの変更なし。
> **系列**: `提案-B34仕様確定資料-2026-07-07.md` / `提案-Phase4-6残作業の設計判断-2026-07-08.md`（以下「4-6メモ」）の続き。4-6メモの結論A（遷移解決は既存 `resolveLeafToContainerTransition` 共有で閉じる）を前提に流用する。
> **表記**: file:line は 2026-07-08 時点のコード実測。要件 = 要件定義書.md、ガイド = 開発ガイド.md。推測・未実証は **unknown** と明記。

---

## 0. 結論先出し

1. **確定経路は新メソッド1本**（仮称 `mergeContainerFeatures`）で平坦化+混在を統合する。両者の結果は「新規集約コンテナ」で共通、差は「末端対象が生き残るか」だけ。`mergeLeafFeatures`（ReassignFeatureParentUseCase.ts:434）は温存。
2. **確定経路に幾何演算はゼロ**。結果形状は保存せず `deriveParentShape` 派生（HierarchyService.ts:244-267）。4-6の論理和（`mergePolygons`）すら不要。4-7の本体は純粋な位相手術＋時刻同期であり、既存 staged エンジンの組み合わせで書ける。
3. **§2.2.3 配線は「結果状態スキャン（検出ゲート）」として初出させ、解決ダイアログの完全配線は defer を推奨**。理由: 平坦化・混在ともリーフの形状・寿命を一切変えないため、有効世界では空間競合が構成的に空になる見込み（§5 で証明スケッチ。ただし**着手時の幾何検証が必須 — unknown**）。過去3回（4-2b/4-4b/4-6a・b）とも計画上の§2.2.3配線が着手時検証で不要化された前例に整合（現状.md:434/436/438）。
4. **新規指摘**: 異なる上位領域に属する対象の結合（暫定制約解除後）は subtree の占有和を変えるため、**移行期間ノード祖先の下で親≡子の和が崩れる = B34 の第4系統**になる。B34（推奨④ロード時正規化）を 4-6残ダイアログ/4-7 の暫定制約解除より**先に**確定するのが安全（§9）。

---

## 1. 前提: 仕様と既存資産の対応表

| 仕様（要件 §2.1） | 実装の受け皿（既存） |
|---|---|
| 平坦化: 直接子を新集約の直接子へ（line 330-332） | childIds 収集 + `updatePlacementFromTime`（ReassignFeatureParentUseCase.ts:548-555 で使用実績）による親付替え |
| 平坦化: 中間階層を保持しない（line 332-333） | 構成そのもの（旧集約は終了、子は結果直下へ） |
| 結果形状 = 新下位領域の和（line 334） | `deriveParentShape` / `deriveParentPolygons`（HierarchyService.ts:244-288、再帰 + `polygonUnionAll`:340 + visited cycle guard :305-309）。**保存しない** |
| 旧集約の存在終了 + 結果の新規開始（line 335, 339） | `terminateFeatureExistenceAt`（:1533）+ `buildContainerFeature`（AddFeatureUseCase.ts:213-233） |
| 子は存在継続・親のみ更新（line 335, 339） | 所属変更 staged エンジン（`updateParentChildIdsFromTime`:925 / `removeEmptyParentRangesFromTime`:1106 / `cascadeParentCleanup`:1042 / `validateStagedHierarchy`:985） |
| 混在: X は名称・種別・属性を維持し存在継続（line 337-338） | X には一切触らず placement.parentId のみ from-time 更新 |
| X と A1/A2 の重なりは §2.2.3 で解決（line 340） | §5-§6 で設計（初出配線） |
| 共通フロー: 上位領域決定/プロパティ決定ダイアログ（line 341-348） | Phase 4-6 残作業（現状.md:508-509）。4-6メモ A/C の型（`DirectlyGovernedOverrideSpec` :1180 準拠）を共有 |
| 旧親領域の自動消滅（line 349） | `removeEmptyParentRangesFromTime` + `cascadeParentCleanup`（4-6a 実証済み: :587-605） |
| 完了時に §2.2.3 を経る（line 350） | §6 の配線設計 |
| 制約: 2つ以上/存在/上位下位同時選択禁止（line 351-354） | `validateMergeFeatures`（MergeService.ts:127-169）。祖先検査 :76-90 は Phase 4-5 で先行確定済み、**4-7 の hasChildren 緩和で初めて実効化**（現状.md:494） |

**撤去する暫定制約**: (a) `validateMerge` の hasChildren 拒否（MergeService.ts:92-99「集約地物の結合は Phase 4-7」）→ 4-7a で解除。(b) 同一上位領域制約（:101-109）→ 上位領域決定ダイアログ導入（4-6残）と同時解除（現状.md:508）。

---

## 2. 論点A: 確定経路の形 — 新メソッド1本で平坦化+混在を統合（推奨）

| 案 | 内容 | 評価 |
|---|---|---|
| ① `mergeLeafFeatures` を3パターン対応へ拡張 | 1メソッド内で結果種別分岐 | リーフ結果（形状論理和+終端クランプ:464-489）とコンテナ結果（幾何なし+剪定駆動寿命）は変異列が別物。1関数に混ぜると 4-6b で精緻化した終端制御の回帰面が広がる |
| **② 新メソッド1本（コンテナ結果系）** | `mergeContainerFeatures` が平坦化+混在を担当。`mergeLeafFeatures` 温存 | **推奨**。平坦化と混在の差は「末端対象が生き残るか」だけで、変異列は同一（§3）。staged エンジン共有（§6.6.9 / ガイド:653）は import 単位で維持 |
| ③ パターン毎に2本 | 平坦化用と混在用を分離 | 変異列がほぼ同一のためコピペドリフトの温床。§6.6.9 の過少共有 |

**パターン分類は `isLeafPolygonAnchor` に一元化する**（ガイド §6.6.8:646-651「リーフ判定をひとつに集約」）: effectiveTime の有効錨で `isLeafPolygonAnchor` → 末端対象（生存）、childIds 非空 → コンテナ系対象（終了・子引継ぎ）。この規則だと**移行期間ノード（shape+childIds、外部データ由来）が結合対象になった場合も自動的にコンテナ系へ分類**され、保持 shape は存在終了とともに歴史へ沈む（要件は移行期間ノード対象の結合を明文化していない — **unknown**。B34④採用ならアプリ内に存在せず論点消滅）。全対象が末端なら既存 `mergeLeafFeatures`、1つでもコンテナ系がいれば `mergeContainerFeatures`。振り分けは `validateMergeFeatures` の返り値に分類を載せて選択時 UI（App.svelte:1318 `addMergeTarget`）と確定時（MergeFeatureCommand.execute:73-77 → UseCase 入口:441）で共有する（§6.6.1、Phase 4-5 の共有ラッパ精神。現状.md:494）。

**Command は `MergeFeatureCommand` をそのまま流用**: 全マップ before/after スナップショットの薄いシェル（MergeFeatureCommand.ts:36-112、captureSnapshot :89-95）は変異内容に依存しない。UseCase 呼び分けだけ追加。undo/redo のイベント対称発火も restoreState:97-111 が既に §6.4.16（ガイド:560）準拠。`mergedFeatureId`（:58-60）は結果コンテナ ID を返し UI 選択再構築に使う（コンテナも sceneEntries 経由で選択可能）。

---

## 3. 論点B: 平坦化（+混在）の変異列 — 1アンドゥ単位の具体設計

`mergeLeafFeatures` の staged 構造（:503-631）を鋳型に、以下の順序（すべて staged 上、検証 throw で無変異 = atomic）:

1. **検証**: `validateMergeFeatures`（緩和後）+ 同一上位領域制約（ダイアログ導入まで）。
2. **新子集合の確定**: `newChildSet = ∪(コンテナ系対象の effectiveTime 直接 childIds) ∪ {生存末端対象}`。対象間で子は重複しない（ツリー制約: 同一時点で親は高々1つ）+ 上位下位同時選択は :76-90 が拒否済み。**≥2 が構成的に保証**される（対象2以上、コンテナ系は childIds≥1）。
3. **結果コンテナ生成**: `buildContainerFeature(effectiveTime, newChildSet, property, resolvedParentId)`（AddFeatureUseCase.ts:213-233）。childIds 空は生成時拒否（:219-221）= 不変条件の生成ステップ（現状.md:520）。錨は open-end（:225-231）で生成し、**寿命は後段の childIds 時刻同期+剪定が決める**（明示的終端クランプ不要。4-6b の `resolveContiguousLeafShapeEnd` はリーフ結果専用であり流用しない — コンテナに「同一形状連続区間」概念はない）。
4. **コンテナ系対象の存在終了**: `terminateFeatureExistenceAt`（:1533。4-6a と同じ「effectiveTime ちょうど開始なら地物ごと消滅」セマンティクス :531-537）。**破棄後続錨だけが親だった子の孤児化**は mergeLeafFeatures (1)(2)（:517-557）を一般化して踏襲: `orphanedChildIds = (終了対象の start≥effectiveTime 錨の childIds) − newChildSet − targetIds` を parentId=null（最上位昇格）へ。※末端結合と違い**有効錨自身も childIds を持つ**ため newChildSet の除外が必須（:517-519 のコメント前提「有効錨は childIds 空」が崩れる点に注意）。
5. **新子の親付替え**: newChildSet 各子の placement を from-time 更新（parentId → 結果コンテナ）。混在の X はここで吸収される（X 自体の錨・形状・名称は不変、要件 line 338）。X の旧親（無関係の P_x）はこの除去で childIds が空けば自動消滅（line 349）。
6. **結果コンテナの childIds 時刻同期 → 剪定 → 親への追加**: Phase 4-3 自治化で確立した順序「新コンテナへの子追加 → `pruneEmptyContainerAnchors`（:1374-1382）→ 剪定後の姿で所属先へ追加」（:814-816 の doc、誤 prune 防止と実寿命同期の理由込み）を踏襲。子が順に死ぬと錨分割で childIds が減り、空区間剪定でコンテナ寿命が自然に決まる = 不変条件の変異ステップ（現状.md:520、ガイド §6.6.8:651「実行時は剪定で自動消滅」）。
7. **旧親たちの childIds 同期**: 終了対象・移動子の旧親を `updateParentChildIdsFromTime` + `removeEmptyParentRangesFromTime` + `cascadeParentCleanup`（:559-605 と同型）。旧上位領域コンテナの自動消滅はここで出る。
8. **`validateStagedHierarchy`（:985）→ §2.2.3 結果状態スキャン（§6）→ commit + イベント発火**（:620-628 と同じループ。§6.4.16）。

**結果コンテナの親カバレッジ**: ダイアログで選んだ親 P の連続カバレッジが newChildSet の残存寿命を覆わない場合、reassign 経路の前例は**拒否**（`assertParentCoversTargetRanges` :679-684 / 自治化の `assertContainerParentValid`、現状.md Phase 4-3 自治化部分）。4-7 も**確定前拒否を推奨**（ダイアログ側で候補検証）。「P 終端以降は子を最上位昇格」案は将来史の黙変を生むため非推奨 — ただし拒否壁の UX が問題化したら再検討（**unknown**）。

**X の将来所属履歴**: X に事前記録済みの将来所属変更（T2 で別親 B へ）がある場合、from-time 親付替えがそれを上書きするか温存するかは既存 reassign 経路のセマンティクスに**完全追従**させる（別規則を作らない。§6.6.9）。既存挙動の確認は着手時（**unknown** — `updatePlacementFromTime` のマッパー条件次第）。4-6残の将来史破棄警告（4-6メモ B、現状.md:510）を混在結合の X にも適用するかは同じ判断で従属的に決まる。

**単一アンドゥ**: 上記 1-8 すべてが 1 回の UseCase 呼び出し内 staged 変異 → `MergeFeatureCommand` 全マップスナップショットが生成/終了/剪定/連鎖消滅を自動同梱（現状.md:521、要件 §2.2.3 line 452「単一のアンドゥ・リドゥ操作」）。redo は afterState 復元で生成 ID 固定（§6.4.12、MergeFeatureCommand.ts:62-67）。

---

## 4. 論点C: 派生形状 — shape プロパティは保存しない（確定経路に幾何演算ゼロ）

- 要件 line 334「結合後の集約地物の形状は新下位領域の和として算出される」は、**保存ではなく構成で満たす**: `buildContainerFeature` は shape=undefined で錨を作り（AddFeatureUseCase.ts:229）、描画・hitTest・検証は既存の派生経路（`deriveParentShape` HierarchyService.ts:244-267 / `deriveParentPolygons` :281-288 / mapSceneEntries の派生優先解決 — B34資料 事実B）が毎スライス算出する。**4-7 で新しい形状導出コードは書かない**（§6.6.9 ガイド:653「同じ shape 解決経路を共有」）。
- 「shape を結合時に一度計算してキャッシュ」する最適化は**禁止**（B34資料 事実C: 定常状態は shape XOR children。移行期間ノードを自ら量産することになる）。
- 再帰・循環は既存の visited ガード（:305-309、§6.4.14）と `polygonUnionAll`（:340、MultiPolygon 保持 §6.6.5）がそのまま効く。多段コンテナ子（平坦化対象の子に集約地物が含まれるケース = Phase 4-4 で許容済みの多層）も `resolveChildPolygonsForUnion`（:352-375）の再帰下降で解決済み。
- 帰結: 4-6a が必要とした `mergePolygons`（MergeService.ts:179-200）と新規頂点生成は 4-7 確定経路に**存在しない**。孤立頂点問題（現状.md:489）も 4-7 では発生しない（終了するコンテナ系対象は shape を持たず、生存末端 X は無変更）。

---

## 5. 論点D: 混在結合の空間競合 — 「構成的に空」の証明スケッチと推奨方針

**証明スケッチ（着手時に幾何検証必須 — 現時点では unknown 扱いの主張）**:

1. 平坦化・混在の変異列（§3）は、**いかなる末端地物の shape も寿命も変えない**。変わるのは (a) コンテナの生死（排他検証対象外）、(b) 子の placement.parentId（位相のみ）、(c) 孤児の最上位昇格（位相のみ）。
2. 末端排他（地図全体・全スライス、要件 §2.1 line 145-153）はロード時 `validateLeafExclusivity`（jsonSerializerValidation.ts:517-550）+ 各操作の構成的維持で成立済み。末端の shape/寿命が不変なら、変異後の任意スライスの末端集合の占有は変異前と同一 → **重なりは新規に生じ得ない**。
3. これは 4-2b「位相変更のみの所属変更は空間競合を生まない」（現状.md:454）と同型で、しかも 4-6a/4-6b（論理和で新形状が生まれるためクランプで守った :410-425）より**強い**（新形状自体がない）。
4. 上位領域決定ダイアログで既存末端 P を結果の親に選ぶ分岐で生まれる直轄領も、直轄領 ⊆ P 旧形状 ⊆ 排他済み領域のため競合なし（4-4b-1 の確定済み証明、現状.md:434 / :1164-1167）。

**要件 line 340 との整合**: 「X が A1/A2 と重なる場合は §2.2.3 で解決」は、要件 line 162 の世界観（違反は操作で生じ得て §2.2.3 が確定時に掃除する）における一般則の言明であり、**重なりが必ず生じるという主張ではない**。line 350 の「完了時に §2.2.3 を経る」は、検出が空でも「経た」ことになる — 4-2b/4-4b/4-6a・b で確立した読み（現状.md:434-438 の3連続の計画訂正）。

**選択肢と推奨**:

| 案 | 内容 | 評価 |
|---|---|---|
| ① 完全配線 | 検出 → ConflictResolutionDialog → resolve → 一括確定を 4-7 で実装（現状.md:522 の現計画） | 検出が構成的に空なら**到達不能なダイアログ配線**を作ることになり `feedback_no_speculative_helpers`（4-2b で明文適用、現状.md:454）に抵触 |
| **② 検出ゲート先行** | 結果状態スキャン（§6）を配線し、非空なら throw で結合拒否（`validateStagedHierarchy` と同格の防御）。ダイアログ完全配線は「検出が実際に非空になり得る最初の経路」まで defer | **推奨**。§6.1.8（ガイド:296-302）の結果状態検証として意味を持ち、空である事実自体を回帰テストで固定（reassignmentSpatialConflictInvariant.test.ts の前例）。配線点・共有ヘルパ・スライス歩行は完全配線と同一なので、後日ダイアログを挿す時は throw をダイアログ起動に差し替えるだけ |
| ③ 配線なし（証明のみ） | 4-2b と同じくテスト+doc で完了 | 検出コードすら置かないと、将来 §3 の変異列に形状を触る変更が入った時に黙って崩れる。②の常時スキャンが安い保険 |

**②の留意**: 現計画（現状.md:516, 522「空間競合検出を 1 アンドゥ単位に同梱」「起動経路の初出は Phase 4-7」）は①を予定している。②はその縮小であり、**4-2b/4-4b/4-6 と同じ「着手時検証 → 計画訂正」の第4回目をユーザー判断で確定する必要がある**。着手時の第一歩は §5 証明スケッチの幾何検証（反例探し: 特に「X の将来錨で shape が変わるケース」「孤児昇格後のスライス」を fixture で検証）。反例が出たら①へ戻す。

---

## 6. §2.2.3 起動配線の初出設計（置き場所と共有経路）

**置き場所**: `mergeContainerFeatures` の staged 変異末尾、`validateStagedHierarchy`（:607 相当）の直後・commit 前。**入力経路（結合パターン種別）で分岐せず**、「本操作で shape または寿命が変わった末端地物」の集合を changedFeatureIds から結果状態で導出してスキャンする（§6.1.8。4-7 ではこの集合が空になるはず — 空でないこと自体が §3 の変異列違反のシグナル）。`mergeLeafFeatures` にも同じスキャンを併設するか（結果リーフが対象になる。4-6a/b は終端制御で構成的に安全 :420-425 だが、スキャン共有は証明の実行時裏付けになる）は着手時判断 — 追加コストはほぼゼロ。

**共有ヘルパ（ドリフト防止の要）**: 新しい幾何コードは書かない。

- 検出本体: `detectConflictsForFeature` / `detectSpatialConflicts`（ConflictDetectionService.ts:103-147 / :54-89）。`Feature[]` を取るため `[...staged.values()]` がそのまま渡せる（未 commit の staged 世界を検証できる）。
- 占有領域解決・重なり判定: `resolveOccupiedTerritories`（:194-230）/ `territorySetsOverlap`（:242-254）— **既に Phase 3 ロード時検証と共有済み**（jsonSerializerValidation.ts:517 の doc「ConflictDetectionService のランタイム排他検証と同じ実装を共有する（§6.6.1）」、worldValidation.ts:19 の import）。4-7 の配線はこの既存共有に乗るだけで、ロード時 `validateLeafExclusivity` との判定ドリフトは構造的に起きない（現状.md:522 の要求をそのまま満たす）。
- リーフ判定: `isLeafPolygonAnchor`（ConflictDetectionService.ts:20 / :117 / :131 で使用済み。移行期間ノード・コンテナは自動的に対象外 = §6.6.8 の射程遵守）。
- スライス選定: 単一時刻でなく**変更地物の錨境界時刻で [effectiveTime, ∞) を歩く**（§6.3.14 ガイド:486「錨境界≠属性変化境界」だが検出の評価点としては境界時刻の網羅で十分 — `validateLeafExclusivity` の `collectSliceTimes` と同じ方式の scoped 版。changedFeatureIds の錨境界に限定して O(n²) 全走査を避け、`detectConflictsForFeature` を変更末端ごとに呼ぶ）。

**§6.6.8 の罠（ガイド:646）**: 結果コンテナ A' や集約対象を検出対象へ「対称性のために」広げない。集約地物ペアの直接非重複検証は要件が明示的に禁止（ConflictDetectionService.ts:10-13 に doc 済み）。

**将来ダイアログを挿す時の形（②→①昇格時の設計メモ）**: 検出非空で `ConflictsPendingError`（検出結果を payload に throw、staged 破棄 = atomic 維持）→ App.svelte が既存 `openConflictDialog`（:1439-1445）+ `ConflictResolutionDialog`（:2321-2333、UI は実装済み）で方針収集 → **解決方針を params に含めて UseCase を再実行**し、差分適用まで含む 1 回の staged 変異 = 単一アンドゥ（要件 §2.2.3 line 439-452 の固定順序に合致）。`onConflictCommit` のスタブ（:1471-1475）はこの時に実装する。既存 `ResolveFeatureAnchorConflictsUseCase` は AnchorEditDraft（単一地物ドラフト :50-71）に結合しており多地物結合には**そのまま流用不可** — 差分適用ロジック（:98-141 の polygonDifference + withShape/期間打切り）だけを参考にし、draft 非依存の適用ヘルパへ切り出すのが筋（ドラフト機構への相乗りは経路二重化で §6.6.9 違反の温床）。

---

## 7. 論点E: 遷移解決枠組み（4-6メモA）の流用

- 4-7 で末端→集約遷移が起き得る地点は**ただ一つ**: 上位領域決定ダイアログで既存末端 P を結果（リーフ結果/コンテナ結果とも）の親に選んだ場合。§3 の変異列内では、childIds を獲得するのは結果コンテナ（新規生成、定義上末端でない）と P のみ。生存末端 X は子側、移動子は子のまま、旧親は子を失う側。
- よって 4-6メモ A の結論をそのまま適用: **確定経路の変異後に `resolveLeafToContainerTransition(staged, …, transitionFeatureId=P, override=プロパティダイアログ入力)` を名指しで呼ぶ**（:1174-1292。override 口 :1180/:1243-1254 は実装済み）。P はダイアログで特定済みのため全体スキャン不要（§6.1.8 の「経路ゲート」には当たらない — 4-6メモ A の論証）。この分岐は 4-6残と 4-7 の**共通確定部に1箇所だけ**実装し、両者で共有する。
- コンテナ結果でも機能することの根拠: P の差分計算は `deriveParentPolygons`（:1223-1228）経由で、コンテナ子 A' は `resolveChildPolygonsForUnion`（HierarchyService.ts:352-368）の再帰下降で解決される。修正不要。
- **着手時の義務**（現状.md:523）: `mergeLeafFeatures` の証明コメント（:427-432）は「上位領域決定ダイアログ導入で前提が崩れる」と自ら予告している。4-7/ダイアログ実装時に「崩れた → P 名指しの遷移解決を共有で解決済み」へ**必ず書き換える**。放置すると証明コメントが嘘になる。

---

## 8. 論点F: 「shape なし ⟹ childIds 非空」不変条件の両ステップ保証

- **生成ステップ**: `buildContainerFeature` の空 childIds 拒否（AddFeatureUseCase.ts:219-221）+ newChildSet ≥ 2 の構成保証（§3 手順2）。Phase 4-1 precedent（現状.md:520）どおり。
- **変異ステップ**: 全ての childIds 変異が既存エンジン経由（`updateParentChildIdsFromTime` の錨分割 → `pruneEmptyContainerAnchors` :1374-1382 / `removeEmptyParentRangesFromTime` :1106 / `cascadeParentCleanup` :1042）で、空化区間は剪定・全消滅は連鎖削除。最終ゲートは `validateStagedHierarchy`（:985、`isEmptyContainerAnchor` 消費 — 現状.md:111 の3経路統一）。
- **新設経路で素の `placement` 書き換えを書かない**こと（§4 手順5 の親付替えも `updatePlacementFromTime` / 既存 reassign 部品を通す）。EditFeature 系3経路の同型リスク（現状.md:529、4-6メモ D）と同じ轍。

---

## 9. B34 との依存関係（新規指摘 — 4-7 着手前にユーザー確認推奨）

- 4-6a のリーフ結合は「同一上位領域内で占有和を保存」するため祖先の親≡子の和に影響しなかった。しかし**暫定制約解除後の異親間結合**（4-6残ダイアログ以降のリーフ結合、および 4-7 の混在・平坦化で対象が異なる上位領域から来るケース）は、**移動元 subtree の占有和を減らし移動先 subtree の和を増やす**。
- 純コンテナ祖先なら派生再計算で自動追従する（影響なし）。だが**移行期間ノード祖先（保持 shape あり）の下では保持 shape ≡ 子の和 が崩れ、保存→再ロードで `validateParentChildUnion`（worldValidation.ts:52）が拒否**する。これは B34（現状.md:32）の削除・所属変更・shape編集 3系統と同型の**第4系統（結合系）**である。
- 対応: B34資料の推奨④（ロード時正規化）を採れば移行期間ノード自体が消え、本系統は発生しない。B34 が未確定のまま 4-6残/4-7 の暫定制約を解除するなら、B34 の系統リストへ「(4) 異親間結合経路」を追記し一律適用の対象に含める（現状.md:32「3 経路すべてへ一律適用」→ 4 経路へ）。**B34資料 §4 は「B34 と結合再設計は独立」と書いたが、それは 4-6a（和保存）時点の話で、異親間結合の解禁により片方向の依存（B34 確定 → 暫定制約解除）が生じる**。

---

## 10. 実装着手順（各サブフェーズ ~15ファイル/~300行の §6.10 スコープ管理内）

1. **4-7a: 事前条件の再編（MergeService のみ）** — hasChildren 拒否（MergeService.ts:92-99）を撤去しパターン分類（`isLeafPolygonAnchor` ベース）を `validateMergeFeatures` の返り値へ追加。祖先検査（:76-90）が実効化することの回帰テスト（Phase 4-5 の前提確定を回収、現状.md:494）。同一上位領域制約（:101-109）は**まだ残す**。選択時 UI のコンテナ拒否メッセージ解除が自動で波及（共有バリデータの配当）。
2. **4-7b: コンテナ結果確定経路（同一上位領域内で先行成立）** — `mergeContainerFeatures`（§3 の変異列）+ `MergeFeatureCommand` 振り分け + 回帰テスト（平坦化/混在/将来史孤児/undo・redo イベント厳密一致/round-trip で `validateParentChildUnion`・`validateLeafExclusivity`・階層整合が通ること = 4-6a の実証テスト様式、現状.md:500）。同一上位領域内なら親が一意でダイアログ不要（4-6a と同じ既定、現状.md:512）。**着手第一歩として §5 証明スケッチの幾何検証**（反例が出たら 4-7c を完全配線へ変更）。
3. **4-7c: §2.2.3 検出ゲート配線 + 空間不変条件の固定** — §6 の結果状態スキャン（`detectConflictsForFeature` 共有 + scoped スライス歩行）+「検出は空」回帰テスト + 証明の doc 化。ここが**§2.2.3 起動配線の初出**（検出までを配線、ダイアログは defer — ユーザー判断で確定）。
4. **4-7d: 共通フロー UI 合流（4-6残と同一変更単位群）** — 上位領域決定/プロパティ決定ダイアログ（4-6メモ A/C）を 3 パターンへ接続、同一上位領域制約の解除、既存末端 P 遷移解決の共有分岐（§7）、`mergeLeafFeatures` 証明コメント更新、将来史破棄警告（4-6メモ B）の混在 X への適用判断。**この前に B34 確定を推奨**（§9）。
5. 4-7d 完了で Phase 4 結合系（4-5〜4-7）が UX 完成し Phase 4 全体が完了（現状.md:516）。要件定義書の同期改訂（§2.3.3.2 は Phase 4-0 で先行済み、残りは §2.2.3 節への実装状況注記）を同じ変更単位で。

順序の根拠: ロジック先行・UI 後続は 4-1→4-2→4-3 / 4-6a→ダイアログ残 と同じ運び。4-7b を 4-6残ダイアログより先に置くのは、最大の未知（コンテナ結果の staged エンジン適合）を UI なしで潰せるため。逆順（ダイアログ先）でも成立するが、その場合もダイアログ出力型を 4-6メモ C の override 型に先に固定しておくこと。

---

## 11. 罠と教訓（引用付き）

- **§6.1.8（ガイド:296）**「状態遷移の検出は入力経路でゲートせず結果状態で判定する」— §6 のスキャン対象導出と §7 の P 名指し解決の両立論法。結合パターン種別で検出を分岐させない。
- **§6.3.14（ガイド:486）**「錨の区間境界を特定属性の変化境界として扱わない」— コンテナ結果の寿命を「対象の錨 end」から作らないこと。寿命は childIds 同期+剪定の帰結として得る（§3 手順6）。
- **§6.4.16（ガイド:560）**「undo/redo の Map 直接復元でもイベント通知を対称に発火」— changedFeatureIds に孤児化した子・X の旧親・連鎖消滅した祖先まで漏らさず載せる。イベント回帰テストは**厳密一致**（toContain 禁止）。
- **§6.4.17（ガイド:569）**「地物削除は全時間軸からの消滅として参照を掃除」— コンテナ系対象の終了時、破棄後続錨の childIds 参照（§3 手順4）を怠ると「保存できるが開けないファイル」。
- **§6.6.8（ガイド:646）**「排他検証は仕様の対象範囲を実装で広げない」— 検出をコンテナへ拡張しない。分類は `isLeafPolygonAnchor` 一元。
- **§6.6.9（ガイド:653）**「同じ shape 解決経路を共有」— 派生形状は既存 derive 群のみ。結合時 shape キャッシュ禁止（§4）。
- **feedback_no_speculative_helpers（現状.md:454 の適用実績）** — 到達不能な解決ダイアログ配線を先行実装しない。§5 の②推奨の根拠。
- **計画訂正の前例（現状.md:434/436/438）** — §2.2.3 配線予定地は 4-4b → 4-6 → 4-7 と3回移動した。いずれも「着手時の幾何検証」が計画を上書きした。4-7 でも**検証を書く前に幾何を検証する**こと。
- **証明コメントの保守（:427-432 / 現状.md:523）** — 前提が崩れる変更を入れる時、崩れる旨を予告したコメントを必ず現実に同期する。

## 12. unknown 一覧（着手時に解消すべき不確実性）

1. §5 証明スケッチ（混在・平坦化の構成的排他）— 幾何検証・反例探し未実施。**4-7b の第一歩**。
2. X の事前記録済み将来所属履歴を from-time 親付替えが上書きするか — 既存 reassign 経路の実挙動確認（§3）。
3. 結果コンテナの親カバレッジ不足時の扱い — 拒否（推奨）か孤児昇格か（§3）。
4. 移行期間ノードを結合対象にした場合の要件解釈 — B34④採用なら消滅（§2）。
5. §5 の②（検出ゲート）採用 = 現計画（現状.md:516/:522 の完全配線）の縮小訂正 — **ユーザー判断が必要**。
6. B34 確定と暫定制約解除の順序 — §9 の依存指摘の採否。

---

*作成: Claude Fable 5（subagent）、2026-07-08。READ-ONLY 調査（要件定義書 §2.1/§2.2.3、現状.md §1.2/§5/§6.10、開発ガイド §6、ReassignFeatureParentUseCase / HierarchyService / MergeService / MergeFeatureCommand / AddFeatureUseCase / ConflictDetectionService / ResolveFeatureAnchorConflictsUseCase / jsonSerializerValidation / worldValidation / App.svelte を精読）。本ファイル新規作成以外の変更なし。*
