# B34 推奨④「ロード時正規化」への反論レビュー（Fable 赤チーム・2026-07-08）

> **位置づけ**: `提案-B34仕様確定資料-2026-07-07.md` の推奨④（ロード時正規化）に対する敵対的レビュー。Fable 5 が赤チーム役として「④が間違っている・危険である最強の理由」を探しにいった結果である。全指摘はコード・文書の実地確認に基づく（file:line 併記）。本ファイルの新規作成以外、リポジトリへの変更はない。

---

## 最強の反論（強い順）

### 反論1 — 「§6.4.15 は構造的に再発不能になる」は**事実として偽**。④は移行期間ノードの「流入」を止めるだけで「発生」を止めない

提案 §4 は「以後アプリ内に移行期間ノードは存在しないため、3経路（将来の第4群も）の操作時対応・ガード・再計算はすべて不要になる」「§6.4.15 の『保存できるが開けない』は構造的に再発不能になる」と断言する。これは提案自身が §1 で引いている 現状.md:529 と矛盾する。

現状.md:529 の申し送りは、第4群（`UpdateFeatureAnchorUseCase.updatePlacement` / `PrepareFeatureAnchorEditUseCase.applyCandidatePatch` / `CommitFeatureAnchorEditUseCase.applyAnchors`）について「いずれも `placement.childIds` を直接書ける口を持ち、**shape を残したまま childIds を非空化した anchor を作成可能**」と明記している。つまり第4群のリスクは「移行期間ノードに対する操作」ではなく「移行期間ノードの**生成**」である。ロード時正規化は JSONSerializer.ts:57-59 直後の1経路にしか立っていないから、UI 配線後にこれらの経路（あるいは Phase 4-4b-1 で実際に起きたのと同型のバグ — 開発ガイド §6.1.8:299 の「自治化の所属先 Q が移行期間ノードのまま残った」実例）がアプリ内で移行期間ノードを作れば:

- shape ≠ 子の和なら → 保存は通り、次のロードで `validateParentChildUnion` が拒否。**「保存できるが開けない」が今日と全く同じ形で再発する**。
- shape ≡ 子の和なら → 次のロードで黙って正規化され、バグの痕跡ごと洗浄される（検出機会の喪失）。

しかも提案 §5-4 は `isShapeEditable`（HierarchyService.ts:554-559）の削除と、移行期間ノード関連テスト12ファイルの「存在しない前提」への書き換えを提案している。つまり④は**病原体を1つの国境検問で止めたと宣言しつつ、国内の免疫系を撤去する**。事実E（経路ゲートは漏れる）は提案自身の根拠なのに、④の実装形態（シリアライザ内の1パス）はまさに「単一入力経路のゲート」であり、§6.1.8 の教訓「入力経路でゲートせず結果状態で判定する」の**最徹底形ではなく、その反対**である。§6.1.8 を本当に徹底するなら、不変条件「shape XOR childIds」を**ドメインモデル（`FeatureAnchor` の生成時不変条件）で表現不能にする**のが正しい層で、ロード時正規化はその系にすぎない。提案はこの主従を逆にしている。

**要求**: 「第4群の操作時対応も不要になる」「§6.4.15 再発不能」の2文は撤回するか、ドメイン不変条件（または既存の `validateStagedHierarchy` 型の変異後ゲート）とセットでのみ成立する条件付き主張に書き直すこと。

### 反論2 — 「無損失」は幾何的にしか真でない。構造情報と頂点のライフサイクルが黙って壊れ、そのコストが「実装コスト: 低」に計上されていない

`multiPolygonsEquivalent` は `polygonClipping.xor(...).length === 0`（BooleanOperationService.ts:333-344）— **幾何同値**であって構造同値ではない。破棄される shape には幾何以外の情報が乗っている: リング分解（穴あり1ポリゴン vs 2ポリゴン）、頂点ID列と頂点の実体、共有頂点グループへの所属、将来編集のための頂点配置。そしてこのコードベース自身が「幾何同値は同一性判定に不十分」という教訓を既に持っている — MergeService は同一形状判定に `polygonShapesStructurallyEqual` を使い「**幾何同値まで広げない保守側に倒す**」（開発ガイド §6.4 の 4-6a→4-6b 教訓、開発ガイド.md:489）。④は自宅のドクトリンに反して、幾何同値だけを根拠に構造を破棄する。

さらに致命的なのは頂点である。提案 §5-1 の実装ヒントは `withShape(undefined)` だけで、**頂点に一言も触れていない**。ところが:

- shape 破棄で旧頂点が孤立する問題は**既知の未解決負債**である。ReassignFeatureParentUseCase.ts:174「遷移で旧 shape を破棄した地物の旧頂点は現状残置する（孤立頂点掃除は本サブフェーズの…）」、現状.md Phase 4-4b「孤立頂点掃除 残」。
- 保存は全頂点を無条件に書き出す（jsonSerializerTransforms.ts:366 `vertices: [...world.vertices.values()]`）。よって外部データをロード→保存するたび、**孤立頂点がユーザーのファイルに黙って焼き込まれる**。アプリ内操作で稀に起きる負債が、④によって「外部データを開くたび・一括で」起きる規模に拡大する。
- 掃除を正しくやるなら共有頂点グループの整理（§6.4.11、グループは2頂点未満で validation エラー: jsonSerializerValidation.ts:108）まで必要で、これは「ロード後1パス＋剪定1箇所」では済まない。

比較表の「④実装コスト: 低」はこの頂点ライフサイクルを丸ごと見落とした値付けである。

### 反論3 — ユーザーのファイルを黙って書き換える設計であり、影響を受けるのは「最も意図的にそのデータを作った人」だけ

④の性質を組み合わせると: 事実B（描画は変わらない＝破壊が**見えない**）＋ ロードは undo の外（＝**取り消せない**。提案はこれを利点として計上している）＋ 保存で確定（ラベル1つ直して保存しただけで、触っていない移行期間ノードの shape が全滅する）。つまり**無警告・不可視・不可逆の他人ファイル書き換え**である。`deserializeWithReport` の `compatibilityWarnings` は UI まで配線済み（App.svelte:879）なのに、提案のロードマップは正規化の通知を一切そこへ流していない。

そして影響を受ける母集団を考えると事態はさらに悪い。float 座標の xor が空になる厳密一致は、親 shape が子と頂点（または座標ビット列）を共有するように**作為的に構築しないとまず通らない**。つまり `validateParentChildUnion` を通過する移行期間ノード入りファイルの作者は、ほぼ定義上「shape ≡ 子の和を意図して構築した人」である。④が黙って潰すのは、偶然そうなったデータではなく、**最も強い意図を持って作られたデータ**だ。提案 §6-2 自身が「移行期間ノードを意図的に作る外部ツールは存在するか？」を未回答のブロッカーとして挙げながら、その回答前に不可逆破棄を推奨している。質問と推奨の順序が逆である。

### 反論4 — 「最後の子削除で親消滅」は要件定義書 §2.1・教訓 §6.4.17 に**明文化された挙動**の黙示的な廃止であり、連鎖消滅まで付いてくる

開発ガイド §6.4.17（開発ガイド.md:571）は「**shape を持つ移行期間ノードは最後の子を失っても末端地物へ戻るだけで消滅しない**」を、要件定義書 §2.1 を引きつつ「有効な状態」として明記している。HierarchyService.ts:614 も同旨。④はこの文書化済みセマンティクスをロード時に黙って廃止する。しかも `planFeatureRemoval` は空コンテナを**連鎖的に**消す（HierarchyService.ts:638-640「全錨が空コンテナ化 → 連鎖消滅」）ので、正規化後は末端1つの削除が祖先チェーンごと消し飛ばし得る。移行期間錨しか持たない地物なら、名前・種別・説明・スタイルを含む**地物そのものが消滅**する。セッション内は全マップスナップショット undo で戻れるが、保存をまたげば戻れない。

このアプリの実ユーザー層（歴史地図・worldbuilding）にとって「帝国は最後の州を失っても残存国家として残る」は自然なモデリング意図であり、現行フォーマットはそれを表現できる。提案はこれを「唯一の実質的トレードオフ」と正しく特定しながら（§4）、ユーザーの回答前に④を推奨し、緩和案（メタデータ記録＋最後の子削除時のみ末端化）を「複雑化に見合うかは疑問」の一言で処理している。判断の核だと自認する論点の扱いとして軽すぎる。

### 反論5 — 比較表に「⑤ロード時拒否」が存在しない。土俵が④に有利に切られている

「この状態を廃止したい」が結論なら、**移行期間ノード入りファイルをロード時に明確なメッセージ付きで拒否する**案（⑤）が当然比較対象に入る。⑤は: 経路網羅=④と同等（入口で存在を消す）、undo 影響なし、新モデル整合=最高、**情報損失=真にゼロ**（ファイル無傷）、頂点ライフサイクル問題なし、挙動変化の黙示的廃止なし、実装コスト=④より低い（validation 1本追加のみ）、そして正直（黙った書き換えでなくエラーメッセージ）。gimoza のロード契約は既に「厳密一致か拒否か」（`validateParentChildUnion` が現に拒否する）なのだから、⑤はその一貫した延長である。唯一の負けは「ファイルが開けない」ことだが、反論3のとおり該当ファイルはほぼ作為的構築物であり、作者に外部で直させるのが誠実という見方すら成り立つ。⑤の不在は、④が勝つ軸だけで表が組まれた疑いを残す。

あわせて事実Dの値付けにも誤りがある: 削除経路の undo は B32 修正で**既に**全マップスナップショット型へ移行済み（開発ガイド §6.4.17:573「修正は before/after 全マップスナップショット undo = `ReassignFeatureParentCommand` と同型への移行」）。①の「全マップ型へ移行必要」というコストは、少なくとも主経路（削除）については既払いであり、表は①に対して過大請求をしている。

---

## 却下案の再評価

**②操作拒否 — 「次点にもならない」は藁人形論法。** 提案は②を「全経路の入口にガード必要（漏れ実績あり）」と描くが、それは②の最悪の実装である。§6.1.8 が確立したパターン（変異後の結果状態スキャン: `resolveLeafToContainerTransition`、`validateStagedHierarchy` の変異後ゲート — 現状.md:530 は後者を既存3経路統一の実態として記録している）を使えば、②は**コマンド確定点の単一チョークポイント**で実装できる。経路ごとのガード網は不要で、事実Eは当たらない。「行き止まり UX」も、明示的な「純コンテナ化する」コマンド（undo 可能・ユーザーが選ぶ）を出口として添えれば解消する。これは④と同じ終着点（純コンテナ化）に**本人の同意と undo 可能性つき**で到達する設計であり、データ・スチュワードシップでは④に明確に勝る。負けるのは実装の簡潔さだけだ。

**①操作時破棄 — 実はアプリ自身のドクトリンに最も近い。** Phase 4-4b-1 の末端→集約遷移はまさに「shape 破棄＋差分の直轄領化」を結果状態スキャンで行っており（現状.md Phase 4-4b-1 エントリ）、検証済みデータでは差分が空だから直轄領生成は発生せず、①は「既存の遷移解決を移行期間ノードにも適用する」だけに縮む。削除経路の全マップ undo は既払い（前述）。反論1で見たとおり第4群の遷移解決義務はどのみち残るのだから、①系（結果状態での破棄）は④採用時にも**結局併設が必要になる**。「④なら経路対応が全部消える」は幻想で、実態は「④＋①の変異後ゲート」が完成形である。

**③縮退 — 死んだままでよい。** 常に子の和と等しい shape を毎操作再計算して維持する案に擁護の余地はない。提案の却下理由は正当。

---

## 率直な評決

**要修正。**

④の核にある洞察 — 「検証が等値を保証したデータは入口で正規形に畳める」 — は本物で、②単独や現状放置より筋がよい。しかし提案されたままの④は採用してはならない。理由は上の順で: (1) 看板の保証「§6.4.15 再発不能・第4群も不要」が 現状.md:529 と矛盾する偽の主張であること、(2) 「無損失」が幾何限定で、頂点ライフサイクル（既知の未解決負債）を無視した「コスト: 低」であること、(3) 無警告・不可逆のユーザーファイル書き換えを、まさに意図的にそのデータを作った作者に対して行うこと、(4) 文書化済みの削除セマンティクスを黙って廃止すること、(5) ⑤ロード時拒否を比較すらしていないこと。

生き残る条件は最低限次の5つ: 不変条件「shape XOR childIds」をドメイン層（生成時不変条件または変異後ゲート）で強制し、ロード時正規化はその系として置く／正規化を `compatibilityWarnings` 経由でユーザーに通知し、少なくとも初回保存前に破棄の確定について同意を取る（または in-memory 正規化に留めて保存時に選ばせる）／頂点・共有頂点グループの処理を仕様に含めてコストを再見積もりする／§6-2（外部ツールの存在）の回答を推奨確定の**前提条件**に格上げする／比較表に⑤を加えて再評価する。これらを満たさない④は、「保存できるが開けない」を治す代わりに「開いただけで壊れているのに誰も気づけない」を導入する提案である。

---

*作成: Claude Fable 5（赤チーム反論）、2026-07-08。引用は worldValidation.ts / JSONSerializer.ts / HierarchyService.ts / BooleanOperationService.ts / FeatureAnchor.ts / jsonSerializerTransforms.ts / jsonSerializerValidation.ts / ReassignFeatureParentUseCase.ts / 現状.md / 開発ガイド.md / App.svelte の実地確認に基づく。*
