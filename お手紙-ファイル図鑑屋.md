# time-map-app-3（gimoza）さんへ — ファイル図鑑屋からのお手紙

---

## 【自己紹介】ファイル図鑑屋とは

はじめまして。「ファイル図鑑屋」と申します。これは、asteainさんのPC全体に眠るファイルやプロジェクトを、まるでポケモン図鑑のように一つひとつ棚卸し・カタログ化し、コードについてはレビューして改善点をまとめる "屋（サービス）" です。2026-06-14 の「トークン燃やし会」で、AI（Claude Code）として生まれました。活動は徹底して**非破壊**で、ファイルは**読むだけ**、本家リポジトリは決して書き換えません。

このお手紙は、そのファイル図鑑屋が、このリポジトリ（`time-map-app-3` = gimoza）を読ませていただいたお礼と、見つけた改善のご提案を、敬意をもってお届けするものです。

---

## 1. ごあいさつと、読ませてもらった感想

gimoza、拝見しました。ひとことで言うと、**ポートフォリオの旗艦にできる "教科書級" の Electron アプリ**です。

架空世界の地理を「時間軸つき」で編纂できるという発想そのものが面白いのですが、それを支える実装が本当に丁寧でした。23,049 行の非テスト TypeScript に対して型注釈の `:any` は **1 箇所**（`ipcHandlers.ts:8`）、`as any` キャストも **2 箇所**（`PrepareFeatureAnchorEditUseCase.ts:202,205`）だけ、`@ts-ignore` **0**、`TODO/FIXME/HACK` も **1 件のみ**（`VertexHandles.svelte:77`）、そしてユニットテスト **1,756 ケース** + E2E **21 spec**。この型規律とテスト密度は、正直なところ商用プロダクトでもなかなかお目にかかれません。

ドメイン駆動の 4 層構成（domain → application → infrastructure → presentation）、時間ごとのスナップショットを丸ごと持つ **bitemporal な Feature/FeatureAnchor モデル**、`shape` の有無で leaf/container を判定する二相ノードモデル、依存ゼロの自前 ZIP（`.gimoza`）+ 手書き CRC32 … どれも「なぜこう作ったか」が一貫していて、読んでいて気持ちのよいコードでした。

その上で、いくつか「より安全に・より速く」できる点が見えましたので、押し付けでなくご提案として並べます。設計が間違っているという話ではありません。多くは Electron 特有の脅威モデルの穴と、小さな仕上げです。

---

## 2. 改善点の目録

重要度: 🔴=配布前に対応したい / 🟡=直すと安心・速くなる / 🟢=お好みで

| # | 項目 | 重要度 | 概要 | 直し方の所在 |
|---|------|:---:|------|------|
| S2 | SVG の `{@html}` 注入 + 正規表現サニタイザのすり抜け | 🔴 | プロジェクトファイル由来の base-map SVG を `{@html}` で描画。サニタイザは正規表現3本で、未クォート `onload=alert(1)`・`<foreignObject>`・実体参照化した `javascript:`・`<animate attributeName="href">` 等を素通しする。悪意ある `.gimoza` を開くと renderer でスクリプト実行 → 後述 S1 と合わさり**ローカル任意ファイル R/W/削除**まで連鎖。検証済みの最重要点。 | パッチ `patches/timemap-svg-xss.md`（構造的な許可リスト方式サニタイザ + 厳格 CSP、ドロップイン可） |
| S1 | filesystem IPC が renderer から任意絶対パスを受理 | 🔴 | `src/main/ipcHandlers.ts` の `file:read/write/writeBinary/append/delete/list` がパスの封じ込めなしに何でも実行。単体では「信頼された renderer」前提だが、S2 の XSS を任意ファイル操作に増幅してしまう。 | パッチ `patches/timemap-ipc-hardening.md`（セッションスコープの許可リスト `assertAllowedPath`、dialog 選択パスは祝福、`..` 拒否） |
| S3 | `shell.openExternal` がスキーム検証なし | 🔴 | `src/main/window.ts:84-87` が要求 URL を全部 `openExternal` へ転送。`file:`/`smb:`/独自プロトコルも OS ハンドラに渡る。 | パッチ `patches/timemap-ipc-hardening.md`（`https:`/`http:`/`mailto:` のみ許可） |
| S4 | CSP が一切ない | 🔴 | `src/renderer/index.html` に CSP meta なし、main からの注入もなし。`{@html}` がある以上、最も安価で効果の高い多層防御。 | パッチ `patches/timemap-svg-xss.md`（index.html の CSP meta + main の `onHeadersReceived` 注入） |
| S5 | `contextIsolation`/`nodeIntegration`/`sandbox` 未固定 | 🟡 | `window.ts` は `preload` と `sandbox:false` のみ。既定値は今は安全だが、明示ピン留めが将来のリグレッション保険。preload は `contextBridge`/`ipcRenderer` だけなので `sandbox:true` 化も実現可能。 | パッチ `patches/timemap-ipc-hardening.md`（3 つを明示ピン留め、`sandbox:true`） |
| C3 | `.gimoza` 読込時に CRC32 を検証していない | 🟡 | `zipArchive.ts:145-187` は書込時に CRC32 を格納するが、読込時に再計算・照合しない。サイズ整合だけ保った破損アーカイブが「壊れている」ではなく後段の謎 JSON parse エラーとして表面化。整合性の穴。 | リポ報告 R2（差分付き）+ テスト案（`crosscut-tests.md` / `repo-time-map-app-3.md` §4-1） |
| C1 | `EventBus.emit` がライブ Set を反復 | 🟡 | `EventBus.ts:94-100`。dispatch 中に listener が `on`/解除すると、新規 listener が同一 emit 内で走る/二重呼ばれる可能性。`world:loaded` で再購読する箇所があり潜在順序バグ。 | リポ報告 R1（`for (const l of [...set])` で snapshot、1 行） |
| C4 | `Coordinate.normalize()` の経度 wrap が無限 `while` | 🟡 | `Coordinate.ts:21-22`。`while (nx>180) nx-=360`。NaN は `NaN>180===false` で素通り、巨大値は反復多数。算術 modulo で定数時間化を推奨。 | リポ報告 R3（差分付き、`((((nx+180)%360)+360)%360)-180`、NaN ガード付き） |
| P1 | 重なり検出が broad-phase なしの O(n²) | 🟡 | `ConflictDetectionService.ts:64-83`。全 leaf ペアで polygon-clipping 交差を実行。大規模世界で支配的コスト。AABB の事前フィルタで clip 呼び出しを激減（結果は厳密に不変）。 | リポ報告 R7（AABB broad-phase 差分 + 差分テスト案） |
| P2 | 内ループ内で ring 座標を再解決 | 🟢 | `ConflictDetectionService.ts:69-70` が `a` の polygon を `j` 反復ごとに再解決。`ringsA` をループ外へ巻き上げ。R7 に同梱。 | リポ報告 R7 |
| C2 | `TimePoint.equals` と `compareTo` が精度不一致時に食い違う | 🟢 | `compareTo` は欠落月日を 1 補完、`equals` は生フィールド比較。`equals` を `compareTo===0` の意味で使う境界処理がないか確認 + doc コメント。 | リポ報告 C2（`equalsByOrder()` 補助 + ドキュメント） |
| M1 | `ReassignFeatureParentUseCase.ts` が 1,641 行 | 🟢 | 正しく動く中核だが 1 ファイルとして肥大。anchor 分割 / 空コンテナ cascade 剪定 / コンテナ連鎖組立 を専用サービスへ抽出すると各機構を個別にテストしやすい。 | リポ報告 R8（抽出案 3 モジュール） |
| M2 | `buggy data.json` が git 追跡されている | 🟢 | リポ root に再現用アーティファクトが commit 済み。`ARCHIVE/` 移動 or untrack 推奨（USER による `git rm --cached`）。 | リポ報告 M2 |
| M3 | `IpcHandler` 型の `...args: any[]`（型注釈の `:any` はここ 1 箇所、`ipcHandlers.ts:8`） | 🟢 | チャネル→型付き引数のマップ化で `any` を解消可能。S1 ハードニングと自然に併せられる。なお別カテゴリだが `as any` キャストが `PrepareFeatureAnchorEditUseCase.ts:202,205` に 2 箇所あり、こちらも併せて型を絞れます。 | リポ報告 M3 / R4 |

> 念のため: 過去監査（162aaa/251a4）が挙げた「event 名→payload の型付け」と「undo 履歴の上限」は、**すでに実装済み**（`EventMap` 8 イベント / `UndoRedoManager.MAX_HISTORY=100`）です。再提案ではないので、もし指摘を見かけても気にされませんよう。

---

## 3. 良いところ（正直に）

お世辞抜きで、ここは本当に立派だと思った点です。

- **型規律とテスト密度が突出**。23k 行で型注釈の `:any` 1・`as any` 2・`@ts-ignore` 0・TODO 1 件、テスト 1,756 ケース。ここまで徹底できているプロジェクトは稀です。
- **bitemporal アンカーモデル**が秀逸。「独立 → 併合 → 連邦」のような歴史的フェーズ変化を、差分ではなくフルスナップショットで素直に表現していて、ドメインの本質を捉えています。
- **leaf 判定を 1 つのヘルパー（`isLeafPolygonAnchor`）に集約**し、約 3 つの検証パス間でのドリフトを防いでいる設計判断が玄人。
- **依存ランタイムが 2 つだけ**（`polygon-clipping` / `xstate`）。ZIP も CRC32 も自前で、依存を極限まで絞っている潔さ。
- **preload が狭い型付き API（`window.api`）のみ公開**し、生 `ipcRenderer` を露出していない。`onOpenProjectPath` の payload 文字列ガード、`isSupportedProjectPath` の argv フィルタ、single-instance lock も正しい。セキュリティの土台はちゃんとできています。
- 型付き EventBus、純 DI コンテナ（非 Electron テスト環境でも構築可）、XState v5 の宣言的編集モード — どれも "後から効いてくる" 種類の良い投資です。

総合評価は **⭐⭐⭐⭐⭐**（設計品質）。ただし**広く配布する前に、上の 🔴（S1/S2/S3/S4）のセキュリティハードニングだけは入れておきたい**、というのが正直な所感です。

---

## 4. 同梱のご案内（workshop パス）

このお手紙は SELF-CONTAINED ですが、実際に手を動かせるドロップイン素材を workshop に用意しました。いずれも**本家は未変更**、graft（接ぎ木）用のテキストです。

- **セキュリティハードニング パッチ（S1/S3/S5）**
  `/e/WORKSPACE/file-zukan/_workshop-2026-06-14/engineering/patches/timemap-ipc-hardening.md`
  → `ipcHandlers.ts`/`window.ts` への unified diff + 新規テスト `tests/electron/ipcHardening.test.ts`（フル・実行可、新規依存なし）。既存 `window.test.ts`/`ipcHandlers.test.ts` の更新点も明記。
- **SVG XSS パッチ（S2/S4）**
  `/e/WORKSPACE/file-zukan/_workshop-2026-06-14/engineering/patches/timemap-svg-xss.md`
  → 正規表現サニタイザを**構造的許可リスト方式**（DOM 非依存・Node テスト環境でそのまま動く）に置換する diff、`index.html` の CSP meta、main の `onHeadersReceived` 注入、新規テスト `tests/presentation/components/mapCanvasUtils.svgSanitize.test.ts`（敵対的 SVG 12 ベクタ + 良性ジオメトリ保持）。
- **詳細レビュー本体（findings C1-C4 / S1-S5 / P1-P3 / M1-M4 の全文・差分・テスト案・README 草案）**
  `/e/WORKSPACE/file-zukan/_workshop-2026-06-14/engineering/repo-time-map-app-3.md`
- **クロスカット テスト集**
  `/e/WORKSPACE/file-zukan/_workshop-2026-06-14/engineering/patches/crosscut-tests.md`
- **適用ガイド**
  `/e/WORKSPACE/file-zukan/_workshop-2026-06-14/engineering/patches/00-APPLY-GUIDE.md`

各パッチには `git apply --check` の手順と `npm run check` / `npm run test` の VERIFY 手順を載せています。オフセットがずれた場合の手当てメモも添えてあります。

---

## 5. 締めの一言

gimoza は、設計の素直さと型・テストの誠実さが両立した、作者さんの "旗艦" と呼ぶにふさわしいアプリでした。読ませていただいて、こちらが勉強になった部分がたくさんあります。ありがとうございました。

提案した 🔴 のセキュリティ点は、いずれも「設計を作り直す」話ではなく、Electron という器に**蓋を一枚足す**ような、局所的で安全な変更です。同梱パッチはそのまま貼れる形にしてありますので、お時間のあるときに、お好きなものから少しずつどうぞ。

これからも、この世界の歴史が安全に編まれていきますように。

ファイル図鑑屋より、敬意とともに。
