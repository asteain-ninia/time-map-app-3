# 提案 — B15 要件定義書仕様補完ドラフト（2026-07-08）

> **位置づけ**: 未確定のドラフト。現状.md §1.3 B15「機能の詳細な仕様が要件定義書に不足。実装と要求にズレが生じている可能性」（§5 修正優先順位の2番）を埋めるための「あるべき仕様文」の提案であり、要件定義書.md 本体へは一切書き込んでいない。**採用時に USER が各ドラフト文を §8 の対応表に従って該当§へ転記する**。作成は READ-ONLY 調査＋本ファイル新規作成のみ（Claude Fable 5、2026-07-08 系列、実作成 07-10）。
> **表記**: file:line は 2026-07-10 時点のコード実測。「要件」= 要件定義書.md、「line n」= 要件定義書.md の行番号。推測・未実証は **unknown** と明記。
> **同日提案群との関係**: `提案-Phase4-6残作業の設計判断-2026-07-08.md`（4-6メモ）、`提案-Phase4-7設計-2026-07-08.md`（4-7設計）、`提案-00-INDEX-2026-07-08.md` で確定しつつある設計（遷移解決の共有、override 型の共有、§2.2.3 検出ゲート先行）は、USER 判断待ちである間は確定仕様文にせず「未確定注記」付きで反映した（§2.2 ドラフトB-1 末尾、§3.3、§7-8）。
> **ドラフト文の文体**: 要件定義書本体（第2章）の仕様記述調（箇条書き・「〜する／〜とする」体）に合わせた。ギャップ分析は簡潔に留める。

---

## 1. トピック1: 操作モード体系（表示モード／編集モードで何ができるか）

### 1.1 現状の記述ギャップ

要件はモードに**散発的に**言及する（line 55-58 パン操作、line 77 測量モード、§2.3.2 追加モード、§2.3.3 編集モード）が、以下を定義する節が存在しない:

- モードの全列挙と相互排他性（§2.3.2 line 537 の排他記述は「追加⇔編集」のみ。実装は表示・追加・編集・測量の4モードすべてが排他）
- モード切替時に何が破棄され何が保持されるか
- **各モードで何ができ何ができないかの対照**（B15 が名指しする欠落。現状.md §2.2 のマニュアル整備項目でも重点解説対象）
- 表示モードでの地物選択・プロパティ閲覧の可否

### 1.2 提案する仕様文ドラフト（新設 §2.3.0「操作モード」。§2.3.1 の直前へ挿入）

- **操作モードの体系**:
    - アプリケーションは「表示モード」「追加モード」「編集モード」「測量モード」の4つの操作モードを持つ。
    - 常にいずれか1つのモードのみが有効である（4モードは相互に排他）。起動直後およびプロジェクト読み込み直後は表示モードとする。
    - モードはツールバーのボタンで切り替える。
- **モード切替時の状態リセット**:
    - モード切替時には、進行中の一時的な操作状態をすべて破棄する:
        - 描画途中の形状（追加モードの頂点列、穴/飛び地の描画途中、分断線の入力途中）
        - アクティブ地物・頂点選択セット・頂点選択コンテキスト
        - 結合対象の選択リスト
        - 開いている操作ダイアログ（分割・結合・所属変更）
        - 地物移動サブモード（編集モードへ戻った際は頂点編集サブモードから開始する）
    - 確定済みの編集内容、測量線、アンドゥ・リドゥ履歴はモード切替では破棄しない。
- **各モードで可能な操作の対照**:

| 操作 | 表示 | 追加 | 編集 | 測量 |
| --- | :-: | :-: | :-: | :-: |
| パン（左ボタンドラッグ） | ○ | × | × | × |
| パン（中ボタンドラッグ） | ○ | ○ | ○ | ○ |
| ズーム（ホイール） | ○ | ○ | ○ | ○ |
| タイムライン操作・時間移動 | ○ | ○ | ○ | ○ |
| 地物の選択（クリック／一覧から） | ○ | × | ○ | × |
| プロパティ・歴史の錨一覧の閲覧 | ○ | × | ○ | × |
| プロパティの編集・地物の削除 | × | × | ○ | × |
| 点・線・面情報の追加 | × | ○ | × | × |
| 頂点・形状の編集（移動・追加・削除） | × | × | ○ | × |
| 地物移動（平行移動） | × | × | ○ | × |
| 分割・結合・所属変更・穴/飛び地追加 | × | × | ○ | × |
| コンテキストメニュー | × | × | ○ | × |
| 2点間距離の測量・クリック座標表示 | × | × | × | ○ |

- 表示モードは閲覧専用であり、プロジェクトデータを変更するいかなる操作も受け付けない。
- 測量モードのパン操作は中ボタンドラッグとする（追加・編集モードと同様）。

### 1.3 根拠

- 4モード排他のステートマシン: `src/renderer/presentation/state/toolMachine.ts:19`（`ToolMode = 'view' | 'add' | 'edit' | 'measure'`）、:93-271（MODE_CHANGE 遷移）
- パン可否: `src/renderer/presentation/components/MapCanvas.svelte:391-393`（view は左/中ボタン、他モードは中ボタンのみ。**測量モード=中のみは実装事実であり line 55-58 に規定が無い**）
- モード切替時リセット: `src/renderer/App.svelte:835-854`（`resetInteractionState`: 選択・結合リスト・描画途中・全モーダルを破棄）、:951-958（`onModeChange`: 地物移動サブモード解除）
- 表示モードでの選択: `src/renderer/App.svelte:1064-1088`（`toolMode === 'view' || 'edit'` で hitTest 選択）
- コンテキストメニューは編集モードのみ: `src/renderer/App.svelte:1955-1957`
- 地物移動サブモード中の頂点ハンドル非表示: `src/renderer/App.svelte:2256`
- **ズレ注記（USER判断）**: 実装ではプロパティ編集がモードでゲートされておらず、表示モードでも選択→サイドバーからプロパティ編集・地物削除が可能（`App.svelte:2288-2305` で Sidebar に toolMode を渡していない、`onPropertyChange` `App.svelte:491-507` にモード判定なし）。既存要件 §2.4.3 line 833「編集機能を統合する（編集モード時）」と不整合。本ドラフトは既存要件の精神に合わせ「表示モードは閲覧専用」を仕様とし実装側の修正を想定した。逆に「モード非依存で属性編集可」を正とするなら上表の該当2行を修正する。

---

## 2. トピック2: 結合ツール（＋結合 vs 所属変更フローの使い分け）

### 2.1 現状の記述ギャップ

§2.1 line 315-355 はデータモデル面（3パターン・共通フロー・制約）を詳述済みで、ここは薄くない。欠けているのは:

- **(a) 結合結果の有効期間（終端）規則** — 記述ゼロ。実装は「同一形状連続区間の最小終端＋親カバレッジクランプ」で結果地物の寿命を決めており、「結合結果が未来のある時点で自動的に消滅する」というユーザー可視の挙動に仕様がない。
- **(b) 結合対象の将来の錨の破棄** — line 329「存在を終了する」の一文のみ。結合時刻以降に打たれていた錨（将来の所属変更・形状変化）が破棄されることの明文と、警告の要否（4-6メモ B）がない。
- **(c) 操作手順のズレ** — line 323「Shiftキー+クリック」は実装（結合対象トグルボタン／コンテキストメニュー方式）と異なる。
- **(d) 選択時のリアルタイム検証** — 制約検証を「操作開始前（対象追加時）と確定前」の両方で行い追加自体を拒否する挙動は、line 354 に上位下位制約についてのみ記述があり一般化されていない。
- **(e) 使い分けの解説** — line 317-320 に「違い」はあるが、現状.md §2.2 が「ユーザーが直感的に混同しやすい」と認める通り、判断基準と対照表の形になっていない。

### 2.2 提案する仕様文ドラフト

#### ドラフトB-1: 結合結果の有効期間（§2.1「合体（結合）機能」の「共通フロー」の直後へ挿入）

- **結合結果の有効期間**:
    - 結合結果の地物は、結合を実行した作中時間を開始時刻とする歴史の錨で新規開始する。
    - 結合結果の有効期間の終端は、各結合対象が「（結合しなかったならば）末端地物として同一形状のまま連続して存在し続けたであろう区間」の終端のうち、最も早いものとする。対象の形状変化・存在終了・非存在期間の開始は、この連続区間を打ち切る。歴史の錨が形状変化以外の理由（所属変更・属性変更など）で分かれているだけの場合は、連続区間は打ち切られない。
    - 結合結果が上位領域に属する場合、有効期間の終端は上位領域の連続した存在期間の終端を超えない。
    - 上記いずれの打ち切りも生じない場合、結合結果の錨は終了時刻を持たない（無期限）。
    - 結合対象が結合時刻より後に持っていた歴史の錨（将来の所属変更・形状変化など）は、結合対象の存在終了に伴い破棄される。
    - （**未確定** — 4-6メモ B 採用時のみ追記）破棄対象となる将来の錨を持つ対象が含まれる場合に限り、確定前に「この結合は N 件の将来の変更履歴を破棄します」として破棄される錨の種類と時刻を列挙した確認警告を表示する。将来の錨を持つ対象のみの場合は警告を出さない。

#### ドラフトB-2: 操作方法の現実同期（§2.1「操作方法」line 321-324 の置換）

- **操作方法**:
    - 編集モードで結合対象としたい面情報をアクティブ地物として選択し、ツールバーの「結合対象」ボタンまたはコンテキストメニューの「結合」で結合対象リストへ追加する。追加済みの地物へ同じ操作を行うとリストから除去される（トグル式）。
    - 結合対象への追加時、システムはその時点の結合対象全体に対して結合可否（後述の制約）を検証し、違反する場合は追加自体を拒否して理由を通知する。同じ検証は確定時にも再実行する（操作開始前と確定前の二重検証）。
    - 結合対象が2つ以上になったとき「結合実行」で確定フロー（上位領域決定・プロパティ決定のダイアログ）を開始する。
    - 「解除」操作で結合対象リストを空にできる。モードの切替・プロジェクトの読み込みでもリストは破棄される。
    - 結合の確定後は、結合結果の地物がアクティブ地物として選択された状態になる。

#### ドラフトB-3: 使い分け対照表（§2.1「所属変更フローとの違い」line 317-320 の直後へ挿入）

- **使い分けの判断基準と対照表**:
    - 判断基準は「元の地物が、操作後も別個の存在として歴史に残り続けるか」である。残り続けるなら所属変更フロー（連邦の構成国はそれぞれ存在し続ける）、残らず1つに融合するなら結合（合併した王国は消え、新王国だけが残る）。

| 観点 | 合体（結合） | 所属変更フロー |
| --- | --- | --- |
| 目的 | 複数の面情報を1つの結果地物へ統合する | 地物のツリー位相（親領域）を変える |
| 地物の数 | 「結合の主体」の数だけ減り、結果地物が1つ増える | 原則不変（新規上位領域作成サブフローでは増える） |
| 地物の同一性 | 結合の主体は存在終了する。結果地物は新規生成（別の識別子） | すべての地物の同一性が保たれる |
| 形状 | 末端地物のみの結合では論理和による新形状が生じる | どの地物の保持形状も変わらない（集約形状の導出結果のみ変わる） |
| 元の階層構造 | 保持されない（集約の結合は直接下位領域を平坦化） | 保持される |
| 典型例 | 王国Aと王国Bが合併して王国Cになる | 州Xが国Aから国Bへ割譲される |
| 「複数の最上位国を1つの上位の下へ束ねたい」（連邦化） | **使わない** | **新規上位領域作成サブフローを使う** |

### 2.3 根拠

- 有効期間の終端規則: `src/renderer/domain/services/MergeService.ts:260-293`（`resolveContiguousLeafShapeEnd`: 同一形状連続区間の評価。形状不変の錨分割では打ち切らない=§6.3.14）、`src/renderer/application/ReassignFeatureParentUseCase.ts:464-495`（終端の最小値採用と親カバレッジクランプ）
- 将来錨の破棄: `ReassignFeatureParentUseCase.ts:126-133`（doc: 元リーフは effectiveTime で存在終了）、4-6メモ B（警告は条件付き推奨・未確定）
- トグル選択と二重検証: `src/renderer/App.svelte:1318-1337`（`addMergeTarget`: 追加時に `validateMergeFeatures` で拒否）、:1339-1350（実行前再検証）、:1354-1380（確定後に `command.mergedFeatureId` を選択）、`src/renderer/presentation/components/EditToolbar.svelte:100-127`（結合対象／結合実行／解除ボタン）、`App.svelte:2007`（コンテキストメニュー経由）
- 制約検証の共有: `src/renderer/domain/services/MergeService.ts:57-169`（`validateMerge`/`validateMergeFeatures`: 2つ以上・重複禁止・上位下位同時選択禁止・末端のみ〔暫定〕・同一上位領域〔暫定〕）
- **実装状況注記（仕様文には入れない）**: 上位領域決定・プロパティ決定ダイアログ（line 341-348）は未実装。現行は「同一上位領域に属する末端地物のみ」の暫定制約下にあり（`MergeService.ts:92-109`）、確定時のモーダルは名称のみ入力可能（`MergeConfirmModal`、属性・所属は先頭対象から継承 `ReassignFeatureParentUseCase.ts:497-500`）。ダイアログの最終仕様は 4-6メモ A/C・4-7設計 §10（4-7d）で確定予定のため、本ドラフトでは line 341-348 の既存記述を変更しない。

---

## 3. トピック3: 所属変更フロー

### 3.1 現状の記述ギャップ

§2.1 line 281-313（概念フロー・親候補制約・典型シナリオ）と §2.3.3.2 line 735-742（操作手順の要約）は既に厚い。欠けているのは:

- **(a) ダイアログの具体 UI 仕様** — 対象範囲（選択地物のみ／下位領域すべて）の選択、3択の提示方法、親候補リストの絞り込み規則、確定可否条件が本文にない。
- **(b) 「変更なし確定」の扱い** — 現在の親と同じ選択で確定できるかが未規定。
- **(c) 複数選択のズレ** — line 286「対象地物（複数選択可）」に対し、実装は単一のアクティブ地物のみを起点とする（複数地物の一括所属変更 UI は未実装）。

### 3.2 提案する仕様文ドラフト（§2.3.3.2「所属変更ツール」の末尾へ追記）

- **所属変更ダイアログ**:
    - **対象範囲の選択**: 「選択地物のみ」「下位領域すべて」のいずれかを選ぶ。選択地物が集約地物の場合は「下位領域すべて」のみ選択できる（2.1節の制約に対応）。
    - **新しい上位領域の指定**: 「既存の面情報から選ぶ」「親なし（最上位領域へ）」「新規上位領域を作成する」の3択を提示する。
    - 既存の面情報から選ぶ場合、候補リストには循環参照を生む地物（対象地物自身、および対象地物の下位領域すべて）を含めない。対象地物の上位領域は候補に含める（直轄化）。
    - 新規上位領域を作成する場合、階層ごとに名称・種別ラベルを入力し、「さらに上位を作成する」操作で再帰的に上位を積み上げられる。最終段では「既存の最上位領域を選ぶ」または「新規最上位領域とする（最上位フラグを真にする）」を選択する。
    - **変更なし確定の禁止**: 現在の上位領域と同一の選択のままでは確定できない（確定操作を無効化する）。
    - **直轄領の上書き入力**: 操作の結果、既存の末端地物が下位領域を獲得して集約地物へ遷移する場合、自動生成される直轄領の名称・種別ラベルをダイアログ内で上書き入力できる。既定値は名称「`<元の末端地物名> 直轄領`」、種別は元の末端地物の種別とする。
    - Escapeキーまたはキャンセル操作でダイアログを閉じた場合、何も変更しない。

### 3.3 根拠

- ダイアログ実装: `src/renderer/presentation/components/ParentTransferDialog.svelte:35-39`（scope='selected'|'children'、parentMode、新規親チェーン配列＝再帰積み上げ）、:56（親候補の循環除外）、:79（3択→確定意図）、:118（現在親と同一なら確定不可）、:203-216（createNewParent spec 構築）、:258（「下位領域すべて」UI）
- 直轄領既定値の一元化: `src/renderer/domain/services/HierarchyService.ts:587-589`（`buildDirectlyGovernedDefaultName`）、`ReassignFeatureParentUseCase.ts:96`（`DirectlyGovernedOverrideSpec`）、:1174（`resolveLeafToContainerTransition`、override 口 :1180）
- 単一選択起点: `src/renderer/App.svelte:1394-1397`（`onStartParentTransfer` は `selectedFeatureId` 単一のみ）— **ズレ**: line 286「複数選択可」は未実装。連邦化（line 302「複数の最上位面情報を選択し」）は現行 UI では1地物ずつ実行するか「下位領域すべて」スコープで代替する必要がある（§7 ズレ#3）。
- **同日提案との整合**: 4-6メモ A/C と 4-7設計 §7 は、結合フローの上位領域決定・プロパティ決定ダイアログの出力型を所属変更の `DirectlyGovernedOverrideSpec` に合わせ、`resolveLeafToContainerTransition` を共有する設計を推奨している。採用時は、本節の「直轄領の上書き入力」の規定が結合側ダイアログにもそのまま適用される旨を §2.1「共通フロー」（line 341-348）へ1文追記する: 「既存の末端地物を上位領域に選んだ結果生じる直轄領の名称・種別の上書きは、所属変更フローと同一の入力 UI で提供する。」（**未確定** — 4-6メモ A/C の採否待ち）

---

## 4. トピック4: 共有頂点

### 4.1 現状の記述ギャップ

§2.1 line 184-216 は共有化・移動・解除・抑制の挙動を詳述済み。欠けているのは:

- **(a) 複数スナップ候補時の確定規則** — 候補が複数表示され得ることと、確定は最近接1件のみであることが未規定。
- **(b) 共有頂点グループの整理規則（ライフサイクル）** — グループが常に2頂点以上を保ち、1頂点以下で自動削除されることは §4.1 line 1238 に実装補遺として1行あるのみで、仕様本文（削除・分割との相互作用）がない。
- **(c) 頂点座標の時間非依存原則** — 共有頂点を含む「頂点の移動」が、その頂点を参照する**すべての時刻**の形状へ波及するという、時間管理機能と形状編集をつなぐ根本原則がどこにも明文化されていない（→ トピック5と共通。§7 ズレ#6 の核）。

### 4.2 提案する仕様文ドラフト

#### ドラフトD-1: スナップ確定規則とグループ整理（§2.1「共有頂点システム」の該当箇条へ追記）

- スナップ候補が同時に複数存在する場合、候補すべてを視覚表示してよいが、ドロップ時に共有化が確定するのは**最も近い1候補のみ**とする。
- 共有頂点グループは常に2頂点以上のメンバーを持つ。共有解除・頂点削除・地物削除・形状変更のいずれの操作の結果であっても、メンバーが1頂点以下になったグループは自動的に削除される。
- 地物の削除や形状の変更によりどの形状からも参照されなくなった頂点は、所属していた共有頂点グループから除去される。
- 面情報の分割時の共有頂点の引き継ぎは、既述の「ナイフツール（二分割）— 共有頂点の引き継ぎ」に従う。

#### ドラフトD-2: 頂点座標の時間非依存原則（新設。§2.2.2 の末尾、または §2.1「地理情報の表示」の前へ独立項として挿入）

- **頂点座標の時間非依存**:
    - 頂点の座標は時間に依存しない単一の値である。頂点の移動（単独の頂点移動・共有頂点の連動移動・複数頂点の同時移動・地物移動のいずれでも）は、その頂点を参照する**すべての時刻の形状**へ同時に反映される。
    - これは意図的な仕様である: 海岸線の微修正のような「全時代に共通する描き直し」を1回の操作で全時刻へ行き渡らせ、共有頂点による境界連動を時間軸を跨いで維持するため。
    - 特定の時刻からのみ形状を変えたい場合は、頂点の座標変更ではなく、**頂点ID列を変える操作**（頂点の追加・削除、分割、結合、穴/飛び地の追加）によって新しい歴史の錨を構成する。
    - 「未来の歴史の錨の保護」（2.2.2節）との関係: 未来の錨が保護するのは**状態定義（どの頂点をどの順で参照するか・属性・所属）**であり、参照される頂点の座標値そのものは保護の対象外とする。

### 4.3 根拠

- 最近接1件のみ確定: `src/renderer/App.svelte:1864-1866`（「複数スナップ候補のうち最近接（[0]）のみを共有化対象とする」、`MoveVertexCommand` へ `snapIndicators[0]` を渡す）、`src/renderer/application/commands/MoveVertexCommand.ts:52-66`（ドロップ時に `isSharedVertexMergeAllowed` を再検証してから `mergeVertices`）
- グループ整理: `src/renderer/domain/services/SharedVertexService.ts:420-434`（`unmergeVertex`: 除去後 `shouldBeRemoved()` でグループ削除）、:337-408（`mergeVertices` の4ケース: 新規／片方参加／グループ統合）
- 参加制限（同一リング・同一線形の禁止）: `SharedVertexService.ts:194-256`（形状コンポーネント単位の禁止 = line 195-198 と一致）、:258-288（隣接頂点との共有禁止 = ゼロ長エッジ防止、line 200-201 と整合）
- 時間非依存原則: `src/renderer/application/VertexEditUseCase.ts:38-46`（`moveVertex` は Vertex 座標のみ更新、錨は触らない）、`src/renderer/domain/services/MergeService.ts:205-211`（doc「頂点座標は全時間軸で共有される（頂点移動は当該頂点を参照する全錨へ同時に効く）」）、要件 §4.1 line 1173-1177（Vertex に時間概念なし）
- **ズレ注記**: (1) 共有解除はドメイン実装済み（`SharedVertexService.ts:420`、`EditFeatureUseCase.ts:186`）でコンテキストメニュー項目も存在する（`contextMenuBuilder.ts:52`）が、App 側ハンドラがスタブで**UI 未接続**（`App.svelte:2001-2003`「共有解除は後続の詳細化で拡充」）。(2) 自動共有化抑制（line 214-216）は `UnmergeSuppression` クラスがドメインに存在する（`SharedVertexService.ts:482-523`）が**どこからも使用されていない**（未配線）。いずれも要件が正で実装が未達（§7 ズレ#4・#5）。ドラフトD-1/D-2 はこの2点の仕様を変更しない。

---

## 5. トピック5: 地物移動

### 5.1 現状の記述ギャップ

§2.3.3.2 line 674-678 は4行のみで、以下が未規定:

- サブモード（頂点編集と排他のトグル）としての位置づけと、サブモード中の頂点ハンドル非表示
- 移動を開始できる条件（選択済み地物自身の上でのドラッグ）
- 検証と拒否の挙動（エッジ滑りは適用されるのか、違反時どうなるのか）
- 全時刻への波及（トピック4のドラフトD-2 参照）
- さらに line 678「操作確定時に歴史の錨を更新し、『2.2.3 整合性維持プロセス』をトリガーする」は**実装と不一致**（錨は更新されず、§2.2.3 プロセスも起動しない。現在時刻での直接検証・拒否のみ）→ §7 ズレ#6・#8。

### 5.2 提案する仕様文ドラフト（§2.3.3.2「地物移動ツール（平行移動）」line 674-678 の置換）

- **地物移動ツール（平行移動）**:
    - 編集モードのサブモードとして「地物移動」を提供する。ツールバーの「地物移動」ボタンで ON/OFF を切り替え、頂点編集サブモードと排他とする。編集モードを離れると自動的に解除される。
    - 地物移動サブモード中は頂点ハンドル・エッジハンドルを表示せず、頂点単位の選択・編集・矩形選択はできない。
    - アクティブ地物として選択済みの地物**自身の上**でドラッグを開始した場合にのみ移動を開始する。他の地物や何もない場所でのドラッグでは移動しない。穴/飛び地の描画中・分断線の入力中は移動を開始しない。
    - 移動対象は、現在時刻の有効な歴史の錨が参照する全頂点、および共有頂点グループを介してそれらと連動する全頂点とする。共有先の地物の形状も同時に平行移動する。
    - 移動量は全対象頂点へ同一ベクトルで適用する。緯度は ±90 度の範囲へ制限する。
    - ドラッグ中は移動後の形状をリアルタイムでプレビュー表示する。地物移動ではエッジ滑り（頂点編集時の衝突回避動作）は適用しない。
    - ドロップ時、移動後の形状が幾何制約（リングの自己交差、穴・飛び地の階層関係、末端地物排他〔地図全体・現在時刻〕）のいずれかに違反する場合、**移動全体を拒否して**元の位置へ戻し、理由を通知する。部分的な移動は行わない。
    - 移動は「頂点座標の時間非依存」原則（ドラフトD-2）に従い、対象頂点群を参照するすべての時刻の形状へ波及する。
    - 移動は単一のアンドゥ単位として記録する。移動量が実質ゼロのドロップは編集として記録しない。

### 5.3 根拠

- サブモード・排他・ハンドル非表示: `src/renderer/App.svelte:968-984`（`setEditInteractionMode`/`onToggleFeatureMove`）、:955-957（編集モード離脱時に 'vertex' へ）、:2256（`showVertexHandles={toolMode === 'edit' && editInteractionMode !== 'featureMove'}`）、`src/renderer/infrastructure/rendering/editInteractionUtils.ts:3`（`EditInteractionMode = 'vertex' | 'featureMove'`）
- 開始条件: `editInteractionUtils.ts:65-72`（`shouldStartFeatureDrag`: edit×featureMove×選択済×hit一致×リング/ナイフ描画中でない）
- 移動対象と共有連動: `src/renderer/presentation/app/appFeatureDragging.ts:47-72`（現在時刻の錨頂点＋共有リンク先を収集）、:119-153（プレビューの平行移動と `clampLatitude`）
- 確定と検証: `src/renderer/application/commands/MoveFeatureCommand.ts`（execute 内で移動後座標の仮マップを作り `validatePolygonFeatureIdsOrThrow` — 自己交差・リング検証・末端排他〔現在時刻〕。throw で移動全体が不成立）、`App.svelte:1613-1651`（`commitFeatureDrag`: `hasFeatureDragMoved` が偽なら記録しない、例外は `validationMessage` へ）
- エッジ滑りが単頂点ドラッグ専用であること: `App.svelte:663-666`（`resolveVertexDragCoordinate` は複数頂点移動時に素通し）、地物ドラッグ経路（:1937-1947）はエッジ滑り解決を経由しない
- **ズレ注記**: 現行実装は移動時に歴史の錨を更新しない（`MoveFeatureCommand` は Vertex 座標と共有グループ代表座標のみ変更）。line 678 の「歴史の錨を更新し」は頂点共有アーキテクチャ（座標は錨の外にある）と両立しないため、本ドラフトは「錨を更新する」記述を撤去し D-2 の時間非依存原則へ置き換えた。また検証は**現在時刻のスライスのみ**で行われるため、同じ頂点を過去・未来の錨が別リング構成で参照している場合に他時刻で排他違反が生じ、「保存できるが開けないファイル」（B34 と同型）に到達する可能性がある（**unknown** — 未実証。B34 検討と同時に確定推奨。§7 ズレ#6 備考）。

---

## 6. トピック間で共通の実装状況注記（§2.2.3 の起動配線）

要件の多数の箇所（line 544, 553, 564, 678, 683, 687-689, 705, 724, 727, 733, 742, 777, 789, 804）にある「完了時に『2.2.3 編集時の時間的・空間的整合性維持プロセス』をトリガーする」は、現行実装では**保存前の直接検証（違反時は操作拒否）**で代替されており、競合解決ダイアログへの完全配線は未完である:

- 競合解決ダイアログの UI は実装済みだが、確定処理がスタブ（`src/renderer/App.svelte:1470-1474` `onConflictCommit`「競合解決の適用は後続の Resolve UseCase と連携して行う」）。
- 4-7設計 §5-§6 は「検出ゲート先行（検出が非空なら拒否、ダイアログは defer）」への計画縮小を提案中（USER 判断待ち = INDEX §5 判断#8）。
- **本ドラフトの扱い**: 各操作の「2.2.3 をトリガーする」という要件記述は**最終仕様として維持**し、変更を提案しない。ただし採用判断の際、4-7設計の②案（検出ゲート先行）が確定した場合は、§2.2.3 line 448 の後に「競合が検出されなかった場合、本プロセスは検証のみで完了する（ダイアログは表示されない）」の1文を追記することを推奨する（これは 4-2b/4-4b/4-6 で確立した読み line 91〔4-7設計 §5〕の明文化であり、実装の現状とも一致する）。

---

## 7. 実装と要件のズレ一覧（B15「ズレが生じている可能性」の実測結果）

| # | ズレ | 要件側 | 実装側（実測） | 本ドラフトの扱い |
| --- | --- | --- | --- | --- |
| 1 | プロパティ編集がモード非依存 | §2.4.3 line 833「編集機能を統合する（編集モード時）」 | 表示モードでも編集可（App.svelte:2288-2305, 491-507 にモードゲートなし） | ドラフト1で「表示=閲覧専用」を仕様化（実装修正を想定。逆採用も可、USER判断） |
| 2 | 結合対象の選択方法 | line 323「Shiftキー+クリック」 | 「結合対象」トグルボタン／コンテキストメニュー（EditToolbar.svelte:100-127, App.svelte:1318-1337, 2007） | ドラフトB-2で実装挙動へ仕様を同期（置換） |
| 3 | 所属変更の複数選択 | line 286「対象地物（複数選択可）」 | 単一のアクティブ地物のみ起点（App.svelte:1394-1397）。複数一括UIなし | 要件が正・実装未達として残す（仕様変更しない）。連邦化UXの実装課題として現状.md 追記を推奨 |
| 4 | 共有解除 | line 212-213・§2.3.3.3 line 798-799 | ドメイン実装済みだが UI ハンドラがスタブ＝未接続（App.svelte:2001-2003） | 要件が正・実装未達（仕様変更しない） |
| 5 | 自動共有化抑制 | line 214-216 | `UnmergeSuppression` 実装済みだが未配線（SharedVertexService.ts:482、使用箇所なし） | 要件が正・実装未達（仕様変更しない） |
| 6 | 頂点移動・地物移動と歴史の錨 | line 678, 705, 724「歴史の錨が更新され」＋ §2.2.2「未来の錨の保護」 | 錨は更新されず、頂点座標の変更が全時刻へ波及（VertexEditUseCase.ts:38-46, MoveFeatureCommand, MergeService.ts:205-211 doc） | **ドラフトD-2 で「頂点座標の時間非依存」を明文化し、未来錨保護の対象を状態定義に限定**（実装アーキテクチャを正とする。座標にも保護を及ぼすなら頂点の錨別複製が必要になり共有頂点の意味論が壊れるため非推奨）。備考: 他時刻スライスの排他検証は未実施であり「保存できるが開けないファイル」への到達可能性が unknown（B34 と同時確定推奨） |
| 7 | 頂点追加・削除時の錨分割 | §2.2.2.1 line 396「同時刻の錨が存在しない場合…新しい錨を追加」 | 有効錨を in-place 置換（VertexEditUseCase.ts:87-89, 146-149, 261-273）。過去開始の錨を編集すると形状変化が錨開始時刻まで遡及する | 要件が正・実装未達と判断（`UpdateFeatureAnchorUseCase.splitAnchorAt` は存在するが頂点編集経路から未使用）。仕様変更しない。ただし D-2 採用時は「頂点ID列を変える操作は編集時刻で錨を分割する」ことがD-2の前提になるため、実装課題として明示を推奨 |
| 8 | §2.2.3 の起動配線 | 多数箇所「プロセスをトリガーする」 | 直接検証＋拒否で代替、ダイアログ確定はスタブ（App.svelte:1470-1474） | §6 のとおり最終仕様は維持。検出ゲート先行②確定時のみ1文追記 |
| 9 | 結合の対象範囲 | line 316「末端地物・集約地物のいずれでも」 | 末端のみ・同一上位領域のみの暫定制約（MergeService.ts:92-109） | 要件が正・Phase 4-6/4-7 で解消予定（仕様変更しない） |
| 10 | 測量モードのパン | 規定なし（line 55-58 は表示/追加/編集のみ） | 中ボタンのみ（MapCanvas.svelte:391-393） | ドラフト1で補完 |

---

## 8. 挿入先対応表

| ドラフト | 内容 | 挿入先（要件定義書.md） | 挿入方法 |
| --- | --- | --- | --- |
| ドラフト1（§1.2） | 操作モード体系・対照表 | §2.3 冒頭（新設 §2.3.0「操作モード」、line 513 の直後） | 新設。§2.3.2 line 537 の排他記述は「4モードは相互排他（2.3.0節参照）」へ簡略化 |
| ドラフトB-1（§2.2） | 結合結果の有効期間・将来錨の破棄 | §2.1「合体（結合）機能」共通フロー末尾（line 350 の後） | 追記 |
| ドラフトB-2（§2.2） | 結合の操作方法 | §2.1「操作方法」line 321-324 | 置換 |
| ドラフトB-3（§2.2） | 結合 vs 所属変更の使い分け対照表 | §2.1「所属変更フローとの違い」line 320 の直後 | 追記 |
| ドラフト3（§3.2） | 所属変更ダイアログ UI 仕様 | §2.3.3.2「所属変更ツール」末尾（line 742 の後） | 追記 |
| ドラフトD-1（§4.2） | スナップ確定規則・グループ整理 | §2.1「共有頂点システム」内（line 205 付近と line 213 の後） | 追記 |
| ドラフトD-2（§4.2） | 頂点座標の時間非依存原則 | §2.2.2 末尾（line 391 の後）に独立項として | 新設（§2.1 共有頂点・§2.3.3.2 各編集ツールから参照） |
| ドラフト5（§5.2） | 地物移動ツール詳細 | §2.3.3.2「地物移動ツール（平行移動）」line 674-678 | 置換 |
| §6 の1文 | 競合ゼロ時のプロセス完了の明文化 | §2.2.3 line 448 の後 | **4-7設計②案の確定後のみ**追記 |
| 用語集への追加 | 「操作モード」「地物移動サブモード」「頂点座標の時間非依存」 | §6 用語集 | 採用ドラフトに応じて追加 |

---

*作成: Claude Fable 5、2026-07-08 系列（実作成 2026-07-10）。READ-ONLY 調査（要件定義書.md 全文、現状.md §1.3/§2.2/§5、提案3本、toolMachine/toolStore/App.svelte/MapCanvas/EditToolbar/MergeService/ReassignFeatureParentUseCase/HierarchyService/SharedVertexService/VertexEditUseCase/MoveFeatureCommand/MoveVertexCommand/appFeatureDragging/editInteractionUtils/polygonValidation/ParentTransferDialog を精読）。本ファイル新規作成以外、リポジトリへの変更は一切なし。*
