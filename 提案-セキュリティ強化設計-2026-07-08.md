# 提案 — gimoza 配布前セキュリティ強化設計（S1–S5、2026-07-08）

> **位置づけ**: 未確定の提案。作者本人の依頼による**防御目的**（自作 Electron アプリを配布前に堅牢化）。作成は Claude Fable 5 subagent、2026-07-08。本ファイル新規作成以外、既存ファイルの変更・削除は一切していない（**READ-ONLY 調査＋設計のみ、パッチ非適用**）。
> **対象**: `提案-次の一手-2026-07-07.md` §2「優先度0 セキュリティ4点セット」の設計具体化。図鑑屋レビュー（06-14）の 🔴 S1–S4 と、`timemap-ipc-hardening.md` が追加指摘した S5 を扱う。
> **根拠**: 全主張に file:line を付す。実機挙動の未確認箇所は `unknown` を明記する。

---

## 0. 要約（先に結論）

- **S1–S5 はすべて現物で実在を確認**（§1）。
- **既製パッチ2本は現行コードにドリフトなくクリーンに当たる**（§2）。ヌケモレ点検（07-07）の「手紙以後コード無変更」は正しい。文脈行は期待行番号どおり一致。
- **重要な訂正**: 現状.md / 次の一手メモは「S3/S4 にはパッチが無い」とするが、**精読すると S3・S4・S5 はすべて既製2本のパッチ内に実装済み**（§2.3）。新規に一から設計する必要はなく、本書は「パッチの設計を検証し、残ギャップを補う」立場を取る。
- **新規指摘（パッチが塞げていない残ギャップ2件）**（§6）:
  - **G1（要修正）**: S1 パッチの許可リストは `.gimoza` の **OS ファイル関連付け／CLI 引数起動**経路を祝福しないため、ダブルクリックでの起動が**壊れる**。
  - **G2（要検証）**: S4 CSP の `connect-src 'self'` × パッケージ版 `file://` オリジンで、プリセット地図の `fetch()` が通るか実機未確認。**完了ゲートの中核リスク**。
- **完了ゲートは「コードが直った」ではなく「配布物を生成しインストーラ経由の初回起動を通した」**（§8）。現状.md:56「初回起動体験」の未完了項に直結。

---

## 1. 対象コード現況照合（実在確認）

| ID | 脆弱性 | 現在の file:line | 実在 | 要点 |
|---|---|---|---|---|
| S1 | filesystem IPC が任意絶対パス受理 | `src/main/ipcHandlers.ts:57-112` | ✅ | `file:read/write/readBinary/writeBinary/append/exists/list/delete` の8ハンドラが renderer 由来の `filePath` を無検証で `fs.*` に渡す |
| S2 | SVG `{@html}` 注入＋正規表現サニタイザすり抜け | `src/renderer/presentation/components/mapCanvasUtils.ts:122-127`（サニタイザ）／同 `MapCanvas.svelte:328,332`（受渡）／`mapCanvas/MapCanvasSvgLayers.svelte:122`（`{@html}`） | ✅ | denylist 3正規表現。未クォート `onload`、entity化 `javascript:`、`<foreignObject>`/`<animate>` 等をすり抜け |
| S3 | `shell.openExternal` にスキーム検証なし | `src/main/window.ts:84-87` | ✅ | `setWindowOpenHandler` が全 URL を無検証で `openExternal`。`file:`/`smb:` 等が OS ハンドラへ |
| S4 | CSP 皆無 | `src/renderer/index.html:1-12`（meta なし）／`src/main/index.ts` 全体（`session`/`onHeadersReceived` なし） | ✅ | インライン script が無制限。S2 すり抜け時の二段目防御が存在しない |
| S5 | `webPreferences` 未ピン留め | `src/main/window.ts:72-75` | ✅ | `{ preload, sandbox:false }` のみ。`contextIsolation`/`nodeIntegration` は Electron 41 既定任せ（現状は安全だが未固定） |

**S1×S2 連鎖の実在確認**: renderer が SVG 由来 XSS で乗っ取られると、`window.api.readFile/writeFile/deleteFile`（`src/preload/api.ts:27-42`）経由で S1 の無制限 IPC を叩ける。データフロー実物:

- 攻撃投入面: プロジェクト同梱 SVG → `MapCanvas.svelte:345` `fetch(url)` または `.gimoza` 内の base-map 文字列 → `applyParsedBaseMap`（`MapCanvas.svelte:327-335`）→ `parseSvgMap`（`mapCanvasUtils.ts:129`）→ `sanitizeSvgContent`（同:141 で呼出、実体 122-127）→ `baseMapContent`（`MapCanvas.svelte:332`）→ `MapCanvasSvgLayers` へ props（`MapCanvas.svelte:653-655`）→ `{@html baseMapContent}`（`MapCanvasSvgLayers.svelte:122`）。
- S5 の `sandbox:false` は preload に Node を許すが、preload 実体（`src/preload/index.ts:1-4`, `api.ts` 全体）は `contextBridge`/`ipcRenderer` しか使わない → **`sandbox:true` へ引き上げ可能**（実行可能性確認済み）。

---

## 2. 既製パッチ2本の適用可否（ドリフト照合）

検証対象（読むだけ・適用しない）: `E:\WORKSPACE\file-zukan\_workshop-2026-06-14\engineering\patches\` の `timemap-ipc-hardening.md`・`timemap-svg-xss.md`。

### 2.1 timemap-ipc-hardening（S1 + S3 + S5）— ドリフト無し／クリーンに当たる

| パッチが触る箇所 | パッチ期待の文脈 | 現行実物 | 判定 |
|---|---|---|---|
| `ipcHandlers.ts` import | `import { dirname, join } from 'path';` | `:1` 同一 | ✅ |
| `FsLike` 直後に helper 挿入 | `interface FsLike {…}` 末尾 | `:35-45` 同一 | ✅ |
| `createIpcHandlers` 冒頭に policy 生成 | `}): Record<…> {` → `return {` | `:55-56` 同一 | ✅ |
| 各 `file:*` に `assertAllowedPath` | 8ハンドラ本体 | `:57-112` 全て文脈一致 | ✅ |
| `dialog:open/save` に `allowDirectory` | `return canceled ? …` | `:133,140` 同一 | ✅ |
| `window.ts` webPreferences | `sandbox:false` 他 | `:72-75` 同一 | ✅ |
| `window.ts` openExternal gate | `setWindowOpenHandler((details)=>{ void shell.openExternal… })` | `:84-87` 同一 | ✅ |

**結論**: 文脈行は期待行番号どおり一致。`git apply --check` は通る見込み（ヌケモレ点検の「手紙以後コード無変更」を裏付け）。

**ただし付随して必須の既存テスト更新（パッチ自身が明記、現況でも真）**:
- `tests/electron/window.test.ts:82` が `sandbox: false` を `toMatchObject` で固定 → `sandbox:true` へ1行更新が必要（**確認**: 現に82行が `sandbox: false,`）。
- `tests/electron/ipcHandlers.test.ts` の正常系フィクスチャが**シード根の外**を使うため落ちる（**確認**）: `/mock/save/world.json`（:56）、`/mock/logs/gimoza.log`（:69）、`/mock/save/world.gimoza`（:83）、`/mock/savebackup`（:101）、`dialog:open` 期待値 `/mock/save/world.json`（:126,134）。シード根は `getAppPath()→/mock/app`（:17）から `/mock/app/savebackup`・`/mock/app/logs` なので、これらは含有判定に落ちる。→ フィクスチャを in-root パスへ機械的に修正、またはダイアログ祝福を通す前処理が必要。

### 2.2 timemap-svg-xss（S2 + S4）— ドリフト無し／クリーンに当たる

| パッチが触る箇所 | パッチ期待の文脈 | 現行実物 | 判定 |
|---|---|---|---|
| `mapCanvasUtils.ts` サニタイザ置換 | `@@ -119,12` の `: null;` / `}` / `function sanitizeSvgContent…` | `:119-127` 同一（サニタイザ実体 122-127） | ✅ |
| `index.html` CSP meta 追加 | `<meta charset> … <title>` | `:4-5` 同一（meta 挿入位置あり） | ✅ |
| `main/index.ts` import に `session` | `import { app, …, ipcMain, shell } from 'electron';` | `:1` 同一 | ✅ |
| `main/index.ts` CSP 関数挿入 | `sendProjectPathToWindow…}` → `function registerMainProcessAdapters` | `:32-35` 同一 | ✅ |
| `main/index.ts` `installContentSecurityPolicy()` 呼出 | `app.whenReady().then(()=>{ registerMainProcessAdapters();` | `:93-94` 同一 | ✅ |

**結論**: 文脈一致。テスト非依存（新規テストは追加のみ、既存テスト破壊なし）。CSP の `style-src 'self' 'unsafe-inline'` は Svelte5+Vite のランタイム inline style のため必須で妥当。`script-src 'self'`（`unsafe-inline` なし）が S2 すり抜け時の二段目防御。

### 2.3 パッチ被覆マップ（現状.md 記述への訂正）

現状.md §5 系・次の一手メモは「S3/S4 にはパッチが無い」とするが、**パッチ内容を読むと以下のとおり全件が既製2本で被覆される**:

| ID | 被覆パッチ | 実装箇所（パッチ内） |
|---|---|---|
| S1 | timemap-ipc-hardening | `createPathPolicy` + 各 `file:*` に `assertAllowedPath` |
| S2 | timemap-svg-xss | 構造的許可リスト式 `sanitizeSvgContent` 全面置換 |
| S3 | timemap-ipc-hardening | `setWindowOpenHandler` に `new URL()`＋`https/http/mailto` gate |
| S4 | timemap-svg-xss | `index.html` の CSP meta ＋ `main` の `onHeadersReceived` 注入 |
| S5 | timemap-ipc-hardening | `contextIsolation:true`/`nodeIntegration:false`/`sandbox:true` ピン留め |

→ **「S3/S4/S5 を新規設計」ではなく「既製実装の設計妥当性検証＋残ギャップ補完」が実態**。以降 §3–§5 はパッチの設計を追認・評価し、§6 でパッチが塞げていない残りを新規に指摘する。

---

## 3. S3 設計（openExternal スキーム許可リスト）— パッチ追認

**目標状態**（timemap-ipc-hardening が実装、`window.ts:84-87` を置換）:

```
setWindowOpenHandler((details) => {
  try {
    const { protocol } = new URL(details.url);
    if (protocol === 'https:' || protocol === 'http:' || protocol === 'mailto:') {
      void shell.openExternal(details.url);
    }
  } catch { /* 不正URLはOSに渡さない */ }
  return { action: 'deny' };
});
```

**設計要点**: `new URL()` で解析→`protocol` 完全一致の許可リスト（`https:`/`http:`/`mailto:` のみ）→それ以外・パース失敗は握りつぶして `openExternal` を呼ばない。`action:'deny'` は常に返しアプリ内新窓を禁止。`file:`/`smb:`/`javascript:`/`data:`/カスタムスキームは全拒否。**評価**: 妥当。追加推奨は無し（`mailto:` を外すとより厳格だが、アプリが About 等でメールリンクを開くなら残す。現状 `mailto` 利用は `unknown` — 使わないなら `https`/`http` のみに絞ってよい）。

---

## 4. S4 設計（CSP）— パッチ追認＋file:// リスク明示

**目標ポリシー**（timemap-svg-xss が meta＋header の二層で実装）:

```
default-src 'self';
img-src 'self' data:;
font-src 'self' data:;
style-src 'self' 'unsafe-inline';   ← Svelte5/Vite ランタイム inline style のため必須
script-src 'self';                  ← unsafe-inline 無し = S2 二段目防御の要
connect-src 'self';                 ← base-map.svg の fetch がここに依存（後述）
object-src 'none'; base-uri 'none'; frame-src 'none'; form-action 'none';
```

**各ディレクティブ設計根拠**（このアプリ固有）:
- `{@html baseMapContent}`（`MapCanvasSvgLayers.svelte:122`）は**インライン SVG 要素**を DOM 構築する。これは `img-src` ではなく DOM 挿入なので、防御は S2 サニタイザが主・`script-src`（inline JS 禁止）が二段目。
- SVG 内 `<image href>` は `img-src` 管轄 → `'self' data:` で `data:` は許容・外部 URL（追跡ビーコン等）は遮断。
- **`connect-src 'self'` が load-bearing**: プリセット地図は `MapCanvas.svelte:345` の `fetch('./assets/maps/base-map.svg')` で取得する（DOM 埋め込みではなく HTTP 取得）。`fetch` は `connect-src` 管轄。
- **meta 方式 ＋ `onHeadersReceived` 方式の両建て**が正しい: パッケージ版は `loadFile`（`window.ts:92`）で meta が効くが、レスポンスヘッダにも同 CSP を載せ多層化。dev は `loadURL`（`window.ts:90`）でヘッダ注入側が主。
- **preload/contextIsolation との兼ね合い**: CSP は renderer 世界のみに掛かり preload/main には無関係。`contextIsolation:true`（S5）で `window.api` は隔離ブリッジ経由のまま、CSP と独立に両立。

**評価と G2 リスク**: §6.2 参照。dev（`http://localhost:5173` オリジン）では `'self'` が素直に一致するが、パッケージ版の `file://` オリジンで `fetch('./…')`＋`connect-src 'self'` が通るかは Electron バージョン依存で**実機確認必須**（`unknown`）。ここが完了ゲートの中核。

---

## 5. S5 設計（webPreferences ピン留め）— パッチ追認

**目標状態**（timemap-ipc-hardening が実装、`window.ts:72-75` と型 `MainWindowOptions:15-18` を拡張）:

```
webPreferences: {
  preload: join(dirname, '../preload/index.js'),
  sandbox: true,            // false→true
  contextIsolation: true,   // 明示ピン
  nodeIntegration: false,   // 明示ピン
}
```

**設計要点**: 3値を Electron 既定任せにせず明示固定し、将来のバージョン/設定回帰でサイレントに危険側へ倒れるのを防ぐ。`sandbox:true` は preload が Node 非依存（`preload/index.ts:1-4`・`api.ts` が `contextBridge`/`ipcRenderer` のみ）なので安全に引き上げ可能。**評価**: 妥当。唯一の注意は §2.1 の `window.test.ts:82` 更新。もし将来 preload が Node 組込みを import したら `sandbox:true` は諦め、`contextIsolation:true`/`nodeIntegration:false` は維持する（パッチ APPLY 注記どおり）。

---

## 6. 未パッチの残ギャップ（新規指摘）

### 6.1 G1【要修正・配布機能を壊す】S1 許可リストが `.gimoza` ファイル関連付け起動を拒否する

**事実確認したデータフロー**:
1. `package.json:89-97` が `.gimoza` を `fileAssociations` 登録 → **ダブルクリック起動は正規の想定フロー**。
2. 起動パスの入口は3つ、いずれも**ダイアログを経ない**:
   - CLI/関連付け引数: `main/index.ts:19` `findProjectPathArg(process.argv)`
   - macOS OS open: `main/index.ts:88-91` `app.on('open-file', …)`
   - 起動済み二重起動(Windows): `main/index.ts:77-86` `second-instance`
3. 3経路とも `sendProjectPathToWindow`（`projectOpen.ts:29-47`）→ `webContents.send('app:openProjectPath', filePath)`。
4. renderer 受信: `App.svelte:902` `window.api.onOpenProjectPath((filePath)=>…)`。
5. その先で読込: `JSONWorldRepository.ts:161` `readFile: (filePath)=>window.api.readFile(filePath)` → `file:read` IPC（`ipcHandlers.ts:57`）。

**問題**: S1 パッチの許可リストは `savebackup`/`logs` ＋**ダイアログ祝福ディレクトリ**のみをシードする。上記の OS 関連付け／argv で来たパスは**どのダイアログも経ていない**ため許可リストに載らず、`assertAllowedPath` が `Path outside allowed roots` を投げ、**`.gimoza` ダブルクリック起動が失敗する**（回帰）。

**修正設計**: main プロセスは当該パスが OS/ユーザ起点で正当だと知る唯一の主体なので、**ダイアログ祝福と同じ原理で、`findProjectPathArg`／`open-file`／`second-instance` で受理したプロジェクトパスの親ディレクトリを許可リストに追加する**。実装候補（いずれも main 側、パッチの `allowDirectory` を再利用）:
- 案A（最小）: `createIpcHandlers` が返す `allowDirectory` を main に公開し、`queueOrSendProjectPath`（`index.ts:21`）／`whenReady` 初期 `pendingOpenProjectPath` 確定時に `allowDirectory(filePath)` を呼ぶ。
- 案B: `assertAllowedPath` に「拡張子が `.gimoza`/`.json` かつ main が発行した open トークンに一致するパス」を通す明示 API を足す。
- **推奨=案A**（既存 `allowDirectory` 再利用、面が最小、ダイアログ祝福と対称）。

**この修正が無いと完了ゲート（§8）の「ダブルクリック起動」チェックで必ず露見する** — だからこそゲートを実機初回起動に置く価値がある。

### 6.2 G2【要検証】CSP `connect-src 'self'` × パッケージ版 `file://` での base-map fetch

**リスク**: §4 のとおりプリセット地図は `fetch('./assets/maps/base-map.svg')`（`MapCanvas.svelte:345`）。dev は `http://localhost:5173` オリジンで `connect-src 'self'` に素直に乗る。だが**パッケージ版は `loadFile`（`window.ts:92`）で `file://` オリジン**になり、`file://` に対する `fetch`＋`'self'` の扱いは Electron/Chromium バージョンで差がある（`unknown`）。CSP が厳しすぎると**パッケージ版でだけプリセット地図が出ない**サイレント破損になり得る。

**同梱経路の追加注意**: `package.json:58-62` は `extraResources` で base-map.svg を exe 隣 `assets/maps/` へコピーするが、`fetch('./assets/maps/base-map.svg')` は renderer（asar 内 `out/renderer/`）**相対**で解決される。両者は別物 — 実際にどちらが取得されるか（Vite `public/` バンドル分か extraResources 分か）は**実機確認必須**。この不一致自体は CSP と直交だが、初回起動でプリセットが出るか＝両方の要因が絡むので、ゲートで一括確認する。

**検証設計**: 完了ゲート（§8）でパッケージ版初回起動時に (a) プリセット地図が描画される (b) DevTools Network に base-map.svg の取得成功 (c) Console に `Refused to connect`（connect-src 違反）が無い、を確認。もし違反が出たら緩和は最小に留める（例: `connect-src 'self' file:` を検討、ただし `file:` 追加はローカル読取面を広げるので、代替として base-map をビルド時に文字列 import して `fetch` を廃す方が CSP を締めたまま解決できる — これは描画変更を伴うため今回のスコープ外、`unknown`/将来課題）。

---

## 7. 適用順・非干渉性

**並行可能性**: 5件は main プロセス（`ipcHandlers.ts`/`window.ts`/`index.ts`）と presentation（`mapCanvasUtils.ts`/`index.html`）に閉じ、**階層モデル置換の主戦場（domain/application 層）とファイルが重ならない**（次の一手メモ §2「独立性」を実物で追認）。Phase 4-6/4-7 と衝突せず並行できる。

**推奨適用順**（依存の弱い順、各ステップ独立にテスト green を保つ）:
1. **S2+S4**（timemap-svg-xss）: 既存テスト非破壊で入れられる。renderer 単独で完結。
2. **S1+S3+S5**（timemap-ipc-hardening）: 同時に §2.1 の既存テスト更新（`window.test.ts:82`、`ipcHandlers.test.ts` フィクスチャ）を必ずセットで。
3. **G1 修正**（§6.1 案A）: S1 適用と同一変更単位で入れる（S1 を入れた瞬間に関連付け起動が壊れるため、S1 と G1 は不可分に扱う）。
4. **G2 検証**（§6.2）: コード変更ではなくゲート（§8）で確認。違反時のみ最小緩和。

**非干渉性の担保**: 上記いずれも `domain/`・`application/` に触れないため、B34 ロード時正規化（次の一手メモ §2 の 1.5）や階層モデル置換と**マージ衝突しない**。唯一の共有面は `tests/electron/` の既存テスト更新のみ。

---

## 8. 完了ゲート手順（「配布物を生成し初回起動を通す」）

現状.md:56「初回起動体験｜中｜… `npm run dist:win` で配布物生成と base-map.svg 同梱設定は通過。**インストーラ経由の初回起動確認は未完了**」に接続。**完了条件はコード修正ではなく以下の実機通過**:

1. **ビルド**: `npm run check`（svelte-check/tsc）green → `npm run test`（Vitest、更新済み既存テスト＋新規テスト含む）green。
2. **配布物生成**: `npm run dist:win`（`package.json:21` = build + electron-builder --win、nsis＋portable、`asar:true`、base-map.svg を extraResources 同梱: `:58-62`）。`dist/` に NSIS インストーラ生成を確認。
3. **インストーラ経由インストール**: 生成 NSIS を実行し実際にインストール（`oneClick:false`/`allowToChangeInstallationDirectory:true`: `:82-84`）。dev ツリーからの直接起動ではなく**インストール済みバイナリ**で以降を実施。
4. **初回起動 — 描画ゲート（S4/S5/G2）**: 初回起動でウィンドウ表示 → **プリセット base-map が描画される**こと（= `fetch`＋CSP `connect-src`＋`sandbox:true` preload が全て健全）。DevTools Console に CSP 違反（`Refused to connect/execute`）が無いこと。
5. **関連付け起動ゲート（S1/G1）**: `.gimoza` ファイルをエクスプローラで**ダブルクリック** → 正常に開く（G1 未修正だとここで `Path outside allowed roots` により失敗＝ゲートで露見）。
6. **ダイアログ経路ゲート（S1）**: アプリ内「開く」ダイアログから `.gimoza` を開く／「保存」で任意ディレクトリへ保存 → 成功（ダイアログ祝福が機能）。
7. **敵対 SVG ゲート（S2/S4）**: `<script>`／未クォート `onload`／`<foreignObject><img onerror>`／entity 化 `javascript:` を仕込んだ base-map を持つ `.gimoza` を開く → **スクリプト非実行**（`alert` 出ない・Console クリーン）、かつ正規ジオメトリは描画される。
8. **外部リンクゲート（S3）**: アプリ内から `https:` リンクは既定ブラウザで開く／`file:` 等は無反応（`openExternal` されない）を確認。

**ゲート合格 = 配布可**。不合格箇所（特に 4・5）はコードへ差し戻し。

---

## 9. 回帰テスト観点

**自動（Vitest、パッチ同梱の新規＋更新）**:
- `tests/electron/ipcHardening.test.ts`（新規、ipc-hardening 同梱）: シード根内許可・根外拒否（`/etc/passwd` 等）・`..` traversal 拒否・兄弟接頭辞（`/mock/appEVIL`）非通過・ダイアログ祝福後の書込許可・セッション間非漏洩。
- `tests/presentation/components/mapCanvasUtils.svgSanitize.test.ts`（新規、svg-xss 同梱）: 敵対 SVG 各ベクタの無害化＋良性ジオメトリ保存＋viewBox 抽出契約維持。
- **更新必須の既存**（§2.1）: `window.test.ts:82`（`sandbox:true`）、`ipcHandlers.test.ts:56/69/83/101/126/134`（in-root パスへ）。
- **G1 用に追加すべき観点**（本書の新規指摘）: 「main が argv/`open-file` で受理したプロジェクトパスが `file:read` で通る」テスト。案A を採るなら `allowDirectory` 呼出後に当該パス読取が resolve することを固定。

**手動/実機**（§8 と重複、自動化困難な面）:
- CSP × `file://` の base-map fetch（G2、パッケージ版でのみ再現）。
- `.gimoza` ダブルクリック関連付け（G1、OS 統合面）。
- `openExternal` の実ブラウザ起動（S3）。

**既存テストへの波及確認**: `preloadApi.test.ts:12` は `listFiles(['savebackup'])→file:list` のチャネル配線のみ検証で allow-list 非依存 → 影響なし。`closeGuard.test.ts`/`projectOpen.test.ts` はスコープ外 → 影響なし。

---

## 10. 未確定事項（unknown）

- **U1（G2）**: パッケージ版 `file://` オリジンで `fetch('./assets/maps/base-map.svg')` が `connect-src 'self'` を通るか。Electron 41 実機で要確認。違反時の最小緩和策も未確定。
- **U2**: プリセット取得が Vite `public/` バンドル分（asar 内）と `extraResources` 分（exe 隣）のどちらに解決されるか。`electron.vite.config` 未読。
- **U3**: アプリが `mailto:` リンクを実際に開く UI を持つか（持たなければ S3 許可リストを `https`/`http` のみに締められる）。
- **U4**: G1 修正の最終形（案A/案B）。本書は案A推奨だが、main 側の open 経路配線は作者判断。
- **U5**: base-map を `fetch` 廃止→ビルド時文字列 import へ替える将来案（CSP を締めたまま G2 を根絶）はスコープ外・未設計。

---

*作成: Claude Fable 5 subagent、2026-07-08。READ-ONLY 調査＋設計のみ。本ファイル新規作成以外の変更なし。全 file:line は 2026-07-08 時点の現物実測。パッチ2本は `file-zukan\_workshop-2026-06-14\engineering\patches\` を検証用に読んだのみ（適用せず）。*
