# テスト改善計画

> 2026-04-09 作成。テスト品質の底上げとエッジケースカバレッジの強化を目的とする。

## 0. 方針

### 0.1 カバレッジ目標

業界のベストプラクティス（Google Engineering Practices, Martin Fowler "TestCoverage"）を参考に、
レイヤー別に目標を設定する。100%は保守コスト対効果が悪いため目指さない。

| レイヤー | 目標 | 根拠 |
| --- | --- | --- |
| ドメイン層（entities / value-objects / services） | 90%+ | 純粋ロジック。副作用なし。最もテストしやすく、バグの影響が大きい |
| アプリケーション層（use cases / commands / queries） | 85%+ | ビジネスフロー。ドメイン層を組み合わせるため結合テスト的性質 |
| インフラ層（rendering / persistence / config） | 75%+ | 外部依存あり。間接カバレッジ（上位テスト経由）を含めて評価 |
| プレゼンテーション層（utils / state machines） | 80%+ | 純粋関数部分はユニット、Svelteコンポーネントは E2E で補完 |
| Electron / Preload | 60%+ | エントリポイントは E2E 依存。DI化済みハンドラはユニットで検証 |

### 0.2 テスト追加の方法

- **追加予定のテスト**: `it.todo('説明')` で宣言し、テストスイートを green のまま維持
- **バグを発見したテスト**: テストを書いて失敗を確認 → `it.skip` にして `現状.md` に不具合登録
- **正常に通るテスト**: そのままコミット

### 0.3 テストの質についての指針

テストは「数」よりも「検出力」が重要。以下のパターンを優先する:

1. **境界値**: 0, 1, 上限, 上限+1, 負数, NaN, Infinity
2. **空/null**: 空配列, 空Map, null/undefined, 空文字列
3. **型の網羅**: union型（Point/LineString/Polygon）の全分岐
4. **状態遷移**: 初期状態, 遷移直後, 連続操作, 不正遷移
5. **相互作用**: 複数機能の組み合わせ（例: 共有頂点 + ポリゴン検証）

---

## 1. 現状サマリー

- ユニットテスト: **1228件** / 81ファイル / 全パス
- E2Eテスト: **173件** / 22ファイル / 全パス
- テスト対象ソースファイル: 約85件（index.ts / 型定義のみのファイルを除く）
- テスト済みファイル: 81件（ファイル単位カバレッジ: 約95%）

### 1.1 レイヤー別現状

| レイヤー | テスト済 / 対象 | テスト数 | 評価 |
| --- | --- | --- | --- |
| ドメイン entities | 6/6 | 51 | 良好 |
| ドメイン value-objects | 5/5 | 59 | 良好 |
| ドメイン services | 12/13 | 341 | PolygonValidationService 未テスト |
| アプリケーション use cases | 11/11 | 208 | 良好 |
| アプリケーション commands | 7/7 | 65 | 良好 |
| アプリケーション queries | 1/1 | 4 | テスト数が少ない |
| アプリケーション utils | 0/1 | 0 | polygonValidation.ts 未テスト |
| インフラ core | 4/4 | 76 | 良好 |
| インフラ persistence | 2/2 | 40 | 分割後モジュールは間接カバレッジ |
| インフラ rendering | 16/16 | 229 | 優秀 |
| プレゼンテーション state | 4/4 | 105 | 優秀 |
| プレゼンテーション utils | 7/7 | 45 | テスト数が少ないファイルあり |
| プレゼンテーション app | 2/2 | 4 | テスト数が少ない |
| Electron / Preload | 2/2 | 13 | 良好 |

---

## 2. 未テストファイル（ファイル単位ギャップ）

### 優先度 高

| ファイル | 行数 | 理由 |
| --- | --- | --- |
| `domain/services/PolygonValidationService.ts` | 85 | 自己交差・リング階層・競合検出を統合する検証オーケストレータ。他サービスの結合テスト的価値 |
| `application/polygonValidation.ts` | 128 | 一時的 Feature 生成、頂点→地物逆引き、バリデーション+例外送出。UI操作の安全弁 |

### 優先度 低（テスト不要 / 間接カバレッジ十分）

| ファイル | 理由 |
| --- | --- |
| `application/AnchorEditDraft.ts` | 型定義のみ（interface / type）。ロジックなし |
| `persistence/jsonSerializerErrors.ts` | Error サブクラス1つ。JSONSerializer.test.ts で間接テスト済み |
| `persistence/jsonSerializerTypes.ts` | 型定義のみ。ロジックなし |
| `persistence/jsonSerializerTransforms.ts` | JSONSerializer.test.ts（33件）で全関数が間接テスト済み |
| `persistence/jsonSerializerValidation.ts` | 同上。間接テスト済み |
| `domain/repositories/WorldRepository.ts` | インターフェース定義のみ |
| 各 `index.ts` | re-export バレル。ロジックなし |

---

## 3. エッジケース不足（既存テストの改善）

### 3.1 プレゼンテーション層

#### `appSelection.ts`（現在2件 → 目標8件、+6件）

| # | 種別 | テスト内容 | 状態 |
| --- | --- | --- | --- |
| A1 | 分岐網羅 | `buildPropertyPanelSelectionState`: selectedFeatureId 指定時に `kind: 'empty'` を返す | todo |
| A2 | 分岐網羅 | 同: `vertexSelectionContext.kind === 'single'` で `kind: 'empty'` を返す | todo |
| A3 | 分岐網羅 | 同: `kind === 'unknown'` で `kind: 'unknown'` を返す | todo |
| A4 | 境界値 | 同: featureIds が6件以上のとき featureSummaries が5件 + remainingCount が正しい | todo |
| A5 | 型網羅 | `collectAnchorVertexIds`: Point 型（1頂点）を返す | todo |
| A6 | 型網羅 | `collectAnchorVertexIds`: LineString 型（複数頂点）を返す | todo |

#### `appProjectSettings.ts`（現在2件 → 目標7件、+5件）

| # | 種別 | テスト内容 | 状態 |
| --- | --- | --- | --- |
| B1 | 境界値 | `normalizeWorldSettings`: zoomMin === zoomMax のとき等値を維持 | todo |
| B2 | 境界値 | 同: 全フィールドが正常値ならそのまま返す（恒等性テスト） | todo |
| B3 | 分岐 | `hasProjectSettingsChanged`: customPalettes の長さだけ異なる場合 true | todo |
| B4 | 分岐 | 同: customPalettes の中身だけ異なる（長さ同じ）場合 true | todo |
| B5 | 境界値 | `areLayersEqual`: 空配列同士は true | todo |

#### `mapCanvasUtils.ts`（現在4件 → 目標9件、+5件）

| # | 種別 | テスト内容 | 状態 |
| --- | --- | --- | --- |
| C1 | 型網羅 | `getAnchorVertexCount`: Point 型で 1 を返す | todo |
| C2 | 型網羅 | 同: LineString 型で vertexIds.length を返す | todo |
| C3 | 境界値 | `normalizeVertexMarkerDisplayLimit`: 負数入力で 1 を返す | todo |
| C4 | 境界値 | `formatSurveyDistance`: ちょうど 100km で整数表示 | todo |
| C5 | 境界値 | `normalizeZoomLimits`: 同値入力（min === max）で同値を返す | todo |

### 3.2 アプリケーション層

#### `QueryServices.test.ts`（現在4件 → 目標10件、+6件）

| # | 種別 | テスト内容 | 状態 |
| --- | --- | --- | --- |
| D1 | 空入力 | `FeatureQueryService.getFeatures`: 地物ゼロ時に空 Map を返す | todo |
| D2 | コピー安全 | 同: 返り値の Map を変更しても内部状態に影響しない | todo |
| D3 | 空入力 | `LayerQueryService.getLayers`: レイヤーゼロ時に空配列 | todo |
| D4 | 存在しないID | `LayerQueryService.getLayerById`: 存在しない ID で undefined | todo |
| D5 | 境界値 | `TimelineQueryService.getCurrentTime`: 初期状態の時刻が正しい | todo |
| D6 | 複合 | `ProjectQueryService`: metadata と settings の両方が正しく返る | todo |

#### `polygonValidation.ts`（現在0件 → 目標10件、+10件）- **新規テストファイル**

| # | 種別 | テスト内容 | 状態 |
| --- | --- | --- | --- |
| E1 | 正常系 | `createTransientPolygonFeature`: 3頂点で Feature と vertices を生成 | todo |
| E2 | 境界値 | 同: coords の緯度がクランプされる（±90超） | todo |
| E3 | 正常系 | `collectImpactedFeatureIdsByVertexIds`: 該当頂点を持つ地物のみ返す | todo |
| E4 | 空入力 | 同: 該当なしで空配列 | todo |
| E5 | 型網羅 | 同: Point/LineString/Polygon 各型で正しく検出 | todo |
| E6 | 正常系 | `validatePolygonOrThrow`: 正常ポリゴンで例外なし | todo |
| E7 | 異常系 | 同: 自己交差で PolygonValidationError | todo |
| E8 | 異常系 | 同: リング階層不正で PolygonValidationError | todo |
| E9 | 異常系 | 同: 同レイヤー重複で PolygonValidationError（メッセージに相手ID含む） | todo |
| E10 | 型スキップ | `validatePolygonFeatureIdsOrThrow`: Line 型の地物はスキップ | todo |

### 3.3 ドメイン層

#### `PolygonValidationService.ts`（現在0件 → 目標6件、+6件）- **新規テストファイル**

| # | 種別 | テスト内容 | 状態 |
| --- | --- | --- | --- |
| F1 | 正常系 | 正常ポリゴンで `isValid: true` | todo |
| F2 | 型スキップ | 非ポリゴン地物（Point）で `isValid: true` を返す | todo |
| F3 | 異常系 | 自己交差リングで `selfIntersectingRingIds` に ID が入る | todo |
| F4 | 異常系 | リング階層不正で `ringValidationErrors` にエラーが入る | todo |
| F5 | 異常系 | 同レイヤー重複で `conflicts` にエントリが入る | todo |
| F6 | 境界値 | 頂点が存在しない ID を含む場合にエラー送出 | todo |

### 3.4 インフラ層

#### `featureRenderingUtils.test.ts`（現在31件 → +2件）

| # | 種別 | テスト内容 | 状態 |
| --- | --- | --- | --- |
| G1 | 境界値 | `wrapLongitudeToPrimaryRange`: ちょうど 180 / -180 の扱い | todo |
| G2 | 境界値 | 同: 0 の恒等性 | todo |

#### `DIContainer.test.ts`（現在4件 → +3件）

| # | 種別 | テスト内容 | 状態 |
| --- | --- | --- | --- |
| H1 | 安全性 | `createUnavailableFileSystem`: 各メソッドが例外を投げる | todo |
| H2 | 安全性 | `createDialog`: 各メソッドが例外を投げる | todo |
| H3 | 構造 | `getContainer` がシングルトンを返す | todo |

---

## 4. 実施計画

### フェーズ 1: エッジケース補強（優先度 高）

既存テストファイルへの追加。比較的短時間で実施可能。

| 対象 | 追加件数 | 推定工数 |
| --- | --- | --- |
| appSelection.test.ts | +6 | 小 |
| appProjectSettings.test.ts | +5 | 小 |
| mapCanvasUtils.test.ts | +5 | 小 |
| QueryServices.test.ts | +6 | 小 |
| **小計** | **+22** | |

### フェーズ 2: 新規テストファイル（優先度 高）

未テストの重要モジュールに対するテスト新規作成。

| 対象 | 追加件数 | 推定工数 |
| --- | --- | --- |
| polygonValidation.test.ts（新規） | +10 | 中 |
| PolygonValidationService.test.ts（新規） | +6 | 中 |
| **小計** | **+16** | |

### フェーズ 3: 補助的改善（優先度 低）

| 対象 | 追加件数 | 推定工数 |
| --- | --- | --- |
| featureRenderingUtils.test.ts | +2 | 小 |
| DIContainer.test.ts | +3 | 小 |
| **小計** | **+5** | |

### 合計

| フェーズ | 追加テスト数 |
| --- | --- |
| フェーズ 1 | +22 |
| フェーズ 2 | +16 |
| フェーズ 3 | +5 |
| **合計** | **+43** |

完了後の目標テスト数: **1271件以上**（バグ発見テストがskipになる分を除く）

---

## 5. 発見済みバグの追跡

テスト作成中に発見された不具合は、テストを `it.skip` にしたうえで `現状.md` に登録する。

| テストID | 対象 | 不具合概要 | 現状.md 記載 |
| --- | --- | --- | --- |
| （まだなし） | | | |

---

## 6. 定期レビュー

- テスト改善タスク完了後、本文書の「状態」列を `todo` → `done` / `skip` に更新する
- 新機能追加時は §3 に対応するエッジケースを追記する
- `it.todo` が残っている限り、本文書は `現状.md` からリンクされ続ける
