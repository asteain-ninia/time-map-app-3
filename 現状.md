# 現状.md — 未解決課題・既知不具合・残作業

> 完了した実装の履歴は `実装済み.md` を参照。
> テスト品質改善の計画と実施記録は `テスト改善計画.md` を参照。
> 完了した課題は即時削除する（開発ガイド§重要な注意事項 参照）。

## テスト総数

- ユニットテスト: 1478件（97ファイル、全パス）
- E2Eテスト: 172件（21ファイル、全パス）
- `npm run build`: 通過
- `npm run check`: 0 errors / 0 warnings

---

## 1. 既知の不具合

状態ラベル: **壊** = 機能が壊れている / **未接続** = ロジックはあるがUI未接続 / **部分** = 一部のみ実装

### 1.1 重大（データ整合性・基本機能に影響）

> 重大課題がない場合もこの見出しは維持し、`追跡無し` と明記する。

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| - | 追跡無し | 追跡中の重大不具合なし | |

### 1.2 中（機能不完全）

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| - | 追跡無し | 追跡中の中程度不具合なし | |

### 1.3 低（UX・表示）

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| B15 | 未対応 | 機能の詳細な仕様が要件定義書に不足。実装と要求にズレが生じている可能性 | 結合ツール・レイヤー操作・表示/編集モード・共有頂点・地物移動など、あるべき姿の文書化が追いついていない |
| B26 | 未対応 (§6 新モデル Phase 2 で解消) | 階層モデルが旧 `Layer` 前提のままで、リーフ排他・親子順序の整合性が地図全体で検証されていない | グローバルレイヤー序列ではなく「ツリー位置から派生する depth + リーフ排他を地図全体に適用」へ転換する。現行の `Layer` エンティティ／`FeatureAnchor.placement.layerId` は新モデルでは廃止対象。詳細は §6 |
| B27 | 未対応 (§6 新モデル Phase 2 で解消) | 子地物を持つ面情報が自身のリング（`FeatureAnchor.shape`）を保持し続ける | 新モデルでは `FeatureAnchor.shape` は optional で、リーフ（子を持たない面情報）のみが保持する。コンテナは shape プロパティ自体を持たず、形状は子の和として実行時に導出する。`.gimoza` 互換性は破棄する方針。詳細は §6 |

---

## 2. 未実装機能

### 2.1 統合品質（最優先）

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| 手動テスト用コーナーケース完備セーブデータ整備 | 中 | `TEST-savedata.gimoza` は現状 gitignore 済み。レイヤー/共有頂点/穴内島/時間範囲境界/長大ポリゴン/マルチポリゴン結合などを網羅した .gimoza を作成し、完成次第追跡対象へ戻してリポジトリに同梱する |

### 2.2 機能要件

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| **階層モデルの新仕様への置換** | **高** | 詳細は §6 を参照。`Layer` 廃止 + `FeatureAnchor.placement.isTopLevel` / `FeatureAnchor.property.kind` を錨側に持つツリーモデルへ全面置換する（時間依存スナップショット）。既存 `.gimoza` 互換は破棄。既存「所属変更 (既存親指定)」「面追加時の親指定」UI と `ReassignFeatureParentUseCase` / `AddFeatureCommand.parentId` は新モデル前提で再設計対象。Phase 計画と進捗・残作業の詳細は §6.10 を参照。要件定義書の他セクション（旧「レイヤー」概念の言及が残存）は各 Phase 着手と同じ変更単位で改訂する（古い仕様参照を防ぐ）: **Phase 3 着手時** に §2.2.3 整合性維持プロセスの構造的改訂（バリデーションの前提）— ※ §2.2.3 内の「同一レイヤー」用語そのもの（line 442 / 447）は Phase 2-D-6-3a で先行同期済み、**Phase 4 着手時** に §2.3.2 追加モードの親指定 UI / §2.3.3 編集ツールの操作 UseCase / Command 再設計反映 — ※ §2.3.2 / §2.3.3 内の「同一レイヤー」用語そのもの（line 564 / 565 / 713 / 721）は Phase 2-D-6-3a で先行同期済み、**Phase 5 着手時** に §2.7 UI 内のレイヤースライダー言及（表示フォーカス UI への置換）、**Phase 6 着手時** に §2.4.4 自動配色 / §2.4.4.6 レイヤー表示とテーマ設定（自動配色実装の前提。§2.4.4.3 line 851 の「同一レイヤー内」用語は本フェーズで対応）、**Phase 7 着手時** に §2.7 UI 内のサイドバー構成（Sidebar UI 整理の前提）、**Phase 2-D-7 着手時**（Layer エンティティ最終削除）に §2.3.3.1「所有者不明の場合」の旧モデル文言「可視レイヤー上の所有者が特定できない場合は、…可視レイヤーの地物を直接選択してください。」を改訂（Phase 2-D-6-1 完了時のレビューで開発ガイド §6.0.1 検出観点1 として検出。本コミット時点では責務外として記録のみ）。各 Phase の実装着手前にスコープ章を改訂しておく |
| スクショ付き詳細マニュアル整備 | 中 | README / 要件定義書 / 技術方針 / 開発ガイドとは別に、エンドユーザー向けの操作マニュアル（地物作成・時間軸操作・分割/結合/所属変更・保存/読込 などをスクリーンショット付きで解説）を整備する。**重点解説項目**: 結合機能と所属変更フローの使い分け（結合は選択対象を1つの結果地物にまとめる操作で**結合の主体**（リーフのみは選択リーフ全体、集約のみは選択集約全体、混在は選択集約のみ）が同一性を終了する。混在では末端地物が存在継続し新統合集約地物の下位領域へ取り込まれるため選択地物の総数は減らない場合がある。所属変更は地物数を保ったままツリー位相を変える。連邦化など階層を保ったまま束ねたい場合は結合ではなく所属変更フローの新規上位領域作成サブフローを使う）。ユーザーが直感的に混同しやすいため、操作別の使い分け対照表とスクリーンショットで明示する |
| 初回起動体験 | 中 | プリセット地図表示は通常起動で動作。`npm run dist:win` で配布物生成と `base-map.svg` の同梱設定は通過。インストーラ経由の初回起動確認は未完了 |
| エラーメッセージのユーザー向け改善 | 低 | ファイル操作エラーはユーザー向け文面へ改善済み。ドメイン検証・ZIP破損などの詳細分類は未整理 |

### 2.3 非機能要件

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| Electron実機統合E2E | 中 | IPC/preload/ファイルアクセスは Vitest で補強済み。Electron 実起動の統合E2Eは未整備 |
| パフォーマンス最適化（空間インデックス、LOD、カリング） | 低 | |

---

## 3. 構造的課題

外部レビュー（2026-03-25）で指摘された、不具合ではないが改善すべき構造問題。

| # | 深刻度 | 問題 | 備考 |
| --- | --- | --- | --- |
| S1 | 中 | `App.svelte` は引き続き大きい。`MapCanvas.svelte` は `mapCanvas/MapCanvasSvgLayers.svelte` / `MapCanvasHud.svelte` へ分割済み | `appPolygonEditing.ts` などへの抽出で前進したが、開発ガイドの200行目安にはなお未達。引き続き `App.svelte` のコントローラ分割が必要 |
| S5 | 中 | E2EがElectron統合を検証できていない | IPC/preload/ファイルアクセスは Vitest で補強済み。Chromium 136+ CLI制限により Electron 実起動 E2E は未整備 |

---

## 4. テスト品質

> 詳細は [`テスト改善計画.md`](テスト改善計画.md) を参照。

| 項目 | 状態 |
| --- | --- |
| フェーズ1: エッジケース補強 | 完了（+53件） |
| フェーズ2: カバレッジ駆動改善 | 進行中（今回 `.gimoza` / IPC / SVG / EdgeSlide接続 / 測量クリアに +17件、結合/共有頂点移動/EdgeSlide交差/境界始点/境界接触/No-op Undo/地物ドラッグ復元/複数territory再マージ/穴内島マージに +15件、任意SVGプロジェクト設定/保存連携に +8件、ロード時データ整合性検証/共有頂点付き複数頂点移動に +7件、EdgeSlide境界滑り/移動辺-頂点衝突に +12件、頂点挿入/削除Undoと分割MultiPolygon保持/複数territoryナイフ確定判定/挿入削除Redo ID固定/複数territory開線判定/分割時リング階層保持/非横断territory保持/PolygonShapeService階層再構築に +17件、追加/分割/結合コマンドの生成ID redo 固定に +3件、OSファイル関連付けの起動パス通知に +11件、プロジェクト境界の描画中toolStoreリセットに +3件、旧形式マイグレーション/互換性警告に +8件、B24分割延長線抑制に +4件、所属変更UI/親指定に +23件、作成時親指定UIに +2件、Phase 2-C-4 shape 保持規則 validation に +10件、Phase 2-D-3 で `ManageLayersUseCase.test.ts` 全削除 -20 件 + `LayerQueryService` テスト -3 件、Phase 2-D-5 で `areLayersEqual` テスト -1 件 + `featureRendererUtils.collectActiveFeatureEntries` 回帰テスト +8 件、Phase 2-D-6-1+2 で hitTest「非表示レイヤー」「上位レイヤー優先」テスト -2 件 + mapCanvasUtils「非表示レイヤー除外」テスト -1 件 + `featureRendererUtils.test.ts` 全削除 -8 件 + `mapSceneEntries.test.ts` 新規 +7 件（連番割当 / 多レイヤー連番 / コンテナ除外 / 非アクティブ除外 / 空入力 / undefined currentTime / 入力順保持）+ `mapSceneEntriesIntegration.test.ts` 新規 +3 件（同じ sceneEntries から 4 経路一致 / 非アクティブ除外連動 / コンテナ除外連動）+ `vertexSelectionContext.test.ts` -1 件（旧 hidden layer 用テスト撤去 → 空 sceneEntries 用テスト追加 -1+1）+ `mapCanvasUtils.test.ts` 「sceneEntries 外は寄与しない」テスト +1 件）。引き続き `EditFeatureUseCase` 61% 等が重点 |
| フェーズ3: 複合シナリオ | 計画済み（+21〜30件予定）。操作連鎖の結合テスト |
| フェーズ4: E2E拡充 | 計画済み（今回、面追加時の既存親指定と親選択リセットに +2件。+10〜20件予定） |
| テスト中に発見された不具合 | なし（発見次第ここに追記） |

---

## 5. 修正優先順位

2026-04-28時点の推奨順序（v1.0.0配布に向けて）:

1. **階層モデルの新仕様への置換 (B26/B27, §6 参照)**
2. 要件定義書の仕様補完（B15）
3. テスト品質改善（`テスト改善計画.md` フェーズ2〜3）
4. 構造改善（S1）
5. E2Eテスト拡充（`テスト改善計画.md` フェーズ4）+ Electron実機統合E2E + パフォーマンス改善

---

## 6. 階層モデルの新仕様 (2026-04-28 確定)

### 6.1 設計の経緯

旧 §6 は「グローバル `Layer` 概念を残し、`Layer.order` 序列とコンテナ rings 空化で階層整合性を担保する」方針だったが、2026-04-28 の設計議論で以下の構造的問題が露見し全面廃棄された:

- レイヤー深さが国ごとに異なるとき (例: 3層国 A vs 4層国 B)、グローバル序列ではリーフ排他を正しく判定できない (異なるツリーのリーフ同士が「下に潜り込める」)
- 「全リーフが何らかのコンテナに包まれる」と要求しても上記問題は解決しない

結論として、**グローバルレイヤー概念そのものを廃止し、ツリー位置から派生する depth で階層を表現する**方針へ転換する。設計意図は「基礎自治体 (リーフ) を面地理情報の唯一の担い手とし、上位はその集合体である純粋なコンテナ。階層深さは可変 (1層〜何十層)。歴史上の領土変更類型を全部カバーする」。

### 6.2 データモデル

- **`Layer` エンティティを廃止する**
- **`FeatureAnchor.placement.layerId` を削除する**
- **`FeatureAnchor.placement` に `isTopLevel: boolean` (最上位フラグ) を追加する** (時間依存。所属変更フローで時刻ごとに変化可能。同一錨内で `isTopLevel === (parentId === null)` 不変条件)
- **`FeatureAnchor.shape` を optional にする** (リーフのみ持つ。コンテナは shape プロパティ自体なし)
- **`FeatureAnchor.property` に `kind` プロパティを追加する** (国 / 州 / 郡 / 町 / 連邦 / 植民地 などの種別ラベル。表示専用で整合性検証には関与しない。錨ごとに保持することで時間軸上の階級変動 — 例: 同一地物の「植民地 → 州」「国 → 帝国 → 共和国」 — を表現可能)
- **既存 `.gimoza` 互換性は破棄する** (移行マイグレーションは実装しない、ユーザー判断済み)。互換破棄が完成するのは Phase 2 サブフェーズ D（`Layer` / `placement.layerId` 削除）時点。サブフェーズ B / C の段階では `Layer` / `layerId` が残っているため、新フィールド単独の migration（`isTopLevel` を `parentId === null` から派生する等）は中間形式の読込補助としてつなぎで実装する

### 6.3 派生概念: depth

- depth は **対象時刻における当該地物の有効錨のツリー位置から派生**: ルート (有効錨の `placement.isTopLevel === true && placement.parentId === null`) は `depth = 0`、子は親地物の同時刻有効錨から導出した depth + 1
- depth は地物自身に保持しない (常に算出する派生値)
- 不変条件: 同一錨内で `placement.isTopLevel === (placement.parentId === null)`
- **用語の固定**: `depth = 0` が **最上位 (国側、上)**。depth 大が下位 (基礎自治体側、下)。ユーザー感覚として「上 = 国 / 下 = 町」
- データ構造は時刻ごとに評価されるツリーの森 (複数ルート)。最上位フラグは錨側に持つため、所属変更フローで時刻ごとに位相変化（独立・帰属・連邦化など）を表現できる

### 6.4 整合性ルール

- **リーフ排他は地図全体で適用** (同一親の子同士に限定せず、異なるツリーのリーフ同士も含む)
- **親 ≡ 子の和 (厳密一致)**。コンテナ内部の隙間は許容しない
    - 直轄領のような隙間ケースは明示的なリーフを置いて埋める (例: 「政府直轄-県」)
    - **リーフ → コンテナ遷移時の差分自動処理**: 所属変更フローでリーフが子を獲得しコンテナ化する瞬間、元リーフの `shape` と新規子の和に差分があれば、差分を自動的に「直轄領リーフ」として生成し新コンテナの直接子に追加する。デフォルト名は「`<元リーフ名> 直轄領`」、`kind` は元リーフの遷移時刻における該当錨の `property.kind` を継承（直轄領の最初の錨に設定）。差分が複数の領土に分かれる場合は1リーフに複数 `territory` リング (飛び地化ロジック) でまとめる。差分が空なら直轄領を生成しない。所属変更ダイアログでユーザーが直轄領の名前/種別を上書き可能
    - 既存コンテナへの子追加 (所属変更フローによる追加のみ) では、コンテナ自身が `shape` を持たないため差分処理は発生しない (形状は子の和として再計算されるだけ)。混在結合では結果コンテナが新規生成されるため、既存コンテナへの追加には該当しない
- 地図全体としての無主地 (どのリーフにも覆われない領域) は許容する
- リーフ判定は「`shape` を保持し、子を持たない」を必要十分条件とする
- コンテナの形状 (描画 / ヒットテスト / 隣接判定) はすべて子の和集合として実行時に導出する
- **下位領域獲得経路**: 既存地物が子を獲得するのは「所属変更フロー」のみ。分裂・結合 (リーフのみ・コンテナのみ・混在のいずれのパターン) は新地物の生成または既存地物の形状変化を扱い、既存地物に後から子を加える経路ではない。混在結合では結果コンテナが新規生成され、混在リーフはその新規コンテナの子として帰属する (元コンテナの同一性は終了)。これは**結果地物（統合地物）の生成を結合の3パターン全てで「新規生成」に統一する**設計判断 (Undo/Redo・履歴の錨・ID継続の実装方針を結合機能で一致させる)。なお混在パターンでは選択された末端地物は存在継続し新統合集約地物の下位領域へ取り込まれるため、選択地物の総数は減らない場合がある（減るのは「結合の主体」となる地物の数）
- **結合対象の選択集合制約**: 結合対象は互いに上位・下位関係にあってはならない (集約地物 A と、A の直接・間接いずれかの下位領域に含まれる地物 X を同時選択することは禁止)。下位関係にある地物の同時選択は重複 child 参照や不正なツリー更新の原因となるため、結合実行時に検出し操作を拒否する

### 6.5 操作モデル

- 「**征服 (既存親への割譲)**」と「**連邦化 (新規共通親の作成)**」は **同じ所属変更フローで表現する**
- 新規親作成ループは現行 `ParentTransferDialog` の延長として実装する:
    - 名前 / 種別 (`kind`) 入力
    - 末端で「**既存最上位を選ぶ**」or「**新規最上位を作成する** (`isTopLevel: true`)」を選択
    - 再帰的に親を積み上げ可能 (中間階層を後付けで挿入できる)
- 親候補は depth 制約ではなく「対象地物自身および対象地物の上位領域すべて (直接の親、その親、…と最上位まで連鎖して辿った上位領域の集合) に含まれないこと (循環防止) + 親 ≡ 子の和 制約を将来満たせること」で絞る

#### 例: アメリカ合衆国爆誕シナリオ

1. 時刻 T において、13 個の最上位国 (各々の T 時点有効錨で `placement.isTopLevel: true` / `parentId: null`) を複数選択
2. 「親変更」(現行の所属変更フロー) を起動
3. 親候補として「**新規地物を作成**」を選択
4. 新規親作成フローで「合衆国」を新規地物として作成し、時刻 T で開始する錨に `placement.isTopLevel: true` / `parentId: null` を設定
5. 13 国の各々に時刻 T で開始する新しい錨を追加し、`placement.isTopLevel: false` / `parentId: 合衆国ID` を設定（時刻 T 未満の錨は `isTopLevel: true` / `parentId: null` のまま継続）。13 国の同一性（Feature ID）は維持されたまま、時間軸上で位相のみが変化する
6. 全ステップを 1 Undo にまとめる

### 6.6 自動配色

- **同 depth の兄弟で 4 色塗り分け** (同じ親を持つ子同士の中で 4 色定理を適用)
- 個別 feature が optional `style` を持つ。未指定なら自動配色を継承

### 6.7 表示フォーカス (戦略ゲー風)

- **グローバル depth スライダー**: 全地物を `depth ≤ N` で集約表示 (N = 0 で最上位ルートだけ、N 増で深く展開)
- **個別フォーカス** (グローバルと併存): 「選択地物の縦方向領域 (上位領域すべて + 自身 + 下位領域すべて) だけ depth M、その他は depth N まで」のように選択した地物の縦方向領域だけ深掘りする
- 想定 UI 参考: 戦略ゲー (HoI4 / EU4 等) の depth 集約表示
- 縦方向領域の絞り込み: 選択中地物の上位領域すべて + 自身 + 下位領域すべて (縦方向に繋がる領域) だけを集約対象として提示し、認知負荷を下げる
- 性能上、空間インデックス (R-tree など) の導入が必要になる可能性 (Phase 概略の後段で評価)

### 6.8 UI 整理 (`kind` ラベル)

- レイヤー名と地物名の混合 (例: 県レイヤーの「東京」が「東京都-県」になる) を避けるため、`kind` は **表示用ラベルとして錨側 (`FeatureAnchor.property.kind`) に持つ** (整合性検証には関与しない。時間軸上の階級変動を表現可能)
- 種別の制約: 親が「子の種別」を強制しない (例: 日本のように県の下に市町村が混ざる柔軟性を維持)
- Sidebar の現行 `レイヤー / プロパティ / 地物一覧` 3タブ構成は廃止または再構成する。具体的なタブ構成は実装着手時にユーザー判断:
    - 「選択地物中心」のタブ (プロパティ + 縦方向領域ナビゲーション = 上位領域・自身・下位領域の連鎖を辿る UI)
    - 「世界俯瞰」のタブ (地物一覧 + 深度フォーカス制御)
    - 種別管理 (`kind` の命名規則) はプロジェクト設定または独立タブへ

### 6.9 想定する歴史的領土変更類型

新モデルで以下の類型がすべて表現できることを確認する。個別 UI を設けるのではなく、データモデルとして表現可能であれば良い。

#### A. ツリー位相変化

| 類型 | 操作 |
| --- | --- |
| 改名・属性変更 | プロパティ編集 |
| 国境調整 | リーフの頂点編集 |
| 割譲 | 所属変更 (新親 = 既存地物) |
| 独立 | 所属変更 (`isTopLevel = true` へ昇格) |
| 帰属 | 所属変更 (新親 = 既存地物) |
| 直轄化 (中間階層スキップ) | 所属変更で中間コンテナを抜かして、より上位の上位領域 (直接の親の親、その親、…) へ移す |
| 自治化 (中間階層挿入) | 新規親作成 (既存子と旧親の間に新コンテナを挿入) |

#### B. 数量変化

| 類型 | 操作 |
| --- | --- |
| 分裂 (二分割) | ナイフ分割。**元リーフは形状変化として継続 (プロパティ引き継ぎ先) + もう片方が新規リーフとして生成** (穴あけと同型)。元地物終了+新2地物の旧仕様からの変更点に注意 (現行実装は旧仕様。Phase 4 で移行) |
| 結合 (リーフのみ) | 結合。形状の論理和。元リーフ全消滅+結果リーフ新規生成 |
| 結合 (コンテナのみ平坦化) | 結合。元コンテナそれぞれの直接子を新コンテナの直接子へ平坦化統合 (中間階層は保持しない)。階層保持は所属変更フローで表現 |
| 結合 (リーフ+コンテナ混在の自動帰属) | 結合。混在リーフを新コンテナの直接子として帰属。リーフは存在継続、コンテナは新規生成 (旧コンテナは終了) |
| 連邦化 (複数独立国 → 共通親配下に統合) | 所属変更フローで「親候補 = 新規地物作成」を選択 (§6.5)。**結合ではなく所属変更フローを使う**。結合は結合の主体（リーフのみは選択リーフ全体、集約のみは選択集約全体、混在は選択集約のみ）の同一性を終了させ結果地物を新規生成する操作、所属変更は地物数を保ったまま位相を変える操作という設計分離 |
| 解体 | 全子を `isTopLevel = true` へ昇格。コンテナは子全消失で自動消滅 |

#### C. リーフ ↔ コンテナ遷移

| 類型 | 操作 |
| --- | --- |
| リーフ拡大 (X が拡大しもとの X が一単位として残る) | 新規親作成 (新コンテナ Y を作って既存リーフ X をその子に) |
| コンテナ自動消滅 | 子全消失で自動 (既存挙動) |

#### D. 階層変動 (深さ変化)

| 類型 | 操作 |
| --- | --- |
| 階級変動 | `kind` プロパティ更新 (depth は親変更で従う) |
| 多層国 ↔ 1層国の編入・編出 (深さ非対称) | 所属変更 + 必要なら `kind` 変更 |
| 構造改革 (道州制等の大規模再編) | 複数の新規親作成 + 所属変更の連続適用 |

#### E. 政治的特殊形態 (単一親モデルで近似)

「高々 1 つの上位コンテナに属する」制約を維持したまま、別エンティティ化や時間付き所属切替で表現する。

| 類型 | 表現方法 |
| --- | --- |
| 連邦 (例: 米合衆国、EU、神聖ローマ帝国) | 連邦エンティティをコンテナとして最上位に置き、構成国・州を子に。`kind = "連邦"` などで政体名を表現 |
| 二重君主国 (例: オーストリア = ハンガリー) | 連邦と同じく上位コンテナで表現 (オーストリア = ハンガリーがコンテナ、オーストリア・ハンガリーが子) |
| 同君連合 (例: ハプスブルク朝) | 領土変更ではなく統治者メタデータ。プロパティで表現 |
| 共同統治 (例: 英埃領スーダン、アンドラ) | 「○○共同統治領」として独立した最上位エンティティ。宗主はプロパティで列記 |
| 占領地 (例: 第二次大戦中のヴィシー政権下フランス) | 「○○国占領地」として戦時独立エンティティ + 基礎自治体は時間ごとに所属親を切替 (時間付き `FeatureAnchor.placement.parentId` で対応) |
| 係争地 (例: 国家承認が分かれる地域) | 「○○ (係争中)」として独立した最上位エンティティ + 主張する側はプロパティで列記 |

### 6.10 実装 Phase 概略

新階層モデルへの置換は以下の Phase 順で実施する。Phase 2 はサブフェーズ A〜E に分割し、Phase 2.5 (コンテナ UI 基盤) を Phase 3 の前に挿入する。

#### スコープ管理ルール (全 Phase 共通)

- 1サブフェーズの差分は **~15 ファイル / ~300 行** を上限とする。超えたらさらに分割する
- 自レビューで「対概念」と感じた修正は、そのレビューで全部直さず別サブフェーズへ切り出す
- ドメイン基本ルール (排他・整合・階層・時間) に触れる変更は、着手前に要件定義書 §2.1 / §4.1 / 現状.md §6.4 の該当節を直接引用してから着手する
- 集約地物の競合検証は **要件定義書 §2.1 line 145-153 により禁止**。`ConflictDetectionService` を末端地物以外に拡張しない
- 「対称性確保」「自然な拡張」と感じた変更ほど、仕様で例外規定されている可能性を疑う

#### 着手前ゲート (Phase 2 / Phase 2.5 で必ず引用)

- **要件定義書 §4.1**: 「`shape`?: オブジェクト。地物の形状定義。**末端地物（リーフ）のみが保持する**。集約地物（コンテナ）はこのプロパティ自体を持たず、形状は下位領域の和として実行時に導出する」
- **要件定義書 §2.1 line 145-153**: 「**末端地物の排他性は地図全体に適用する**」「**排他検証は末端地物同士のみ**。集約地物には直接の排他制約を課さない」「**集約地物ペアに対して直接の非重複検証を実装してはいけない**」
- **§6.4**: 「リーフ判定は『shape を保持し、子を持たない』を必要十分条件とする」「親 ≡ 子の和（厳密一致）」

#### Phase 1: 要件定義書 §2.1 改訂 — **2026-04-28 完了**

新モデルに基づき仕様本文を全面書き直し。レイヤー概念を削除し、ツリー / depth / リーフ排他全体適用 / 新規親作成フロー / 深度フォーカス表示モデル / 政治的特殊形態の表現方針を明文化。仕様確定が以降の前提作業。

#### Phase 2: データモデル全面置換

`Layer` 削除、`FeatureAnchor` 構造変更 (`placement.isTopLevel` / `property.kind` / `shape` optional / `placement.layerId` 削除)、depth 派生サービス追加。サブフェーズ A〜E に分割。

##### Phase 2-A: `property.kind` 追加 — **2026-04-28 完了**

optional 表示専用ラベル + シリアライザ / マイグレーション / `PropertyProjection` 反映。

##### Phase 2-B: `placement.isTopLevel` 追加 — **2026-04-28 完了**

不変条件 `isTopLevel === (parentId === null)` + `createAnchorPlacement` ヘルパー集約 + 旧形式 migration / バリデーション。

##### Phase 2-C: `shape` optional 化 (C-1〜C-4 に再分割)

> 2026-04-29 に一括実装で試みたが自レビュー10往復になり全撤回 (実装済み.md 2026-04-29 エントリ参照)。下記の C-1〜C-4 に再分割して順次着手する。

###### Phase 2-C-1: ドメイン型 + 永続化 (~7-8 ファイル) — **2026-04-29 完了**

- `FeatureAnchor.shape` を `FeatureShape | undefined` に変更
- コンストラクタ + `withShape(undefined)` 受容
- `requireLeafShape(anchor)` ヘルパー追加 (C-2 で利用)
- `JsonFeatureAnchor.shape` optional 化
- `serializeAnchor`: shape 未定義時はフィールド自体を出力しない
- `deserializeAnchor`: shape 欠落時 undefined
- `normalizeShape` (migration) の判定:
    - shape 欠落 + `featureType === 'Polygon'` + `placement.childIds.length > 0` → undefined（コンテナ）
    - shape 欠落 + Point/Line → `SerializationError`
    - shape 欠落 + Polygon + childIds 空 → `SerializationError`
- 既存 validator (`validateOrphanedVertices` / `validatePolygons`): undefined shape をスキップ
- テスト: FeatureAnchor の optional 化 / `withShape(undefined)` / `requireLeafShape` / シリアライザ ラウンドトリップ / migration の各分岐
- **触らない**: consumer narrow / UI / 描画 / ヒットテスト / 新規 validation 規則

`tsc -p tsconfig.web.json` では consumer に narrow 漏れエラーが噴出するが、`npm run check` (svelte-check on root tsconfig) は通る。runtime も不変（実データに undefined shape が無いため narrow パスは未走行）。意図的に「型エラーを残したまま runtime green」で C-2 へ進む。**C-2 完了時点で Domain + Application 層の narrow 漏れエラーは消滅した（残るのは Infrastructure / Presentation 層のみ。C-3 で解消予定）。**

###### Phase 2-C-2: Consumer narrowing — Domain + Application 層 (~15 ファイル) — **2026-04-30 完了**

- 対象（18 ファイル / +60 / -24 行）:
    - `domain/value-objects/FeatureAnchor.ts`: `isLeafPolygonAnchor` ヘルパー + `LeafPolygonAnchor` 型 predicate を新設（§6.6.8 リーフ判定の集約）
    - `domain/services/`: `LayerService` / `SharedVertexService` / `ConflictDetectionService`（リーフ判定ベース）/ `PolygonValidationService`（リーフ判定ベース）
    - `application/`: `polygonValidation` / `AddFeatureUseCase` / `DeleteFeatureUseCase` / `EditFeatureUseCase` / `VertexEditUseCase` / `ReassignFeatureParentUseCase` / `ResolveFeatureAnchorConflictsUseCase` / `PrepareFeatureAnchorEditUseCase`
    - `application/commands/`: `MoveFeatureCommand` / `MergeFeatureCommand` / `SplitFeatureCommand` / `DeleteVertexCommand` / `splitFeatureSharedEdgeInsertion`
- 適用パターン:
    - 排他検証経路（`ConflictDetectionService` / `PolygonValidationService`）: `if (!anchor || !isLeafPolygonAnchor(anchor)) continue;`（§6.6.8 遵守: 移行期間ノードを排他検証から除外）
    - その他のループ内: `if (!anchor.shape) continue;`
    - 既存型チェック拡張: `if (anchor.shape.type !== 'X')` → `if (!anchor.shape || anchor.shape.type !== 'X')`
    - リーフ前提の関数: `const shape = requireLeafShape(anchor);` で取り出し（C-1 で導入したヘルパー。今回は適用箇所が発生せず保留）
- **触らない**: Infrastructure / Presentation / 描画 / UI
- テスト: `isLeafPolygonAnchor` の判定 4 ケース（リーフ / 移行期間ノード / コンテナ / Point リーフ）のみ追加（+4件、1471 → 1475件）。consumer 経路の振る舞いは runtime 不変
- **既知制約 (Phase 2.5-A defer)**: `HierarchyService.deriveParentShape` は子コンテナの孫リーフを再帰下降していない（§6.6.9 規則「子地物が集約地物であれば孫の和を再帰呼び出しで取り込む」未充足）+ `polygonUnion + polygons[0]` で多 polygon 片落ち（§6.6.5 違反）+ visited cycle guard 無し（§6.4.14 違反）。Phase 2-C 時点では containers が実データに無いため runtime 影響なし。Phase 2.5-A で `polygonUnionAll` ＋ 再帰下降 ＋ visited cycle guard を備えた正式実装へ差し替え予定

###### Phase 2-C-3: Consumer narrowing — Infrastructure + Presentation 層 (~12 ファイル) — **2026-04-30 完了**

- 対象（実施した 14 ファイル）:
    - `infrastructure/rendering/`: `hitTestUtils.ts` / `vertexSelectionContext.ts` / `vertexHandleUtils.ts`（`getShapeVertexPositions` / `getShapeEdgePositions` を `FeatureShape | undefined` 受容化、コンテナで空配列を返す）
    - `presentation/app/`: `appPolygonEditing.ts`（`isPolygonAnchor` 型 predicate を新設）/ `appFeatureDragging.ts` / `appSelection.ts` / `edgeSlideConstraintCollectors.ts`
    - `presentation/components/`: `labelRendererUtils.ts` / `mapCanvasUtils.ts` / `parentTransferDialogUtils.ts` / `FeatureRenderer.svelte`（`getLayerFeatures` でコンテナを除外）/ `PropertyPanel.svelte`（種別表示を `?? '集約地物'` フォールバックへ変更）
    - `App.svelte`（5 箇所の `anchor.shape.X` 直接参照に narrow 追加）+ `MapCanvasHud.svelte`（EditToolbar 表示条件に `selectionAnchor.shape` ガード追加）
- パターンは C-2 と同じ（描画・選択・ラベル経路は `if (!anchor.shape) continue/return;` の単純 narrow。排他検証ではないため `isLeafPolygonAnchor` は使わない）
- コンテナ描画・ヒットテスト・選択 UI は実装しない（→ Phase 2.5）。コンテナは現時点で skip / 不可視扱い
- `tsc -p tsconfig.web.json` で残存していた Infrastructure / Presentation 層の shape narrow 漏れエラーは全消滅

###### Phase 2-C-4: shape 保持規則の validation (1 ファイル + テスト) — **2026-05-03 完了**

- `jsonSerializerValidation.ts` に `validateShapePresence` を追加し `validateJsonWorld` に組み込んだ。検証は単一錨内で完結する規則のみ:
    - `shape === undefined` ⟹ `featureType === 'Polygon'` かつ `placement.childIds.length > 0`
    - `shape !== undefined` ⟹ `featureType` と `shape.type` が整合 (Point↔"Point" / Line↔"LineString" / Polygon↔"Polygon")
- 手編集 JSON / 外部連携 JSON 経路で migration を経由しないケース、および migration を通過する型不整合（例: `featureType: 'Point' + shape.type: 'Circle'`）への保険として機能する
- テスト: 新規 `tests/infrastructure/persistence/jsonSerializerValidation.test.ts` で +10件（合格 4 / shape 欠落エラー 3 / 整合不一致エラー 3）。既存 `JSONSerializer.test.ts` の `Unknown shape type` 期待を新エラー文言に差し替え（migration を通過した不整合は `validateShapePresence` が先に検出するため）
- **スコープ外 (Phase 3 本体へ defer)**: 子参照健全性 (childIds 存在・Polygon 型)、親子相互整合 (時間区間カバレッジ)、循環検出 (時間スライス)、リーフ排他 (地図全体)、親 ≡ 子の和

###### Phase 2-C 完了後の状態

- 型・永続化・shape 規則 validation で「shape は optional」が成立
- 全 consumer が narrow 済み（containers が来ても crash しない、ただし describe・操作・選択は不可）
- containers は実データに無いので UI 上は不変
- Phase 4 で containers を作れる前提が整う

##### Phase 2-D: `Layer` 削除 + `placement.layerId` 削除

`Layer` エンティティ / `ManageLayersUseCase` / Sidebar レイヤータブを削除し、`LayerService` は `HierarchyService` へリネーム。`placement.layerId` を参照する全箇所を撤去または削除。`.gimoza` 互換破棄が完成する地点。`placement.layerId` 参照は実コードで 36 ファイル超、`Layer` 直接参照は 13 ファイル超に及ぶため、§6.10 スコープ管理ルール（1サブフェーズ ~15 ファイル / ~300 行）に従って D-1 〜 D-7 に分割して実施する。

###### Phase 2-D-1: 排他検証から layerId 依存撤去 — **2026-05-03 完了**

- `ConflictDetectionService` から `targetLayerIds` フィルタおよび `groupPolygonsByLayer` を撤去し、末端地物（`isLeafPolygonAnchor`）の地図全体ペアワイズ排他検証へ移行。`SpatialConflict.layerId` フィールドも削除
- `detectConflictsForFeature` のオーバーロードを単一シグネチャに整理、`explicitLayerId` パラメータを撤去。`PolygonValidationService.validatePolygonFeature` / `polygonValidation.validatePolygonOrThrow` も同様に `explicitLayerId` を撤去
- 呼び出し側（`EditFeatureUseCase` / `PrepareFeatureAnchorEditUseCase` / `App.svelte` / `commands/AddFeatureCommand` / `presentation/app/appPolygonEditing`）から `layerId` 引数を撤去。`ConflictResolutionDialog.svelte` のレイヤー欄表示も削除
- 振る舞い変更: 「同一レイヤー内のみ排他」→「末端地物同士は地図全体で排他」（要件定義書 §2.1 line 145-153 / §6.6.8 / §6.4 遵守）。エラーメッセージは「ポリゴンが同一レイヤーの地物 ... と重なっています」→「ポリゴンが他の末端地物 ... と重なっています」へ変更
- **触らない**: `placement.layerId` フィールド自体 / `Layer` エンティティ / `LayerService` / `ManageLayersUseCase` / `LayerQueryService` / Sidebar レイヤータブ / 描画グルーピング / ヒットテスト order / 自動配色 / ProjectSettings レイヤー順

###### Phase 2-D-2: `LayerService` を `HierarchyService` にリネーム — **2026-05-03 完了**

- 要件定義書 §5.2 で既に `HierarchyService` 命名が確定（Phase 1 改訂時）。コード側のみ `LayerService` のまま乖離していたため、§6.0.1 検出観点1（仕様文書での用語撤廃宣言時の同一文書同期）に従い実装を仕様に揃えた
- 変更内容（純粋なシンボル名変更で挙動変化なし）:
    - `src/renderer/domain/services/LayerService.ts` → `HierarchyService.ts` にリネーム + ヘッダーコメントの「レイヤー階層ドメインサービス」を「階層ナビゲーション・形状導出ドメインサービス」へ更新（旧モデルでの「グローバル `Layer`」廃止を明記）
    - `tests/domain/services/LayerService.test.ts` → `HierarchyService.test.ts` にリネーム + import path / `describe('LayerService')` → `describe('HierarchyService')`
    - import path 更新: `services/index.ts` / `application/DeleteFeatureUseCase.ts` / `application/ReassignFeatureParentUseCase.ts` / `presentation/components/FeatureRenderer.svelte`
    - 開発ガイド §1.2 ドメインサービス一覧 / §6.6.9 「派生サービス (`LayerService.deriveParentShape`)」の文言を `HierarchyService` に同期
- **触らない**: `Layer` エンティティ / `placement.layerId` フィールド / `HierarchyService.buildParentChildLink` 内の `parentActive.placement.layerId` 引き渡し（`placement.layerId` 自体は D-6 で削除予定。コード側にも Phase 2-D-6 で消える経路である旨の注記を追加）
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npx vitest run` 1486 passed（96 ファイル、件数増減なし）

###### Phase 2-D-3: `ManageLayersUseCase` / `LayerQueryService` 撤去 + `LayerPanel.svelte` 削除 — **2026-05-03 完了**

旧計画では「UseCase / Query 撤去」を D-3、「LayerPanel.svelte 削除」を D-4 前半に分けていたが、`LayerPanel.svelte` が `ManageLayersUseCase` / `LayerQueryService` に強く依存しており、UseCase / Query を先に削除すると LayerPanel がコンパイル不能になるため、依存上の必然から両者を D-3 に統合した。

- 削除: `src/renderer/application/ManageLayersUseCase.ts` / `src/renderer/application/queries/LayerQueryService.ts` / `src/renderer/presentation/components/LayerPanel.svelte` / `tests/application/ManageLayersUseCase.test.ts`
- 修正: `Sidebar.svelte`（レイヤータブ撤去 + LayerPanel 参照削除 + `focusedLayerId` / `onFocusLayerChange` プロップ削除）/ `App.svelte`（Sidebar への `focusedLayerId` / `onFocusLayerChange` 渡しを削除。`focusedLayerId` 自体は描画グルーピングに残存し、UI 入力経路がないため常に null）/ `DIContainer.ts`（`manageLayers` / `layers` 登録削除）/ `SaveLoadUseCase.ts`（`manageLayers` コンストラクタ引数を撤去し、`assembleWorld` / `distributeWorld` / `resetProjectState` で内部 `layers` フィールドを保持）/ `application/index.ts` / `queries/index.ts`（export 削除）
- テスト調整: `SaveLoadUseCase.test.ts` / `QueryServices.test.ts` / `AddFeatureCommand.test.ts` で ManageLayersUseCase 参照を撤去
- **触らない**（後続サブフェーズで対応）: `Layer` エンティティ / `placement.layerId` フィールド / 描画グルーピング / ヒットテスト order / 自動配色 / `World.layers`

###### Phase 2-D-4: `focusedLayerId` 残骸の整理 + `unsubLayersChanged` 購読撤去 — **2026-05-15 完了**

旧 D-3 と D-4 前半を D-3 へ統合した結果、新 D-4 は「focusedLayerId / unsubLayersChanged の dead state / dead listener 整理」に絞り込まれた。

- 撤去: `App.svelte` の `focusedLayerId` state + 同期 `$effect` + `getAddTargetLayer()` のフォーカス分岐 + `MapCanvas` への `{focusedLayerId}` 受け渡し / `MapCanvas.svelte` / `MapCanvasSvgLayers.svelte` / `FeatureRenderer.svelte` の `focusedLayerId` プロップ全段 / `FeatureRenderer.svelte` の opacity 計算で焦点外レイヤーを 0.22 倍に薄める分岐
- `App.svelte` の `unsubLayersChanged = eventBus.on('layers:changed', …)` 購読と onDestroy 内呼び出しを撤去（emit 経路は D-3 で消滅済みで永久に発火しない dead listener だった）
- **触らない**（後続サブフェーズで対応）: 描画グルーピング（レイヤー単位 `<g>` + `visibleLayerIds` フィルタ）/ hitTest の `layerPriority` tie-break / `lockedLayerIds` 派生 / `placement.layerId` フィールド / EventBus 型定義 — これらは Layer エンティティ本体の可視性・順序機能および placement.layerId に紐づくため D-5〜D-7 / Phase 2.5-C で順次対応

###### Phase 2-D-5: `ProjectSettingsDialog.svelte` のレイヤー編集セクション撤去 + 自動配色レイヤー単位グルーピング撤去 — **2026-05-15 完了**

- 撤去: ProjectSettingsDialog の「レイヤー管理」セクション 全体（新規レイヤー追加フォーム + draftLayers リスト + 削除ボタン + add/update/delete 関数 + draftLayers/newLayerName state + layers/lockedLayerIds props）/ 関連 CSS / App.svelte の `lockedLayerIds` 派生 / `onSettingsSave` の `nextLayers` 引数経路（`saveLoad.setLayers(nextLayers)` + `refreshLayerData()` 呼び出し）/ `appProjectSettings.ts` の `areLayersEqual` 関数 + `hasProjectSettingsChanged` の layers 引数
- 移行: `FeatureRenderer.svelte` の自動配色を「`{#each visibleLayers}` ループ内の `{@const polygonAutoColors = resolvePolygonAutoColors(...)}`」から「コンポーネント先頭の `$derived` で表示文脈下 1 回算出」へ移行。`activeFeatureEntries` 導出を `$derived.by` で共通化し、`visibleLayerIds.has(anchor.placement.layerId)` で非表示レイヤー所属地物を除外。`getLayerFeatures(layerId)` は filter のみに簡素化。`AutoColorService.ts` 冒頭コメントを「共有境界を持つ面が同色にならないように」へ更新（同一レイヤー内前提の文言を撤去）
- 振る舞い変更: ユーザーが新規レイヤーを追加・削除・改名できなくなる（default の「レイヤー1」のみ固定）。自動配色は「全地物に同一パレットを適用」となる。Phase 6 で「同 depth 兄弟」基準自動配色へ置換予定の中間期間挙動
- **featureIndex / 自動配色のセマンティクス変化**: featureIndex は「レイヤー内連番」から「表示文脈（可視レイヤー全体）連番」へ変更。`resolvePolygonAutoColors` の隣接グラフも同一レイヤー内から表示文脈全体へ変わる。複数の可視レイヤーを含む既存 `.gimoza` を読み込むと、**Polygon の `polygonAutoColors.get(feature.id)` 値そのものが旧と異なる**（greedy coloring の隣接対象が増えるため）。**Point / LineString も** `resolvePointStyle(featureIndex)` / `resolveLineStyle(featureIndex)` 経由で getAutoColor のパレット index が変わる。非表示レイヤーの所属地物は表示文脈に含まれないため自動配色の入力にも入らない。default 1 レイヤーのみの新規プロジェクトでは旧=新（互換破棄方針の射程内）
- **触らない**（後続サブフェーズで対応）: `Layer` エンティティ / `Layer.ts` / `placement.layerId` フィールド / `World.layers` / `App.svelte` の `let layers` state + `refreshLayerData()` + `getAddTargetLayer()` + `executeAddFeatureCommand({ layerId })` 経路 / `MapCanvas.svelte` / `vertexSelectionContext.ts` / `hitTestUtils.ts` の `visibleLayerIds.has(...)` フィルタ + `layerPriority.get(...)` tie-break / `FeatureRenderer.svelte` の `{#each visibleLayers}` レイヤー単位 `<g opacity>` グルーピング / `SaveLoadUseCase.getLayers()` / `setLayers()` 暫定 API / EventBus `layers:changed` 型
- **後続フェーズへ持ち越す改訂**:
    - 開発ガイド §6.6.6「隣接・自動配色はレイヤーと表示経度の文脈を揃える」（同一レイヤー前提の教訓）は旧モデル前提のまま残存。Phase 2-D 完了時 or Phase 6 着手時に「同 depth 兄弟」基準の新教訓へ改訂
    - 開発ガイド §6.6.3「結合対象の事前条件を明示する」line 568「同一レイヤーに属すること」は旧モデル前提の事前条件のまま残存。Phase 4（操作 UseCase / Command 再設計）で結合 UseCase 本体の再設計に合わせ「結合対象が互いに上位下位関係にないこと」（要件定義書 §2.1 / 現状.md §6.4 line 146）へ改訂。Phase 2-D-5 以降は実質単一レイヤー前提のため runtime 観測挙動の差はないが、教訓本文として残置中
    - 要件定義書 §2.4.4.3「同一レイヤー内で共有境界を持つ面が同色にならないように」/ §2.4.4.6「レイヤー表示とテーマ設定」も旧モデル前提のまま。Phase 6（自動配色）着手時に §2.2 の Phase 計画通り改訂

###### Phase 2-D-6: hitTest / 選択 / 描画経路の `placement.layerId` 参照撤去 → D-6-1+2 統合 / D-6-3 に再分割

実コード（src 30 ファイル）と tests（30 ファイル）に `placement.layerId` 参照が散らばっており、§6.10 スコープ管理ルール（~15 ファイル / ~300 行）を超える。当初 D-6-1 / D-6-2 / D-6-3 の 3 段に分割したが、D-6-1 着手後のレビューで「描画経路（FeatureRenderer 側）は引き続き `Layer.visible` で絞っており、hitTest / 頂点選択 / wrapOffsets だけ全地物を見るため『描画されないがクリック選択できる』中間状態が発生する（開発ガイド §6.1.2 / §6.6.9 / §6.0.1 検出観点2 違反）」との指摘を受け、D-6-1 と D-6-2 を統合して **sceneEntries 共通中間表現** を導入する形に再設計した。

###### Phase 2-D-6-1+2: sceneEntries 共通中間表現の導入 + 描画/hitTest/頂点選択/wrapOffsets の一元化 — **2026-05-16 完了**

- 新規: `src/renderer/presentation/components/mapSceneEntries.ts`（`MapSceneEntry` 型 + `collectMapSceneEntries(features, currentTime)`）+ `tests/presentation/components/mapSceneEntries.test.ts` + `tests/presentation/components/mapSceneEntriesIntegration.test.ts`（同じ sceneEntries から描画/hitTest/owner map/wrapOffsets が一致することを固定する対概念整合性回帰テスト）
- 削除: `src/renderer/presentation/components/featureRendererUtils.ts` + `tests/presentation/components/featureRendererUtils.test.ts`（`collectActiveFeatureEntries` は `collectMapSceneEntries` に置換）
- 撤去: `FeatureRenderer.svelte` の `visibleLayers = $derived(layers.filter(l => l.visible))` + `visibleLayerIds.has(...)` フィルタ + `{#each visibleLayers as layer}` レイヤー単位 `<g opacity={layer.opacity}>` グルーピング + `getLayerFeatures(layerId)` 経路 + `layers` props / `hitTestUtils.ts` の `layers` 引数 + `visibleLayerIds` フィルタ + `layerPriority` tie-break + `currentTime` 引数 / `vertexSelectionContext.ts` の `layers` 引数 + `visibleLayerIds` フィルタ + `currentTime` 引数（`buildVisibleVertexOwnerMap` → `buildSceneVertexOwnerMap` に改名）/ `mapCanvasUtils.ts` の `computeRenderWrapOffsets` の `features` / `currentTime` / `visibleLayerIds` 引数 / `MapCanvas.svelte` の `visibleLayerIds` state + `vertexHandleEntries` 内フィルタ + `layers` props / `MapCanvasSvgLayers.svelte` の `layers` props / `App.svelte` の `<MapCanvas {layers}>` 受け渡し + `hitTest` 呼び出し2箇所の `layers` / `currentTime` 引数
- 導入: 全 4 経路（描画・ヒットテスト・頂点選択コンテキスト・wrapOffsets）が `sceneEntries: readonly MapSceneEntry[]` を受け取り、`App.svelte` の `sceneEntries = $derived(collectMapSceneEntries(features, currentTime))` から伝播する
- 振る舞い変更:
    - hitTest はレイヤー可視性で絞らず最初にヒットした地物を返す。同点複数候補があった場合の上位レイヤー優先 tie-break は消滅（Phase 2.5-C で depth + DOM target ベースに再設計予定。開発ガイド §6.2.25 採用ポリシー）
    - `buildSceneVertexOwnerMap` も Layer.visible フィルタを撤去し、sceneEntries に含まれる全地物を対象とする
    - 自動配色は地図全体（sceneEntries 全体）で 1 回算出。`<g opacity={layer.opacity}>` 経由のレイヤー透明度反映機能は撤去（Phase 6 で個別 style ベースに再設計予定）
    - default 1 レイヤー / 全 visible=true 前提では旧=新で実害なし。互換破棄方針（§6.10 / 現状.md §6.2）の射程
- **触らない**（D-6-3 で対応）: `placement.layerId` フィールド本体 / `createAnchorPlacement` シグネチャ / `JsonFeatureAnchor.placement.layerId` + serialize / deserialize / migration / `validateJsonWorld`
- **触らない**（D-7 で対応）: `Layer` エンティティ / `Layer.ts` / `World.layers` / `SaveLoadUseCase.getLayers/setLayers` 暫定 API / EventBus `layers:changed` / `layer:visibilityChanged` イベント型 / `App.svelte` 初期化の `new Layer('default', ...)`
- スコープ規模: 15 ファイル / +661 -670 行（実装済み.md の `git diff --numstat` 集計参照）。§6.10 ルール「~15 ファイル / ~300 行」のうちファイル数は上限ちょうど、行数は超過。FB「コミット可否: このFB対応前のD-6-1単独では修正が必要」「sceneEntries へ統合し対象一致をテストで固定できればコミット可能」の指示により D-6-1 と D-6-2 を統合する判断は確定済み。-670 行のうち約 200 行は削除のみのファイル（featureRendererUtils）、+195 行は新規回帰テスト、+131 行は新規ユーティリティのテスト。実装ロジックの実質変更行数は約 ±300 行で、レビュー往復のリスクは抑えられる範囲

###### Phase 2-D-6-3: `placement.layerId` フィールド本体削除 → 3-3a / 3-3b / 3-3c に再分割

Phase 2-D-6-1+2 完了後の調査で、src 22 + tests 29 = 計 51 ファイルに `placement.layerId` 参照が散らばっており、§6.10 スコープ管理ルール（~15 ファイル / ~300 行）を大幅に超えるため、`feedback_subphase_scope_split.md`「サブフェーズは1経路ずつ」に従い 3 段へ分割した。

###### Phase 2-D-6-3a: EdgeSlide collector の同一レイヤー絞り込み撤去 + 関数リネーム — **2026-05-18 完了**

- 撤去: `edgeSlideConstraintCollectors.ts` の `collectSourceLayerIds` ヘルパー + `sourceLayerIds` 経由の「同一レイヤー絞り込み」フィルタ（Phase 2-D-1 で `ConflictDetectionService` が末端地物排他「地図全体」へ移行済みなのに、EdgeSlide の障害物検出だけ旧モデル『同一レイヤー内のみ障害物』に残存していた整合性ズレを是正。要件定義書 §2.1 / 開発ガイド §6.6.8 遵守）
- リネーム: `collectSameLayerPolygonObstacleRings` → `collectPolygonObstacleRings` / `collectSameLayerPolygonObstacleVertices` → `collectPolygonObstacleVertices`（"SameLayer" 用語撤廃に同期。§6.0.1 検出観点1 grep で App.svelte 呼び出し2箇所 + テスト import + 2 テスト呼び出しを一括同期）
- テスト更新: `tests/presentation/app/appPolygonEditing.test.ts` の 2 テスト description を「同一レイヤーの他ポリゴンだけを...集める」→「ソース地物を除く全ての面地物の...を集める」へ書き換え + 期待値修正（other-layer のリングも障害物に含まれる）。テスト件数は変更なし
- 振る舞い変更: 別レイヤーの面地物も EdgeSlide 障害物に含むようになる。Phase 2-D-5 でユーザーが新規レイヤーを追加・削除できなくなった後は実質単一レイヤー前提のため runtime 観測挙動は変わらない（dead semantics 撤去）
- スコープ: src 2 ファイル（`edgeSlideConstraintCollectors.ts` 全書き直し + `App.svelte` import/呼び出し3箇所）+ tests 1 ファイル / +約 35 -約 35 行。§6.10 上限内
- **触らない**（D-6-3b で対応）: `placement.layerId` フィールド本体 / `createAnchorPlacement` シグネチャ / 他 src consumer の `anchor.placement.layerId` 参照（App.svelte / PropertyProjection / jsonSerializerValidation / UpdateFeatureAnchorUseCase / MergeFeatureCommand / ReassignFeatureParentUseCase / SplitFeatureCommand / HierarchyService / DeleteFeatureUseCase / parentTransferDialogUtils / PropertyPanel / FeatureAnchor 本体）/ `JsonFeatureAnchor.placement.layerId` + serialize / deserialize / migration / `validateJsonWorld`

###### Phase 2-D-6-3b: src 層の `placement.layerId` 全 consumer narrow 撤去 + `createAnchorPlacement` シグネチャ変更（未着手）

- 撤去: 残る src consumer（App.svelte / `PropertyProjection.ts` / `UpdateFeatureAnchorUseCase` / `MergeFeatureCommand` / `ReassignFeatureParentUseCase` / `SplitFeatureCommand` / `HierarchyService` 内 4 箇所 / `DeleteFeatureUseCase` / `parentTransferDialogUtils.ts` / `PropertyPanel.svelte`）の `anchor.placement.layerId` 参照と引き渡し
- `createAnchorPlacement(layerId, parentId, childIds)` → `createAnchorPlacement(parentId, childIds)` シグネチャ変更
- `FeatureAnchor.AnchorPlacement.layerId` 型フィールド削除
- **触らない**（D-6-3c で対応）: JSON 永続化（型 / serialize / deserialize / migration / `validateJsonWorld`）+ テスト fixture の `layerId:` リテラル

###### Phase 2-D-6-3c: JSON 永続化の `layerId` 撤去 + テスト fixture リテラル同期撤去（未着手）

- `JsonFeatureAnchor.placement.layerId` 型 + `serializeAnchor` / `deserializeAnchor` の layerId 入出力 + `jsonSerializerMigration.ts` の layerId 派生 + `jsonSerializerValidation.ts` の layerId 存在検証撤去
- `.gimoza` 形式から layerId フィールド消滅。旧形式 fallback も撤去（互換破棄が完成する地点）
- テスト側の `layerId: 'l1'` / `'default'` / `'layer-1'` リテラルを 29 ファイル横断で同期撤去

###### Phase 2-D-7（未着手）

- `Layer` エンティティ / `Layer.ts` / `World.layers` フィールドの最終削除 + EventBus `layers:changed` / `layer:visibilityChanged` イベント型の削除 + `SaveLoadUseCase` 暫定 `getLayers()` / `setLayers()` API 撤去 + `App.svelte` 初期化の `new Layer('default', ...)` 撤去（D-3〜D-6-3 で参照が消えた後のクリーンアップ）
- **`<MapCanvas {features}>` props 経路の整理**: D-6-1+2 で `MapCanvas` 内の `features` 利用箇所は `selectionAnchor` derive の `features.find(...)` のみになった。選択中地物が必ず sceneEntries に含まれる前提が成立すれば `sceneEntries.find(e => e.feature.id === selectionFeatureId)?.anchor` で同等の結果を得られ、`features` props を撤去できる。ただし「sceneEntries に入らない時刻外の選択中地物」が発生し得るかは Phase 2-D-7 着手時に検証が必要（現状経路は安全側）。Phase 2-D-6-1+2 完了時のレビュー Low / Info として記録
- **コメント内 phase 参照の棚卸し**: Phase 2-D 完全終了時点で、コード内 JSDoc / コメントに残る `Phase 2-D-X` / `Phase 2-C-X` / `Phase 2-B` 等の参照は履歴情報となり stale 化リスクが高い。該当箇所を grep し（`hitTestUtils.ts:174-176` / `vertexSelectionContext.ts` / `featureRendererUtils.ts` / `AutoColorService.ts` 等）、現在の挙動説明として有用なもののみ残し、移行記録に過ぎないものは削除する。残す場合も「Phase X 後の挙動」ではなく「現状の挙動」を主語にする

各サブフェーズ着手前にスコープを明示し、~15 ファイル / ~300 行上限を守る。**削除のみのサブフェーズでも削除行数を着手宣言に含める**（ファイル削除は modified より対概念リスクは低いが、依存連鎖が広がるため着手前の規模把握は必須。Phase 2-D-3 では削除 940 行 + 修正 130 行で実態としては上限近接だった）。

##### Phase 2-E: depth 派生サービス追加

D-2 でリネームした `HierarchyService` に depth 派生関数（対象時刻における当該地物の有効錨のツリー位置から階層深度を派生算出）を追加する。Phase 2.5 / Phase 5 / Phase 6 で利用。

#### Phase 2.5: コンテナ UI 基盤 (Phase 3 の前に挿入)

Phase 2-C 完了時点では containers が UI に出ない。Phase 4 が containers を作り始めると不可視・選択不能になるため、本 Phase で UI 基盤を揃える。各ステップを独立サブフェーズとして切る (Phase 2-C の経験から1段で全部やると追補無限ループになる)。

- **2.5-A**: `HierarchyService.deriveParentShape` の正式実装。再帰下降 + `polygonUnionAll` で多 polygon 保持 + `visited` セットによる cycle guard
- **2.5-B**: 描画。`FeatureRenderer` + `LabelRenderer` でコンテナを派生形状から描画。同レイヤー描画順は depth 昇順で「親→子」順
- **2.5-C**: ヒットテスト。`hitTestUtils` でコンテナをヒット対象に。layer + depth + DOM target の3キー tie-break。`App.svelte` で hitTest を最終決定として採用。**Phase 2-D-6-1 完了時点では `App.svelte:1057-1062` の `clickedFeatureId`（DOM 由来 `data-feature-id`）即採用パスが残存しており、開発ガイド §6.2.25 採用ポリシー（hitTest 優先 + DOM target を第3 tie-break）と逆向きになっている。本 Phase 2.5-C で hitTest を最終決定に格上げし、`preferredFeatureId` に DOM target を渡す 3 キー tie-break へ再設計する**（Phase 2-D-6-1 完了時のレビューで Info として記録）
- **2.5-D**: `PropertyPanel` のコンテナ表示。種別「集約地物」、親子・style 編集 UI
- **2.5-E**: `parentTransferDialog` のコンテナ許容。`getActivePolygonAnchor` を `feature.featureType === 'Polygon'` 基準に

#### Phase 3: バリデーション

リーフ排他を地図全体で適用、親 ≡ 子の和、同一錨内で `placement.isTopLevel === (placement.parentId === null)` 不変条件。

#### Phase 4: 操作 UseCase / Command 再設計

新規親作成ループ、所属変更の連邦化対応、最上位フラグ遷移、Undo / Redo 同梱。

#### Phase 5: 表示フォーカス UI

グローバル depth スライダー + 個別フォーカス併存。

#### Phase 6: 自動配色 + 個別スタイル

同 depth 兄弟基準。

#### Phase 7: `kind` プロパティ運用、Sidebar UI 整理

#### Phase 8: 新規サンプルプロジェクト

`.gimoza` 互換破棄に伴いゼロから組み直す。

### 6.11 完了後の作業

- 開発ガイド §6 へ追加すべき新教訓:
    - 「リーフ排他は地図全体で適用 (同一親内に限定しない)」
    - 「親 ≡ 子の和を厳守し、隙間は明示的リーフで埋める」
    - 「コンテナは `shape` プロパティ自体を持たず、形状は子の和として導出する」
    - 「同一錨内で `placement.isTopLevel === (placement.parentId === null)` 不変条件を全コマンドで維持する」
    - 「`kind` は表示専用で整合性に関与させない」
    - 「征服と連邦化は同一所属変更フロー (UI 分岐は新規親作成サブフロー)」
- 既存実装で再設計対象になるもの (現状 main にある旧モデル前提コード):
    - `Layer` 関連残置: `Layer.ts` / `World.layers` フィールド（`LayerService.ts` は Phase 2-D-2 で `HierarchyService.ts` にリネーム済み。`ManageLayersUseCase.ts` / `LayerQueryService.ts` / `LayerPanel.svelte` / Sidebar レイヤータブは Phase 2-D-3 で削除済み。`ProjectSettingsDialog.svelte` のレイヤー編集セクションは Phase 2-D-5 で削除済み）
    - `FeatureAnchor.placement.layerId` を参照する全箇所 (描画 / 自動配色 / 排他判定 / ヒットテスト) — 排他判定は Phase 2-D-1 で撤去済み、自動配色のレイヤー単位グルーピングは Phase 2-D-5 で撤去済み、残りは D-6 のスコープ
    - `SaveLoadUseCase` の暫定 `getLayers()` / `setLayers()` API（Phase 2-D-3 で導入、Phase 2-D-7 で `World.layers` 廃止と同時に撤去）
    - `ReassignFeatureParentUseCase` / `ReassignFeatureParentCommand` (連邦化対応の追加)
    - `AddFeatureCommand.parentId` (新規親作成ループ統合)
    - `ParentTransferDialog` (新規親作成サブフロー追加)
    - `JSONSerializer` / `.gimoza` 形式 (互換破棄、新形式へ全面置換)

### 6.12 用語と内部実装識別子の対応

要件定義書 (§2.1 改訂時) は仕様本文で和訳語のみを使用し、英語識別子は内部実装補遺へ集約する (開発ガイド §重要な注意事項 の規約)。新 §6 で導入する概念の対応表を以下に固定する。既存仕様で確立済みの語 (歴史の錨 / 親領域 / 下位領域 / リング / 領土リング / 穴リング / 飛び地 / リーフノード) はそのまま継承する。

#### 概念・データモデル

| 仕様用語 (和訳) | 内部実装識別子 | 補足 |
| --- | --- | --- |
| 地物 | `Feature` | 既存訳の継承 |
| 歴史の錨 | `FeatureAnchor` | 既存訳の継承 |
| 形状 | `FeatureAnchor.shape`? (要件定義書 §4.1 で optional 化確定。Phase 2 でコード反映予定) | 新モデルでは optional (末端地物のみ保持)。現行コードは required。Phase 2 で型変更 |
| 親領域 (親) | `parent` / `FeatureAnchor.placement.parentId` | 既存訳の継承 |
| 下位領域 (子) | `childIds` / `FeatureAnchor.placement.childIds` | 既存訳の継承 |
| 最上位フラグ | `FeatureAnchor.placement.isTopLevel` | 真のとき当該錨の有効期間において親領域を持たないルート地物。時間依存（同一地物でも錨ごとに異なる）。Phase 2 サブフェーズ B で実装済み（`src/renderer/domain/value-objects/FeatureAnchor.ts`）。生成は `createAnchorPlacement(layerId, parentId, childIds)` ヘルパー経由で `parentId === null` から派生する |
| 種別 | `FeatureAnchor.property.kind`? (要件定義書 §4.1 で識別子確定。Phase 2 でコード実装予定) | 表示専用ラベル (国 / 州 / 郡 / 町 / 連邦 / 植民地 など)。整合性検証に関与しない。錨側に保持（時間依存）。実コード未実装 |
| 末端地物 (リーフ) | leaf (概念名) | 形状を保持し下位領域を持たない地物。「リーフノード」既存訳の継承。コード上の専用型は未定 (Phase 3 で `Feature` 型ガードまたは派生型として実装予定) |
| 集約地物 (コンテナ) | container (概念名) | 下位領域を持ち形状プロパティを持たない地物。新モデルで導入する語。コード上の専用型は未定 (Phase 3 で `Feature` 型ガードまたは派生型として実装予定) |
| 階層深度 | depth (概念名、Phase 3 で派生サービス実装予定) | 派生値。最上位は 0、子は親の階層深度 + 1。実コード未実装 |
| 個別スタイル | (Phase 6 で命名予定。仮称 `Feature.style` (optional)) | 未指定時は自動配色を継承。実コード未実装 |
| プロジェクトファイル | `.gimoza` | 既存訳の継承 (新モデルでは互換破棄) |

#### 不変条件・整合性

| 仕様用語 (和訳) | 内部表現 |
| --- | --- |
| 「最上位フラグの真と親領域なしは同値」 | (同一錨内) `FeatureAnchor.placement.isTopLevel === (FeatureAnchor.placement.parentId === null)`。Phase 2 サブフェーズ B で実装済み（生成は `createAnchorPlacement` ヘルパー集約 + JSON migration / `validatePlacementInvariants` バリデーション + 主要コマンド・UseCase が同ヘルパー経由で再構築） |
| 「末端地物排他は地図全体で適用」 | leaf-leaf overlap forbidden across the entire map |
| 「親領域は下位領域の和に厳密一致」 | parent shape ≡ union of children shapes |

#### 操作

| 仕様用語 (和訳) | 内部実装識別子 | 補足 |
| --- | --- | --- |
| 所属変更 | `ReassignFeatureParentUseCase` / `ReassignFeatureParentCommand` | 既存訳の継承 |
| 所属変更ダイアログ | `ParentTransferDialog` | 既存訳の継承 |
| 新規親作成サブフロー | (新規) `CreateNewParentCommand` (仮称) | 所属変更フロー末端で起動。再帰的に親を積み上げ可能 |
| 地物追加コマンドの親指定 | `AddFeatureCommand.parentId` | 既存訳の継承 |
| 征服 (既存親領域への割譲) | (Phase 4 で命名予定。仮称 `conquest`) | 仕様用語は「割譲」(既存訳)。新モデルでは新規親作成サブフローと同一所属変更フローで表現。実コード未実装 |
| 連邦化 (新規共通親領域の作成) | (Phase 4 で命名予定。仮称 `federation`) | 同上。新規親作成サブフローを使う所属変更。実コード未実装 |

#### 表示

| 仕様用語 (和訳) | 内部実装識別子 | 補足 |
| --- | --- | --- |
| 階層深度スライダー (グローバル深度フォーカス) | (Phase 5 で命名予定。仮称 `global depth slider`) | 全地物を「階層深度 ≤ N」で集約表示。実コード未実装 |
| 個別フォーカス (個別深度フォーカス) | (Phase 5 で命名予定。仮称 `individual focus`) | 選択地物の縦方向領域 (上位領域すべて + 自身 + 下位領域すべて) だけを深掘り。実コード未実装 |
| 縦方向領域 | (Phase 5 で命名予定。仮称 `lineage`) | 上位領域すべて + 自身 + 下位領域すべての集合。仕様本文では「祖先・子孫・系譜」用語を使わず説明的表現に揃える (時間軸の祖先・子孫と概念混同を避けるため)。実コード未実装。Phase 5 着手時に正式な内部識別子を確定する |
| 縦方向領域の絞り込み | (Phase 5 で命名予定。仮称 `lineage filter`) | 縦方向領域に登場する種別だけを認知対象に絞る。実コード未実装 |
