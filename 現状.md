# 現状.md — 未解決課題・既知不具合・残作業

> 完了した実装の履歴は `実装済み.md` を参照。
> テスト品質改善の計画と実施記録は `テスト改善計画.md` を参照。
> 完了した課題は即時削除する（開発ガイド§重要な注意事項 参照）。

## テスト総数

- ユニットテスト: 1288件（527ファイル、全パス）
- E2Eテスト: 173件（22ファイル、全パス）
- `npm run build`: 通過
- `npm run check`: 0 errors / 0 warnings

---

## 1. 既知の不具合

状態ラベル: **壊** = 機能が壊れている / **未接続** = ロジックはあるがUI未接続 / **部分** = 一部のみ実装

### 1.1 重大（データ整合性・基本機能に影響）

> 重大課題がない場合もこの見出しは維持し、`追跡無し` と明記する。

追跡無し

### 1.2 中（機能不完全）

追跡無し

### 1.3 低（UX・表示）

追跡無し

---

## 2. 未実装機能

### 2.1 統合品質（最優先）

追跡無し

### 2.2 機能要件

| 機能 | 優先度 | 備考 |
| --- | --- | --- |
| Logger | 低 | |

### 2.3 非機能要件

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| Electron実機統合E2E | 中 | IPC/preload/ファイルアクセスは Vitest で補強済み。Electron 実起動の統合E2Eは未整備 |
| a11yセマンティクス改善 | 低 | 主要メニュー/ダイアログと `App` の D&D 受け口はネイティブ/ウィンドウイベント化済み。`MapCanvas` / `VertexHandles` に少数残存 |
| パフォーマンス最適化（空間インデックス、LOD、カリング） | 低 | |

---

## 3. 構造的課題

外部レビュー（2026-03-25）で指摘された、不具合ではないが改善すべき構造問題。

| # | 深刻度 | 問題 | 備考 |
| --- | --- | --- | --- |
| S1 | 中 | `App.svelte` 1809行。`MapCanvas.svelte` は 590行まで縮小し `mapCanvas/MapCanvasSvgLayers.svelte` / `MapCanvasHud.svelte` へ分割済み | `appPolygonEditing.ts` などへの抽出で前進したが、開発ガイドの200行目安にはなお未達。引き続き `App.svelte` のコントローラ分割が必要 |
| S5 | 中 | E2EがElectron統合を検証できていない | IPC/preload/ファイルアクセスは Vitest で補強済み。Chromium 136+ CLI制限により Electron 実起動 E2E は未整備 |
| S6 | 低 | `ResolveFeatureAnchorConflictsUseCase.createResolvedId` が `Date.now()` + `Math.random()` で非決定的ID生成 | UndoRedo時にIDが再生成される可能性あり。現状は問題ないが、ID永続化が必要になった場合は改修が必要 |
| S7 | 低 | `ResolveFeatureAnchorConflictsUseCase.resolve` が `ReadonlyMap` を `Map` にキャストしてVertex Storeを直接変更 | CQRSの原則からはcommand経由が理想。動作上は問題ないが、副作用の追跡が難しくなる |

---

## 4. テスト品質

> 詳細は [`テスト改善計画.md`](テスト改善計画.md) を参照。

| 項目 | 状態 |
| --- | --- |
| フェーズ1: エッジケース補強 | 完了（+53件） |
| フェーズ2: カバレッジ駆動改善 | 計画済み（+42〜62件予定）。`ResolveFeatureAnchorConflictsUseCase` 24%, `EditFeatureUseCase` 61% 等が重点 |
| フェーズ3: 複合シナリオ | 計画済み（+21〜30件予定）。操作連鎖の結合テスト |
| フェーズ4: E2E拡充 | 計画済み（+10〜20件予定） |
| テスト中に発見された不具合 | なし（発見次第ここに追記） |

---

## 5. 修正優先順位

外部レビュー・要件差分・2026-04-09時点の実装状況を統合した推奨順序:

1. テスト品質改善（`テスト改善計画.md` フェーズ2〜3）
2. 構造改善（S1）
3. E2Eテスト拡充（`テスト改善計画.md` フェーズ4）+ Electron実機統合E2E + 非機能改善
4. Logger
