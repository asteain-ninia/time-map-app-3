# 現状.md — 未解決課題・既知不具合・残作業

> 完了した実装の履歴は `実装済み.md` を参照。
> 完了した課題は即時削除する（開発ガイド§重要な注意事項 参照）。

## テスト総数

- ユニットテスト: 1212件（77ファイル、全パス）
- E2Eテスト: 172件（22ファイル、全パス）
- `npm run build`: 通過
- `npm run check`: 0 errors / 0 warnings

---

## 1. 既知の不具合

状態ラベル: **壊** = 機能が壊れている / **未接続** = ロジックはあるがUI未接続 / **部分** = 一部のみ実装

### 1.1 重大（データ整合性・基本機能に影響）

> 重大課題がない場合もこの見出しは維持し、`追跡無し` と明記する。

追跡無し

### 1.2 中（機能不完全）

追跡無し

### 1.3 低（UX・表示）

追跡無し

---

## 2. 未実装機能

### 2.1 統合品質（最優先）

追跡無し

### 2.2 機能要件

| 機能 | 優先度 | 備考 |
| --- | --- | --- |
| 2周超（720°超）地物の表示 | 低 | 描画タイル数の限界により2周超の地物は表示されない。実用上は問題にならない |
| 排他判定の経度範囲拡張 | 低 | 重なり判定が-180〜180前提のため、180°超に延伸した部分と通常範囲の地物の重なりが検出されない |
| Logger | 低 | |

### 2.3 非機能要件

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| Electron実機統合E2E | 中 | IPC/preload/ファイルアクセスは Vitest で補強済み。Electron 実起動の統合E2Eは未整備 |
| a11yセマンティクス改善 | 低 | 主要メニュー/ダイアログはネイティブ要素化済み。`MapCanvas` / `VertexHandles` / `App` に少数残存 |
| パフォーマンス最適化（空間インデックス、LOD、カリング） | 低 | |

---

## 3. 構造的課題

外部レビュー（2026-03-25）で指摘された、不具合ではないが改善すべき構造問題。

| # | 深刻度 | 問題 | 備考 |
| --- | --- | --- | --- |
| S1 | 中 | App.svelte 1958行、MapCanvas.svelte 853行 | 開発ガイドの200行目安を大幅超過。機能別コントローラへの分割が必要 |
| S5 | 中 | E2EがElectron統合を検証できていない | IPC/preload/ファイルアクセスは Vitest で補強済み。Chromium 136+ CLI制限により Electron 実起動 E2E は未整備 |

---

## 4. 修正優先順位

外部レビュー・要件差分・2026-04-08時点の実装状況を統合した推奨順序:

1. 構造改善（S1）
2. Electron実機統合E2Eと非機能改善（残a11y / パフォーマンス）
3. 低優先の描画/判定制限と Logger
