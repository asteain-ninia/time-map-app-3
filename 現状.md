# 現状.md — 未解決課題・既知不具合・残作業

> 完了した実装の履歴は `実装済み.md` を参照。
> 完了した課題は即時削除する（開発ガイド§重要な注意事項 参照）。

## テスト総数

- ユニットテスト: 1207件（76ファイル、全パス）
- E2Eテスト: 172件（22ファイル、全パス）
- `npm run build`: 通過
- `npm run check`: 0 errors / 0 warnings

---

## 1. 既知の不具合

状態ラベル: **壊** = 機能が壊れている / **未接続** = ロジックはあるがUI未接続 / **部分** = 一部のみ実装

### 1.1 重大（データ整合性・基本機能に影響）

> 重大課題がない場合もこの見出しは維持し、`追跡無し` と明記する。

追跡無し

### 1.2 中（機能不完全）

追跡無し

### 1.3 低（UX・表示）

追跡無し

---

## 2. 未実装機能

### 2.1 統合品質（最優先）

追跡無し

### 2.2 機能要件

| 機能 | 優先度 | 備考 |
| --- | --- | --- |
| 2周超（720°超）地物の表示 | 低 | 描画タイル数の限界により2周超の地物は表示されない。実用上は問題にならない |
| 排他判定の経度範囲拡張 | 低 | 重なり判定が-180〜180前提のため、180°超に延伸した部分と通常範囲の地物の重なりが検出されない |
| Logger | 低 | |

### 2.3 非機能要件

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| Electron統合テスト（IPC/preload/ファイルアクセス） | 中 | ブラウザE2E中心。Electron固有経路の実検証が弱い |
| a11yセマンティクス改善 | 低 | `svelte-ignore` が複数残り、クリック可能 `div` も残存 |
| パフォーマンス最適化（空間インデックス、LOD、カリング） | 低 | |

---

## 3. 構造的課題

外部レビュー（2026-03-25）で指摘された、不具合ではないが改善すべき構造問題。

| # | 深刻度 | 問題 | 備考 |
| --- | --- | --- | --- |
| S1 | 中 | App.svelte 1843行、MapCanvas.svelte 786行 | 開発ガイドの200行目安を大幅超過。機能別コントローラへの分割が必要 |
| S3 | 低 | JSONSerializer.ts 548行 | 300行目安を超過 |
| S5 | 中 | E2EがElectron統合を検証できていない | Chromium 136+ CLI引数制限でElectron直接起動断念。IPC/preload/ファイル操作の実検証が弱い |

---

## 4. 修正優先順位

外部レビュー・要件差分・2026-04-08時点の実装状況を統合した推奨順序:

1. 構造改善（S1 / S3）
2. Electron統合テストと非機能改善（a11y / パフォーマンス）
3. 低優先の描画/判定制限と Logger
