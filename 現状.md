# 現状.md — 未解決課題・既知不具合・残作業

> 完了した実装の履歴は `実装済み.md` を参照。
> テスト品質改善の計画と実施記録は `テスト改善計画.md` を参照。
> 完了した課題は即時削除する（開発ガイド§重要な注意事項 参照）。

## テスト総数

- ユニットテスト: 1383件（92ファイル、全パス）
- E2Eテスト: 177件（22ファイル、全パス）
- `npm run build`: 通過
- `npm run check`: 0 errors / 0 warnings

---

## 1. 既知の不具合

状態ラベル: **壊** = 機能が壊れている / **未接続** = ロジックはあるがUI未接続 / **部分** = 一部のみ実装

### 1.1 重大（データ整合性・基本機能に影響）

> 重大課題がない場合もこの見出しは維持し、`追跡無し` と明記する。

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| - | 追跡無し | 追跡中の重大不具合なし | |

### 1.2 中（機能不完全）

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| - | 追跡無し | 追跡中の中程度不具合なし | |

### 1.3 低（UX・表示）

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| B15 | 未対応 | 機能の詳細な仕様が要件定義書に不足。実装と要求にズレが生じている可能性 | 結合ツール・レイヤー操作・表示/編集モード・共有頂点・地物移動など、あるべき姿の文書化が追いついていない |
| B21 | 未対応 | ファイル変更の自動リロード後、モードを切り替えるとリロード前の地物が復元されて見える（逆に自動リロード直後は地物が表示されない可能性） | リロード時のストア更新と Svelte 再描画の同期に不整合。リリース後は主要経路でない |

---

## 2. 未実装機能

### 2.1 統合品質（最優先）

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| 手動テスト用コーナーケース完備セーブデータ整備 | 中 | `TEST-savedata.gimoza` は現状 gitignore 済み。レイヤー/共有頂点/穴内島/時間範囲境界/長大ポリゴン/マルチポリゴン結合などを網羅した .gimoza を作成し、完成次第追跡対象へ戻してリポジトリに同梱する |

### 2.2 機能要件

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| 所属変更UI（割譲・合邦・親指定） | 中 | 要件定義書§2.1で定義され、[MergeService](src/renderer/domain/services/MergeService.ts) に `TransferType` と `validateTransfer/buildAnnexation/buildCession` は実装済み。ただし UI 接続が未実装。[AddFeatureUseCase.ts:228](src/renderer/application/AddFeatureUseCase.ts#L228) も常に `parentId: null` で作成しており、地物作成時の親指定ダイアログも未実装 |
| README 拡充 | 中 | 現 README は最小限。プロジェクト概要・スクリーンショット・主要機能一覧・インストール/起動/ビルド手順・開発フロー・ライセンスなどを整備する |
| スクショ付き詳細マニュアル整備 | 中 | README / 要件定義書 / 技術方針 / 開発ガイドとは別に、エンドユーザー向けの操作マニュアル（レイヤー管理・地物作成・時間軸操作・分割/結合・保存/読込 などをスクリーンショット付きで解説）を整備する |
| .gimoza ファイル形式のOS統合 | 中 | `.gimoza` の無圧縮ZIP読み書き（project.json + assets/base-map.svg）は実装済み。ファイル関連付け・専用アイコンは未実装。アプリへのアイコン追加も必要。 |
| 初回起動体験 | 中 | プリセット地図表示は通常起動で動作。地図SVG欠損時に `base-map.svg` を確実に同梱・表示する配布時検証が未完了 |
| データ互換性警告・マイグレーション | 中 | version不一致検出は実装済み。旧形式からの自動マイグレーションとユーザー向け差分警告は未実装 |
| エラーメッセージのユーザー向け改善 | 低 | ファイル操作エラーはユーザー向け文面へ改善済み。ドメイン検証・ZIP破損などの詳細分類は未整理 |

### 2.3 非機能要件

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| Electron実機統合E2E | 中 | IPC/preload/ファイルアクセスは Vitest で補強済み。Electron 実起動の統合E2Eは未整備 |
| electron-builder導入・パッケージング設定 | 高 | 配布用インストーラ(.exe等)の生成。electron-updaterはv1.0.0では不要 |
| package.jsonメタデータ整備 | 低 | author/licenseは設定済み。repository URLは未設定 |
| パフォーマンス最適化（空間インデックス、LOD、カリング） | 低 | |

---

## 3. 構造的課題

外部レビュー（2026-03-25）で指摘された、不具合ではないが改善すべき構造問題。

| # | 深刻度 | 問題 | 備考 |
| --- | --- | --- | --- |
| S1 | 中 | `App.svelte` は引き続き大きい。`MapCanvas.svelte` は `mapCanvas/MapCanvasSvgLayers.svelte` / `MapCanvasHud.svelte` へ分割済み | `appPolygonEditing.ts` などへの抽出で前進したが、開発ガイドの200行目安にはなお未達。引き続き `App.svelte` のコントローラ分割が必要 |
| S5 | 中 | E2EがElectron統合を検証できていない | IPC/preload/ファイルアクセスは Vitest で補強済み。Chromium 136+ CLI制限により Electron 実起動 E2E は未整備 |

---

## 4. テスト品質

> 詳細は [`テスト改善計画.md`](テスト改善計画.md) を参照。

| 項目 | 状態 |
| --- | --- |
| フェーズ1: エッジケース補強 | 完了（+53件） |
| フェーズ2: カバレッジ駆動改善 | 進行中（今回 `.gimoza` / IPC / SVG / EdgeSlide接続 / 測量クリアに +17件、結合/共有頂点移動/EdgeSlide交差/境界始点/境界接触/No-op Undo/地物ドラッグ復元/複数territory再マージ/穴内島マージに +15件、任意SVGプロジェクト設定/保存連携に +8件、ロード時データ整合性検証/共有頂点付き複数頂点移動に +7件、EdgeSlide境界滑り/移動辺-頂点衝突に +12件、頂点挿入/削除Undoと分割MultiPolygon保持/複数territoryナイフ確定判定/挿入削除Redo ID固定/複数territory開線判定/分割時リング階層保持/非横断territory保持/PolygonShapeService階層再構築に +17件、追加/分割/結合コマンドの生成ID redo 固定に +3件）。引き続き `EditFeatureUseCase` 61% 等が重点 |
| フェーズ3: 複合シナリオ | 計画済み（+21〜30件予定）。操作連鎖の結合テスト |
| フェーズ4: E2E拡充 | 計画済み（+10〜20件予定） |
| テスト中に発見された不具合 | なし（発見次第ここに追記） |

---

## 5. 修正優先順位

2026-04-18時点の推奨順序（v1.0.0配布に向けて）:

1. 配布準備（electron-builder、Windows .ico、ファイル関連付け、repositoryメタデータ）
2. 旧形式マイグレーション + ユーザー向け差分警告
3. 要件定義書の仕様補完（B15）
4. テスト品質改善（`テスト改善計画.md` フェーズ2〜3）
5. 構造改善（S1）
6. E2Eテスト拡充（`テスト改善計画.md` フェーズ4）+ Electron実機統合E2E + パフォーマンス改善
