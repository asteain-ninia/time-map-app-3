# 現状.md — 未解決課題・既知不具合・残作業

> 完了した実装の履歴は `実装済み.md` を参照。
> テスト品質改善の計画と実施記録は `テスト改善計画.md` を参照。
> 完了した課題は即時削除する（開発ガイド§重要な注意事項 参照）。

## テスト総数

- ユニットテスト: 1561件（97ファイル、全パス）
- E2Eテスト: 172件（21ファイル、全パス）
- `npm run build`: 通過
- `npm run check`: 0 errors / 0 warnings

---

## 1. 既知の不具合

状態ラベル: **壊** = 機能が壊れている / **未接続** = ロジックはあるがUI未接続 / **部分** = 一部のみ実装

### 1.1 重大（データ整合性・基本機能に影響）

> 重大課題がない場合もこの見出しは維持し、`追跡無し` と明記する。

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| - | 追跡無し | 追跡中の重大不具合なし | |

### 1.2 中（機能不完全）

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| - | 追跡無し | 追跡中の中程度不具合なし | |

### 1.3 低（UX・表示）

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| B15 | 未対応 | 機能の詳細な仕様が要件定義書に不足。実装と要求にズレが生じている可能性 | 結合ツール・レイヤー操作・表示/編集モード・共有頂点・地物移動など、あるべき姿の文書化が追いついていない |
| B26 | 未対応 (§6 新モデル Phase 2 で解消) | 階層モデルが旧 `Layer` 前提のままで、リーフ排他・親子順序の整合性が地図全体で検証されていない | グローバルレイヤー序列ではなく「ツリー位置から派生する depth + リーフ排他を地図全体に適用」へ転換する。現行の `Layer` エンティティ／`FeatureAnchor.placement.layerId` は新モデルでは廃止対象。詳細は §6 |
| B27 | 未対応 (§6 新モデル Phase 2 で解消) | 子地物を持つ面情報が自身のリング（`FeatureAnchor.shape`）を保持し続ける | 新モデルでは `FeatureAnchor.shape` は optional で、リーフ（子を持たない面情報）のみが保持する。コンテナは shape プロパティ自体を持たず、形状は子の和として実行時に導出する。`.gimoza` 互換性は破棄する方針。詳細は §6 |

---

## 2. 未実装機能

### 2.1 統合品質（最優先）

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| 手動テスト用コーナーケース完備セーブデータ整備 | 中 | `TEST-savedata.gimoza` は現状 gitignore 済み。レイヤー/共有頂点/穴内島/時間範囲境界/長大ポリゴン/マルチポリゴン結合などを網羅した .gimoza を作成し、完成次第追跡対象へ戻してリポジトリに同梱する |

### 2.2 機能要件

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| **階層モデルの新仕様への置換** | **高** | 詳細は §6 を参照。`Layer` 廃止 + `FeatureAnchor.placement.isTopLevel` / `FeatureAnchor.property.kind` を錨側に持つツリーモデルへ全面置換する（時間依存スナップショット）。既存 `.gimoza` 互換は破棄。既存「所属変更 (既存親指定)」「面追加時の親指定」UI と `ReassignFeatureParentUseCase` / `AddFeatureCommand.parentId` は新モデル前提で再設計対象。Phase 計画と進捗・残作業の詳細は §6.10 を参照。要件定義書の他セクション（旧「レイヤー」概念の言及が残存）は各 Phase 着手と同じ変更単位で改訂する（古い仕様参照を防ぐ）: **Phase 3 着手時** に §2.2.3 整合性維持プロセスの構造的改訂（バリデーションの前提）— ※ §2.2.3 内の「同一レイヤー」用語そのもの（line 442 / 447）は Phase 2-D-6-3a で先行同期済み、**Phase 4 着手時** に §2.3.2 追加モードの親指定 UI / §2.3.3 編集ツールの操作 UseCase / Command 再設計反映 — ※ §2.3.2 / §2.3.3 内の「同一レイヤー」用語そのもの（line 564 / 565 / 713 / 721）は Phase 2-D-6-3a で先行同期済み、**Phase 5 着手時** に §2.7 UI 内のレイヤースライダー言及（表示フォーカス UI への置換）、**Phase 6 着手時** に §2.4.4 自動配色 / §2.4.4.6 レイヤー表示とテーマ設定（自動配色実装の前提。§2.4.4.3 line 851 の「同一レイヤー内」用語は本フェーズで対応）、**Phase 7 着手時** に §2.7 UI 内のサイドバー構成（Sidebar UI 整理の前提）、~~**Phase 2-D-7 着手時**（Layer エンティティ最終削除）に §2.3.3.1「所有者不明の場合」の旧モデル文言を改訂~~（Phase 2-D-7c-2b で改訂済み: 「可視レイヤー上の所有者」→「現在時刻でアクティブな所有地物」/「可視レイヤーの地物を直接選択してください」→「地物を直接選択してください」）。各 Phase の実装着手前にスコープ章を改訂しておく |
| スクショ付き詳細マニュアル整備 | 中 | README / 要件定義書 / 技術方針 / 開発ガイドとは別に、エンドユーザー向けの操作マニュアル（地物作成・時間軸操作・分割/結合/所属変更・保存/読込 などをスクリーンショット付きで解説）を整備する。**重点解説項目**: 結合機能と所属変更フローの使い分け（結合は選択対象を1つの結果地物にまとめる操作で**結合の主体**（リーフのみは選択リーフ全体、集約のみは選択集約全体、混在は選択集約のみ）が同一性を終了する。混在では末端地物が存在継続し新統合集約地物の下位領域へ取り込まれるため選択地物の総数は減らない場合がある。所属変更は地物数を保ったままツリー位相を変える。連邦化など階層を保ったまま束ねたい場合は結合ではなく所属変更フローの新規上位領域作成サブフローを使う）。ユーザーが直感的に混同しやすいため、操作別の使い分け対照表とスクリーンショットで明示する |
| 初回起動体験 | 中 | プリセット地図表示は通常起動で動作。`npm run dist:win` で配布物生成と `base-map.svg` の同梱設定は通過。インストーラ経由の初回起動確認は未完了 |
| エラーメッセージのユーザー向け改善 | 低 | ファイル操作エラーはユーザー向け文面へ改善済み。ドメイン検証・ZIP破損などの詳細分類は未整理 |

### 2.3 非機能要件

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| Electron実機統合E2E | 中 | IPC/preload/ファイルアクセスは Vitest で補強済み。Electron 実起動の統合E2Eは未整備 |
| パフォーマンス最適化（空間インデックス、LOD、カリング） | 低 | |

---

## 3. 構造的課題

外部レビュー（2026-03-25）で指摘された、不具合ではないが改善すべき構造問題。

| # | 深刻度 | 問題 | 備考 |
| --- | --- | --- | --- |
| S1 | 中 | `App.svelte` は引き続き大きい。`MapCanvas.svelte` は `mapCanvas/MapCanvasSvgLayers.svelte` / `MapCanvasHud.svelte` へ分割済み | `appPolygonEditing.ts` などへの抽出で前進したが、開発ガイドの200行目安にはなお未達。引き続き `App.svelte` のコントローラ分割が必要 |
| S5 | 中 | E2EがElectron統合を検証できていない | IPC/preload/ファイルアクセスは Vitest で補強済み。Chromium 136+ CLI制限により Electron 実起動 E2E は未整備 |

---

## 4. テスト品質

> 詳細は [`テスト改善計画.md`](テスト改善計画.md) を参照。

| 項目 | 状態 |
| --- | --- |
| フェーズ1: エッジケース補強 | 完了（+53件） |
| フェーズ2: カバレッジ駆動改善 | 進行中（今回 `.gimoza` / IPC / SVG / EdgeSlide接続 / 測量クリアに +17件、結合/共有頂点移動/EdgeSlide交差/境界始点/境界接触/No-op Undo/地物ドラッグ復元/複数territory再マージ/穴内島マージに +15件、任意SVGプロジェクト設定/保存連携に +8件、ロード時データ整合性検証/共有頂点付き複数頂点移動に +7件、EdgeSlide境界滑り/移動辺-頂点衝突に +12件、頂点挿入/削除Undoと分割MultiPolygon保持/複数territoryナイフ確定判定/挿入削除Redo ID固定/複数territory開線判定/分割時リング階層保持/非横断territory保持/PolygonShapeService階層再構築に +17件、追加/分割/結合コマンドの生成ID redo 固定に +3件、OSファイル関連付けの起動パス通知に +11件、プロジェクト境界の描画中toolStoreリセットに +3件、旧形式マイグレーション/互換性警告に +8件、B24分割延長線抑制に +4件、所属変更UI/親指定に +23件、作成時親指定UIに +2件、Phase 2-C-4 shape 保持規則 validation に +10件、Phase 2-D-3 で `ManageLayersUseCase.test.ts` 全削除 -20 件 + `LayerQueryService` テスト -3 件、Phase 2-D-5 で `areLayersEqual` テスト -1 件 + `featureRendererUtils.collectActiveFeatureEntries` 回帰テスト +8 件、Phase 2-D-6-1+2 で hitTest「非表示レイヤー」「上位レイヤー優先」テスト -2 件 + mapCanvasUtils「非表示レイヤー除外」テスト -1 件 + `featureRendererUtils.test.ts` 全削除 -8 件 + `mapSceneEntries.test.ts` 新規 +7 件（連番割当 / 多レイヤー連番 / コンテナ除外 / 非アクティブ除外 / 空入力 / undefined currentTime / 入力順保持）+ `mapSceneEntriesIntegration.test.ts` 新規 +3 件（同じ sceneEntries から 4 経路一致 / 非アクティブ除外連動 / コンテナ除外連動）+ `vertexSelectionContext.test.ts` -1 件（旧 hidden layer 用テスト撤去 → 空 sceneEntries 用テスト追加 -1+1）+ `mapCanvasUtils.test.ts` 「sceneEntries 外は寄与しない」テスト +1 件、Phase 2-D-6-3c で `jsonSerializerValidation.test.ts` の round-trip test 2 件を互換破棄検証 3 件へ再構成（旧形式拒否 / カスタムレイヤー拒否 / 新形式 round-trip）= +1 件、合計 1477 → 1478 件）。引き続き `EditFeatureUseCase` 61% 等が重点 |
| フェーズ3: 複合シナリオ | 計画済み（+21〜30件予定）。操作連鎖の結合テスト |
| フェーズ4: E2E拡充 | 計画済み（今回、面追加時の既存親指定と親選択リセットに +2件。+10〜20件予定） |
| テスト中に発見された不具合 | なし（発見次第ここに追記） |

---

## 5. 修正優先順位

2026-04-28時点の推奨順序（v1.0.0配布に向けて）:

1. **階層モデルの新仕様への置換 (B26/B27, §6 参照)**
2. 要件定義書の仕様補完（B15）
3. テスト品質改善（`テスト改善計画.md` フェーズ2〜3）
4. 構造改善（S1）
5. E2Eテスト拡充（`テスト改善計画.md` フェーズ4）+ Electron実機統合E2E + パフォーマンス改善

---

## 6. 階層モデルの新仕様 (2026-04-28 確定)

### 6.1 設計の経緯

旧 §6 は「グローバル `Layer` 概念を残し、`Layer.order` 序列とコンテナ rings 空化で階層整合性を担保する」方針だったが、2026-04-28 の設計議論で以下の構造的問題が露見し全面廃棄された:

- レイヤー深さが国ごとに異なるとき (例: 3層国 A vs 4層国 B)、グローバル序列ではリーフ排他を正しく判定できない (異なるツリーのリーフ同士が「下に潜り込める」)
- 「全リーフが何らかのコンテナに包まれる」と要求しても上記問題は解決しない

結論として、**グローバルレイヤー概念そのものを廃止し、ツリー位置から派生する depth で階層を表現する**方針へ転換する。設計意図は「基礎自治体 (リーフ) を面地理情報の唯一の担い手とし、上位はその集合体である純粋なコンテナ。階層深さは可変 (1層〜何十層)。歴史上の領土変更類型を全部カバーする」。

### 6.2 データモデル

- **`Layer` エンティティを廃止する**
- **`FeatureAnchor.placement.layerId` を削除する**
- **`FeatureAnchor.placement` に `isTopLevel: boolean` (最上位フラグ) を追加する** (時間依存。所属変更フローで時刻ごとに変化可能。同一錨内で `isTopLevel === (parentId === null)` 不変条件)
- **`FeatureAnchor.shape` を optional にする** (リーフのみ持つ。コンテナは shape プロパティ自体なし)
- **`FeatureAnchor.property` に `kind` プロパティを追加する** (国 / 州 / 郡 / 町 / 連邦 / 植民地 などの種別ラベル。表示専用で整合性検証には関与しない。錨ごとに保持することで時間軸上の階級変動 — 例: 同一地物の「植民地 → 州」「国 → 帝国 → 共和国」 — を表現可能)
- **既存 `.gimoza` 互換性は破棄する** (移行マイグレーションは実装しない、ユーザー判断済み)。互換破棄が完成するのは Phase 2 サブフェーズ D（`Layer` / `placement.layerId` 削除）時点。サブフェーズ B / C の段階では `Layer` / `layerId` が残っているため、新フィールド単独の migration（`isTopLevel` を `parentId === null` から派生する等）は中間形式の読込補助としてつなぎで実装する

### 6.3 派生概念: depth

- depth は **対象時刻における当該地物の有効錨のツリー位置から派生**: ルート (有効錨の `placement.isTopLevel === true && placement.parentId === null`) は `depth = 0`、子は親地物の同時刻有効錨から導出した depth + 1
- depth は地物自身に保持しない (常に算出する派生値)
- 不変条件: 同一錨内で `placement.isTopLevel === (placement.parentId === null)`
- **用語の固定**: `depth = 0` が **最上位 (国側、上)**。depth 大が下位 (基礎自治体側、下)。ユーザー感覚として「上 = 国 / 下 = 町」
- データ構造は時刻ごとに評価されるツリーの森 (複数ルート)。最上位フラグは錨側に持つため、所属変更フローで時刻ごとに位相変化（独立・帰属・連邦化など）を表現できる

### 6.4 整合性ルール

- **リーフ排他は地図全体で適用** (同一親の子同士に限定せず、異なるツリーのリーフ同士も含む)
- **親 ≡ 子の和 (厳密一致)**。コンテナ内部の隙間は許容しない
    - 直轄領のような隙間ケースは明示的なリーフを置いて埋める (例: 「政府直轄-県」)
    - **リーフ → コンテナ遷移時の差分自動処理**: 所属変更フローでリーフが子を獲得しコンテナ化する瞬間、元リーフの `shape` と新規子の和に差分があれば、差分を自動的に「直轄領リーフ」として生成し新コンテナの直接子に追加する。デフォルト名は「`<元リーフ名> 直轄領`」、`kind` は元リーフの遷移時刻における該当錨の `property.kind` を継承（直轄領の最初の錨に設定）。差分が複数の領土に分かれる場合は1リーフに複数 `territory` リング (飛び地化ロジック) でまとめる。差分が空なら直轄領を生成しない。所属変更ダイアログでユーザーが直轄領の名前/種別を上書き可能
    - 既存コンテナへの子追加 (所属変更フローによる追加のみ) では、コンテナ自身が `shape` を持たないため差分処理は発生しない (形状は子の和として再計算されるだけ)。混在結合では結果コンテナが新規生成されるため、既存コンテナへの追加には該当しない
- 地図全体としての無主地 (どのリーフにも覆われない領域) は許容する
- リーフ判定は「`shape` を保持し、子を持たない」を必要十分条件とする
- コンテナの形状 (描画 / ヒットテスト / 隣接判定) はすべて子の和集合として実行時に導出する
- **下位領域獲得経路**: 既存地物が子を獲得するのは「所属変更フロー」のみ。分裂・結合 (リーフのみ・コンテナのみ・混在のいずれのパターン) は新地物の生成または既存地物の形状変化を扱い、既存地物に後から子を加える経路ではない。混在結合では結果コンテナが新規生成され、混在リーフはその新規コンテナの子として帰属する (元コンテナの同一性は終了)。これは**結果地物（統合地物）の生成を結合の3パターン全てで「新規生成」に統一する**設計判断 (Undo/Redo・履歴の錨・ID継続の実装方針を結合機能で一致させる)。なお混在パターンでは選択された末端地物は存在継続し新統合集約地物の下位領域へ取り込まれるため、選択地物の総数は減らない場合がある（減るのは「結合の主体」となる地物の数）
- **結合対象の選択集合制約**: 結合対象は互いに上位・下位関係にあってはならない (集約地物 A と、A の直接・間接いずれかの下位領域に含まれる地物 X を同時選択することは禁止)。下位関係にある地物の同時選択は重複 child 参照や不正なツリー更新の原因となるため、結合実行時に検出し操作を拒否する

### 6.5 操作モデル

- 「**征服 (既存親への割譲)**」と「**連邦化 (新規共通親の作成)**」は **同じ所属変更フローで表現する**
- 新規親作成ループは現行 `ParentTransferDialog` の延長として実装する:
    - 名前 / 種別 (`kind`) 入力
    - 末端で「**既存最上位を選ぶ**」or「**新規最上位を作成する** (`isTopLevel: true`)」を選択
    - 再帰的に親を積み上げ可能 (中間階層を後付けで挿入できる)
- 親候補は depth 制約ではなく「対象地物自身および対象地物の上位領域すべて (直接の親、その親、…と最上位まで連鎖して辿った上位領域の集合) に含まれないこと (循環防止) + 親 ≡ 子の和 制約を将来満たせること」で絞る

#### 例: アメリカ合衆国爆誕シナリオ

1. 時刻 T において、13 個の最上位国 (各々の T 時点有効錨で `placement.isTopLevel: true` / `parentId: null`) を複数選択
2. 「親変更」(現行の所属変更フロー) を起動
3. 親候補として「**新規地物を作成**」を選択
4. 新規親作成フローで「合衆国」を新規地物として作成し、時刻 T で開始する錨に `placement.isTopLevel: true` / `parentId: null` を設定
5. 13 国の各々に時刻 T で開始する新しい錨を追加し、`placement.isTopLevel: false` / `parentId: 合衆国ID` を設定（時刻 T 未満の錨は `isTopLevel: true` / `parentId: null` のまま継続）。13 国の同一性（Feature ID）は維持されたまま、時間軸上で位相のみが変化する
6. 全ステップを 1 Undo にまとめる

### 6.6 自動配色

- **同 depth の兄弟で 4 色塗り分け** (同じ親を持つ子同士の中で 4 色定理を適用)
- 個別 feature が optional `style` を持つ。未指定なら自動配色を継承

### 6.7 表示フォーカス (戦略ゲー風)

- **グローバル depth スライダー**: 全地物を `depth ≤ N` で集約表示 (N = 0 で最上位ルートだけ、N 増で深く展開)
- **個別フォーカス** (グローバルと併存): 「選択地物の縦方向領域 (上位領域すべて + 自身 + 下位領域すべて) だけ depth M、その他は depth N まで」のように選択した地物の縦方向領域だけ深掘りする
- 想定 UI 参考: 戦略ゲー (HoI4 / EU4 等) の depth 集約表示
- 縦方向領域の絞り込み: 選択中地物の上位領域すべて + 自身 + 下位領域すべて (縦方向に繋がる領域) だけを集約対象として提示し、認知負荷を下げる
- 性能上、空間インデックス (R-tree など) の導入が必要になる可能性 (Phase 概略の後段で評価)

### 6.8 UI 整理 (`kind` ラベル)

- レイヤー名と地物名の混合 (例: 県レイヤーの「東京」が「東京都-県」になる) を避けるため、`kind` は **表示用ラベルとして錨側 (`FeatureAnchor.property.kind`) に持つ** (整合性検証には関与しない。時間軸上の階級変動を表現可能)
- 種別の制約: 親が「子の種別」を強制しない (例: 日本のように県の下に市町村が混ざる柔軟性を維持)
- Sidebar の現行 `レイヤー / プロパティ / 地物一覧` 3タブ構成は廃止または再構成する。具体的なタブ構成は実装着手時にユーザー判断:
    - 「選択地物中心」のタブ (プロパティ + 縦方向領域ナビゲーション = 上位領域・自身・下位領域の連鎖を辿る UI)
    - 「世界俯瞰」のタブ (地物一覧 + 深度フォーカス制御)
    - 種別管理 (`kind` の命名規則) はプロジェクト設定または独立タブへ

### 6.9 想定する歴史的領土変更類型

新モデルで以下の類型がすべて表現できることを確認する。個別 UI を設けるのではなく、データモデルとして表現可能であれば良い。

#### A. ツリー位相変化

| 類型 | 操作 |
| --- | --- |
| 改名・属性変更 | プロパティ編集 |
| 国境調整 | リーフの頂点編集 |
| 割譲 | 所属変更 (新親 = 既存地物) |
| 独立 | 所属変更 (`isTopLevel = true` へ昇格) |
| 帰属 | 所属変更 (新親 = 既存地物) |
| 直轄化 (中間階層スキップ) | 所属変更で中間コンテナを抜かして、より上位の上位領域 (直接の親の親、その親、…) へ移す |
| 自治化 (中間階層挿入) | 新規親作成 (既存子と旧親の間に新コンテナを挿入) |

#### B. 数量変化

| 類型 | 操作 |
| --- | --- |
| 分裂 (二分割) | ナイフ分割。**元リーフは形状変化として継続 (プロパティ引き継ぎ先) + もう片方が新規リーフとして生成** (穴あけと同型)。元地物終了+新2地物の旧仕様からの変更点に注意 (現行実装は旧仕様。Phase 4 で移行) |
| 結合 (リーフのみ) | 結合。形状の論理和。元リーフ全消滅+結果リーフ新規生成 |
| 結合 (コンテナのみ平坦化) | 結合。元コンテナそれぞれの直接子を新コンテナの直接子へ平坦化統合 (中間階層は保持しない)。階層保持は所属変更フローで表現 |
| 結合 (リーフ+コンテナ混在の自動帰属) | 結合。混在リーフを新コンテナの直接子として帰属。リーフは存在継続、コンテナは新規生成 (旧コンテナは終了) |
| 連邦化 (複数独立国 → 共通親配下に統合) | 所属変更フローで「親候補 = 新規地物作成」を選択 (§6.5)。**結合ではなく所属変更フローを使う**。結合は結合の主体（リーフのみは選択リーフ全体、集約のみは選択集約全体、混在は選択集約のみ）の同一性を終了させ結果地物を新規生成する操作、所属変更は地物数を保ったまま位相を変える操作という設計分離 |
| 解体 | 全子を `isTopLevel = true` へ昇格。コンテナは子全消失で自動消滅 |

#### C. リーフ ↔ コンテナ遷移

| 類型 | 操作 |
| --- | --- |
| リーフ拡大 (X が拡大しもとの X が一単位として残る) | 新規親作成 (新コンテナ Y を作って既存リーフ X をその子に) |
| コンテナ自動消滅 | 子全消失で自動 (既存挙動) |

#### D. 階層変動 (深さ変化)

| 類型 | 操作 |
| --- | --- |
| 階級変動 | `kind` プロパティ更新 (depth は親変更で従う) |
| 多層国 ↔ 1層国の編入・編出 (深さ非対称) | 所属変更 + 必要なら `kind` 変更 |
| 構造改革 (道州制等の大規模再編) | 複数の新規親作成 + 所属変更の連続適用 |

#### E. 政治的特殊形態 (単一親モデルで近似)

「高々 1 つの上位コンテナに属する」制約を維持したまま、別エンティティ化や時間付き所属切替で表現する。

| 類型 | 表現方法 |
| --- | --- |
| 連邦 (例: 米合衆国、EU、神聖ローマ帝国) | 連邦エンティティをコンテナとして最上位に置き、構成国・州を子に。`kind = "連邦"` などで政体名を表現 |
| 二重君主国 (例: オーストリア = ハンガリー) | 連邦と同じく上位コンテナで表現 (オーストリア = ハンガリーがコンテナ、オーストリア・ハンガリーが子) |
| 同君連合 (例: ハプスブルク朝) | 領土変更ではなく統治者メタデータ。プロパティで表現 |
| 共同統治 (例: 英埃領スーダン、アンドラ) | 「○○共同統治領」として独立した最上位エンティティ。宗主はプロパティで列記 |
| 占領地 (例: 第二次大戦中のヴィシー政権下フランス) | 「○○国占領地」として戦時独立エンティティ + 基礎自治体は時間ごとに所属親を切替 (時間付き `FeatureAnchor.placement.parentId` で対応) |
| 係争地 (例: 国家承認が分かれる地域) | 「○○ (係争中)」として独立した最上位エンティティ + 主張する側はプロパティで列記 |

### 6.10 実装 Phase 概略

新階層モデルへの置換は以下の Phase 順で実施する。Phase 2 はサブフェーズ A〜E に分割し、Phase 2.5 (コンテナ UI 基盤) を Phase 3 の前に挿入する。

#### スコープ管理ルール (全 Phase 共通)

- 1サブフェーズの差分は **~15 ファイル / ~300 行** を上限とする。超えたらさらに分割する
- 自レビューで「対概念」と感じた修正は、そのレビューで全部直さず別サブフェーズへ切り出す
- ドメイン基本ルール (排他・整合・階層・時間) に触れる変更は、着手前に要件定義書 §2.1 / §4.1 / 現状.md §6.4 の該当節を直接引用してから着手する
- 集約地物の競合検証は **要件定義書 §2.1 line 145-153 により禁止**。`ConflictDetectionService` を末端地物以外に拡張しない
- 「対称性確保」「自然な拡張」と感じた変更ほど、仕様で例外規定されている可能性を疑う

#### 着手前ゲート (Phase 2 / Phase 2.5 で必ず引用)

- **要件定義書 §4.1**: 「`shape`?: オブジェクト。地物の形状定義。**末端地物（リーフ）のみが保持する**。集約地物（コンテナ）はこのプロパティ自体を持たず、形状は下位領域の和として実行時に導出する」
- **要件定義書 §2.1 line 145-153**: 「**末端地物の排他性は地図全体に適用する**」「**排他検証は末端地物同士のみ**。集約地物には直接の排他制約を課さない」「**集約地物ペアに対して直接の非重複検証を実装してはいけない**」
- **§6.4**: 「リーフ判定は『shape を保持し、子を持たない』を必要十分条件とする」「親 ≡ 子の和（厳密一致）」

#### Phase 1: 要件定義書 §2.1 改訂 — **2026-04-28 完了**（実装済み.md 2026-04-28 Phase 1 エントリ参照）

新モデルに基づき仕様本文を全面書き直し。レイヤー概念を削除し、ツリー / depth / リーフ排他全体適用 / 新規親作成フロー / 深度フォーカス表示モデル / 政治的特殊形態の表現方針を明文化。仕様確定が以降の前提作業。

#### Phase 2: データモデル全面置換

`Layer` 削除、`FeatureAnchor` 構造変更 (`placement.isTopLevel` / `property.kind` / `shape` optional / `placement.layerId` 削除)、depth 派生サービス追加。サブフェーズ A〜E に分割。

##### Phase 2-A: `property.kind` 追加 — **2026-04-28 完了**（実装済み.md 2026-04-28 Phase 2 サブフェーズ A エントリ参照）

optional 表示専用ラベル + シリアライザ / マイグレーション / `PropertyProjection` 反映。

##### Phase 2-B: `placement.isTopLevel` 追加 — **2026-04-28 完了**（実装済み.md 2026-04-28 Phase 2 サブフェーズ B エントリ参照）

不変条件 `isTopLevel === (parentId === null)` + `createAnchorPlacement` ヘルパー集約 + 旧形式 migration / バリデーション（`validatePlacementInvariants` がロード時に強制）。

##### Phase 2-C: `shape` optional 化 (C-1〜C-4 に再分割)

> 2026-04-29 に一括実装で試みたが自レビュー10往復になり全撤回（実装済み.md 2026-04-29 Phase 2 サブフェーズ C: 一括実装試行 → ロールバック エントリ参照）。下記の C-1〜C-4 に再分割して順次着手した。

###### Phase 2-C-1: ドメイン型 + 永続化 (~7-8 ファイル) — **2026-04-29 完了**（実装済み.md 2026-04-29 Phase 2 サブフェーズ C-1 エントリ参照）

`FeatureAnchor.shape` を `FeatureShape | undefined` に変更し、コンストラクタ / `withShape(undefined)` / `requireLeafShape` ヘルパー / `JsonFeatureAnchor.shape` optional 化 / serialize-deserialize / migration の `shape` 欠落分岐（コンテナ判定 vs `SerializationError`）を整備。consumer narrow / UI / 描画 / 新規 validation 規則は触らず、意図的に「型エラーを残したまま runtime green」で C-2 へ進む方針（実データに undefined shape が無いため narrow パスは未走行）。

###### Phase 2-C-2: Consumer narrowing — Domain + Application 層 (~15 ファイル) — **2026-04-30 完了**（実装済み.md 2026-04-30 Phase 2 サブフェーズ C-2 エントリ参照）

`domain/value-objects/FeatureAnchor` に `isLeafPolygonAnchor` ヘルパー + `LeafPolygonAnchor` 型 predicate を新設（§6.6.8 リーフ判定の集約）。Domain services / Application UseCase / Command の計 18 ファイルに narrow を適用し、排他検証経路は `isLeafPolygonAnchor` ベース・その他は単純 `if (!anchor.shape) continue;` で統一。Infrastructure / Presentation / 描画 / UI は触らず C-3 へ。

###### Phase 2-C-3: Consumer narrowing — Infrastructure + Presentation 層 (~12 ファイル) — **2026-04-30 完了**（実装済み.md 2026-04-30 Phase 2 サブフェーズ C-3 エントリ参照）

Infrastructure rendering（`hitTestUtils` / `vertexSelectionContext` / `vertexHandleUtils`）+ Presentation（`appPolygonEditing` / `labelRendererUtils` / `mapCanvasUtils` / `parentTransferDialogUtils` / `FeatureRenderer.svelte` / `PropertyPanel.svelte` 等）の 14 ファイルに narrow 追加。コンテナは現時点で skip / 不可視扱い（実装は Phase 2.5）。`tsc -p tsconfig.web.json` で残存していた Infrastructure / Presentation 層の shape narrow 漏れエラーは全消滅。

###### Phase 2-C-4: shape 保持規則の validation (1 ファイル + テスト) — **2026-05-03 完了**（実装済み.md 2026-05-03 Phase 2 サブフェーズ C-4 エントリ参照）

`jsonSerializerValidation.ts` に `validateShapePresence` を追加し `validateJsonWorld` に組み込み。検証は単一錨内で完結する規則のみ（`shape === undefined` ⟹ Polygon + childIds 非空 / `shape !== undefined` ⟹ `featureType` と `shape.type` 整合）。子参照健全性・親子相互整合・循環検出・リーフ排他・親 ≡ 子の和は Phase 3 本体へ defer（Phase 3-1 / 3-2 で前者2項目は完了済み）。

###### Phase 2-C 完了後の状態

- 型・永続化・shape 規則 validation で「shape は optional」が成立
- 全 consumer が narrow 済み（containers が来ても crash しない、ただし describe・操作・選択は不可）
- containers は実データに無いので UI 上は不変
- Phase 4 で containers を作れる前提が整う

##### Phase 2-D: `Layer` 削除 + `placement.layerId` 削除

`Layer` エンティティ / `ManageLayersUseCase` / Sidebar レイヤータブを削除し、`LayerService` は `HierarchyService` へリネーム。`placement.layerId` を参照する全箇所を撤去または削除。`.gimoza` 互換破棄が完成する地点。`placement.layerId` 参照は実コードで 36 ファイル超、`Layer` 直接参照は 13 ファイル超に及ぶため、§6.10 スコープ管理ルール（1サブフェーズ ~15 ファイル / ~300 行）に従って D-1 〜 D-7 に分割して実施する。

###### Phase 2-D-1: 排他検証から layerId 依存撤去 — **2026-05-03 完了**（実装済み.md 2026-05-03 Phase 2 サブフェーズ D-1 エントリ参照）

`ConflictDetectionService` から `targetLayerIds` フィルタ・`groupPolygonsByLayer`・`SpatialConflict.layerId` を撤去し、末端地物（`isLeafPolygonAnchor`）の地図全体ペアワイズ排他検証へ移行。`PolygonValidationService` / `polygonValidation` / 呼び出し側 UseCase / Command / `App.svelte` / `ConflictResolutionDialog.svelte` の `layerId` 引数も連鎖撤去。仕様 §2.1 line 145-153「末端地物の排他は地図全体」へ収束。

###### Phase 2-D-2: `LayerService` を `HierarchyService` にリネーム — **2026-05-03 完了**（実装済み.md 2026-05-03 Phase 2 サブフェーズ D-2 エントリ参照）

純粋なシンボル名変更で挙動変化なし。要件定義書 §5.2 で既に確定していた `HierarchyService` 命名へコード側を揃え（§6.0.1 検出観点1 仕様文書の用語撤廃宣言時の同一文書同期）、`services/index.ts` / 各 consumer / 開発ガイド §1.2 / §6.6.9 文言を一括同期。

###### Phase 2-D-3: `ManageLayersUseCase` / `LayerQueryService` 撤去 + `LayerPanel.svelte` 削除 — **2026-05-03 完了**（実装済み.md 2026-05-03 Phase 2 サブフェーズ D-3 エントリ参照）

`ManageLayersUseCase` / `LayerQueryService` / `LayerPanel.svelte` / 関連テストを削除し、`Sidebar.svelte` のレイヤータブ撤去 + `App.svelte` / `DIContainer` / `SaveLoadUseCase` から関連依存を撤去。旧計画では D-3 / D-4 前半に分割していたが、`LayerPanel` の依存上の必然から両者を D-3 へ統合（実装側で発覚した依存の見落としを実装履歴として残存）。

###### Phase 2-D-4: `focusedLayerId` 残骸の整理 + `unsubLayersChanged` 購読撤去 — **2026-05-15 完了**（実装済み.md 2026-05-15 Phase 2 サブフェーズ D-4 エントリ参照）

D-3 統合後の残務として `focusedLayerId` state / 同期 `$effect` / フォーカスベース opacity 0.22 倍分岐 / `MapCanvas` 等への `focusedLayerId` props 全段を撤去。`eventBus.on('layers:changed', …)` の dead listener も撤去（emit 経路は D-3 で消滅済み）。

###### Phase 2-D-5: `ProjectSettingsDialog.svelte` のレイヤー編集セクション撤去 + 自動配色レイヤー単位グルーピング撤去 — **2026-05-15 完了**（実装済み.md 2026-05-15 Phase 2 サブフェーズ D-5 エントリ参照）

ProjectSettingsDialog の「レイヤー管理」セクション全体（フォーム / draftLayers / add-update-delete / props）と関連 CSS / `appProjectSettings.areLayersEqual` を撤去。`FeatureRenderer.svelte` の自動配色を「`{#each visibleLayers}` ループ内算出」から「コンポーネント先頭の `$derived` で表示文脈下 1 回算出」へ移行し、`featureIndex` を「レイヤー内連番」から「表示文脈連番」へセマンティクス変更。default 1 レイヤーのみの新規プロジェクトでは旧 = 新（互換破棄方針の射程）。後続フェーズへの持ち越し改訂: 開発ガイド §6.6.6 / §6.6.3 / 要件定義書 §2.4.4.3 / §2.4.4.6 の旧モデル前提文言は Phase 6 / Phase 4 着手時に同期改訂。

###### Phase 2-D-6: hitTest / 選択 / 描画経路の `placement.layerId` 参照撤去 → D-6-1+2 統合 / D-6-3 に再分割

実コード（src 30 ファイル）と tests（30 ファイル）に `placement.layerId` 参照が散らばっており、§6.10 スコープ管理ルール（~15 ファイル / ~300 行）を超える。当初 D-6-1 / D-6-2 / D-6-3 の 3 段に分割したが、D-6-1 着手後のレビューで「描画経路（FeatureRenderer 側）は引き続き `Layer.visible` で絞っており、hitTest / 頂点選択 / wrapOffsets だけ全地物を見るため『描画されないがクリック選択できる』中間状態が発生する（開発ガイド §6.1.2 / §6.6.9 / §6.0.1 検出観点2 違反）」との指摘を受け、D-6-1 と D-6-2 を統合して **sceneEntries 共通中間表現** を導入する形に再設計した。

###### Phase 2-D-6-1+2: sceneEntries 共通中間表現の導入 + 描画/hitTest/頂点選択/wrapOffsets の一元化 — **2026-05-16 完了**（実装済み.md 2026-05-16 Phase 2 サブフェーズ D-6-1+2 エントリ + 2026-05-18 D-6-1+2 追補 エントリ参照）

`mapSceneEntries.ts`（`MapSceneEntry` 型 + `collectMapSceneEntries`）を新設し、旧 `featureRendererUtils.ts` を削除。`FeatureRenderer.svelte` / `hitTestUtils` / `vertexSelectionContext`（`buildVisibleVertexOwnerMap` → `buildSceneVertexOwnerMap` 改名）/ `mapCanvasUtils` / `MapCanvas.svelte` 等から `visibleLayerIds` / `layerPriority` / `layers` props / `currentTime` 引数を全撤去し、全 4 経路（描画・ヒットテスト・頂点選択コンテキスト・wrapOffsets）が同一 `sceneEntries` を受け取る一元化に成功。同点複数候補の上位レイヤー優先 tie-break は消滅（Phase 2.5-C で depth + DOM target ベースに再設計）。スコープ 15 ファイル / +661 -670 行で §6.10 上限ちょうど（実装ロジックの実質変更は ±300 行、残りは新規回帰テストと削除のみのファイル）。

###### Phase 2-D-6-3: `placement.layerId` フィールド本体削除 → 3-3a / 3-3b / 3-3c に再分割

Phase 2-D-6-1+2 完了後の調査で、src 22 + tests 29 = 計 51 ファイルに `placement.layerId` 参照が散らばっており、§6.10 スコープ管理ルール（~15 ファイル / ~300 行）を大幅に超えるため、`feedback_subphase_scope_split.md`「サブフェーズは1経路ずつ」に従い 3 段へ分割した。

###### Phase 2-D-6-3a: EdgeSlide collector の同一レイヤー絞り込み撤去 + 関数リネーム — **2026-05-18 完了**（実装済み.md 2026-05-18 Phase 2 サブフェーズ D-6-3a エントリ + 2026-05-19 D-6-3a 追補 エントリ参照）

`edgeSlideConstraintCollectors` の `collectSourceLayerIds` ヘルパー + `sourceLayerIds` 経由の「同一レイヤー絞り込み」フィルタを撤去（Phase 2-D-1 の末端地物排他全マップ移行と整合性ズレを是正）。`collectSameLayerPolygonObstacle*` 系を `collectPolygonObstacle*` 系へリネームし "SameLayer" 用語を撤廃。dead semantics 撤去で実観測挙動は変わらず。

###### Phase 2-D-6-3b: src 層の `placement.layerId` 全 consumer narrow 撤去 + `createAnchorPlacement` シグネチャ変更 — **2026-05-19 完了**（実装済み.md 2026-05-19 Phase 2 サブフェーズ D-6-3b エントリ参照）

src 11 ファイルの `anchor.placement.layerId` 参照と引き渡しを全撤去 + 依存連鎖により `AddFeatureUseCase` / `AddFeatureCommand` / `appPolygonEditing` / `polygonValidation` の `layerId` 引数も全段撤去。`createAnchorPlacement(layerId, parentId, childIds)` → `createAnchorPlacement(parentId, childIds)` へシグネチャ変更し、`AnchorPlacement.layerId` を required → optional に変更（完全削除は D-6-3c）。JSON shim は `serializeAnchorPlacement` で `'default'` 固定値出力、`deserializeAnchorPlacement` で `json.layerId` 読み捨てに統一。`validateLayerReferences` も本サブフェーズで前倒し撤去。**重要な是正**: 初版宣言の tests touch は 10 ファイルだったが、addPoint/addLine/addPolygon の旧シグネチャ呼び出しが残 19 ファイルに残置していた（tsconfig.web.json が tests/ を含まないため `npm run check` で検出されず silent fail）。レビュー追補で計 29 ファイル touch + 開発ガイド §6.0.1 検出観点1 に「関数シグネチャ撤去では tests/ にも grep を広げる」を追記。

###### Phase 2-D-6-3c: JSON 永続化の `layerId` 撤去 + テスト fixture リテラル同期撤去 — **2026-05-20 完了**（実装済み.md 2026-05-20 Phase 2 サブフェーズ D-6-3c エントリ参照）

`JsonAnchorPlacement.layerId` 型フィールド + `serializeAnchorPlacement` の `'default'` shim + `deserializeAnchorPlacement` の `json.layerId` 読み捨て + `jsonSerializerMigration` の defaultLayerId 派生 + `AnchorPlacement.layerId` optional フィールド本体を完全撤去（互換破棄が完成する地点）。**旧 .gimoza は (1) version 欠落、(2) version 0.X.X、(3) placement.layerId 含む のいずれかで `SerializationError` 拒否**（要件定義書 §2.5.2 / 現状.md §6.2 遵守）。fixture リテラル `placement: { layerId: 'l1', ... }` を 22 ファイル touch し撤去（`MergeService.test.ts` は API シグネチャ自体が Phase 4 まで残るため touch せず）。

###### Phase 2-D-7: クリーンアップ（D-3〜D-6-3 で参照が消えた後の残骸撤去） → サブフェーズ a 以降に分割

§6.10 スコープ管理ルール（~15 ファイル / ~300 行）に従い、Phase 2-D-7 全体を以下のサブフェーズに分割して実施する。

###### Phase 2-D-7a: EventBus dead 型撤去 — **2026-05-20 完了**（実装済み.md 2026-05-20 Phase 2 サブフェーズ D-7a エントリ参照）

`EventBus` の `LayerVisibilityChangedEvent` interface / `LayersChangedEvent` 型 / `EventMap` の `'layer:visibilityChanged'` / `'layers:changed'` エントリと再エクスポートを撤去。emit / subscribe ともに既に経路なしの dead type 状態を解消（型定義削除のみで振る舞い変更なし）。

###### Phase 2-D-7b: テストヘルパー dead `layerId` 引数の sweep — **2026-05-20 完了**（実装済み.md 2026-05-20 Phase 2 サブフェーズ D-7b エントリ参照）

tests/ 配下 8 ファイルのヘルパー関数（`makePolygonFeature` / `makePointFeature` / `makeLineFeature` / `createPointFeature` / `createPolygonFeature` / `polygonFeatureWithChildren` 等）の `layerId: string` 引数と呼び出し側 `'layer-1'` 等リテラルを撤去。`tsconfig.web.json` の include 対象外でビルドに影響なし。

###### Phase 2-D-7c-1: 旧形式 migration ロジック dead code の sweep + `resolveVersion` の `_ctx` 引数撤去 — **2026-05-23 完了**（実装済み.md 2026-05-23 Phase 2 サブフェーズ D-7c-1 エントリ参照）

`jsonSerializerMigration.ts` の `normalizeLegacyFeatureAnchor` / `normalizeLegacyPolygonRing` / `normalizeShapeType` / `hasShapeSource` / `normalizeParentId` / `firstRecord` 等の関数本体と各種旧形式フォールバック（旧 `time` / `startYear` / `properties[0]` / `featureType` 小文字 / anchor 直下 `vertexIds` 等）を撤去。Phase 2-D-6-3c の version 拒否完成により到達不能になっていた dead code。`shapeTypeFromFeatureType` → `defaultShapeType` リネームで意味論明確化。レビュー追補1〜3 で v1.0.0 旧構造の拒否テストを 332 行追加。

###### Phase 2-D-7c-2a: `App.svelte` の Layer 残骸撤去 + `SaveLoadUseCase` の `getLayers/setLayers/layers` 暫定 API 撤去 — **2026-05-24 完了**（実装済み.md 2026-05-24 Phase 2 サブフェーズ D-7c-2a エントリ参照）

`App.svelte` の `Layer` import / `let layers` state / `refreshLayerData()` / `getAddTargetLayer()` / `new Layer('default', ...)` 構築 + `SaveLoadUseCase` の暫定 `getLayers/setLayers/layers` API を撤去。`assembleWorld()` は常に `layers: []` を出力（型側の撤去は D-7c-2b で実施）。

###### Phase 2-D-7c-2b: `Layer` エンティティ本体 / `World.layers` / JSON 永続化 `layers` の最終削除 — **2026-05-24 完了**（実装済み.md 2026-05-24 Phase 2 サブフェーズ D-7c-2b エントリ参照）

`Layer.ts` ファイル削除 + `domain/entities/index.ts` の Layer export 撤去 + `World.ts` の `Layer` import / `layers` フィールド / コンストラクタ第 4 引数 / `getSortedLayers()` / `createEmpty()` を撤去。`jsonSerializerTypes` / `jsonSerializerTransforms` / `jsonSerializerMigration` / `JSONWorldRepository` / `SaveLoadUseCase.assembleWorld()` で `JsonLayer` interface / `JsonWorld.layers` / serialize/deserialize/normalize Layer 関数群を撤去。互換破棄テストに `expect('layers' in resaved).toBe(false)` アサーション追加。**Layer 概念のコードからの完全消滅地点**。

###### Phase 2-D-7d: コード内コメントの Phase 参照棚卸し — **2026-05-25 完了**（実装済み.md 2026-05-25 Phase 2 サブフェーズ D-7d エントリ参照）

src/ 配下 12 ファイルのコメントから完了済み Phase 番号（`Phase 2-D-X` / `Phase 2-C-X` / `Phase 2-B`）の移行記録参照を削除し現状挙動主語へ書き換え。未着手フェーズの計画ポインタ（Phase 2.5-A / 2.5-C / 3 / 5 / 6）は「現状の暫定実装の根拠 + 今後の再設計予定」として残置。ロジック・型・公開 API 変更なし。

###### Phase 2-D-7e: `<MapCanvas {features}>` props 経路の整理 — **2026-05-25 完了**（実装済み.md 2026-05-25 Phase 2 サブフェーズ D-7e エントリ参照）

`MapCanvas.svelte` の `features` props と `selectionAnchor` derive 内の `features.find(...).getActiveAnchor(currentTime)` 経路を撤去し、`sceneEntries.find((e) => e.feature.id === selectionFeatureId)?.anchor ?? null` に置換。旧経路 / 新経路で時刻外選択時の挙動同一を検証してから移行（`vertexHandleEntries` / `MapCanvasHud` の使用先はいずれも null と shape なしで同じ非表示挙動）。コンテナの選択は現状 UI 経路（hitTest / DOM data-feature-id / Sidebar）で発生し得ず dead path。**これにより Phase 2-D 全体の `Layer` / `placement.layerId` / `features` 直接走査の旧モデル経路は完全に消滅した**（Phase 2-D-6 計画当初の終端到達）。

各サブフェーズ着手前にスコープを明示し、~15 ファイル / ~300 行上限を守る。**削除のみのサブフェーズでも削除行数を着手宣言に含める**（ファイル削除は modified より対概念リスクは低いが、依存連鎖が広がるため着手前の規模把握は必須。Phase 2-D-3 では削除 940 行 + 修正 130 行で実態としては上限近接だった）。

##### Phase 2-E: depth 派生サービス追加 — **2026-05-26 完了**（実装済み.md 2026-05-26 Phase 2-E エントリ参照）

D-2 でリネームした `HierarchyService` に `deriveDepth(feature, allFeatures, time)` を追加。Phase 2.5 / Phase 5 / Phase 6 の消費者は本サブフェーズ範囲外。

#### Phase 2.5: コンテナ UI 基盤 (Phase 3 の前に挿入)

Phase 2-C 完了時点では containers が UI に出ない。Phase 4 が containers を作り始めると不可視・選択不能になるため、本 Phase で UI 基盤を揃える。各ステップを独立サブフェーズとして切る (Phase 2-C の経験から1段で全部やると追補無限ループになる)。

- **2.5-A**: `HierarchyService.deriveParentShape` の正式実装。再帰下降 + `polygonUnionAll` で多 polygon 保持 + `visited` セットによる cycle guard — **2026-05-25 完了**（実装済み.md 2026-05-25 Phase 2.5-A エントリ参照）
- **2.5-B**: 描画 + ヒットテストのコンテナ受容 — **2026-05-27 完了**（実装済み.md 2026-05-27 Phase 2.5-B エントリ参照）。`mapSceneEntries` がコンテナ（`shape` なし + `childIds` 非空）を派生形状で受容し、`FeatureRenderer` + `LabelRenderer` + `hitTestUtils` がコンテナを描画・ラベル・hitTest 対象として扱う。同レイヤー描画順は `deriveDepth` 昇順で「親→子」順。当初 Phase 2.5-B = 描画 / Phase 2.5-C = ヒットテストと分離する計画だったが、§6.6.9 と §6.1.2「描画される領域 = 選択できる領域」を遵守するため、本サブフェーズで描画 + hitTest 受容を統合実装した（§6.0.1 検出観点2「対概念の同期」適用）。
- **2.5-C**: ヒットテスト tie-break 再設計 — **2026-05-27 完了**（実装済み.md 2026-05-27 Phase 2.5-C エントリ参照）。`hitTest` を全ヒット収集 + (1) depth 降順 + (2) `preferredFeatureId` 一致の 2 キー tie-break に再設計し、`App.svelte` の `clickedFeatureId` 即採用ショートサーキットを撤去して hitTest 最終決定 + DOM target を `preferredFeatureId` として渡す方式へ統一。`MapSceneEntry.depth` フィールドを新設し描画順ソート（ASC）と hitTest tie-break（DESC）で派生値を共有。開発ガイド §6.2.25 採用ポリシーは Phase 2-D で `Layer` 撤去済みのため旧 3 キー（layerOrder 含む）から 2 キーへ同期改訂。
- **2.5-D**: `PropertyPanel` のコンテナ表示 — **2026-06-02 完了**（実装済み.md 2026-06-02 Phase 2.5-D エントリ参照）。polygon-like 判定を `propertyPanelUtils.isPolygonLikeFeature(feature)`（`feature.featureType === 'Polygon'`）に統一し、集約地物コンテナ（shape なし）も leaf polygon と同じ経路で親子・面スタイルセクションを表示。種別欄はコンテナで「集約地物」、形状情報欄は「下位領域の和として導出（N 領域）」を表示。`applyChanges` のスタイル構築ゲートを同一ヘルパー `buildUpdatedAnchorProperty` 経由に集約し、表示ゲートとの対概念ドリフトを防止（開発ガイド §6.6.9 / §6.0.1 検出観点2 適用）
- **2.5-E**: `parentTransferDialog` のコンテナ許容 — **2026-06-03 完了**（実装済み.md 2026-06-03 Phase 2.5-E エントリ参照）。`getActivePolygonAnchor` の `anchor.shape` 必須チェックを撤去し `feature.featureType === 'Polygon'` 基準に統一（PropertyPanel の `isPolygonLikeFeature` と同一基準。parentTransferDialog は sceneEntries を介さず Feature ベースで動くため `feature.featureType` を採用）。これで集約地物コンテナが所属変更の親候補・対象として受容される。確定経路（`ReassignFeatureParentUseCase`）は placement ベースで shape を見ないためコンテナでも既存ロジックで成立。コンテナ自身の所属変更（連邦編入・最上位フラグ遷移）は `canTransferSelectedFeature` = false + `assertLeafFromTime` で二重に拒否し Phase 4 へ defer。**本サブフェーズで Phase 2.5（コンテナ UI 基盤）は全完了**

#### Phase 3: バリデーション

`.gimoza` ロード時 (`validateJsonWorld`) のデータ整合性チェックを新モデルへ拡充する（要件定義書 §2.5.2「データ読み込み時の詳細な挙動」line 934-941）。同一錨内の `placement.isTopLevel === (placement.parentId === null)` 不変条件は Phase 2-B（`validatePlacementInvariants`）、形状保持規則は Phase 2-C-4（`validateShapePresence`）で実装済み。残りを以下のサブフェーズに分割して実施する（§6.10 スコープ管理ルール / `feedback_subphase_scope_split`「1経路ずつ」）。

##### Phase 3-1: 階層参照の構造的健全性 — **2026-06-03 完了**（実装済み.md 2026-06-03 Phase 3-1 エントリ参照）

`validateHierarchyReferences` を `validateJsonWorld` に追加。各錨ローカルで完結する参照整合のみを対象とする: parentId/childIds の (1) 参照先の存在、(2) 参照先が Polygon、(3) 自己参照禁止、(4) childIds 重複禁止、(5) 階層参加の型制約 = childIds 非空 ⟹ 自身が Polygon かつ parentId 非 null ⟹ 自身が Polygon（要件定義書 §2.1 line 160「線情報と点情報は面情報との干渉を受けない」= 階層に参加できるのは面情報のみ）。要件定義書 §2.5.2 line 936「ツリー位相の不整合（…子の親側参照欠落、親の子側参照欠落 など）」のうち参照整合部分を担う（§2.1 集約地物・上位領域は Polygon のみ / 開発ガイド §6.6.8）。ランタイム版 `HierarchyService.validateHierarchy`（所属変更時）と対をなすロード時検証（開発ガイド §6.6.1）。dangling 参照を持っていた既存テスト fixture（`createWorldWithPolygon` の `childIds: ['f2']`、isTopLevel 派生テストの `parentId: 'p-something'`）を有効なデータへ修正。

##### Phase 3-2: 親子相互整合 + 循環検出（時間スライス）— **2026-06-03 完了**（実装済み.md 2026-06-03 Phase 3-2 エントリ参照）

`validateHierarchyConsistency` を `validateJsonWorld` に追加。placement は錨ごとに時間依存のため、全錨の境界時刻（start / end）で区切る時間スライスごとに有効錨を `getActiveJsonAnchor`（`Feature.getActiveAnchor` 相当: 配列末尾優先 + 半開区間 `[start, end)`）で解決し、(1) 親の子側参照欠落（子→親が親の childIds に列挙されない）、(2) 子の親側参照欠落（親→子が子の parentId に指されない）、(3) 時間区間カバレッジ（親 or 子が同時刻に非有効）、(4) 時間スライスごとの循環親子参照（親方向に辿って自身へ戻る循環メンバーを検出。尾は除外）を検証（要件定義書 §2.5.2 line 936）。ランタイム版 `HierarchyService.validateHierarchy`（循環・親存在）/ `ReassignFeatureParentUseCase.validateChildReferences`（親→子整合）と判定を揃え、非存在・自己参照は Phase 3-1 が担うため二重報告を避けてスキップ。Phase 3-2 はロード時検証（根拠は §2.5.2 に閉じる）のため §2.2.3（編集確定時プロセス）改訂は対象外（Phase 3-1 と同じ扱い）。

##### Phase 3-3: リーフ排他（地図全体）のロード時検証 — 未着手

末端地物同士の空間重複を地図全体で検出（要件定義書 §2.1 line 145-153 / §2.5.2 line 938）。`ConflictDetectionService` を再利用し集約地物は対象外（開発ガイド §6.6.8）。

##### Phase 3-4: 親 ≡ 子の和 — 未着手

集約地物の形状（子の和として導出）と整合する厳密一致を検証（要件定義書 §2.5.2 line 939 / §6.4）。`HierarchyService.deriveParentShape` を再利用。

##### Phase 3 着手に伴う要件定義書 §2.2.3 改訂の扱い

§2.2 の Phase 計画は「Phase 3 着手時に §2.2.3 整合性維持プロセスの構造的改訂（バリデーションの前提）」を求めるが、§2.2.3 は**編集確定時**のプロセス（競合検出・解決ダイアログ）であり、Phase 3-1（**ロード時**の参照整合、根拠は §2.5.2 に閉じる）は §2.2.3 の対象外。§2.2.3 への親子整合・親≡子の和の検証前提の追記は、編集時プロセス本体を扱う Phase 4（操作 UseCase / Command 再設計）着手時、もしくは同じ幾何検証を共有する Phase 3-3 / 3-4 着手時に同期する。

#### Phase 4: 操作 UseCase / Command 再設計

新規親作成ループ、所属変更の連邦化対応、最上位フラグ遷移、Undo / Redo 同梱。

- **着手時の制約（Phase 2.5-E からの申し送り）**: in-memory でコンテナ（shape なし Polygon）を構築・変異させる経路を新設するため、生成・変異の各ステップで「shape なし ⟹ childIds 非空」の不変条件を保証すること。この不変条件は現状ロード時 `validateShapePresence` でのみ保証され、`parentTransferDialogUtils.isLeafFromTime` 等の childIds-only リーフ判定経路が暗黙依存している（開発ガイド §6.6.8「childIds のみでリーフ判定する経路の不変条件依存」）。保証できない場合は当該経路へ防御的な shape 存在確認を入れる。さもないと不正な「shape なし・childIds 空」Polygon がリーフ誤判定される（クラッシュ・データ破損はしないが分類が狂う）。

#### Phase 5: 表示フォーカス UI

グローバル depth スライダー + 個別フォーカス併存。

#### Phase 6: 自動配色 + 個別スタイル

同 depth 兄弟基準。

#### Phase 7: `kind` プロパティ運用、Sidebar UI 整理

#### Phase 8: 新規サンプルプロジェクト

`.gimoza` 互換破棄に伴いゼロから組み直す。

### 6.11 完了後の作業

- 開発ガイド §6 へ追加すべき新教訓:
    - 「リーフ排他は地図全体で適用 (同一親内に限定しない)」
    - 「親 ≡ 子の和を厳守し、隙間は明示的リーフで埋める」
    - 「コンテナは `shape` プロパティ自体を持たず、形状は子の和として導出する」
    - 「同一錨内で `placement.isTopLevel === (placement.parentId === null)` 不変条件を全コマンドで維持する」
    - 「`kind` は表示専用で整合性に関与させない」
    - 「征服と連邦化は同一所属変更フロー (UI 分岐は新規親作成サブフロー)」
- 既存実装で再設計対象になるもの (現状 main にある旧モデル前提コード):
    - `Layer` 関連残置: なし（Phase 2-D-7c-2b で `Layer.ts` / `World.layers` フィールド / JSON 永続化型 `JsonLayer` / `JsonWorld.layers` / `serializeLayer` / `deserializeLayer` / `normalizeLayer` / `createDefaultLayer` を全撤去済み。`LayerService.ts` は Phase 2-D-2 で `HierarchyService.ts` にリネーム済み。`ManageLayersUseCase.ts` / `LayerQueryService.ts` / `LayerPanel.svelte` / Sidebar レイヤータブは Phase 2-D-3 で削除済み。`ProjectSettingsDialog.svelte` のレイヤー編集セクションは Phase 2-D-5 で削除済み。`App.svelte` の Layer 残骸 + `SaveLoadUseCase.getLayers/setLayers/layers` 暫定 API は Phase 2-D-7c-2a で削除済み）
    - `FeatureAnchor.placement.layerId` を参照する全箇所 (描画 / 自動配色 / 排他判定 / ヒットテスト) — 排他判定は Phase 2-D-1 で撤去済み、自動配色のレイヤー単位グルーピングは Phase 2-D-5 で撤去済み、EdgeSlide 障害物の同一レイヤー絞り込みは D-6-3a で撤去済み、in-memory ドメイン経路の `placement.layerId` 参照は D-6-3b で撤去済み、`AnchorPlacement.layerId` 型本体 / JSON 永続化型 `JsonAnchorPlacement.layerId` / `serializeAnchorPlacement` / `deserializeAnchorPlacement` / `jsonSerializerMigration` の `defaultLayerId` 派生は D-6-3c で完全撤去済み
    - `SaveLoadUseCase` の暫定 `getLayers()` / `setLayers()` API — Phase 2-D-7c-2a で撤去済み
    - `ReassignFeatureParentUseCase` / `ReassignFeatureParentCommand` (連邦化対応の追加)
    - `AddFeatureCommand.parentId` (新規親作成ループ統合)
    - `ParentTransferDialog` (新規親作成サブフロー追加)
    - `JSONSerializer` / `.gimoza` 形式 (互換破棄、新形式へ全面置換)

### 6.12 用語と内部実装識別子の対応

要件定義書 (§2.1 改訂時) は仕様本文で和訳語のみを使用し、英語識別子は内部実装補遺へ集約する (開発ガイド §重要な注意事項 の規約)。新 §6 で導入する概念の対応表を以下に固定する。既存仕様で確立済みの語 (歴史の錨 / 親領域 / 下位領域 / リング / 領土リング / 穴リング / 飛び地 / リーフノード) はそのまま継承する。

#### 概念・データモデル

| 仕様用語 (和訳) | 内部実装識別子 | 補足 |
| --- | --- | --- |
| 地物 | `Feature` | 既存訳の継承 |
| 歴史の錨 | `FeatureAnchor` | 既存訳の継承 |
| 形状 | `FeatureAnchor.shape`? (要件定義書 §4.1 で optional 化確定。Phase 2 でコード反映予定) | 新モデルでは optional (末端地物のみ保持)。現行コードは required。Phase 2 で型変更 |
| 親領域 (親) | `parent` / `FeatureAnchor.placement.parentId` | 既存訳の継承 |
| 下位領域 (子) | `childIds` / `FeatureAnchor.placement.childIds` | 既存訳の継承 |
| 最上位フラグ | `FeatureAnchor.placement.isTopLevel` | 真のとき当該錨の有効期間において親領域を持たないルート地物。時間依存（同一地物でも錨ごとに異なる）。Phase 2 サブフェーズ B で実装済み（`src/renderer/domain/value-objects/FeatureAnchor.ts`）。生成は Phase 2-D-6-3b 完了時点で `createAnchorPlacement(parentId, childIds)` ヘルパー経由で `parentId === null` から派生する |
| 種別 | `FeatureAnchor.property.kind`? (要件定義書 §4.1 で識別子確定。Phase 2 でコード実装予定) | 表示専用ラベル (国 / 州 / 郡 / 町 / 連邦 / 植民地 など)。整合性検証に関与しない。錨側に保持（時間依存）。実コード未実装 |
| 末端地物 (リーフ) | leaf (概念名) | 形状を保持し下位領域を持たない地物。「リーフノード」既存訳の継承。コード上の専用型は未定 (Phase 3 で `Feature` 型ガードまたは派生型として実装予定) |
| 集約地物 (コンテナ) | container (概念名) | 下位領域を持ち形状プロパティを持たない地物。新モデルで導入する語。コード上の専用型は未定 (Phase 3 で `Feature` 型ガードまたは派生型として実装予定) |
| 階層深度 | `HierarchyService.deriveDepth(feature, allFeatures, time)` | 派生値。最上位は 0、子は親の階層深度 + 1。Phase 2-E で実装済み（`src/renderer/domain/services/HierarchyService.ts`）。consumer は Phase 2.5 / Phase 5 / Phase 6 で導入 |
| 個別スタイル | (Phase 6 で命名予定。仮称 `Feature.style` (optional)) | 未指定時は自動配色を継承。実コード未実装 |
| プロジェクトファイル | `.gimoza` | 既存訳の継承 (新モデルでは互換破棄) |

#### 不変条件・整合性

| 仕様用語 (和訳) | 内部表現 |
| --- | --- |
| 「最上位フラグの真と親領域なしは同値」 | (同一錨内) `FeatureAnchor.placement.isTopLevel === (FeatureAnchor.placement.parentId === null)`。Phase 2 サブフェーズ B で実装済み（生成は `createAnchorPlacement` ヘルパー集約 + JSON migration / `validatePlacementInvariants` バリデーション + 主要コマンド・UseCase が同ヘルパー経由で再構築） |
| 「末端地物排他は地図全体で適用」 | leaf-leaf overlap forbidden across the entire map |
| 「親領域は下位領域の和に厳密一致」 | parent shape ≡ union of children shapes |

#### 操作

| 仕様用語 (和訳) | 内部実装識別子 | 補足 |
| --- | --- | --- |
| 所属変更 | `ReassignFeatureParentUseCase` / `ReassignFeatureParentCommand` | 既存訳の継承 |
| 所属変更ダイアログ | `ParentTransferDialog` | 既存訳の継承 |
| 新規親作成サブフロー | (新規) `CreateNewParentCommand` (仮称) | 所属変更フロー末端で起動。再帰的に親を積み上げ可能 |
| 地物追加コマンドの親指定 | `AddFeatureCommand.parentId` | 既存訳の継承 |
| 征服 (既存親領域への割譲) | (Phase 4 で命名予定。仮称 `conquest`) | 仕様用語は「割譲」(既存訳)。新モデルでは新規親作成サブフローと同一所属変更フローで表現。実コード未実装 |
| 連邦化 (新規共通親領域の作成) | (Phase 4 で命名予定。仮称 `federation`) | 同上。新規親作成サブフローを使う所属変更。実コード未実装 |

#### 表示

| 仕様用語 (和訳) | 内部実装識別子 | 補足 |
| --- | --- | --- |
| 階層深度スライダー (グローバル深度フォーカス) | (Phase 5 で命名予定。仮称 `global depth slider`) | 全地物を「階層深度 ≤ N」で集約表示。実コード未実装 |
| 個別フォーカス (個別深度フォーカス) | (Phase 5 で命名予定。仮称 `individual focus`) | 選択地物の縦方向領域 (上位領域すべて + 自身 + 下位領域すべて) だけを深掘り。実コード未実装 |
| 縦方向領域 | (Phase 5 で命名予定。仮称 `lineage`) | 上位領域すべて + 自身 + 下位領域すべての集合。仕様本文では「祖先・子孫・系譜」用語を使わず説明的表現に揃える (時間軸の祖先・子孫と概念混同を避けるため)。実コード未実装。Phase 5 着手時に正式な内部識別子を確定する |
| 縦方向領域の絞り込み | (Phase 5 で命名予定。仮称 `lineage filter`) | 縦方向領域に登場する種別だけを認知対象に絞る。実コード未実装 |
