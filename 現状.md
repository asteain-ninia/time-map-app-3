# 現状.md — 未解決課題・既知不具合・残作業

> 完了した実装の履歴は `実装済み.md` を参照。
> 完了した課題は即時削除する（開発ガイド§重要な注意事項 参照）。

## テスト総数

- ユニットテスト: 1172件（71ファイル、全パス）
- E2Eテスト: 167件（167合格）
- `npm run build`: 通過
- `npm run check`: 0 errors / 0 warnings

---

## 1. 既知の不具合

状態ラベル: **壊** = 機能が壊れている / **未接続** = ロジックはあるがUI未接続 / **部分** = 一部のみ実装

### 1.1 重大（データ整合性・基本機能に影響）

> 重大課題がない場合もこの見出しは維持し、`追跡無し` と明記する。

追跡無し

### 1.2 中（機能不完全）

追跡無し

### 1.3 低（UX・表示）

追跡無し

---

## 2. 未実装機能

### 2.1 統合品質（最優先）

追跡無し

### 2.2 機能要件

| 機能 | 優先度 | 備考 |
| --- | --- | --- |
| CQRS コマンド/クエリ分離基盤 | 高 | イベントバスは実装済み。コマンド/クエリの正式分離は未実装 |
| レイヤースライダー | 中 | レイヤー一覧で可視/透明度は操作できるが、要件定義書§2.1の離散レイヤースライダーは未実装 |
| フォーカス外レイヤーの薄い表示 | 中 | 手動の透明度調整はあるが、フォーカス連動の自動減衰はない |
| レイヤー間の面形状導出（下位領域の和） | 中 | `LayerService.deriveParentShape()` とテストはあるが、描画/UIからは未使用 |
| 経度シフト機能 | 中 | 横方向ラップ表示は実装済みだが、±10°ボタン/スライダーと中心経度表示はない |
| 時間バー表示 / リスト表示の切替 | 中 | 現在は `PropertyPanel.svelte` の単純なアンカー一覧のみ |
| 緯度経度ラベル表示 | 低 | `GridRenderer.svelte` は線のみ描画し、ラベルを出していない |
| カスタムパレット登録 | 低 | 組み込みテーマ（クラシック/パステル/ハイコントラスト）は実装済み。ユーザー定義パレットUI/保存は未実装 |
| 2周超（720°超）地物の表示 | 低 | 描画タイル数の限界により2周超の地物は表示されない。実用上は問題にならない |
| 排他判定の経度範囲拡張 | 低 | 重なり判定が-180〜180前提のため、180°超に延伸した部分と通常範囲の地物の重なりが検出されない |
| 検索機能 | 低 | |
| 折りたたみ可能パネル | 低 | |
| Logger | 低 | |

### 2.3 非機能要件

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| Electron統合テスト（IPC/preload/ファイルアクセス） | 中 | ブラウザE2E中心。Electron固有経路の実検証が弱い |
| a11yセマンティクス改善 | 低 | `svelte-ignore` が複数残り、クリック可能 `div` も残存 |
| パフォーマンス最適化（空間インデックス、LOD、カリング） | 低 | |

---

## 3. 構造的課題

外部レビュー（2026-03-25）で指摘された、不具合ではないが改善すべき構造問題。

| # | 深刻度 | 問題 | 備考 |
| --- | --- | --- | --- |
| S1 | 中 | App.svelte 1843行、MapCanvas.svelte 786行 | 開発ガイドの200行目安を大幅超過。機能別コントローラへの分割が必要 |
| S2 | 中 | appState.tsとDIContainer.tsの二重経路 | 技術方針§2.2ではDI一元管理だが実態はグローバルシングルトン。どちらかに一本化する |
| S3 | 低 | JSONSerializer.ts 548行 | 300行目安を超過 |
| S5 | 中 | E2EがElectron統合を検証できていない | Chromium 136+ CLI引数制限でElectron直接起動断念。IPC/preload/ファイル操作の実検証が弱い |

---

## 4. 修正優先順位

外部レビュー・要件差分・2026-04-02時点の実装状況を統合した推奨順序:

1. 構造改善（S1〜S3）と CQRS/DI の責務整理
2. レイヤー表示要件の残件実装（レイヤースライダー、非フォーカス薄表示、下位領域導出）
3. カスタムパレット登録
4. 経度シフト、タイムライン詳細表示、検索などの未実装UI
5. Electron統合テストと非機能改善（a11y / パフォーマンス）
