# 現状.md — 未解決課題・既知不具合・残作業

> 完了した実装の履歴は `実装済み.md` を参照。
> テスト品質改善の計画と実施記録は `テスト改善計画.md` を参照。
> 完了した課題は即時削除する（開発ガイド§重要な注意事項 参照）。

## テスト総数

- ユニットテスト: 1334件（89ファイル、全パス）
- E2Eテスト: 176件（22ファイル、全パス）
- `npm run build`: 通過
- `npm run check`: 0 errors / 0 warnings

---

## 1. 既知の不具合

状態ラベル: **壊** = 機能が壊れている / **未接続** = ロジックはあるがUI未接続 / **部分** = 一部のみ実装

### 1.1 重大（データ整合性・基本機能に影響）

> 重大課題がない場合もこの見出しは維持し、`追跡無し` と明記する。

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| - | - | 追跡無し | - |

### 1.2 中（機能不完全）

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| B3 | 部分 | 任意SVGのプロジェクト設定UIが未接続。固定パスのベースマップ読み込みは汎用SVG viewBox/width/heightに対応済みだが、ユーザー指定SVGのコピー・切替は未実装 | 出界図形式固定のスケール依存は解消。assets二重管理の整理と変換スクリプトは残 |

### 1.3 低（UX・表示）

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| B15 | 未対応 | 機能の詳細な仕様が要件定義書に不足。実装と要求にズレが生じている可能性 | 結合ツール・レイヤー操作・表示/編集モード・共有頂点・地物移動など、あるべき姿の文書化が追いついていない |

---

## 2. 未実装機能

### 2.1 統合品質（最優先）

追跡無し

### 2.2 機能要件

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| 地図SVG汎化 | 中 | 固定パスのSVG正規化・不適合時フォールバックは実装済み。プロジェクト設定から任意SVGを指定し、コピー・切替するUI/保存連携が未実装 |
| .gimoza ファイル形式のOS統合 | 中 | `.gimoza` の無圧縮ZIP読み書き（project.json + assets/base-map.svg）は実装済み。ファイル関連付け・専用アイコンは未実装 |
| 初回起動体験 | 中 | プリセット地図表示は通常起動で動作。地図SVG欠損時に `base-map.svg` を確実に同梱・表示する配布時検証が未完了 |
| データ整合性チェック拡充 | 中 | 読み込み時の孤立頂点・不正レイヤー参照・時間範囲・version検証は実装済み。共有頂点グループ整合性や全ポリゴン妥当性のロード時検証は未拡充 |
| データ互換性警告・マイグレーション | 中 | version不一致検出は実装済み。旧形式からの自動マイグレーションとユーザー向け差分警告は未実装 |
| エラーメッセージのユーザー向け改善 | 低 | ファイル操作エラーはユーザー向け文面へ改善済み。ドメイン検証・ZIP破損などの詳細分類は未整理 |

### 2.3 非機能要件

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| Electron実機統合E2E | 中 | IPC/preload/ファイルアクセスは Vitest で補強済み。Electron 実起動の統合E2Eは未整備 |
| electron-builder導入・パッケージング設定 | 高 | 配布用インストーラ(.exe等)の生成。electron-updaterはv1.0.0では不要 |
| アプリアイコン(.ico) | 高 | ロゴSVGからWindows用.icoを生成 |
| package.jsonメタデータ整備 | 低 | author/licenseは設定済み。repository URLは未設定 |
| パフォーマンス最適化（空間インデックス、LOD、カリング） | 低 | |

---

## 3. 構造的課題

外部レビュー（2026-03-25）で指摘された、不具合ではないが改善すべき構造問題。

| # | 深刻度 | 問題 | 備考 |
| --- | --- | --- | --- |
| S1 | 中 | `App.svelte` は引き続き大きい。`MapCanvas.svelte` は `mapCanvas/MapCanvasSvgLayers.svelte` / `MapCanvasHud.svelte` へ分割済み | `appPolygonEditing.ts` などへの抽出で前進したが、開発ガイドの200行目安にはなお未達。引き続き `App.svelte` のコントローラ分割が必要 |
| S5 | 中 | E2EがElectron統合を検証できていない | IPC/preload/ファイルアクセスは Vitest で補強済み。Chromium 136+ CLI制限により Electron 実起動 E2E は未整備 |

---

## 4. テスト品質

> 詳細は [`テスト改善計画.md`](テスト改善計画.md) を参照。

| 項目 | 状態 |
| --- | --- |
| フェーズ1: エッジケース補強 | 完了（+53件） |
| フェーズ2: カバレッジ駆動改善 | 進行中（今回 `.gimoza` / IPC / SVG / EdgeSlide接続 / 測量クリアに +17件、結合/共有頂点移動/EdgeSlide交差/境界始点/境界接触/No-op Undo/地物ドラッグ復元/複数territory再マージ/穴内島マージに +15件）。引き続き `EditFeatureUseCase` 61% 等が重点 |
| フェーズ3: 複合シナリオ | 計画済み（+21〜30件予定）。操作連鎖の結合テスト |
| フェーズ4: E2E拡充 | 計画済み（+10〜20件予定） |
| テスト中に発見された不具合 | なし（発見次第ここに追記） |

---

## 5. 修正優先順位

2026-04-17時点の推奨順序（v1.0.0配布に向けて）:

1. 配布準備（electron-builder、Windows .ico、ファイル関連付け、repositoryメタデータ）
2. 任意地図SVGのプロジェクト設定UI接続 + assets二重管理整理
3. データ整合性チェック拡充 + 旧形式マイグレーション
4. 要件定義書の仕様補完（B15）
5. テスト品質改善（`テスト改善計画.md` フェーズ2〜3）
6. 構造改善（S1）
7. E2Eテスト拡充（`テスト改善計画.md` フェーズ4）+ Electron実機統合E2E + パフォーマンス改善
