# 現状.md — 未解決課題・既知不具合・残作業

> 完了した実装の履歴は `実装済み.md` を参照。
> テスト品質改善の計画と実施記録は `テスト改善計画.md` を参照。
> 完了した課題は即時削除する（開発ガイド§重要な注意事項 参照）。

## テスト総数

- ユニットテスト: 1302件（86ファイル、全パス）
- E2Eテスト: 174件（22ファイル、全パス）
- `npm run build`: 通過
- `npm run check`: 0 errors / 0 warnings

---

## 1. 既知の不具合

状態ラベル: **壊** = 機能が壊れている / **未接続** = ロジックはあるがUI未接続 / **部分** = 一部のみ実装

### 1.1 重大（データ整合性・基本機能に影響）

> 重大課題がない場合もこの見出しは維持し、`追跡無し` と明記する。

追跡無し

### 1.2 中（機能不完全）

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| B3 | 部分 | 地図背景SVGが出界図形式専用。固定の地図しか読み込めない。さらに二重管理されており片方が機能していない | 手動での置き換え再ビルドでの変更はできるが、そもそも出界図以外のSVGへの対応が未整備 |
| B5 | 壊 | 測量モードのマーカー表示中に頂点ドラッグすると巨大ハンドルに化ける | 測量SVGマーカーと頂点ハンドルの座標系/z-index衝突の疑い |
| B6 | 未接続 | 頂点滑り（EdgeSlide）が未接続 | `EdgeSlideService` はドメイン層に実装済みだが `App.svelte` から参照されていない |
| B7 | 部分 | ウィンドウクローズ時の未保存警告がElectron側で不完全 | `onBeforeUnload` はあるが、mainプロセスの `close` イベントハンドリングが未実装。dev/本番で挙動が異なる可能性 |

### 1.3 低（UX・表示）

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| B4 | 未対応 | レイヤー透明度スライダーが透明度操作であることが視覚的にわかりにくい | 機能自体は正常動作（Layer.opacity → 描画反映済み）。ラベル・アイコン等でのUI改善が必要 |
| B8 | 未対応 | 測量モードのマーカーを能動的にクリアするUIがない | Escapeキーで消せるが測量モード選択中のみ。クリアボタンが必要 |

---

## 2. 未実装機能

### 2.1 統合品質（最優先）

追跡無し

### 2.2 機能要件

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| 地図SVG汎化 | 高 | 出界図形式以外のSVGを受け付けるためのバリデーション・自動補正。不適合時はエラー通知しプリセット出界図にフォールバック。変換スクリプトの提供も検討 |
| .gimoza ファイル形式（無圧縮ZIP） | 高 | project.json + assets/base-map.svg をバンドルする無圧縮ZIP形式。拡張子 `.gimoza`。ファイル関連付け・専用アイコンも必要。READMEにZIPである旨を明記すること |
| 初回起動体験 | 中 | 地図SVGがない状態での起動時にプリセット出界図（base-map-1）を表示。詳細は要件定義書§2.1参照 |
| データ整合性チェック | 中 | ファイル読み込み時のワールド全体の整合性検証（孤立頂点、不正参照等）。現状はポリゴンバリデーション・競合検出のみ |
| データ互換性警告・マイグレーション | 中 | ファイルにversion情報はあるがバージョンチェック・自動マイグレーションは未実装 |
| エラーメッセージのユーザー向け改善 | 低 | 開発者向けエラーを最低限ユーザーが読める形にし、「開発者へ報告してください」の案内を付与 |

### 2.3 非機能要件

| 要件 | 優先度 | 備考 |
| --- | --- | --- |
| Electron実機統合E2E | 中 | IPC/preload/ファイルアクセスは Vitest で補強済み。Electron 実起動の統合E2Eは未整備 |
| electron-builder導入・パッケージング設定 | 高 | 配布用インストーラ(.exe等)の生成。electron-updaterはv1.0.0では不要 |
| アプリアイコン(.ico) | 高 | ロゴSVGからWindows用.icoを生成 |
| バージョン1.0.0化 | 高 | package.json + AboutDialog version定数 |
| package.jsonメタデータ整備 | 中 | author, license, repository等 |
| LICENSEファイル | 中 | 配布形態に応じたライセンス選定 |
| README整備（エンドユーザー向け） | 中 | インストール・使い方の案内 |
| パフォーマンス最適化（空間インデックス、LOD、カリング） | 低 | |

---

## 3. 構造的課題

外部レビュー（2026-03-25）で指摘された、不具合ではないが改善すべき構造問題。

| # | 深刻度 | 問題 | 備考 |
| --- | --- | --- | --- |
| S1 | 中 | `App.svelte` 1908行。`MapCanvas.svelte` は 653行で `mapCanvas/MapCanvasSvgLayers.svelte` / `MapCanvasHud.svelte` へ分割済み | `appPolygonEditing.ts` などへの抽出で前進したが、開発ガイドの200行目安にはなお未達。引き続き `App.svelte` のコントローラ分割が必要 |
| S5 | 中 | E2EがElectron統合を検証できていない | IPC/preload/ファイルアクセスは Vitest で補強済み。Chromium 136+ CLI制限により Electron 実起動 E2E は未整備 |

---

## 4. テスト品質

> 詳細は [`テスト改善計画.md`](テスト改善計画.md) を参照。

| 項目 | 状態 |
| --- | --- |
| フェーズ1: エッジケース補強 | 完了（+53件） |
| フェーズ2: カバレッジ駆動改善 | 進行中（`ResolveFeatureAnchorConflictsUseCase` / `CommitFeatureAnchorEditUseCase` に +3件）。引き続き `EditFeatureUseCase` 61% 等が重点 |
| フェーズ3: 複合シナリオ | 計画済み（+21〜30件予定）。操作連鎖の結合テスト |
| フェーズ4: E2E拡充 | 計画済み（+10〜20件予定） |
| テスト中に発見された不具合 | なし（発見次第ここに追記） |

---

## 5. 修正優先順位

2026-04-16時点の推奨順序（v1.0.0配布に向けて）:

1. 不具合修正（B3: 地図SVG汎化、B5: 巨大ハンドル、B6: 頂点滑り接続、B7: ウィンドウクローズ警告）
2. .gimoza ZIP形式実装 + 配布準備（electron-builder、アイコン、v1.0.0化、メタデータ・LICENSE・README）
3. UX改善（B4: 透明度スライダー、B8: 測量クリアボタン）+ データ整合性チェック・互換性警告
4. テスト品質改善（`テスト改善計画.md` フェーズ2〜3）
5. 構造改善（S1）
6. E2Eテスト拡充（`テスト改善計画.md` フェーズ4）+ Electron実機統合E2E + パフォーマンス改善
