# 実装済み.md — 実装履歴アーカイブ

> この文書は実装フェーズの完了記録。アクティブな課題・不具合は `現状.md` を参照。

## 概要

初期実装フェーズ1〜7完了。現時点で全1533ユニットテスト＋172 E2Eテスト。

> **テスト件数の自動同期について**: 現状.md / 実装済み.md に書かれている総数（ユニットテスト件数・テストファイル数・E2E件数）は、コミット時に pre-commit フック（[`scripts/sync-test-counts.sh`](scripts/sync-test-counts.sh) 経由）が `vitest` を実行して自動更新する。**新規エントリでは hook が自動更新する箇所（本欄・E2E ファイル別テーブル）以外に件数を書かない方針**（`memory/feedback_no_test_count_in_narrative.md`）。`+15 件 / -3 件` のような累積差分や per-module 件数を narrative に手書きするとレビュー往復が増えるため、追加内容は質的記述（テスト名・カバレッジ対象）で表現する。過去エントリの件数記述はそのまま歴史記録として残す。

---

## 2026-06-02 階層モデル新仕様 Phase 2.5-D: PropertyPanel のコンテナ表示

Phase 2.5-C 完了後、Phase 2.5（コンテナ UI 基盤）の 4 番目のサブフェーズに着手。現状.md §6.10 に従い、`PropertyPanel` が集約地物コンテナ（`featureType === 'Polygon'` だが自身の `shape` を持たない地物）を選択時に表示できるようにする。Phase 2.5-B/C で描画・ヒットテスト対象になったコンテナが、選択後にプロパティパネルで親子・面スタイルを確認・編集できない状態を解消する。

- 着手前ゲート（開発ガイド §5.1.2 / 指示.md §共通ルール）:
    - 要件定義書 §4.1 line 1190「`shape`?: …**末端地物（リーフ）のみが保持する**。集約地物（コンテナ）はこのプロパティ自体を持たず、形状は下位領域の和として実行時に導出する」+ line 1184「`style`?: …面情報のスタイル定義。面情報以外の地物では省略」+ line 1212「`featureType`: …("Point", "Line", "Polygon")」 → コンテナも面情報（`featureType === 'Polygon'`）なのでスタイル・親子を持ち得る。表示ゲートを `anchor.shape?.type === 'Polygon'` から `feature.featureType === 'Polygon'` へ移す
    - 開発ガイド §6.6.9「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」適用パターン「判定をデータ側に寄せる」: 「新規経路（PropertyPanel のコンテナ表示 等）が増えても同じ判定基準を共有できる」 → PropertyPanel は sceneEntries を介さず Feature を直接受け取る経路のため、現状.md §6.10 の 2.5-D 指定どおり polygon-like 判定を `feature.featureType === 'Polygon'` に統一
    - 開発ガイド §6.0.1 検出観点2「対概念の同期」: 「ある概念に仕様判断を加えたら、その対概念にも同じ判断を反映する」 → テンプレート側の「ポリゴンセクション表示ゲート」と `applyChanges` 側の「面スタイル構築ゲート」は対概念で、片方だけ直すと「スタイル UI は表示されるが保存されない」ドリフトが残る。両者を単一ヘルパー `isPolygonLikeFeature` / `buildUpdatedAnchorProperty` に集約して構造的に同期
    - 開発ガイド §6.6.8「排他検証は仕様の対象範囲を実装で広げない」: 今回は表示のみ。コンテナの `kind` は表示専用（Phase 7 で運用）で整合性検証には一切触れない
- 変更内容（src 2 ファイル + tests 1 ファイル）:
    - 新規 `src/renderer/presentation/components/propertyPanelUtils.ts`（既存 `propertyPanelHistoryUtils.ts` に倣ったテスト可能ユーティリティ）:
        - `isPolygonLikeFeature(feature)`: `feature?.featureType === 'Polygon'`。leaf polygon / コンテナの双方を面情報と判定する単一の真実
        - `buildUpdatedAnchorProperty(feature, anchor, form)`: 編集フォーム値から更新後 `AnchorProperty` を構築。面情報のときのみフォームの面スタイルを採用し、点情報・線情報では既存 `style` を保持する。テンプレート表示ゲートと同じ `isPolygonLikeFeature` を内部で共有
    - `src/renderer/presentation/components/PropertyPanel.svelte`:
        - `propertyPanelUtils` から 2 ヘルパーを import。`isPolygonFeature = $derived(isPolygonLikeFeature(feature))` / `isContainer = $derived(isPolygonFeature && !anchor?.shape)` を追加
        - `applyChanges` のインライン分岐（`anchor.shape?.type === 'Polygon' ? {...style} : anchor.property.style`）を `buildUpdatedAnchorProperty(...)` 呼び出しへ置換
        - 形状情報セクションに `{:else if isContainer}` 分岐を追加し「下位領域の和として導出（N 領域）」を表示（コンテナは自身の shape を持たずリング数/頂点数が出ないため）
        - 親・下位領域 + 面スタイルセクションの表示ゲートを `{#if anchor.shape?.type === 'Polygon'}` → `{#if isPolygonFeature}` へ変更。これでコンテナでも親子・パレット・塗り色・選択色・自動配色を表示・編集可能に
        - 種別欄（`anchor.shape?.type ?? '集約地物'`）はコンテナで「集約地物」を表示する既存挙動を維持
    - テスト: 新規 `tests/presentation/components/propertyPanelUtils.test.ts`。`isPolygonLikeFeature`（leaf polygon → true / コンテナ shape なし → true / Point・Line → false / null → false）と `buildUpdatedAnchorProperty`（名前・説明は全種別で反映 / leaf polygon・コンテナで面スタイル採用 / 点・線で既存 style 保持 / kind 等の既存フィールド保持）を固定
- 振る舞い変更:
    - 集約地物コンテナを選択すると PropertyPanel が親・下位領域・面スタイルを表示し、面スタイルを編集・保存できるようになる。現行データにはコンテナが存在しないため observable な runtime 差はなく、Phase 4 のコンテナ生成 UseCase 実装後に効く
    - leaf polygon / Point / Line の表示は不変（`feature.featureType === 'Polygon'` は leaf polygon に対し従来の `anchor.shape?.type === 'Polygon'` と同値、Point/Line は引き続き除外）
- 検証:
    - `npm run check`: 0 errors / 0 warnings
    - `npm run build`: 通過
    - `npx vitest run`: 全テストファイル通過。新規 propertyPanelUtils テスト（コンテナで面スタイル採用 / 点・線で除外 等）が pass。既存テストも継続通過
- スコープ: src 2 ファイル + tests 1 ファイル / 約 +60 -15 行。§6.10 上限「~15 ファイル / ~300 行」内
- 残置 / 後続:
    - **Phase 2.5-E**: parentTransferDialog のコンテナ許容（`getActivePolygonAnchor` を `feature.featureType === 'Polygon'` 基準に）
    - 種別欄は現状 shape 由来の型ラベル（Point/LineString/Polygon/集約地物）を表示。`property.kind`（国/州/連邦 など）の表示・編集 UI は Phase 7（`kind` プロパティ運用、Sidebar UI 整理）で対応
    - Sidebar の `レイヤー / プロパティ / 地物一覧` タブ構成の再構成は §2.7 改訂とあわせて Phase 7 で対応（現状.md §6.10）
- 教訓反映:
    - 参照した既存教訓: 開発ガイド §6.6.9「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」適用パターン「判定をデータ側に寄せる」（PropertyPanel を 5 番目の consumer として `feature.featureType === 'Polygon'` 判定で追加）/ §6.0.1 検出観点2「対概念の同期」（表示ゲートと保存ゲートの同期）/ §6.6.8「排他検証は仕様の対象範囲を実装で広げない」（表示のみで整合性検証に触れない）
    - 新規教訓の要否: 不具合は発生せず、適用した教訓はいずれも既存（特に §6.6.9 は「PropertyPanel のコンテナ表示」を将来 consumer として明記済み）。§6 への新規追加は不要

## 2026-05-27 階層モデル新仕様 Phase 2.5-C: hitTest tie-break 再設計

Phase 2.5-B 完了後、Phase 2.5（コンテナ UI 基盤）の 3 番目のサブフェーズに着手。現状.md §6.10 と開発ガイド §6.2.25 採用ポリシーに従い、`hitTest` を「最初にヒットした entry を返す」から「全ヒット収集 → tie-break ソート → 1 件目を返す」へ再設計し、`App.svelte` の `clickedFeatureId` 即採用ショートサーキットを撤去して hitTest 最終決定経路へ統一する。

- 着手前ゲート（開発ガイド §5.1.2 / 指示.md §共通ルール）:
    - 開発ガイド §6.2.25「クリック経路の最終決定は一元化する」: 「DOM 由来 ID を即採用すると、render order によっては親集約地物のパスが子末端の上に描画されたとき子をクリック選択できなくなる」「採用ポリシー: hitTest を最終決定として優先し、`preferredFeatureId` に DOM target を渡して tie-break キーとする」 → `App.svelte:onMapClick` の `if (clickedFeatureId) { selectedFeatureId = clickedFeatureId; return; }` 即採用パスを撤去し、hitTest を最終決定として使う。`onMapMouseDown`（ドラッグ開始判定）も同じ採用ポリシーへ統一
    - 開発ガイド §6.6.9「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」適用パターン「判定をデータ側に寄せる」: 「経路ごとに『polygon-like か』をハードコードで書くと、規約変更時に経路ごとに追従漏れが発生する。共通中間表現に規約を寄せ、規約自体を unit テストで固定する」 → tie-break で使う `depth` を経路ごとに `deriveDepth` 呼び出しではなく `MapSceneEntry.depth` フィールドに集約。描画順ソート（ASC）と hitTest tie-break（DESC）が同じ派生値を共有し、対概念矛盾を構造的に排除
    - 開発ガイド §6.1.2「非表示要素の選択・操作の混入」: 「ユーザー操作の対象は『現在UI上に表示されている要素』に限定する」 → hitTest 自体は sceneEntries に含まれない地物を返してはならない。DOM target フォールバック判断は呼び出し側（`App.svelte`）の責務として明示
    - 開発ガイド §6.0.1 検出観点1「関数シグネチャ撤去・引数撤去では tests/ にも grep を広げる」: hitTest シグネチャに `preferredFeatureId` 引数を追加するが、既存全呼び出し（src 2 + tests 約 25）はデフォルト値（`null`）で後方互換のため touch 不要。grep 検証で漏れ 0 件確認
- 設計判断 — Layer 撤去後の tie-break キー: 開発ガイド §6.2.25 採用ポリシー本文の旧 3 キー「(1) layerOrder 降順、(2) depth 降順、(3) DOM target 一致」のうち、Phase 2-D で `Layer` 概念を撤去済みのため (1) は永久に不在。実効的に (1) depth 降順 + (2) DOM target 一致の 2 キーで運用。教訓本文も同じ変更単位で「(1) depth 降順、(2) DOM target 一致」+ 背景注記「旧版（〜Phase 2-D-6）では 3 キーだったが Layer 撤去で消滅」へ改訂（§6.0.1 検出観点1「仕様文書での用語撤廃宣言時の同一文書同期」適用）
- 変更内容（src 3 ファイル + tests 3 ファイル + 開発ガイド 1 ファイル）:
    - `src/renderer/presentation/components/mapSceneEntries.ts`:
        - `MapSceneEntry` interface に `depth: number` フィールドを追加。`collectMapSceneEntries` 内部で既に算出している `depthMap` の値（`deriveDepth(...) ?? Number.POSITIVE_INFINITY`）をそのまま entry 側に流す。経路間での `deriveDepth` 重複呼び出しを排除
        - JSDoc に「`depth` は `HierarchyService.deriveDepth` の結果を解決済みで保持する（Phase 2.5-C）」「描画順ソート（ASC）と hitTest tie-break（DESC）が同じ値を共有」と規約として明示
    - `src/renderer/infrastructure/rendering/hitTestUtils.ts`:
        - `hitTest(clickCoord, sceneEntries, vertices, thresholdDeg, preferredFeatureId: string | null = null)` シグネチャに `preferredFeatureId` 引数を追加（デフォルト `null` で後方互換）
        - 関数本体を「ループで最初にヒットした entry を即返却」→「全ヒットを `hits: MapSceneEntry[]` に収集 → (1) `depth` 降順、(2) `preferredFeatureId` 一致を優先でソート → `hits[0]` を返す」に変更
        - depth が `Number.POSITIVE_INFINITY`（壊れたデータ）の場合は `Number.NEGATIVE_INFINITY` 扱いに揃え、ソート安定性を保持
        - JSDoc に「tie-break ポリシー」「`preferredFeatureId` は DOM target 由来」「フォールバック判断は呼び出し側の責務」を明示
    - `src/renderer/App.svelte`:
        - `onMapClick`: `view` / `edit` モード分岐内の `if (clickedFeatureId) { selectedFeatureId = clickedFeatureId; return; }` 即採用パスを撤去。常に `hitTest(coord, sceneEntries, vertices, getHitThreshold(), clickedFeatureId)` を呼び、戻り値があればその featureId を採用、なければ `clickedFeatureId` にフォールバック、それも null なら選択解除
        - `onMapMouseDown`（ドラッグ開始判定）: `let hitFeatureId = clickedFeatureId; if (!hitFeatureId && ...)` の DOM 優先パスを撤去。`selectedFeatureId && currentTime` のとき `hitTest(..., clickedFeatureId)` を呼び、戻り値があればそれを採用、なければ `clickedFeatureId` フォールバック。`shouldStartFeatureDrag` は `selectedFeatureId === null` でガード済みなので、selectedFeatureId が null のときの hitFeatureId は null 固定で安全
    - テスト調整:
        - `tests/infrastructure/rendering/hitTestUtils.test.ts`: `describe('tie-break ポリシー (depth DESC + preferredFeatureId)')` ブロックを新設。「親子コンテナで深い子（depth DESC）を選ぶ」「入力順を逆にしても深い子が勝つ」「同 depth 複数ヒットで `preferredFeatureId` 一致を優先」「`preferredFeatureId` 指定なしでは stable sort で入力順が勝つ」「`preferredFeatureId` 指定でも depth DESC が優先される（DOM target が浅いとき）」「点がポリゴン上で `preferredFeatureId` で点を選べる」の 6 観点を固定。既存「集約地物のヒットテスト」テストの期待値を `'container'` → `'leaf'` に修正（新 tie-break で深い子が勝つため）
        - `tests/presentation/components/mapSceneEntries.test.ts`: `describe('MapSceneEntry 規約: depth フィールドは派生階層値を保持する')` ブロックを新設。「最上位地物の depth は 0」「多段階層の depth が親→子で 0 → 1 → 2」「Point リーフの depth は 0」の 3 観点を固定
        - `tests/presentation/components/mapSceneEntriesIntegration.test.ts`: 「集約地物（shape なし + childIds 非空）は描画・hitTest・wrapOffsets に含まれ、頂点 owner map にのみ現れない」の hitTest 期待値を `'container'` → `'leaf'` に修正。「派生が空でない子を持つとき、3 経路が派生形状（子の和）に揃う」は leaf-child の parentId が null（テストヘルパー `polygonFeature` 由来でリンクされていない）で depth が両方 0 同点となるため期待値 `'transitional'` のまま維持し、コメントで「Phase 2.5-C tie-break 第1キー検証は `hitTestUtils.test.ts > tie-break ポリシー` 群に集約」と明示
    - `開発ガイド.md` §6.2.25:
        - 教訓本文の tie-break キー記述を「(1) layerOrder 降順、(2) depth 降順、(3) DOM target 一致」→「(1) depth 降順、(2) DOM target 一致」へ更新
        - 採用ポリシー本文を「`preferredFeatureId` に DOM target を渡して第3 tie-break キーとする」→「`preferredFeatureId` に DOM target を渡して tie-break キーとする」へ簡素化
        - 「データ側に寄せる」段落を追加（`MapSceneEntry.depth` フィールドへの集約 = §6.6.9 適用パターン）
        - 「フォールバック判断は呼び出し側の責務」と「hitTest 自体は sceneEntries に含まれない地物を返してはならない」を明示
        - 背景段落を追加（Phase 2-D での Layer 撤去によって第1キー消滅 → 2 キー運用に移行した経緯）
- 振る舞い変更:
    - 親集約地物と子末端地物が重なる位置で、これまで render order に依存していたクリック選択結果が「常に深い子（depth DESC）が勝つ」に統一される。Phase 2.5-B で集約地物がヒットテスト対象になったが、現行データには集約地物が存在しないため observable な runtime 差はない。Phase 4 でコンテナ生成 UseCase 実装後に効く
    - DOM target ベースの即採用がなくなるため、`data-feature-id` が指す要素と hitTest 判定が異なるケースで結果が変わる可能性がある。ただし sceneEntries は描画対象と一致するため、hitTest 経路が描画と一致しない状況は構造的に発生しない（§6.1.2 / §6.6.9 遵守）
    - hitTest が空振りした場合の DOM target フォールバックは維持（点/線の閾値由来取りこぼし対策。§6.2.25 採用ポリシー）
- 検証:
    - `npm run check`: 0 errors / 0 warnings
    - `npm run build`: 通過
    - `npx vitest run`: 全テストファイル通過。新規 tie-break テスト群（depth DESC で深い子優先 / 同 depth で preferredFeatureId 一致 / preferredFeatureId 指定でも depth DESC が優先 / 点をポリゴン上に置いて preferredFeatureId で点を選ぶ）と MapSceneEntry depth 規約テストがすべて pass。既存テスト（hitTestUtils の集約地物 / mapSceneEntriesIntegration の対概念整合性等）も継続通過
- スコープ: src 3 ファイル + tests 3 ファイル + 開発ガイド 1 ファイル / +約 200 -約 30 行（src の実装ロジック変更は約 +50 -25 行、テスト追加が約 +130 行、ドキュメント約 +15 -5 行）。§6.10 上限「~15 ファイル / ~300 行」内
- 残置 / 後続:
    - **Phase 2.5-D**: PropertyPanel のコンテナ表示（種別「集約地物」、親子・style 編集 UI）
    - **Phase 2.5-E**: parentTransferDialog のコンテナ許容
- 教訓反映:
    - 参照した既存教訓: 開発ガイド §6.2.25「クリック経路の最終決定は一元化する」/ §6.6.9「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」適用パターン「判定をデータ側に寄せる」/ §6.1.2「非表示要素の選択・操作の混入」/ §6.0.1 検出観点1（用語撤廃宣言時の同一文書同期）
    - 既存教訓の改訂: 開発ガイド §6.2.25 を Phase 2-D（Layer 撤去）完了後の現状に同期し、tie-break キーを 3 → 2 へ更新 + データ側に寄せる段落追加 + 旧版背景注記
- 手動動作確認の手順:
    - 本サブフェーズは Phase 4 でコンテナ作成 UseCase が実装されるまで実データへの runtime 影響を持たない（現行データに集約地物が存在しないため）
    - 既存 .gimoza を読み込み、複数地物が重なっていない通常の位置をクリックして従来通り選択できることを確認: 通常起動 → 既存プロジェクトを開く → 任意の Polygon リーフをクリック → 該当 Polygon がプロパティパネルに表示されることを確認（hitTest 経路は変わるが空振り時の DOM target フォールバックが効くため観測挙動は不変）
    - Phase 4 完了後にコンテナと末端地物の重なり位置をクリックし、深い末端地物が選択されることを E2E に追加予定

### 2026-05-28 追補: shouldStartFeatureDrag の DOM target 即採用バグ修正 + 古いコメント同期

レビュー指摘(高)「drag 開始判定がまだ DOM ID で通る。`shouldStartFeatureDrag` が `clickedFeatureId === selectedFeatureId` で OR 条件を持つため、親が選択中 / 重なり位置の hitTest は子 / DOM target は親 のケースで親ドラッグが開始される。§6.2.25『hitTest 優先、DOM は空振り時だけ fallback』と矛盾」を本コミット前に修正。

- 変更内容（src 2 ファイル + tests 1 ファイル + src コメント 2 箇所）:
    - `src/renderer/infrastructure/rendering/editInteractionUtils.ts`:
        - `FeatureDragStartParams` から `clickedFeatureId` フィールドを撤去
        - `shouldStartFeatureDrag` の戻り条件を `clickedFeatureId === selectedFeatureId || hitFeatureId === selectedFeatureId` → `hitFeatureId === selectedFeatureId` のみへ変更
        - JSDoc に「`hitFeatureId` は呼び出し側が hitTest 経路で確定した最終ターゲット ID」「DOM target を OR 条件に入れると子クリックが親ドラッグに化ける」と理由を明示
    - `src/renderer/App.svelte`: `shouldStartFeatureDrag` 呼び出しから `clickedFeatureId` 引数を撤去
    - `tests/infrastructure/rendering/editInteractionUtils.test.ts`:
        - `baseParams` から `clickedFeatureId` を撤去し、`hitFeatureId: 'f1'` を含む形に再構成
        - 旧「ヒットテスト結果が選択中地物ならクリック元が空でも開始できる」テストを「hitFeatureId が選択中地物と一致すれば開始する」へ簡素化
        - 新規ケース: 「hitFeatureId が null なら開始しない」（hitTest 空振りで DOM target フォールバックも無いケース）
        - 新規ケース: 「hitFeatureId が選択中地物と異なれば開始しない（親が選択中で hitTest が子を返したケース）」← §6.2.25 の核心回帰テスト
- コメント同期（レビュー指摘(低)）:
    - `src/renderer/App.svelte:1050-1053` のコメントを「第3 tie-break キーとして」→「tie-break キー（depth 同点時の決め手）として」に修正（現在 2 キー運用）
    - `src/renderer/presentation/components/mapSceneEntries.ts:75-77` のコメント「hitTest は sceneEntries を順次走査して最初にヒットした要素を返す。... tie-break は Phase 2.5-C で DOM target ベースに再設計する計画」を「hitTest は全ヒットを収集後 (1) depth 降順 + (2) `preferredFeatureId` 一致で tie-break する（§6.2.25 採用ポリシー）」へ更新
- 文書件数（レビュー指摘(中)）:
    - 2026-05-27 Phase 2.5-C エントリの narrative に書いていた `「全 96 ファイル / 1522 件 pass」` を質的記述（新規テスト名 + 既存継続確認）に置き換え（`memory/feedback_no_test_count_in_narrative.md` 遵守）。テスト総数の自動同期欄は pre-commit フックが算出するため手動編集不要
- 振る舞い変更:
    - 親が選択中で、重なり位置の hitTest が子（深い側）を返すクリックでは drag が開始されなくなる（DOM target 経由でも開始しない）。子の選択は onMapClick の hitTest 結果で行われる
    - hitFeatureId が selectedFeatureId と一致するケース（同じ地物を再度クリック）の drag 開始は不変
- 教訓反映:
    - 既存教訓 §6.2.25 採用ポリシーの「hitTest 優先 + DOM は空振り時だけ fallback」は **select 経路だけでなく drag 開始判定にも適用する**ことを今回の修正で固定。drag 経路で OR 条件を残していたのは「対概念の同期」（§6.0.1 検出観点2）の漏れ事例。本追補のテスト「hitFeatureId が選択中地物と異なれば開始しない」が回帰として機能する
- 検証: `npm run check` 0 errors / 0 warnings、`npx vitest run` 全テストファイル通過

## 2026-05-27 階層モデル新仕様 Phase 2.5-B: 描画 + ヒットテストのコンテナ受容

Phase 2-E 完了後、Phase 2.5（コンテナ UI 基盤）の 2 番目のサブフェーズに着手。`mapSceneEntries` がコンテナ（`shape` なし + `childIds` 非空）を派生形状で受容し、`FeatureRenderer` + `LabelRenderer` + `hitTestUtils` がコンテナを描画・ラベル・hitTest 対象として扱うようにする。

- 着手前ゲート（開発ガイド §5.1.2 / 指示.md §共通ルール）:
    - 開発ガイド §6.6.9「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」: 「集約地物の形状を『子の和として導出する』とき、描画・ヒットテスト・派生サービスの3経路は同じ判定で動かす。判定条件のドリフトがあると、画面では描画されているのに選択できない、ラベル位置が描画と異なる、移行期間ノードの祖先派生が落ちる、といった対概念矛盾が次々と出る」「MapCanvas の選択コンテキストアンカー取得（`selectionAnchor` derive）も同じ shape 解決経路を共有する 4 経路目」 → 本サブフェーズは `mapSceneEntries.collectMapSceneEntries` をコンテナ受容に変える際、描画・hitTest・wrapOffsets・vertex owner map・selectionAnchor の 5 経路が同じ判定で動くことを回帰テストで固定する
    - 開発ガイド §6.1.2「非表示要素の選択・操作の混入」: 「ユーザー操作の対象は『現在UI上に表示されている要素』に限定する」 → 描画はされるが選択できない（あるいは逆）の中間状態を作らない。コンテナを sceneEntries に含めるなら描画・hitTest 両方を同時に受容させる
    - 開発ガイド §6.0.1 検出観点2「対概念の同期」: 「描画 ↔ ヒットテスト」は典型的な対概念。
- スコープ拡張判断:
    - 現状.md §6.10 では Phase 2.5-B = 描画 / Phase 2.5-C = ヒットテストと分離する計画だったが、§6.6.9 と §6.1.2 を厳格に遵守するには、コンテナを sceneEntries に含めた瞬間に描画・hitTest 両方の経路で受容する必要がある（中間状態を残すと §6.6.9「描画される領域 = 選択できる領域」違反になる）。本サブフェーズで両者を統合実装し、Phase 2.5-C は「layer + depth + DOM target の 3 キー tie-break と `App.svelte` の `clickedFeatureId` 即採用パスを hitTest 最終決定方式へ再設計」のみへ縮小する判断を取った
- 変更内容（src 4 ファイル + tests 4 ファイル）:
    - `src/renderer/presentation/components/mapSceneEntries.ts`:
        - `collectMapSceneEntries` のエントリ収集ループで `if (!anchor || !anchor.shape) continue;` の shape 必須ガードを撤去。`feature.featureType === 'Polygon'` の場合は `resolvePolygonRings` の戻り値で受容可否を判定し、`null`（不正データ / コンテナ派生空）のみ除外。`[]`（リーフで vertex 解決失敗）は旧挙動を維持して entries に含める
        - `resolvePolygonRings` のシグネチャを `(feature, anchor, features, vertexCoordinates, currentTime)` に変更（`shape` 引数を撤去）。判定を「リーフ（`shape` あり + `childIds` 空）」「集約地物（`shape` なし + `childIds` 非空、派生空なら null）」「移行期間ノード（`shape` あり + `childIds` 非空、派生空なら自身の `shape` を fallback）」の 3 分岐に再構成
        - 収集後、`deriveDepth(feature, features, currentTime)` で各エントリの depth を算出し、昇順（親→子）でソート。`undefined`（壊れたデータ）は `Number.POSITIVE_INFINITY` として最後尾へ送り、同一 depth は features 入力順を保持（Array.sort は ECMAScript 2019 で stable 化済み）
        - ソート後、`featureIndex` を 0 始まりで連番付与
    - `src/renderer/presentation/components/FeatureRenderer.svelte`:
        - `polygonAutoColors` 算出の filter を `anchor.shape!.type === 'Polygon'` → `anchor.shape?.type === 'Polygon'` へ緩和。集約地物（`shape === undefined`）は `AutoColorFeatureEntry.shape` 必須の現行 API 制約により自動配色グラフから除外（Phase 6 の「同 depth 兄弟」基準自動配色まで集約地物はパレット index ベースの fallback 色で描画）
        - 描画ブロックの Polygon 分岐条件を `anchor.shape!.type === 'Polygon'` → `feature.featureType === 'Polygon'` へ変更。`polygonRings` 経由で派生形状（集約地物・移行期間ノード）と shape 直接（リーフ）を統一して描画
    - `src/renderer/presentation/components/labelRendererUtils.ts`:
        - `isPolygonLikeAnchor(anchor, polygonRings)` ヘルパーを追加。`anchor.shape?.type === 'Polygon'` || （`anchor.shape` なし + `polygonRings` 非空）= true を返す
        - `getFeatureLabelPosition` / `measureFeatureLabelArea` / `shouldRenderFeatureLabel` の `if (!anchor.shape) return ...` ガードを `isPolygonLikeAnchor` 経由に置き換え。集約地物でも `polygonRings` から重心・面積・表示判定を返す
    - `src/renderer/infrastructure/rendering/hitTestUtils.ts`:
        - `hitTest` 関数の `if (!anchor.shape) continue;` ガードを撤去。Polygon 振り分け条件を `anchor.shape.type === 'Polygon'` → `feature.featureType === 'Polygon'` に変更し、集約地物も `entry.polygonRings` 経由で `hitTestPolygon` の対象になるよう統一
    - テスト調整:
        - `tests/presentation/components/mapSceneEntries.test.ts`:
            - `polygonShape` ヘルパーが返すリングを `{ type: 'territory', vertexIds }` リテラルから `Ring` インスタンスに修正（派生形状ルート `resolvePolygonAnchorPolygons` が `ring.ringType` を参照するため。旧テストではリーフ単独描画 `resolveAnchorPolygonRings` だけが使われ silent pass していた盲点を是正）
            - 旧「shape を持たない錨（コンテナ）は除外する」を「派生形状を解決できない集約地物（vertexCoordinates 空）は除外する」へ書き換え（コンテナのみ除外、リーフは polygonRings 空でも entries に含めることを明示）
            - 新規ケース: 集約地物の派生形状受容 / 移行期間ノードの派生優先 / depth 昇順ソート（親→子） / 多段階層の depth 順 / 同 depth の入力順保持
        - `tests/presentation/components/mapSceneEntriesIntegration.test.ts`:
            - 旧「shape を持たないコンテナ錨は 4 経路すべてから除外される」を「集約地物は描画・hitTest・wrapOffsets に含まれ、頂点 owner map にのみ現れない（shape プロパティを持たないため）」へ書き換え
            - 新規ケース: 派生形状を解決できない集約地物（child が features に含まれない）は 4 経路すべてから除外される
        - `tests/presentation/components/labelRendererUtils.test.ts`: 集約地物（`shape` なし + `polygonRings` あり）のラベル位置算出 / 面積算出 / `shouldRenderFeatureLabel` の表示可否 / `polygonRings` 未指定時の null 戻り値を固定する `describe('集約地物（shape なし + polygonRings あり）のラベル')` ブロックを追加
        - `tests/infrastructure/rendering/hitTestUtils.test.ts`: 集約地物のヒットテスト（派生形状内ヒット / 派生形状外非ヒット）を固定する `describe('集約地物のヒットテスト')` ブロックを追加
- 振る舞い変更:
    - 集約地物（実データに現在は存在せず、Phase 4 でコンテナ生成 UseCase 実装時に初めて runtime に現れる）が描画・hitTest・wrapOffsets の対象になる
    - sceneEntries の並びが入力順から `deriveDepth` 昇順（親→子順）に変わる。同一 depth では入力順を保持
    - SVG では後ろの要素ほど手前に描画されるため、親（depth 小）が先 → 子（depth 大）が後で描画され、視覚的に子が親を覆って見える適切な重なり順となる
    - 集約地物（`shape === undefined`）は自動配色グラフから除外し、`resolveStyle` でパレット index ベースの fallback 色を取得（Phase 6 で「同 depth 兄弟」基準自動配色に再設計予定）
    - `vertexSelectionContext.buildSceneVertexOwnerMap` は `if (!anchor.shape) continue;` ガードを維持。集約地物は shape プロパティを持たず頂点を所有しないため、ownerMap への登録は意味的に成立しない（変更不要）
    - Phase 2-D 完了時点では実データに集約地物が存在しないため、現行プロジェクトファイル経路では runtime 観測挙動は完全に不変
- 検証:
    - `npm run check`: 0 errors / 0 warnings
    - `npm run build`: 通過
    - `npx vitest run`: 全テストファイル通過。`collectMapSceneEntries` の depth 順ソート / コンテナ受容 / hitTest の集約地物受容 / LabelRenderer の集約地物ラベル / integration の対概念整合性回帰テストがすべて pass。既存テスト（hitTestUtils / mapCanvasUtils / vertexSelectionContext / labelRendererUtils 等）も継続通過
- スコープ: src 4 ファイル + tests 4 ファイル / 約 +280 -50 行（src の振る舞い変更は約 +60 -30 行、テスト追加が約 +220 -20 行）。§6.10 上限「~15 ファイル / ~300 行」内
- 残置 / 後続:
    - **Phase 2.5-C**: ヒットテスト tie-break 再設計（layer + depth + DOM target の 3 キー、`App.svelte:1057-1062` の `clickedFeatureId` 即採用パスを hitTest 最終決定 + `preferredFeatureId` に DOM target を渡す方式へ）
    - **Phase 2.5-D**: PropertyPanel のコンテナ表示
    - **Phase 2.5-E**: parentTransferDialog のコンテナ許容
    - **Phase 6**: 自動配色を「同 depth 兄弟」基準に再設計。集約地物も自動配色グラフへ参加させる
- 教訓反映:
    - 参照した既存教訓: 開発ガイド §6.6.9「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」/ §6.1.2「非表示要素の選択・操作の混入」/ §6.0.1 検出観点2「対概念の同期」
    - 既存教訓の追記: 開発ガイド §6.5 に「テスト fixture のリテラルは実体クラスの値オブジェクト/エンティティ用のプロパティを欠いていても、テスト経路で参照されないと silent pass する」観点を追記（本サブフェーズで `polygonShape` ヘルパーが `Ring` インスタンスではなくリテラル `{ type: 'territory', vertexIds }` を返していた問題を発見し、派生形状ルートが silent fail していた盲点を是正）
- 手動動作確認の手順:
    - 本サブフェーズは Phase 4 でコンテナが作成可能になるまで実データへの runtime 影響を持たない。検証は単体テストの集約地物描画・hitTest・ラベル・派生空除外で固定済み
    - Phase 4（操作 UseCase / Command 再設計）でコンテナ生成 UseCase 実装時、ナイフ分割・所属変更フロー経由でコンテナ feature を生成し、地図上で描画・クリック選択・ラベル表示・wrapOffsets 反映が動作することを視認確認する手順を E2E に追加予定

### 2026-05-27 追補: §6.6.9 判定基準ドリフト対策 + wrapOffsets 経路の集約地物受容 + §6.7 表同期

レビュー指摘(中)「判定基準が経路ごとに分岐している（`feature.featureType === 'Polygon'` vs `isPolygonLikeAnchor`）。将来 sceneEntries 規約を変えたとき、ラベル経路だけが追従漏れする経路ができる。判定基準をデータ側へ寄せる選択肢がある」と、続くレビュー指摘(中)「mapCanvasUtils.ts の `getSceneEntryLongitudeBounds` が `if (!anchor.shape) return null;` で集約地物を即除外しており、本サブフェーズ本体が記録した『wrapOffsets も同じ判定』が偶然 leaf 由来の経度範囲に依存していた。§6.6.9 / §6.1.2『描画・hitTest・wrapOffsets の shape 解決経路共有』とズレている」を本コミット前に解消。

- 変更内容（src 4 ファイル + tests 2 ファイル + 開発ガイド）:
    - `MapSceneEntry` の JSDoc に「Polygon entry は必ず `polygonRings !== null`、Point/LineString entry は必ず `polygonRings === null`」を **規約（Phase 2.5-B 以降）** として明示
    - `hitTestUtils.ts` の Polygon 振り分け条件を `feature.featureType === 'Polygon'` → `entry.polygonRings !== null` に変更
    - `FeatureRenderer.svelte` の Polygon 描画分岐を `feature.featureType === 'Polygon'` → `polygonRings !== null` に変更
    - `labelRendererUtils.ts` の `isPolygonLikeAnchor` を「`polygonRings !== undefined` なら `polygonRings !== null` で判定、`undefined` なら anchor.shape fallback」へ再構成（sceneEntries 経由の呼び出しは規約に従い、既存の単独呼び出し fallback は anchor.shape ベースの旧判定を維持）
    - `mapCanvasUtils.ts` の `getSceneEntryLongitudeBounds` を `entry.polygonRings !== null` を Polygon パスの第一条件として振り分ける構造に変更し、`if (!anchor.shape) return null;` 即除外を撤去。集約地物（`anchor.shape === undefined` + childIds 非空）も `entry.polygonRings` の派生形状から経度範囲を算出する
    - `mapSceneEntries.test.ts` に `describe('MapSceneEntry 規約: polygonRings の null/non-null は featureType と 1:1 対応')` ブロックを追加し、Polygon リーフ entry が non-null / vertex 解決失敗時も空配列で non-null / 集約地物 entry が non-null / Point entry が null / LineString entry が null の各観点を固定
    - `mapSceneEntriesIntegration.test.ts` に「集約地物の派生形状が遠方経度にあるとき、wrapOffsets がそれを反映する」テストを追加。leaf を遠方（経度 900..910）に配置し、コンテナだけの sceneEntries でも `-720` / `-1080` ラップタイルを返すことを固定（leaf 由来の偶然依存を排除し §6.6.9 の対概念整合性を回帰テストとして固定）
    - 開発ガイド §6.6.9 適用パターンに「判定をデータ側に寄せる」観点を追記（共通中間表現に規約を寄せる + 規約自体を unit テストで固定する + 新規経路が増えても同じ判定基準を共有できる）
- レビュー指摘(低)「§6.7 対応確認表が §6.5.5 を反映していない」: 「テスト環境・ツール依存」行を `6.5.1-6.5.4` → `6.5.1-6.5.5` に修正し、代表履歴に「2026-05-27 Phase 2.5-B テスト fixture リテラル silent fail」を追加
- 振る舞い変更:
    - 判定基準統一: 現状の MapSceneEntry 規約下では `feature.featureType === 'Polygon'` と `entry.polygonRings !== null` は機能等価のため、観測挙動は不変
    - wrapOffsets 経路: Phase 2.5-B 本体が「集約地物も wrapOffsets 対象」と記録していた挙動が、本追補で実際に動作するようになる。集約地物が実データに現れる Phase 4 以降で初めて runtime に効く（現行データには集約地物が存在しないため observable な差はない）
- 検証: `npm run check` 0 errors / 0 warnings、`npx vitest run` 全テストファイル通過

## 2026-05-26 階層モデル新仕様 Phase 2-E: depth 派生サービス追加

Phase 2.5-A 完了後、Phase 2 サブフェーズの最後として §6.10 計画通り `HierarchyService.deriveDepth(feature, allFeatures, time)` を追加。consumer は Phase 2.5-B 以降 / Phase 5 / Phase 6 で導入するため、本サブフェーズの差分はドメインサービス + テストのみに絞る（`feedback_subphase_scope_split.md` の「1経路ずつ」方針）。

- 着手前ゲート（開発ガイド §5.1.2 / 指示.md §共通ルール）:
    - 開発ガイド §6.4.14「親子グラフを辿る再帰サービスは visited セットで cycle guard を入れる」: 「`getAncestors` / `getDescendants` / `deriveParentShape` のような親子関係を辿る再帰サービスは、壊れたデータ（循環親子参照、parentId と childIds の不一致）でも無限ループしないように、visited セットで再入を遮断する」「再帰関数のラッパーを `func(...args)` として、内部実装 `funcInternal(...args, visited: ReadonlySet<string>)` を持ち、再帰時に visited を伝播する」 → 親方向（parentId 連鎖）を辿る再帰となるため、Phase 2.5-A の `deriveParentShape` で確立したラッパー + 内部実装パターンに揃えて適用
    - 開発ガイド §6.6.9「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」: 派生サービス実装規則の共有 → depth は形状ではなくツリー位置のみを派生するが、最上位フラグ判定 `placement.parentId === null` を Phase 2-B で確立した不変条件（同一錨内で `isTopLevel === (parentId === null)`）に揃える
    - 開発ガイド §6.6.8「排他検証は仕様の対象範囲を実装で広げない」: 仕様（現状.md §6.3）で「depth は派生値、地物自身に保持しない」「ルートは 0、子は親 +1」と明示されているため、コンテナ・移行期間ノードを別計算する等の仕様外拡張は禁止
- 変更内容（src 2 ファイル + tests 1 ファイル）:
    - `src/renderer/domain/services/HierarchyService.ts`:
        - 公開 API `deriveDepth(feature, allFeatures, time): number | undefined` を新設（visited セット初期化）
        - 内部実装 `deriveDepthInternal(..., visited)` で親方向連鎖の cycle guard を実施
        - 走査方向は子 → 親（`placement.parentId` を辿る片方向）。親側 `childIds` への双方向整合は `validateHierarchy` の責務として分離（本関数は前提としない）
        - 仕様規則:
            - 対象時刻に有効錨がない → undefined
            - `placement.parentId === null` → 0
            - 親が `allFeatures` に存在しない → undefined（壊れたデータ）
            - 親方向の連鎖中に circular 検出 → undefined（visited で再入遮断）
            - それ以外: `deriveDepth(親) + 1`
    - `src/renderer/domain/services/index.ts`: ドメインサービス層バレルエクスポートに `deriveDepth` を追加（`deriveParentShape` の直後）。Phase 2.5-B 以降の consumer が `@domain/services` 経由で取得可能に
    - `tests/domain/services/HierarchyService.test.ts`:
        - `describe('deriveDepth')` ブロックを新設（ルート 0 / 直接の子 1 / 独立最上位 0 / 多段階層 0→1→2→3 / 非アクティブ undefined / 親消失 undefined / 親が時刻外 undefined / 循環 undefined / 自己参照 undefined）
- 振る舞い変更: なし（新規 export のみ。consumer 未導入のため runtime 観測挙動は完全に不変）
- 検証:
    - `npm run check`: 0 errors / 0 warnings
    - `npm run build`: 通過
    - `npx vitest run`: 全テストファイル通過。`HierarchyService.test.ts` の `describe('deriveDepth')` ブロックがすべて pass。既存ケース（`getDescendants` 多段階層 / `getAncestors` 多段階層 / `deriveParentShape` 関連）も継続通過
- スコープ: src 1 ファイル + tests 1 ファイル / 約 +120 行。§6.10 上限「~15 ファイル / ~300 行」内
- 残置 / 後続:
    - **`getAncestors` / `getDescendants` の cycle guard 追補は別サブフェーズ**: `HierarchyService.ts` の `getAncestors`（親方向の while ループ）/ `getDescendants`（子方向の再帰呼び出し）はいずれも visited セットを持たず §6.4.14 規則を満たしていない（開発ガイド §6.4.14 本文で両関数とも cycle guard 対象として明記）。`feedback_subphase_scope_split.md`「1経路ずつ」方針に従い本サブフェーズの範囲外とする。Phase 2.5 以降で consumer 側からの不正データ遭遇が懸念される場合、別サブフェーズで両関数を visited 伝播パターンへ移行する
    - **Phase 2.5-B**: 描画。`FeatureRenderer` + `LabelRenderer` でコンテナを派生形状から描画。同レイヤー描画順は `deriveDepth` 昇順で「親→子」順
    - **Phase 5**: 表示フォーカス UI（グローバル depth スライダー / 個別フォーカス）。`deriveDepth` の主要 consumer
    - **Phase 6**: 自動配色（同 depth 兄弟基準）。`deriveDepth` を兄弟集合の決定に利用
- 教訓反映:
    - 参照した既存教訓: 開発ガイド §6.4.14「親子グラフを辿る再帰サービスは visited セットで cycle guard を入れる」/ §6.6.9「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」/ §6.6.8「排他検証は仕様の対象範囲を実装で広げない」
    - 既存教訓の追記: なし（本サブフェーズは Phase 2.5-A で確立したラッパー + visited 伝播パターンを親方向連鎖に素直に適用しただけで、新規教訓化に値する根本原因は発生しなかった）
- 手動動作確認の手順:
    - 本サブフェーズは consumer 未導入のため、runtime での観測挙動は完全に不変。検証は単体テストで固定済み
    - Phase 2.5-B（描画）着手時に、コンテナ feature を生成する直接構築フィクスチャを使った視認確認（depth 順描画）を E2E に追加予定

## 2026-05-25 階層モデル新仕様 Phase 2.5-A: `HierarchyService.deriveParentShape` の正式実装

Phase 2 サブフェーズ D 全体が完了した後、Phase 2.5（コンテナ UI 基盤）の最初のサブフェーズに着手。Phase 2-C-2 完了時点で残置していた既知制約「`HierarchyService.deriveParentShape` は子コンテナの孫リーフを再帰下降していない（§6.6.9 規則未充足）+ `polygonUnion + polygons[0]` で多 polygon 片落ち（§6.6.5 違反）+ visited cycle guard 無し（§6.4.14 違反）」を一括解消する。

- 着手前ゲート（開発ガイド §5.1.2 / 指示.md §共通ルール）:
    - 開発ガイド §6.6.9「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」: 派生サービスの実装規則として「子地物が集約地物（`shape === undefined`）であれば孫の和を再帰呼び出しで取り込む（多段階層）」「`polygonUnion + polygons[0]` のような片落としを避け、`polygonUnionAll` を使って多 polygon を保持する」「cycle guard を入れる」が明文化されている。本サブフェーズでこの 3 規則を deriveParentShape に同時適用
    - 開発ガイド §6.6.5「ブーリアン演算結果の複数片を落とさない」: 「`polygon-clipping` などのブーリアン演算は、単一ポリゴンではなく複数ポリゴン・複数リングを返す前提で扱う」 → 既存の `polygonUnion(accumulated, childRings) + polygons[0]` は最大領域だけに潰す片落とし。`polygonUnionAll` で MultiPolygon を保持する
    - 開発ガイド §6.4.14「親子グラフを辿る再帰サービスは visited セットで cycle guard を入れる」: 「`getAncestors` / `getDescendants` / `deriveParentShape` のような親子関係を辿る再帰サービスは、壊れたデータ（循環親子参照、parentId と childIds の不一致）でも無限ループしないように、visited セットで再入を遮断する」「再帰関数のラッパーを `func(...args)` として、内部実装 `funcInternal(...args, visited: ReadonlySet<string>)` を持ち、再帰時に visited を伝播する」 → このパターンを採用
- 経緯:
    - Phase 2-C-2 でドメイン層 consumer narrowing を完了した際、`HierarchyService.deriveParentShape` が `shape === undefined` の子コンテナを skip する暫定実装のままで、§6.6.9 で要求される多段階層対応が未充足だった。containers が実データに存在しない期間（Phase 2-C / Phase 2-D）は runtime 影響なしとして defer
    - Phase 2 サブフェーズ D 全体が完了し、Phase 2.5（Phase 4 でコンテナ作成が可能になる前の UI 基盤整備）の最初のサブフェーズとして本実装を着手
- 変更内容（src 1 ファイル + tests 1 ファイル / src 約 +75 -40 行 + tests +約 100 行）:
    - `src/renderer/domain/services/HierarchyService.ts`:
        - `import { polygonUnion }` を `import { polygonUnionAll }` に置換（旧 import は本実装でのみ使用していたため削除可）
        - `deriveParentShape(...)` を「公開 API（visited 初期化 + 末尾フラット化）」と「内部実装 `deriveParentPolygonsInternal(...args, visited)`（MultiPolygon 構造を保持したまま再帰）」の 2 層構成に再設計（§6.4.14 適用パターン）
        - 子 1 個分のリング解決を `resolveChildPolygonsForUnion` ヘルパーに切り出し。`childIds` 非空の集約地物 / 移行期間ノードは再帰下降を優先し、派生空かつ `shape` ありの移行期間ノードは `shape.rings` を fallback として返す（§6.6.9 規則の素直な適用）
        - 集約済み polygon 群は最終的に `polygonUnionAll` で結合（§6.6.5 適用）。トップレベル公開 API のみが MultiPolygon 結果を territory/hole の順でフラット化し、呼び出し側の evenodd 描画と整合させる
        - JSDoc を「現状は暫定実装」記述から「§6.6.9 / §6.6.5 / §6.4.14 を満たす正式実装」記述へ書き換え
    - `tests/domain/services/HierarchyService.test.ts` に `describe('deriveParentShape')` の追加テスト:
        - 子 1 件が territory を 2 本持つ場合に飛び地が片落ちしない（§6.6.2 / §6.6.5、`resolvePolygonAnchorPolygons` が単一 anchor 内の複数 territory を polygon 単位で分離することを固定）
        - 離れた子の和は MultiPolygon を保持する（territory 2 本、複数子の `polygonUnionAll` 結果）
        - 集約地物（子コンテナ）の孫リーフ形状を再帰下降で取り込む（多段階層）
        - 循環親子参照でも無限ループせず有限結果で早期 return する（cycle guard）
        - 移行期間ノードの子: 派生空なら自身の shape rings を fallback として採用
- 実装上の発見（自己レビュー1往復で検出）:
    - 当初実装では再帰中も `RingCoords[]`（フラット化済みリング列）を返していたが、孫が MultiPolygon を返した場合に親側の `polygonUnionAll` がそのフラット化リング列を「1 polygon の territory + holes」と誤解釈し、孫の 2 個目以降の territory が hole 扱いになる片落としが発生（多段階層テストで `expected 2 to be 1` で検出）
    - 修正: 内部関数 `deriveParentPolygonsInternal` の戻り値を `readonly RingCoords[][]`（polygon 配列）に変更し、フラット化はトップレベル公開 API のみで行う構造に再設計
    - これは §6.6.9 「`polygonUnionAll` を使って多 polygon を保持する」を再帰下降に展開する際の盲点。教訓化として §6.6.9 に追記（後述）
- 振る舞い変更:
    - 集約地物（`shape === undefined`）の子を持つコンテナの派生形状が、孫リーフの和まで正しく取り込まれるようになる（多段階層対応）
    - 離れた領土を持つ子の和が MultiPolygon として保持される（最大領域だけに潰されない）
    - 循環親子参照（不正データ）でも無限再帰しない
    - Phase 2-D 完了時点では実データに containers が存在しないため、上記いずれも `mapSceneEntries` 経由の描画・hitTest・wrapOffsets / `LabelRenderer` への runtime 影響は無い。Phase 4 でコンテナ生成 UseCase / Command を実装した時点で初めて runtime に効く
- 検証:
    - `npm run check`: 0 errors / 0 warnings
    - `npm run build`: 通過
    - `npx vitest run`: 全テストファイル通過。`HierarchyService.test.ts` の `describe('deriveParentShape')` ブロック（既存ケース + 新規 MultiPolygon 保持 / 多段階層 / cycle guard / 移行期間ノード fallback ケース）がすべて pass。`mapSceneEntriesIntegration.test.ts` の §6.6.9 射程テスト（派生空 fallback / 派生非空優先）も継続通過 — トップレベル fallback は呼び出し側 (`mapSceneEntries`) の責務として旧契約のまま据え置きであることを確認
- スコープ: src 1 ファイル + tests 1 ファイル / 約 +175 -40 行（うち実装ロジック +75 行、テスト追加 +100 行）。§6.10 上限「~15 ファイル / ~300 行」内
- 残置 / 後続:
    - **Phase 2-E（後続）**: depth 派生サービス追加（`HierarchyService` に depth 派生関数。Phase 2.5 / Phase 5 / Phase 6 で利用）
    - **Phase 2.5-B 以降**: 描画 / hitTest / PropertyPanel / parentTransferDialog のコンテナ対応
- 教訓反映:
    - 参照した既存教訓: 開発ガイド §6.6.9「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」/ §6.6.5「ブーリアン演算結果の複数片を落とさない」/ §6.4.14「親子グラフを辿る再帰サービスは visited セットで cycle guard を入れる」
    - 既存教訓の追記: §6.6.9 派生サービスの実装規則に「再帰時は polygon 構造（`RingCoords[][]`）を保持し、トップレベルの公開 API でのみ territory/hole の順でフラット化する。再帰中に flat 化すると、孫の MultiPolygon が親 polygon の hole として誤解釈される（polygon-clipping は受け取った rings 配列の 2 番目以降を hole 扱いする）」を追記。本サブフェーズ実装中に多段階層テストで検出した盲点を再発防止
- 手動動作確認の手順:
    - 本サブフェーズは Phase 4 でコンテナが作成可能になるまで実データへの runtime 影響を持たない。検証は単体テストの多段階層 / MultiPolygon 保持 / cycle guard / 移行期間ノード fallback で固定済み
    - Phase 2.5-B（描画）着手時に、コンテナ feature を生成する直接構築フィクスチャを使った視認確認を E2E に追加予定

## 2026-05-25 階層モデル新仕様 Phase 2 サブフェーズ D-7e: `<MapCanvas {features}>` props 経路の整理

Phase 2-D-6-1+2 で sceneEntries 共通中間表現を導入した後、`MapCanvas` 内に残っていた `features` props 利用は `selectionAnchor` derive の `features.find(...)` 1 箇所のみとなっていた。Phase 2-D-7d 完了時の残置項目として「sceneEntries に入らない時刻外の選択中地物が発生し得るかの挙動検証が必要」と分離していた最終 1 経路を本サブフェーズで撤去する。

- 着手前ゲート（開発ガイド §5.1.2 / 指示.md §共通ルール）:
    - 開発ガイド §6.1.1「状態間の依存関係の更新漏れ」: 「時間が変わったら、選択中の地物がその時間に存在するか検証し、存在しなければ選択解除」 → 本サブフェーズでは選択解除自体は行わず（自動クリアは別経路の責務）、`selectionAnchor` derive が両経路（旧 `features.find()` / 新 `sceneEntries.find()`）で同じ undefined/null を返すことを検証してから置換
    - 開発ガイド §6.1.2「非表示要素の選択・操作の混入」: 「ユーザー操作の対象は『現在UI上に表示されている要素』に限定する。データソース全体を直接検索せず、Svelteのリアクティブ状態（`$derived` 等）が管理する表示状態を基準にする」 → 本サブフェーズの趣旨そのもの。`features.find()` 直接走査を sceneEntries（表示文脈の中間表現）経由に置換
    - 開発ガイド §6.6.9「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」: Phase 2-D-6-1+2 で sceneEntries に集約した shape 解決経路を選択コンテキストにも適用する
- 挙動検証（着手前に App.svelte の selectedFeatureId 管理経路を grep して確認）:
    - **時刻外の選択中地物**: `selectedFeatureId` の自動クリアは時刻変化に追従していない（App.svelte の `selectedFeatureId = ...` 設定箇所を全 grep して確認）。つまり「selectedFeatureId が時刻外の地物を指す」状態は発生し得る
    - **旧経路の挙動**: `features.find((f) => f.id === selectionFeatureId).getActiveAnchor(currentTime)` → 時刻外なら `getActiveAnchor` が undefined を返す → `selectionAnchor = undefined`
    - **新経路の挙動**: `sceneEntries.find(...)` → 時刻外で active anchor がない地物は sceneEntries に含まれない → `find` が undefined を返す → `selectionAnchor = null`（`?? null` で正規化）。消費側は `selectedFeatureId && selectionAnchor()` / `selectionAnchor && selectionAnchor.shape` の falsy 判定のため undefined / null の差は吸収される
    - **コンテナの選択中地物**: 現状の UI ではコンテナを作る経路（add tool）が Phase 4 まで存在しないため発生し得ない dead path。選択経路の filter による dead path 成立ではない点に注意 — hitTest と DOM `data-feature-id` は sceneEntries / 描画経由なのでコンテナを返さないが、Sidebar 地物一覧（`buildFeatureSearchItems`）は `anchor.shape` でフィルタしておらず、コンテナが存在すれば active なものは一覧に出てクリック可能になる。dead path の根拠は「作成側の不在」であり「選択側 filter」ではない。Phase 4 でコンテナ作成が可能になった瞬間、Sidebar 経由でコンテナ選択 → `selectionAnchor = null` → ハンドル / Toolbar 非表示、という新挙動が固定される。妥当な振る舞いだが Phase 4 着手時に「Sidebar の `buildFeatureSearchItems` に shape フィルタを追加するか / PropertyPanel 側でコンテナ専用 UI を持つか」を再検討する必要がある
    - **selectionAnchor 使用先の確認**: `vertexHandleEntries` の `selectedFeatureId && selectionAnchor()` 条件はコンテナ shape なしでハンドル意味なしのため挙動差なし。`MapCanvasHud` の EditToolbar 表示条件は Phase 2-C-3 で追加した `selectionAnchor.shape` ガードがあるため anchor が null でも shape なしでも同じく非表示
    - 結論: 全使用先で挙動同一 → 置換可能
- 変更内容（src 2 ファイル / 約 +20 -10 行）:
    - `presentation/components/MapCanvas.svelte`: `import type { Feature }` 撤去 / `features` props（型・既定値・パラメータ）3 箇所撤去 / `selectionAnchor` derive を `sceneEntries.find((e) => e.feature.id === selectionFeatureId)?.anchor ?? null` に置換。JSDoc に「sceneEntries が空 / コンテナ / 時刻外 のいずれでも旧経路と挙動同一」の検証結果を明記
    - `App.svelte`: `<MapCanvas>` への `{features}` 渡しを撤去（line 2148 削除）
- 振る舞い変更: なし。旧経路の戻り値型は undefined / 新経路は `?? null` で null に正規化。消費側はすべて falsy 判定（`selectedFeatureId && selectionAnchor()` / `selectionAnchor && selectionAnchor.shape`）で吸収するため挙動同一（新コードの方が型が締まる）。コンテナの選択は現状の UI 経路（add tool）で発生し得ず dead path（作成側不在）
- 検証:
    - `npm run check`: 0 errors / 0 warnings
    - `npm run build`: 通過
    - `npx vitest run`: 旧経路と新経路の戻り値型が undefined / null で異なるのみで消費側の falsy 判定により挙動同一のため新規テスト追加不要。既存テストすべて通過
- スコープ: src 2 ファイル / 約 +20 -10 行。§6.10 上限「~15 ファイル / ~300 行」内
- 残置 / 後続:
    - **Phase 2-E（後続）**: depth 派生サービス追加（`HierarchyService` に depth 派生関数。Phase 2.5 / Phase 5 / Phase 6 で利用）
    - **Phase 2.5-A 以降**: コンテナ UI 基盤（描画 / hitTest / PropertyPanel / parentTransferDialog 等）
- 教訓反映:
    - 参照した既存教訓: 開発ガイド §6.1.1「状態間の依存関係の更新漏れ」/ §6.1.2「非表示要素の選択・操作の混入」/ §6.6.9「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」
    - 追加した新規教訓: なし（本サブフェーズは sceneEntries 共通中間表現の射程をデータドリブンに広げた経路整理であり、新規教訓化に値する根本原因は発生しなかった）
    - 既存教訓の追記: §6.6.9 本文に「MapCanvas の選択コンテキストアンカー取得（`selectionAnchor` derive）も同じ shape 解決経路を共有する 4 経路目」と「選択コンテキストアンカーは sceneEntries を直接 `find` するだけで shape 解決済みのアンカーが得られる」を追記。将来「選択コンテキストアンカーは別経路で取ってもよさそう」という誤った判断を防ぐ
- 手動動作確認の手順（外部から観測できる振る舞い不変の確認）:
    - 操作1: 地物を選択 → EditToolbar が表示されることを確認（リーフ地物の場合）
    - 操作2: 地物を選択した状態で時刻スライダーを動かし、選択中地物の active 期間外に移動 → EditToolbar 非表示、頂点ハンドル非表示（旧経路と同じ振る舞い）
    - 操作3: 時刻スライダーを戻して選択中地物の active 期間に戻す → EditToolbar / 頂点ハンドル復活
    - 操作4: 別地物を選択 → 旧地物の表示が消え新地物の表示に切り替わる
    - 期待結果: 全操作で Phase 2-D-7d までの挙動と差異なし

## 2026-05-25 階層モデル新仕様 Phase 2 サブフェーズ D-7d: コード内コメントの Phase 参照を棚卸し

Phase 2-D-7c-2b で `Layer` 概念のコードからの完全消滅が完成した後、コード内 JSDoc / コメントに残っていた `Phase 2-D-X` / `Phase 2-C-X` / `Phase 2-B` 等の移行記録参照を棚卸しする。`<MapCanvas {features}>` props 経路の整理は挙動検証（時刻外選択中地物の存在確認）を伴うため別サブフェーズ（後続）に分離した。

- 着手前ゲート（開発ガイド §5.1.2 / 指示.md §共通ルール）:
    - 開発ガイド §6.0.1 検出観点1「同じ説明表現を別箇所で再利用している場合（用語集、マニュアル備考、他章のサマリ等）も grep で検出して同期する」: 本サブフェーズで Phase 番号参照を grep し（`Phase 2-[A-Z](-[0-9a-z]+)*` パターン）、12 ファイル全てを書き換え対象として確認。書き換え後に再 grep で残存ゼロを確認
    - 現状.md §6.10 Phase 2-D-7c-2b 残置項目「現在の挙動説明として有用なもののみ残し、移行記録に過ぎないものは削除する。残す場合も『Phase X 後の挙動』ではなく『現状の挙動』を主語にする」: 完了済み Phase 番号は全削除、未着手フェーズの計画ポインタ（Phase 2.5-A / 2.5-C / 3 / 5 / 6）は「現状の暫定実装の根拠＋今後の再設計予定」として有用なため残置
    - feedback_subphase_scope_split.md「サブフェーズは1経路ずつ。型変更・consumer・UI・validation を一括で詰め込まない」: `<MapCanvas {features}>` 撤去は別経路（経路変更を含む）のため本サブフェーズに混在させない
- 経緯:
    - Phase 2-D-7c-2b で `Layer` エンティティ・`World.layers`・JSON 永続化 `layers` を最終削除し、Phase 2-D 全体の `Layer` 概念消滅が完成
    - その時点で src/ 配下に残っていた Phase 番号参照は 12 ファイル。多くは「Phase 2-D-X で○○を撤去した」「Phase 2-C で導入」のような移行記録で、Phase 2-D 完全終了後は stale 化リスクが高い
- 変更内容（src 12 ファイル / コメント本文のみ書き換え。ロジック・型変更ゼロ）:
    - `presentation/app/edgeSlideConstraintCollectors.ts`: 3 関数の JSDoc から「Phase 2-D-1 で…」「Phase 2-D-6-3a で同一レイヤー絞り込みを撤去」「Phase 2-D-6-3a 追補」を削除し、リーフ判定運用の現状説明に置換
    - `domain/services/AutoColorService.ts`: ヘッダー JSDoc から「Phase 2-D-5 以前は『同一レイヤー内』が単位だったが、Phase 2-D-5 で地図全体 1 回算出へ移行」を削除し、「現状は地図全体を 1 単位として算出する」表現に置換（Phase 6 計画ポインタは残置）
    - `domain/value-objects/FeatureAnchor.ts`: `requireLeafShape` の JSDoc から「Phase 2-C で導入した」を削除し、「リーフ専用パスでの誤呼び出しを runtime で検知するための防御」のみに圧縮
    - `presentation/components/FeatureRenderer.svelte`: 自動配色 derive の JSDoc から「Phase 2-D-6-1+2: 描画・ヒットテスト・…sceneEntries へ統一」を削除し、現状の責務説明に置換
    - `App.svelte`: sceneEntries 導出 JSDoc から「Phase 2-D-6-1+2: 各経路で features を個別フィルタすると…」を削除し、現状の責務説明に置換
    - `domain/services/HierarchyService.ts`: ヘッダーから「※ レイヤー（旧モデル）の表示制御 UseCase は Phase 2-D-3 で撤去済み」を削除し、「旧モデルの『グローバル `Layer` 概念』は廃止されており、階層はツリー位置から派生する depth で表現する（詳細は現状.md §6）」に圧縮 / `deriveParentShape` の暫定実装説明から「Phase 2-C 時点の暫定実装 — Phase 2.5-A で本格再実装」を削除し「**現状は暫定実装**」に変更（Phase 2.5-A の計画ポインタは残置）
    - `infrastructure/persistence/jsonSerializerMigration.ts`: `placement.layerId` 拒否ロジックの近接コメントから「Phase 2-D-6-3c で `placement.layerId` を完全撤去したため、本フィールドを含むファイルは旧モデルのマーカーとして拒否する」を「`placement.layerId` フィールドを含むファイルは旧モデルのマーカーとして拒否する」に圧縮
    - `infrastructure/persistence/jsonSerializerValidation.ts`: `validateShapePresence` の JSDoc から「現状.md §6.10 Phase 2-C-4 のスコープ」を削除（Phase 3 の計画ポインタは残置）
    - `infrastructure/rendering/hitTestUtils.ts`: `hitTest` の JSDoc から「Phase 2-D-6-1+2: 描画・ヒットテスト・…sceneEntries を参照するように統一した」を削除し、現状責務の説明に置換（Phase 2.5-C の計画ポインタは残置）
    - `infrastructure/rendering/vertexSelectionContext.ts`: `buildSceneVertexOwnerMap` の JSDoc から「Phase 2-D-6-1+2: …」「旧モデルではレイヤー可視性で features を絞っていたが、sceneEntries 化により…責務になったため `buildSceneVertexOwnerMap` へ改名した」を削除し、現状責務の説明に圧縮
    - `presentation/components/mapSceneEntries.ts`: `collectMapSceneEntries` の JSDoc から「Phase 2-C-3 で確立した除外規則」「旧モデルのレイヤー可視性によるフィルタは持たない」を「レイヤー可視性によるフィルタは持たない」に圧縮（Phase 5 / 6 の計画ポインタは残置）
    - `domain/services/ConflictDetectionService.ts`: ヘッダー JSDoc から「Phase 2-D-1（2026-05-03）で `layerId` グルーピングを撤去し全マップ排他へ移行した」と「同一レイヤー」言及を削除（旧モデル時点の経緯を排除）
- 振る舞い変更: なし（コメント本文の書き換えのみ、ロジック・型・公開 API 変更ゼロ）
- 検証:
    - `npm run check`: 0 errors / 0 warnings
    - `npm run build`: 通過
    - `npx vitest run`: 全テスト通過
    - 再 grep（`Phase 2-[A-Z](-[0-9a-z]+)*` パターン）: src/ 配下に該当ヒットゼロ
    - 残存 `Phase \d` 検索結果: Phase 2.5-A（HierarchyService 再実装計画）/ Phase 2.5-C（hitTest tie-break 再設計計画）/ Phase 3（validateShapePresence 拡張計画）/ Phase 5・Phase 6（深度フォーカス・自動配色再設計計画）のみ。いずれも未着手フェーズの計画ポインタとして残置
- スコープ: src 12 ファイル / コメント本文のみ書き換え（おおよそ ±60 行、ロジック行数変化ゼロ）。§6.10 上限「~15 ファイル / ~300 行」内
- 残置 / 後続サブフェーズへ送る:
    - **Phase 2-D-7e（後続）**: `<MapCanvas {features}>` props 経路の整理。`MapCanvas` 内の `features` 利用は `selectionAnchor` derive の `features.find(...)` 1 箇所のみで、`sceneEntries.find(e => e.feature.id === selectionFeatureId)?.anchor` への置換で撤去可能。ただし「sceneEntries に入らない時刻外の選択中地物」が発生し得るかの挙動検証が必要なため別サブフェーズに分離
    - **Phase 2-E（後続）**: depth 派生サービス追加（`HierarchyService` に depth 派生関数を追加。Phase 2.5 / Phase 5 / Phase 6 で利用）
- 教訓反映:
    - 参照した既存教訓: 開発ガイド §6.0.1 検出観点1「同じ説明表現を別箇所で再利用している場合も grep で検出して同期する」
    - 追加した新規教訓: なし（本サブフェーズは仕様判断や設計変更を伴わないコメント整理であり、新規教訓化に値する根本原因は発生しなかった）

## 2026-05-24 階層モデル新仕様 Phase 2 サブフェーズ D-7c-2b: `Layer` エンティティ本体 / `World.layers` / JSON 永続化 `layers` の最終削除

Phase 2-D-7c-2a で App / SaveLoadUseCase 層の Layer 残骸を撤去した後、本サブフェーズで Domain エンティティ + JSON 永続化を最終削除する。Phase 2-D 全体の終端（`Layer` 概念のコードからの完全消滅地点）。

- 着手前ゲート（開発ガイド §5.1.2 / 指示.md §共通ルール）:
    - 開発ガイド §6.4.15「永続化 shim は read / write / validate / round-trip test を同じ変更単位で同期する」: 「保存形式の移行中に一時 shim を入れる場合、`serialize` / `deserialize` / validation / migration / round-trip test を同じ変更単位で揃える」「『保存できるが次に開けない』状態は許容しない」 → 本サブフェーズで `serializeLayer` / `deserializeLayer` / `normalizeLayer` / `JsonWorld.layers` / `JsonLayer` を同時撤去し、互換破棄テスト fixture を「再保存ファイルに `world.layers` フィールドが残らない」アサーションへ拡張
    - 開発ガイド §6.0.1 検出観点1「関数シグネチャ撤去・引数撤去では tests/ にも grep を広げる」: `World` コンストラクタ第 4 引数 `layers` 撤去で tests/ の `new World(...)` 全呼び出しを grep し、漏れた 2 ファイル（`SaveLoadUseCase.test.ts:330` / `QueryServices.test.ts:104`）を sweep（初回コミット案では失敗。テスト実行で `customPalettes` 読み取り例外として検出 → src/ の引数ずれに気付く）
    - 開発ガイド §6.3.13「optional フィールド導入は全層を1サブフェーズに詰めない」: 本サブフェーズは Domain entity + JSON 永続化（types / transforms / migration / Repository）の 2 層のみに修正を限定。新規 UI / 描画は一切触らない
- 経緯:
    - Phase 2-D-3 で `ManageLayersUseCase` 撤去時、`Layer` エンティティ / `World.layers` / JSON `JsonLayer` は描画グルーピング・自動配色用に残置。Phase 2-D-5/6 で描画・hitTest・選択経路から `Layer` 参照が消滅。Phase 2-D-6-3a/b/c で `placement.layerId` 撤去が完成
    - Phase 2-D-7c-2a で App.svelte / SaveLoadUseCase の in-memory Layer 残骸を撤去。`assembleWorld()` は常に `layers: []` を出力、`distributeWorld()` は `world.layers` を読み捨てる中間状態に到達
    - 残存していたのは Domain entity と JSON 永続化のみ。本サブフェーズで最終撤去
- 変更内容（src 8 ファイル + tests 8 ファイル）:
    - **Domain entity 撤去**:
        - `src/renderer/domain/entities/Layer.ts`: ファイル削除
        - `src/renderer/domain/entities/index.ts`: `Layer` export 削除
        - `src/renderer/domain/entities/World.ts`: `import type { Layer }` / `readonly layers: readonly Layer[]` フィールド / コンストラクタ第 4 引数 / `getSortedLayers()` メソッド / `createEmpty()` 内の `[]` 第 4 引数を撤去
    - **JSON 永続化 layers 撤去**（§6.4.15 適用: serialize / deserialize / migration を同じ変更単位で同期）:
        - `jsonSerializerTypes.ts`: `JsonLayer` interface 撤去 / `JsonWorld.layers` フィールド撤去 / `export type { JsonLayer }` 撤去
        - `jsonSerializerTransforms.ts`: `import { Layer }` / `serializeLayer` / `deserializeLayer` 関数撤去。`serializeWorldToJson` から `layers: world.layers.map(serializeLayer)` 撤去 / `deserializeJsonWorld` から `(json.layers ?? []).map(deserializeLayer)` 引数撤去
        - `jsonSerializerMigration.ts`: `JsonLayer` import 撤去 / `normalizeLayer` / `createDefaultLayer` 関数撤去 / `migrateJsonWorld` 本体から `requireArray(raw, 'layers')` 経由の正規化と「レイヤー情報が空の旧形式」warning 撤去 / 返却 `json` 構造から `layers` フィールド撤去
        - `JSONWorldRepository.ts`: `withBaseMapSvgText()` の `new WorldEntity(...)` 引数から `world.layers` 撤去
    - **Application 層追随**:
        - `src/renderer/application/SaveLoadUseCase.ts`: `assembleWorld()` の `new World(...)` から `[]` 第 4 引数撤去（World コンストラクタ引数撤去に追随）
    - **コメント整理**（Phase 2-D 全体終了に伴い「Phase 2-D-6-1+2 で Layer.visible フィルタを撤去」等の移行記録コメントを「旧モデルではレイヤー可視性で features を絞っていたが…」のような現在挙動主語に書き換え）:
        - `presentation/components/mapSceneEntries.ts` / `infrastructure/rendering/vertexSelectionContext.ts` / `tests/presentation/components/mapSceneEntries.test.ts`
    - **テスト調整**:
        - `tests/domain/entities/Layer.test.ts`: ファイル削除
        - `tests/domain/entities/World.test.ts`: `import { Layer }` 撤去 / 「空の layers 配列」テスト撤去 / `describe('getSortedLayers')` 全体撤去
        - `tests/application/SaveLoadUseCase.test.ts`: `Layer` import / `layers` 配列構築 / `World` コンストラクタの `layers` 引数撤去 / 「読み込んだ後の assembleWorld は layers を空配列で再構築する」テスト撤去（`World.layers` 自体がなくなるため）/ assembleWorld describe の「layersは空」アサーション撤去
        - `tests/application/queries/QueryServices.test.ts`: `new World(...)` の `[]` 引数撤去
        - `tests/infrastructure/persistence/JSONSerializer.test.ts`: `Layer` import 撤去 / 全 helper の `layers` 配列構築 + `World` 引数撤去 / `createValidJsonWorld` の `layers` フィールド撤去 / 「レイヤーのdescriptionとopacityをシリアライズできる」「レイヤーがドメインオブジェクトに復元される」テスト撤去 / 全 `layers` 関連アサーション (`parsed.layers` / `restored.layers`) 撤去 / `data 整合性検証` describe 内の raw JSON fixture から `layers: [...]` 削除 / `{"layers":[]}` を `{}` に置換
        - `tests/infrastructure/persistence/JSONWorldRepository.test.ts`: `Layer` import 撤去 / helper の `layers` 配列構築撤去 / `world.layers` アサーション撤去
        - `tests/infrastructure/persistence/jsonSerializerValidation.test.ts`: `createWorld` helper の `layers` 配列撤去 / `baseV1Payload` の `layers` 撤去 / 互換破棄 fixture から `layers: [...]` 撤去 / 互換破棄テストに `expect('layers' in resaved).toBe(false)` アサーション追加（Phase 2-D-7c-2b で完全撤去を固定）/ describe 見出しを「互換破棄の検証 (Phase 2-D-6-3c / 2-D-7c-2b)」へ更新
- 振る舞い変更:
    - **互換性**: 旧 .gimoza（`placement.layerId` 含む形式）は Phase 2-D-6-3c で互換破棄完了済み。新形式 .gimoza では `layers` フィールド自体が出力されなくなる
    - 中間形式（Phase 2-D-7c-2a 形式: `layers: []` を含むが `placement.layerId` を含まない）: migration が `requireArray(raw, 'layers')` を呼ばなくなるため、`layers` フィールドの有無に関わらず読み込み可能（中身は無視）
    - 永続化 round-trip: 新形式 → 再保存 → 再読み込みで `layers` フィールドが消えることを `jsonSerializerValidation.test.ts` の互換破棄テストで固定
- 検証:
    - `npm run check`: 0 errors / 0 warnings
    - `npm run build`: 通過
    - `npx vitest run`: 全テスト通過
    - レビュー追補: 初回コミット案では tests/ の `new World(...)` 全呼び出し sweep が 2 ファイル分漏れ、`distributeWorld()` 経路で `cloneSettings(world.metadata.settings)` が `customPalettes` 読み取り例外を発生させて失敗 → §6.0.1 検出観点1「関数シグネチャ撤去では tests/ にも grep を広げる」を適用して `grep "new World\("` で全 8 ファイルを sweep し漏れ 0 件確認
- スコープ: src 8 ファイル（実装）+ src 2 ファイル（コメント整理: `mapSceneEntries.ts` / `vertexSelectionContext.ts`）+ tests 8 ファイル = 計 18 ファイル / +50 -358 行（コメント整理 2 ファイルを含めると 18 / 約 ±300 行強）。§6.10 上限「~15 ファイル / ~300 行」のうち、ファイル数 18（3 超過）/ 行数 354 は超過するが、内訳は Layer.ts 全消滅 -48 / Layer.test.ts 全消滅 -70 / JSONSerializer.test.ts の機械的 layers リテラル削除 -約 60 / その他は 1-30 行の機械的引数撤去 + コメント整理 5 行。実装ロジックの構造変更は伴わない（Phase 2-D-6-3c の前例同型）。コメント整理 2 ファイル（`mapSceneEntries.ts:50-54` / `vertexSelectionContext.ts:13-17` および `tests/presentation/components/mapSceneEntries.test.ts:62-66`）は本来 Phase 2-D-7c-2c〜「コメント内 phase 参照棚卸し」のスコープだが、`Layer.visible` フィルタ撤去言及が `Layer` エンティティ削除と同タイミングで stale 化するため、本サブフェーズに混在させた（`feedback_minor_comment_fix.md`「軽微な意図補足は Claude 自身がコメント追記で解決」適用範囲内）
- 残置 / 後続サブフェーズへ送る:
    - **Phase 2-D-7c-2c〜**: `<MapCanvas {features}>` props 経路の整理（`selectionAnchor` derive を `sceneEntries.find(...)` に置換できれば features props 撤去可能、ただし時刻外選択中地物の存在確認が必要）/ コード内 JSDoc コメントの phase 参照棚卸し（hitTestUtils / featureRendererUtils 等の `Phase 2-D-X` 移行記録コメントを現状挙動主語に書き換え）
    - Phase 2-D 全体としては本サブフェーズで `Layer` 概念のコードからの完全消滅が完成。`renderer` ディレクトリ全文 grep で残る `Layer` 参照はコメント・ドキュメント中の歴史記述・dev guide §6.6.6 / 要件定義書 §2.4.4.3 のような新モデル移行待ちの教訓・仕様文言のみ
- 教訓反映:
    - 開発ガイド §6.4.15「永続化 shim は read / write / validate / round-trip test を同じ変更単位で同期する」を本サブフェーズで適用。`serializeLayer` / `deserializeLayer` / `normalizeLayer` / `JsonWorld.layers` を全て同じ変更単位で撤去 + 「再保存ファイルに layers が残らない」アサーションを round-trip test に追加
    - 開発ガイド §6.0.1 検出観点1「関数シグネチャ撤去では tests/ にも grep を広げる」を本サブフェーズで適用。`World` コンストラクタ引数撤去では tests/ の `new World(...)` を全件 grep して 8 ファイル全て sweep（初回は 2 ファイル分漏れがテスト実行で発覚）
    - 開発ガイド §6.3.13「optional フィールド導入は全層を1サブフェーズに詰めない」を本サブフェーズで適用。Domain entity + 永続化（types / transforms / migration / Repository）+ 関連テスト sweep の 2 層のみに修正を限定し、UI / 描画 / hitTest / 自動配色は一切触らない（既に Phase 2-D-5/6 で撤去済み）
    - 新規教訓の追加は不要: 既存教訓（§6.4.15 / §6.0.1 / §6.3.13 / §6.10）で全課題を説明できる

---

## 2026-05-24 階層モデル新仕様 Phase 2 サブフェーズ D-7c-2a: `App.svelte` の Layer 残骸撤去 + `SaveLoadUseCase` の `getLayers/setLayers/layers` 暫定 API 撤去

現状.md §6.10 Phase 2-D-7c-2（未着手）から in-memory ドメイン経路の Layer 残骸を切り出した最初のサブフェーズ。`Layer` エンティティ本体 / `World.layers` フィールド / JSON 永続化（types / transforms / migration / Repository）の `layers` 撤去は後続の Phase 2-D-7c-2b で実施する（依存ファイル数 + テスト数で §6.10 上限を超えるため `feedback_subphase_scope_split.md`「サブフェーズは1経路ずつ」に従って分割）。

- 着手前ゲート（開発ガイド §5.1.2 / 指示.md §共通ルール）:
    - 開発ガイド §6.3.13「optional フィールド導入は全層を1サブフェーズに詰めない」: 「1サブフェーズあたりの差分は ~15 ファイル / ~300 行を上限に分割する」「『触る経路』を最大2層で宣言する。宣言外の経路に修正が及びそうになったら別タスクへ切り出す」 → 本サブフェーズは Presentation（`App.svelte`）+ Application（`SaveLoadUseCase`）の 2 層のみに修正を限定。Domain `Layer` / `World.layers` / Infrastructure 永続化は touch しない
    - 開発ガイド §6.4.15「永続化 shim は read / write / validate / round-trip test を同じ変更単位で同期する」: 本サブフェーズは JSON 永続化を一切触らないため `serialize/deserialize/validate/migration` の挙動は完全不変。`World.layers` は引き続き空配列でラウンドトリップ可能（`assembleWorld` が `[]` 固定値を渡し、`distributeWorld` は `world.layers` を読み捨てる）
    - memory/feedback_subphase_scope_split.md「サブフェーズは1経路ずつ」: 型変更・consumer・UI を一括で詰めない → in-memory consumer のみを撤去し、entity 削除と永続化撤去は次サブフェーズへ
- 経緯:
    - Phase 2-D-3 で `ManageLayersUseCase` 撤去時、`SaveLoadUseCase` に暫定的に `getLayers/setLayers/layers` を導入して `World.layers` の round-trip を維持していた（Phase 2-D-7 で `World.layers` 廃止と同時に撤去する設計）
    - Phase 2-D-5 で `ProjectSettingsDialog` のレイヤー編集セクション撤去 + 自動配色レイヤー単位グルーピング撤去後、Phase 2-D-6-1+2 で sceneEntries 共通中間表現の導入により `Layer` 参照は描画・hitTest・選択経路から消滅
    - Phase 2-D-6-3a/b/c で `placement.layerId` 撤去が完成。Phase 2-D-7c-1 で旧形式 migration の dead code を sweep
    - 残存していたのは: `App.svelte` の `let layers = $state<Layer[]>([])` 状態 + `refreshLayerData()` + `getAddTargetLayer()` ガード残骸 + `new Layer('default', 'レイヤー1', 0)` 初期化（init + newProject の 2 箇所）+ `SaveLoadUseCase.getLayers/setLayers/layers` 暫定 API。これらは Phase 2-D-7c-2a の対象
- 変更内容（src 2 ファイル + tests 1 ファイル）:
    - `src/renderer/App.svelte`:
        - `import { Layer } from '@domain/entities/Layer';` を撤去
        - `let layers = $state<readonly Layer[]>([]);` 状態を撤去
        - `refreshLayerData()` 関数を撤去（呼び出し側 4 箇所: 初期化 / `world:loaded` / Ctrl+Z / Ctrl+Y / MenuBar `onUndo` / `onRedo` / `newProject` も同時に撤去）
        - `getAddTargetLayer()` 関数を撤去（呼び出し側 2 箇所: `toolStore.createToolStore` コールバック先頭ガード / 点ツール即追加経路の `if (targetLayer)` ガードも同時に撤去）。レイヤー 0 個での追加抑制は Phase 2-D-5 以降「default 1 レイヤー固定」前提で実質 dead semantics だった
        - 初期化の `if (saveLoad.getLayers().length === 0) { saveLoad.setLayers([new Layer('default', 'レイヤー1', 0)]); }` を撤去
        - `newProject()` 内の `saveLoad.setLayers([new Layer('default', 'レイヤー1', 0)]);` を撤去
    - `src/renderer/application/SaveLoadUseCase.ts`:
        - `import type { Layer }` を撤去
        - `private layers: readonly Layer[] = []` フィールドを撤去
        - `getLayers()` / `setLayers()` メソッドを撤去
        - `resetProjectState()` の `this.layers = []` リセットを撤去
        - `assembleWorld()` の World コンストラクタ第 4 引数を `this.layers` から `[]` リテラルに置換
        - `distributeWorld(world)` の `this.layers = world.layers` 反映を撤去
    - `tests/application/SaveLoadUseCase.test.ts`:
        - 「読み込んだレイヤーがロード後のassembleWorldにラウンドトリップする」テストを「読み込んだ後の assembleWorld は layers を空配列で再構築する」に再構成。`SaveLoadUseCase` から `setLayers/getLayers` 暫定 API が消えたため、`createTestWorld()` の `layers: [l1, l2]` が `distributeWorld` を経由しても `assembleWorld` 側には伝わらず常に `[]` を返すことを固定（Phase 2-D-7c-2b で `World.layers` 自体を撤去するまでの中間挙動）
- 振る舞い変更:
    - レイヤー 0 個での地物追加抑制（`getAddTargetLayer` ガード）が消滅。Phase 2-D-5 以降は default 1 レイヤー固定で実質 runtime 不変
    - `SaveLoadUseCase` がレイヤー一覧を保持しなくなる。`assembleWorld()` は常に `layers: []` を出力。`World.layers` は引き続き空配列で .gimoza にラウンドトリップする（Phase 2-D-7c-2b で `World.layers` 自体を撤去予定）
    - **互換性**: 旧 .gimoza は Phase 2-D-6-3c の `placement.layerId` 拒否で既に互換破棄完了。新規 .gimoza は本サブフェーズ前後で `layers: []` のまま不変
- 検証:
    - `npm run check`: 0 errors / 0 warnings
    - `npm run build`: 通過
    - `npx vitest run`: 全テスト通過。`SaveLoadUseCase.test.ts` の「読み込んだレイヤーがロード後の assembleWorld にラウンドトリップする」を「読み込んだ後の assembleWorld は layers を空配列で再構築する」へ再構成のみ（暫定 API 撤去で常に空配列を返すことを直接固定）
- スコープ: src 2 ファイル + tests 1 ファイル / 約 +20 -50 行（実装ロジック修正 + テスト再構成のみ）。§6.10 上限「~15 ファイル / ~300 行」の範囲内
- 残置 / 後続サブフェーズへ送る:
    - **Phase 2-D-7c-2b**: `Layer` エンティティ本体（`src/renderer/domain/entities/Layer.ts`）削除 + `World.layers` フィールド削除 + `domain/entities/index.ts` の Layer export 削除 + JSON 永続化 layers 撤去（`jsonSerializerTypes.ts` の `JsonLayer` / `JsonWorld.layers` / `jsonSerializerTransforms.ts` の `serializeLayer` / `deserializeLayer` / `jsonSerializerMigration.ts` の `normalizeLayer` / `createDefaultLayer` / `JSONWorldRepository.ts` の `withBaseMapSvgText` 引数 / `SaveLoadUseCase.assembleWorld` の World コンストラクタ引数）+ 関連テスト調整（`Layer.test.ts` 削除 / `World.test.ts` / `SaveLoadUseCase.test.ts` / `JSONSerializer.test.ts` / `JSONWorldRepository.test.ts` / `jsonSerializerValidation.test.ts` の `layers: [...]` リテラル + Layer import + World コンストラクタ引数の sweep）
    - **Phase 2-D-7c-2c〜**: `<MapCanvas {features}>` props 経路の整理 / コメント内 phase 参照棚卸し
- 教訓反映:
    - 開発ガイド §6.3.13「optional フィールド導入は全層を1サブフェーズに詰めない」を本サブフェーズで適用。Phase 2-D-7c-2 全体を 1 サブフェーズで実施すると src 9 + tests 6 ≈ 15 ファイル / ~500 行で上限超過すると見積もり、in-memory 経路（本サブフェーズ）と Domain entity + 永続化（Phase 2-D-7c-2b）に分割
    - 開発ガイド §6.4.15「永続化 shim は read / write / validate / round-trip test を同じ変更単位で同期する」を本サブフェーズで適用。永続化を一切触らないことで shim の対称性破壊を回避（旧 .gimoza の `placement.layerId` 拒否は Phase 2-D-6-3c で固定済み、新形式の `layers: []` ラウンドトリップは本サブフェーズで変化なし）
    - memory/feedback_subphase_scope_split.md「サブフェーズは1経路ずつ」を本サブフェーズで適用。`Layer` エンティティ削除と永続化撤去は次サブフェーズに切り出した
    - 新規教訓の追加は不要: 既存教訓（§6.3.13 / §6.4.15 / §6.10 / `feedback_subphase_scope_split`）で全課題を説明できる

---

## 2026-05-23 階層モデル新仕様 Phase 2 サブフェーズ D-7c-1: 旧形式 migration ロジック dead code の sweep + `resolveVersion` の `_ctx` 引数撤去

現状.md §6.10 Phase 2-D-7c（未着手）のチェックリストから「`resolveVersion(raw, _ctx)` の dead `_ctx` 引数撤去」と「旧形式 migration ロジック dead code の sweep」を統合した1サブフェーズ。Phase 2-D-6-3c で version 拒否（0.x 系 + version 欠落 + `placement.layerId` 含む形式）が完成した結果、`jsonSerializerMigration.ts` 内の旧形式変換コードが到達不能になっていた箇所を撤去する。

- 着手前ゲート（開発ガイド §5.1.2 / 指示.md §共通ルール）:
    - 開発ガイド §6.4.15「永続化 shim は read / write / validate / round-trip test を同じ変更単位で同期する」: 「シリアライズ・デシリアライズ・validation のいずれか片側だけを変えると、ユーザーの目から見ると『保存は成功した、でも次に開けない』という壊れたファイル経路ができる」 → 本サブフェーズは `deserialize` 側 migration から到達不能 dead code のみを撤去。serialize 側は v1.0.0 形式のみを出力する既存挙動を維持。round-trip test（`jsonSerializerValidation.test.ts` の互換破棄検証 + `JSONSerializer.test.ts` の version rejection tests）が引き続き通ることを確認
    - 開発ガイド §6.0.1 検出観点1（grep）: 関数シグネチャ撤去・引数撤去では src/ + tests/ 全体に grep を広げる → 削除予定の関数（`normalizeLegacyFeatureAnchor` / `normalizeLegacyPolygonRing` / `normalizeShapeType` / `firstRecord` / `normalizeParentId` / `hasShapeSource`）が src/ tests/ 双方から呼ばれていないことを事前確認。`shapeTypeFromFeatureType` は内部参照のみで撤去はせず、意味論明確化のため `defaultShapeType` へリネーム（旧名は「feature → shape の変換」に読めるが実体は「type 未指定時の既定値導出」）
    - 開発ガイド §6.3.13「optional フィールド導入は全層を1サブフェーズに詰めない」: 1サブフェーズあたりの差分は ~15 ファイル / ~300 行を上限に分割 → 本サブフェーズは 1 ファイル（`jsonSerializerMigration.ts`）のみへの修正に限定。テスト側は既存の rejection tests が dead code 撤去後も同じ結果を返すため touch 不要
- 経緯:
    - Phase 2-D-6-3c で `resolveVersion` が `version === SUPPORTED_VERSION ('1.0.0')` 以外を `SerializationError` で拒否する厳格化を完成（要件定義書 §2.5.2 / 現状.md §6.2 互換破棄方針）
    - その結果、`jsonSerializerMigration.ts` 内の旧形式変換ロジック（`anchors がない旧形式` 単一地物変換、`featureType: 'LineString'` / 小文字 featureType の変換、`Polygon.vertexIds` → territory リング変換、`normalizeLegacyFeatureAnchor` / `normalizeLegacyPolygonRing` 関数本体）は到達不能になった
    - 加えて `resolveVersion(raw, _ctx)` の `_ctx` 引数は旧バージョン警告経路（`looksLikeLegacyProject` / `isMigratableLegacyVersion`）の撤去で参照されなくなりプレースホルダ化していた
    - 関連 dead code として: `normalizeShape` の `!isRecord(value)` フォールバック（地物直下の形状情報移行）/ `normalizeShape` の `vertexIds`-direct 経路 / `normalizeShapeType` の `'Line'` → `'LineString'` 変換 / `normalizeRing` の `ringType` 推測 / `normalizeTimeRange` の `time` / `startYear` フォールバック / `normalizeAnchorProperty` の `properties[0]` フォールバック も同時に v1.0.0 で reachable な v1.0.0 形式の中間ファイル想定が無くなったため撤去対象に含めた
- 変更内容（src 1 ファイル + tests 1 ファイル / src +34 -175 行 + tests +332 -0 行）:
    - `src/renderer/infrastructure/persistence/jsonSerializerMigration.ts`:
        - `resolveVersion(raw: JsonRecord, _ctx: MigrationContext)` → `resolveVersion(raw: JsonRecord)`。`migrateJsonWorld` 内の呼び出しを `resolveVersion(raw)` に更新
        - `normalizeFeature` の `Array.isArray(record.anchors) ? ... : normalizeLegacyFeatureAnchor(...)` 分岐を撤去し、`record.anchors` が配列でない場合は `SerializationError` を直ちに throw する形へ変更（v1.0.0 では `anchors` は required）
        - `normalizeLegacyFeatureAnchor` 関数本体を削除
        - `normalizeFeatureType(value, ctx)` から `'LineString'` / `'point'` / `'line'` / `'polygon'` の旧形式変換ブランチを撤去し、`'Point'` / `'Line'` / `'Polygon'` 以外は `Unknown feature type: ${value}` で throw する形へ簡素化（`jsonSerializerTransforms.ts:301` の既存メッセージ規約に揃えた。テスト「不正な地物タイプでエラー」が `'Unknown feature type'` を期待していた契約を維持）。`ctx` 引数も不要となり撤去
        - `normalizeShape` のシグネチャから `fallbackSource: JsonRecord` と `ctx: MigrationContext` を撤去。`!isRecord(value)` フォールバック分岐 / `vertexIds`-direct ポリゴン経路 / `'Line'` → `'LineString'` 変換を撤去。`normalizeShapeType` 関数本体も撤去し、`optionalString(source, 'type') ?? defaultShapeType(featureType)` 直結に簡素化
        - `normalizeLegacyPolygonRing` 関数本体を削除（`Polygon.vertexIds` → territory リング変換）
        - `normalizeRing` のシグネチャから `ctx: MigrationContext` を撤去。`ringType` の位置依存推測（`index === 0 ? 'territory' : 'hole'`）を撤去し `requiredString` で必須化。`normalizeParentId` 関数も撤去し、`typeof record.parentId === 'string' ? record.parentId : null` インライン化
        - `normalizeTimeRange(value, fallbackSource, anchorId, ctx)` を `normalizeTimeRange(value, anchorId)` に簡素化。`fallbackSource.time` / `fallbackSource.startYear` / `fallbackSource.endYear` 経由の旧形式フォールバックを撤去
        - `normalizeAnchorProperty(value, fallbackSource, ctx)` を `normalizeAnchorProperty(value)` に簡素化。`fallbackSource.properties[0]` 経由の旧形式 properties 配列フォールバック を撤去
        - `resolveShape` 内の `hasShapeSource(record)` 呼び出しを `isRecord(record.shape)` インラインに置換し、`hasShapeSource` 関数本体を削除（`record.vertexIds` / `record.rings` / `record.vertexId` の地物直下チェックは旧形式専用で v1.0.0 ではすべて `record.shape` 配下）
        - `normalizeAnchor` から `normalizeTimeRange` / `normalizeAnchorProperty` への呼び出しを新シグネチャに同期
        - 未使用となった `firstRecord` ヘルパー関数を撤去
- 検証:
    - `npm run check`: 0 errors / 0 warnings
    - `npm run build`: 通過
    - `npx vitest run`: 全テスト通過。dead code 撤去によりテストアサーション・実行時挙動は不変（version 拒否経路が前段で全旧形式ファイルを排除しているため、削除した変換コードに到達するテストは存在しない）。「不正な地物タイプでエラー」テストはエラーメッセージ規約を `jsonSerializerTransforms.ts:301` の `Unknown feature type: ...` に揃えたことで継続通過
    - **追補1（レビュー指摘 Medium 反映）**: `jsonSerializerValidation.test.ts` に `describe('v1.0.0 旧構造の拒否 (Phase 2-D-7c-1)')` セクションを新設し、削除した分岐の拒否挙動を直接固定するテストを追加。既存 rejection tests の代表例は `version: '0.8.0'` で前段拒否されるため、削除した分岐の拒否挙動を直接固定できていなかった整合性ギャップを是正（開発ガイド §6.4.15 の read / validate / round-trip 同期観点）
    - **追補2（レビュー指摘 Low 反映 — テスト意図と網羅性のズレ是正）**: 初版の追加テストは「timeRange 欠落のみ」「property 欠落のみ」など代表ケース寄りで、削除対象だった具体的な fallback 入力（旧 `time` / `startYear` フィールド、旧 `properties[0]` 配列、`featureType` 小文字、anchor 直下 `vertexIds` / `rings` / `vertexId` 直書き）を直接押さえていなかった。追補2で各 fallback パターンを網羅する describe を再構成し、現コードが各パターンを `SerializationError` で拒否することを直接固定:
        - `describe('anchor の timeRange / 旧 time / 旧 startYear')`: timeRange 欠落 / 旧 `time: { year }` 直書き / 旧 `startYear` + `endYear` 直書きの 3 パターン
        - `describe('anchor の property / 旧 properties[0] 配列')`: property 欠落 / 旧 `properties: [{ name }]` 配列直書きの 2 パターン
        - `describe('featureType の旧形式表記')`: `'LineString'` / `'point'` / `'line'` / `'polygon'` の 4 パターン
        - `describe('anchor 直下の旧形式 shape 表現')`: Polygon + 直下 `vertexIds` / Polygon + 直下 `rings` / Point + 直下 `vertexId` の 3 パターン
        - `features[].anchors` 欠落 / `shape.type: 'Line'` / `ringType` 欠落 / `shape.vertexIds` 直書き は単発 it として保持
    - **追補3（レビュー指摘 Low 反映 — エラーメッセージ pin とコメント整理）**:
        - `shape.type: 'Line'` 拒否テストが `toThrow(SerializationError)` のみで他テスト群と整合性に欠けていた箇所を `toThrow('shape.type "Line" does not match featureType "Line"')` で pin。「migration sweep 後に shape.type 検証が validation 側で動いている」ことを明示
        - legacy `shape.vertexIds` 直書きテストのコメントを履歴記述（「`normalizeLegacyPolygonRing` 経由ではなくなったことを確認」）から現状挙動主語（「shape.rings 配列が無いため空 Polygon となり validation で領土リング欠落として拒否される」）へ整理。`feedback_doc_consistency` 系の stale 化リスクを低減
    - 残存 reachable な v1.0.0 互換 warning は保持: `optionalArray` の `sharedVertexGroups` / `timelineMarkers` 欠落（テスト `'旧形式の欠損した追加フィールドを補完し警告を返す'` がカバー）/ `normalizeIsTopLevel` の `placement.isTopLevel` 欠落（テスト `'旧形式（isTopLevel フィールドなし）は parentId === null から派生する'` がカバー）/ `normalizeAnchorProperty` の `kind` 欠落（テスト `'旧形式（kind フィールドなし）を読み込むと kind は undefined になる'` がカバー）/ `normalizeMetadata` / `normalizeSettings` / `normalizeBaseMapSettings` の欠落 / **`normalizePlacement` の `placement` 自体の欠落フォールバック** — これらは Phase 2-A / 2-B / 2.5 等の中間バージョン (`version: '1.0.0'` のまま) で書かれた `.gimoza` 読込で発火しうる reachable 経路として残置。`normalizePlacement` の欠落フォールバックは Phase 2-B 以前（`placement` フィールド導入前）の中間 v1.0.0 ファイルで `placement` 自体が無いケースを安全側で吸収する経路として保持（`placement.isTopLevel` 単独欠落の `normalizeIsTopLevel` フォールバックと同じ判断ライン）。実体ファイルの有無に依らず `placement` 関連は Phase 2-D-7 全体終了時にまとめて再評価予定
- 振る舞い変更:
    - 旧形式（`LineString` / lowercase featureType / `anchors` 配列なし / `Polygon.vertexIds` 地物直下 / shape 地物直下 / `time` / `startYear` / `properties[0]` 配列 / `ringType` 欠落リング）は migration 経由ではなく `SerializationError` で即拒否される。ただし v1.0.0 形式の正常ファイルでは到達しない経路であり、旧形式（version 0.x または version 欠落）はそもそも `resolveVersion` で先に拒否されるため、ユーザー観測挙動の変化はない
- 残置 / 後続サブフェーズへ送る:
    - **Phase 2-D-7c-2〜**: `App.svelte` の `getAddTargetLayer()` ガード残骸 / `<MapCanvas {features}>` props 経路の整理 / コメント内 phase 参照棚卸し
    - **Phase 2-D-7c の最終段**: `Layer` エンティティ本体 / `Layer.ts` / `World.layers` フィールドの最終削除 + `SaveLoadUseCase.getLayers/setLayers` 暫定 API + `App.svelte` 初期化の `new Layer('default', ...)`
- 教訓反映:
    - 開発ガイド §6.4.15「永続化 shim は read / write / validate / round-trip test を同じ変更単位で同期する」を本サブフェーズで適用。`deserialize` の dead code 撤去にあたり、`serialize` 側挙動 / `validate` 側挙動 / 既存 rejection tests / round-trip test の整合性を再確認し、ユーザー観測挙動の変化なしを担保
    - 開発ガイド §6.0.1 検出観点1（tests/ への grep）を適用。削除予定の関数名（`normalizeLegacyFeatureAnchor` / `normalizeLegacyPolygonRing` / `normalizeShapeType` / `firstRecord` / `shapeTypeFromFeatureType` / `normalizeParentId` / `hasShapeSource`）を src/ tests/ 双方で grep し、外部参照ゼロを着手前に確認
    - 開発ガイド §6.3.13「optional フィールド導入は全層を1サブフェーズに詰めない」を本サブフェーズで適用。スコープを「migration 内部の dead code 撤去」のみに限定し、`serialize` 側 / 型定義 / `validate` 側 / 描画 / UI / Application / Domain / tests/ を一切 touch しない
    - 新規教訓の追加は不要: 既存教訓（§6.4.15 / §6.0.1 / §6.3.13 / §6.10）で全課題を説明できる

---

## 2026-05-20 階層モデル新仕様 Phase 2 サブフェーズ D-7b: テストヘルパー dead `layerId` 引数の sweep

現状.md §6.10 Phase 2-D-7 を §6.10 スコープ管理ルール（~15 ファイル / ~300 行）に従って分割した 2 番目のサブフェーズ。Phase 2-D-6-3c で fixture リテラルから `layerId` プロパティを撤去したが、テストヘルパー関数の `layerId: string` 引数 + 呼び出し側の `'layer-1'` 等リテラルが dead semantics で残置していた箇所を sweep する。

- 着手前ゲート（開発ガイド §5.1.2 / 指示.md §共通ルール）:
    - 開発ガイド §6.0.1 検出観点1（grep）: 「関数シグネチャ撤去・引数撤去では tests/ にも grep を広げる。`tsconfig.web.json` の include は src/renderer のみで tests/ は型チェック対象外、vitest は実行時に型を見ない」 → 本サブフェーズは tests/ の `layerId: string` 引数とその呼び出し側引数を撤去する dead semantics sweep であり、`npm run check` で検出できないため確実に tests/ 全体 grep で漏れゼロを確認する
    - 開発ガイド §6.10 スコープ管理ルール: 1サブフェーズの差分は ~15 ファイル / ~300 行を上限。対概念は別サブフェーズへ切り出す
- 経緯:
    - Phase 2-D-6-3c で `JsonAnchorPlacement.layerId` を完全撤去し fixture リテラルの `placement: { layerId: 'l1', ... }` を 22 ファイル touch して同期撤去した
    - その際、テストヘルパー関数（`makePolygonFeature` / `makePointFeature` / `makeLineFeature` / `createPointFeature` / `createPolygonFeature` / `polygonFeatureWithChildren` 等）の `layerId: string` 引数は未使用のまま残置した。呼び出し側多数の touch を伴うため Phase 2-D-7 sweep へ送る判断（現状.md §6.10 D-6-3c 残置宣言 + 本実装済み.md 2026-05-20 D-6-3c エントリ「残置 / Phase 2-D-7 へ送る」）
    - `npm run check` は通過していたが `tsconfig.web.json` の include 対象外で、呼び出し側の `makePolygonFeature('id', 'layer-1', [...])` 経由で渡される `'layer-1'` 値は実装で参照されない完全 dead semantics
- 変更内容（tests/ 8 ファイル / 約 +0 -50 行）:
    - `tests/application/polygonValidation.test.ts`: `makePolygonFeature` / `makePointFeature` / `makeLineFeature` から `layerId: string` 引数撤去 + 呼び出し側全 9 箇所の `'layer-1'` リテラル撤去
    - `tests/domain/services/ConflictDetectionService.test.ts`: `makePolygonFeature` / `makePolygonFeatureWithRings` / `makeLineFeature` から `layerId` 撤去 + 呼び出し側 36 箇所の `'layer-1'` / `'layer-2'` リテラル撤去
    - `tests/domain/services/PolygonValidationService.test.ts`: `makePolygonFeature` / `makePointFeature` から撤去 + 呼び出し側 8 箇所
    - `tests/infrastructure/rendering/hitTestUtils.test.ts`: `makePointFeature` / `makeLineFeature` / `makePolygonFeature` から撤去 + 呼び出し側 9 箇所の `'l1'` リテラル撤去
    - `tests/infrastructure/rendering/vertexSelectionContext.test.ts`: `createPointFeature` / `createPolygonFeature` から撤去 + 呼び出し側 6 箇所の `'visible'` リテラル撤去
    - `tests/presentation/app/appPolygonEditing.test.ts`: `makePolygonFeature` / `makePointFeature` / `makeLineFeature` から撤去 + 呼び出し側 18 箇所の `'layer-1'` / `'layer-2'` リテラル撤去
    - `tests/presentation/components/mapCanvasUtils.test.ts`: `createPointFeature` から撤去 + 呼び出し側 1 箇所
    - `tests/presentation/components/mapSceneEntriesIntegration.test.ts`: `pointFeature` / `polygonFeature` / `polygonFeatureWithChildren` から撤去 + 呼び出し側 7 箇所の `'layer-a'` / `'layer-b'` / `'l1'` リテラル撤去
- 検証:
    - `npm run check`: 0 errors / 0 warnings
    - `npm run build`: 通過
    - `npx vitest run`: dead 引数撤去のみで実行時挙動は完全不変のため新規テスト追加不要。既存テストすべて通過
    - tests/ 全体 `grep "layerId"`: 残り 3 ファイル（`MergeFeatureCommand.test.ts` コメントのみ / `MergeService.test.ts` Phase 4 まで残置 API シグネチャ / `jsonSerializerValidation.test.ts` 旧形式拒否テスト fixture）。これらは現状.md §6.10 D-6-3c で意図的な残置と明示済み（本サブフェーズのスコープ外）
- 振る舞い変更: なし。src/ 完全ノータッチ。ヘルパー内部で使用されていない引数の撤去のみで、テストアサーション・テスト description・テストロジックは一切変更なし
- 追補（レビュー指摘反映）: 当初は「テスト description の文言改訂はスコープ外」として `ConflictDetectionService.test.ts` line 133 / 144 / 281 / 293 の「異なるレイヤーIDの末端ポリゴン/末端地物…」description と line 225 の「同じレイヤーに3つの…」コメントを残置していたが、レビューで「呼び出し側から layer-1 / layer-2 が消え、テストデータ上も差異を作っていないため、仕様説明としては false に近い。開発ガイド §6.0.1 の『同じ変更単位で同期』と要件定義書 §2.1 の『末端地物排他は地図全体』が接点」と指摘を受け、同サブフェーズ内で文言を「別の末端ポリゴン / 別の末端地物」へ同期。Low 1 の判断ミス再発防止として `feedback_subphase_scope_split.md` の射程に「dead 引数撤去で同サブフェーズ内のテスト description が false 化したら同期する」を組み込む方針（明示的な追記は不要、§6.0.1 検出観点1 で説明可能）
- 残置 / 後続サブフェーズへ送る:
    - **Phase 2-D-7c〜**: `Layer` エンティティ本体 / `Layer.ts` / `World.layers` フィールドの最終削除 + `SaveLoadUseCase.getLayers/setLayers` 暫定 API + `App.svelte` 初期化の `new Layer('default', ...)`
    - **dead code sweep**: 旧形式 migration ロジック（`normalizeLegacyFeatureAnchor` 等） + `resolveVersion(raw, _ctx)` の `_ctx` 引数
    - **`App.svelte` の `getAddTargetLayer()` ガード残骸** + **`<MapCanvas {features}>` props 経路** + **コメント内 phase 参照棚卸し**
    - **`tests/presentation/app/appPolygonEditing.test.ts` の `same-layer` / `other-layer` feature ID + Phase 2-D-6-3a 履歴コメント**: ID は dead semantics 寄りだが、コメントは「Phase 2-D-6-3a で同一レイヤー絞り込みを撤去した経緯」の履歴説明として意味を保持中。Phase 2-D-7 のコメント phase 参照棚卸しに同梱する
- 教訓反映:
    - 開発ガイド §6.0.1 検出観点1（tests/ への grep + 同じ変更単位での同期）を本サブフェーズで適用。`tsconfig.web.json` で tests/ が型チェック対象外なため、`npm run check` で検出できない dead 引数を tests/ 全体 grep で確実に発見し撤去。また、引数撤去で false 化したテスト description を同サブフェーズ内で同期（レビュー追補で対応）
    - 開発ガイド §6.10 スコープ管理ルール「サブフェーズは1経路ずつ」を遵守しつつ、§6.0.1 検出観点1 の「同じ変更単位で同期」と両立。経路は「dead 引数 + 引数撤去で false 化した同サブフェーズ内テスト description / コメント」までとし、src 側の dead code・履歴文脈の独立コメントは後続へ
    - 新規教訓の追加は不要: 既存教訓（§6.0.1 検出観点1 / §6.10 / `memory/feedback_subphase_scope_split.md`）で全課題を説明できる

---

## 2026-05-20 階層モデル新仕様 Phase 2 サブフェーズ D-7a: EventBus dead 型撤去

現状.md §6.10 Phase 2-D-7 を §6.10 スコープ管理ルール（~15 ファイル / ~300 行）に従って分割した最初のサブフェーズ。emit / subscribe ともに src 内に経路が無くなった `layers:changed` / `layer:visibilityChanged` イベント型を撤去する。

- 着手前ゲート（開発ガイド §5.1.2 / 指示.md §共通ルール）:
    - 開発ガイド §6.1.4「コンポーネント間での状態同期漏れ」: emit が消滅した EventBus 購読を残置すると保守時のミスリードになる。dead listener / dead type は早期撤去すべき
    - 開発ガイド §6.10 スコープ管理ルール: 1サブフェーズの差分は ~15 ファイル / ~300 行を上限とする。対概念は別サブフェーズへ切り出す
- 経緯:
    - Phase 2-D-3 で `ManageLayersUseCase` 撤去時に `eventBus.emit('layers:changed', ...)` および `'layer:visibilityChanged'` の emit 経路が消滅
    - Phase 2-D-4 で `App.svelte` の `unsubLayersChanged = eventBus.on('layers:changed', ...)` 購読を撤去（dead listener）
    - 結果として型定義だけ残った dead type 状態（実装済み.md 2026-05-15 D-4 補足 + 2026-05-20 D-6-3c 検証で確認済み）
- 変更内容（src 2 ファイル / 約 -15 行）:
    - `src/renderer/application/EventBus.ts`: `LayerVisibilityChangedEvent` interface / `LayersChangedEvent` 型エイリアス / `EventMap` の `'layer:visibilityChanged'` / `'layers:changed'` エントリを撤去
    - `src/renderer/application/index.ts`: `LayerVisibilityChangedEvent` の再エクスポートを撤去
- 検証:
    - `npm run check`: 0 errors / 0 warnings
    - `npm run build`: 通過
    - `npx vitest run`: 型定義の削除のみで実行時挙動は変わらないため新規テスト追加不要。EventBus のイベント名キー型から消えるため、もし当該キーを参照しているコードがあれば svelte-check / vitest 双方で検出される（実際は src/ tests/ ともに参照なしを grep で確認済み）
    - `grep "LayerVisibilityChangedEvent\|LayersChangedEvent\|layer:visibilityChanged\|layers:changed"` for src/ + tests/: 残存なし（撤去完了）
- 振る舞い変更: なし（emit / subscribe ともに既に経路なし。型定義のみの削除）
- 残置 / 後続サブフェーズへ送る:
    - **Phase 2-D-7b〜**: `Layer` エンティティ本体 / `Layer.ts` / `World.layers` フィールドの最終削除 + `SaveLoadUseCase.getLayers/setLayers` 暫定 API + `App.svelte` 初期化の `new Layer('default', ...)`
    - **dead code sweep**: 旧形式 migration ロジック（`normalizeLegacyFeatureAnchor` 等） + `resolveVersion(raw, _ctx)` の `_ctx` 引数
    - **テストヘルパー dead `layerId` 引数 sweep**: 8 ファイル
    - **`App.svelte` の `getAddTargetLayer()` ガード残骸** + **`<MapCanvas {features}>` props 経路** + **コメント内 phase 参照棚卸し**
- 教訓反映:
    - 開発ガイド §6.1.4「コンポーネント間での状態同期漏れ」を本サブフェーズで適用。emit / subscribe 両端が消えた dead type を残置するとミスリードになるため早期撤去
    - 新規教訓の追加は不要: 既存教訓（§6.1.4 / §6.10）で全課題を説明できる

## 2026-05-20 階層モデル新仕様 Phase 2 サブフェーズ D-6-3c: JSON 永続化の `layerId` 撤去 + `AnchorPlacement.layerId` 完全削除 + テスト fixture リテラル同期撤去

現状.md §6.10 Phase 2-D-6-3 の最終段。D-6-3a で EdgeSlide 経路の同一レイヤー絞り込みと "SameLayer" 用語を撤去、D-6-3b で in-memory ドメイン経路から `placement.layerId` 参照を撤去し型を optional 化、永続化 shim と round-trip test、開発ガイド §6.4.15 を整備済み。本サブフェーズ 3c は「JSON 永続化型 / migration / `AnchorPlacement` 型本体から `layerId` を完全削除し、テスト fixture の `placement: { layerId: ..., ... }` リテラルを一掃する」ことに絞る。Phase 2-D-6 全体の最終段で、`.gimoza` 形式から `layerId` フィールドが消える地点（互換破棄が完成する地点）。

- 着手前ゲート（開発ガイド §5.1.2 / 指示.md §共通ルール）:
    - 要件定義書 §4.1: `placement` に `isTopLevel` / `parentId` / `childIds` のみ。仕様本文に `layerId` 概念は無い
    - 要件定義書 §2.1 line 145-153: 末端地物排他は地図全体（layerId 依存は Phase 2-D-1 で既に撤去済み）
    - 開発ガイド §6.4.15「永続化 shim は read / write / validate / round-trip test を同じ変更単位で同期する」: serialize / deserialize / migration / round-trip test を同じ変更単位で揃え、旧フィールドを読み捨てるなら再保存→再読み込みできることをテストで固定
    - 開発ガイド §6.0.1 検出観点1（tests/ への grep）: `tsconfig.web.json` は tests/ を型チェック対象外。引数撤去では tests/ も一括 grep する
    - 開発ガイド §6.3.13「optional フィールド導入は全層を1サブフェーズに詰めない」: 今回は「型 + 永続化」スコープのみ。UI / 描画 / operation UseCase には触らない
- 変更内容（src 5 ファイル + tests 22 ファイル）:
    - **JSON 永続化型**: `jsonSerializerTypes.ts` の `JsonAnchorPlacement.layerId: string` フィールドを撤去
    - **JSON serialize**: `jsonSerializerTransforms.ts` の `serializeAnchorPlacement` から `pl.layerId ?? 'default'` 出力 shim を撤去 + 説明 JSDoc コメント削除
    - **JSON deserialize**: `deserializeAnchorPlacement` から `json.layerId` 読み捨ての説明 JSDoc を削除（型から消えるため自然消滅）
    - **JSON migration**: `jsonSerializerMigration.ts` の `migrateJsonWorld` から `defaultLayerId` ローカル変数を撤去 + `normalizeFeature` / `normalizeAnchor` / `normalizeLegacyFeatureAnchor` / `normalizePlacement` のシグネチャから `defaultLayerId: string` 引数を全段撤去 + `normalizePlacement` 内 `layerId: optionalString(source, 'layerId') ?? defaultLayerId,` 行を撤去。**旧 .gimoza の `placement.layerId` フィールドを検出した場合は `SerializationError` を投げて読み込みを拒否する**（要件定義書 §2.5.2「旧モデル（レイヤー概念を含む形式バージョン）のファイルは互換性なしとして読み込みエラーで拒否する。マイグレーションは提供しない。」+ 現状.md §6.2「既存 .gimoza 互換性は破棄する（移行マイグレーションは実装しない）」に従う）。レビュー指摘により仕様遵守の方針へ修正
    - **JSON version 検証**: `jsonSerializerMigration.ts` の `resolveVersion` から `looksLikeLegacyProject` / `isMigratableLegacyVersion` を撤去し、`version` 欠落および `0.x` 系を一律 `SerializationError` で拒否（要件定義書 §2.5.2 遵守）。version 欠落時に `Missing version field`、0.x 系では `Unsupported version "0.X.X"` を投げる。内部の旧形式 migration ロジック（`anchors がない旧形式`、小文字 featureType、`Polygon.vertexIds` → territory リング 等）は version 拒否で到達不能になるが、dead code としては残置（Phase 2-D-7 のクリーンアップで sweep 予定）
    - **JSON validation コメント清掃**: `jsonSerializerValidation.ts` 75-82 行目の D-6-3b 時点記述（`serializeAnchorPlacement` の `'default'` shim 説明 + `placement.layerId` は D-6-3c で完全撤去予定 等）を撤去。本コミットで嘘になった内容（レビュー指摘 [Medium] 対応）
    - **in-memory ドメイン型**: `FeatureAnchor.ts` の `AnchorPlacement.layerId?: string` optional フィールドを完全撤去 + 関連 JSDoc 簡素化
    - **テスト fixture 一掃**（22 ファイル / 約 80 occurrences）:
        - 単純リテラル `placement: { layerId: 'l1', parentId: ..., ... }` の `layerId: 'l1',` プロパティを削除: `tests/application/AddFeatureUseCase.test.ts` / `commands/AddFeatureCommand.test.ts` / `polygonValidation.test.ts` / `ReassignFeatureParentUseCase.test.ts` / `SaveLoadUseCase.test.ts` / `UpdateFeatureAnchorUseCase.test.ts` / `domain/entities/Feature.test.ts` / `domain/services/ConflictDetectionService.test.ts` / `PolygonValidationService.test.ts` / `SharedVertexService.test.ts` / `TimeService.test.ts` / `domain/value-objects/FeatureAnchor.test.ts` / `PropertyProjection.test.ts` / `infrastructure/persistence/JSONSerializer.test.ts` / `JSONWorldRepository.test.ts` / `infrastructure/rendering/hitTestUtils.test.ts` / `vertexSelectionContext.test.ts` / `presentation/app/appPolygonEditing.test.ts` / `appSelection.test.ts` / `presentation/components/featureSearchUtils.test.ts` / `labelRendererUtils.test.ts` / `mapCanvasUtils.test.ts` / `mapSceneEntriesIntegration.test.ts` / `parentTransferDialogUtils.test.ts` / `propertyPanelHistoryUtils.test.ts`
        - `tests/presentation/components/mapSceneEntries.test.ts`: ヘルパー `placement(layerId, ...)` / `makeAnchor(..., layerId, ...)` から layerId 引数を撤去し、テスト「複数レイヤー所属の地物が混在しても地図全体（layerId に依らず）で連番割当される」を「複数地物が混在しても地図全体で連番割当される」に改名 + 「入力順を保持する」テスト内 `expect(entries.map((e) => e.anchor.placement.layerId)).toEqual([...])` アサートを `feature.id` 並びアサートへ書き換え
        - `tests/infrastructure/persistence/JSONSerializer.test.ts`: 旧形式 fallback fixture（`features[0].layerId: 'l1'`）と最後の「childIds が指す未存在地物への参照」テスト fixture 内 `placement.layerId: 'l1'` を撤去
        - `tests/application/UpdateFeatureAnchorUseCase.test.ts`: `updatePlacement` テスト引数オブジェクトから `layerId: 'l2',` 行を削除
    - **互換破棄検証テストの更新（§6.4.15 + 仕様遵守）**: `tests/infrastructure/persistence/jsonSerializerValidation.test.ts` の `describe('永続化 shim の round-trip 整合性 (Phase 2-D-6-3b)')` を `describe('互換破棄の検証 (Phase 2-D-6-3c)')` に改名し、JSDoc の文面を「要件定義書 §2.5.2 + 現状.md §6.2 に従い、旧モデル（placement.layerId 含む）は読み込みエラーで拒否」へ更新。テストを 3 件に再構成: (1)「旧形式（placement.layerId を含む）は読み込みエラーで拒否される」+ (2)「カスタムレイヤーを持つ旧形式も同様に拒否される」+ (3)「新形式（layerId なし）→ 再保存 → 再読み込みは正常に往復する」の3点。新形式 round-trip では「再保存ファイルの placement に layerId が含まれない」アサート `expect('layerId' in resavedPlacement).toBe(false);` を維持。`createPlacement` ヘルパーから `layerId: LAYER_ID` を撤去。`LAYER_ID` 定数は `createWorld` の `layers: [{ id: LAYER_ID, ... }]` で引き続き使用するため残置
    - **コメント清掃**: `JSONSerializer.test.ts` の `'終了時間が開始時間より前の場合エラー'` 直前にあった「Phase 2-D-6-3b で validateLayerReferences 撤去」説明コメント（既に文書側で説明済みで stale 化リスク）を削除
- 残置 / Phase 2-D-7 へ送る:
    - **テストヘルパー dead `layerId: string` 引数**: 8 ファイル（`tests/application/polygonValidation.test.ts` / `tests/domain/services/ConflictDetectionService.test.ts` / `PolygonValidationService.test.ts` / `tests/infrastructure/rendering/hitTestUtils.test.ts` / `vertexSelectionContext.test.ts` / `tests/presentation/app/appPolygonEditing.test.ts` / `tests/presentation/components/mapCanvasUtils.test.ts` / `mapSceneEntriesIntegration.test.ts`）の `makePolygonFeature(featureId, layerId: string, ...)` 形式ヘルパー引数は未使用のまま残置。撤去は呼び出し側（多数）の touch を伴うため Phase 2-D-7 クリーンアップに同梱
    - **`MergeFeatureCommand.ts` の `validationTargets.layerId: 'default'` 固定値 shim**: Phase 4（操作 UseCase / Command 再設計）で `MergeService.validateMerge` の同一レイヤー強制を「結合対象が互いに上位下位関係にないこと」へ改訂する際に撤去
    - **`MergeService.validateMerge` API の `{ layerId: string }` 引数**: 同上、Phase 4 で改訂
    - **`JsonWorld.layers` / `EventBus.LayerVisibilityChangedEvent.layerId` / `App.svelte` 初期化の `new Layer('default', ...)` / `SaveLoadUseCase.getLayers/setLayers`**: Phase 2-D-7 で `Layer` エンティティ最終削除と同時に対応
- 振る舞い変更:
    - `.gimoza` 保存時、`placement` オブジェクトから `layerId` フィールドが完全に消える（D-6-3b までは `'default'` 固定値で出力していた）
    - **旧 .gimoza は以下のいずれかが満たされると読み込み時に `SerializationError` で拒否される**（要件定義書 §2.5.2 / 現状.md §6.2 遵守）:
        1. `version` フィールド欠落 → `Missing version field`
        2. `version` が `0.X.X` 系（旧モデル時代）→ `Unsupported version "0.X.X"`
        3. `placement.layerId` フィールドを含む → 「旧モデル...互換性なしとして拒否されました...」
    - 互換破棄が完成する地点。ユーザーは新規プロジェクトを開始する必要がある
- 検証:
    - `npm run check`: 0 errors / 0 warnings
    - `npm run build`: 通過
    - `npx vitest run`: 1478 passed（97 ファイル、互換破棄拒否テスト +1 件 = 1477 → 1478）
    - `placement.layerId` キーワード grep（src）: 2 件（すべて `jsonSerializerMigration.ts` の旧モデル拒否分岐: 説明コメント + エラーメッセージ。挙動仕様の一部として正当な参照）
    - `placement.layerId` キーワード grep（tests）: 3 件（すべて `jsonSerializerValidation.test.ts` の互換破棄検証テスト: JSDoc + テスト名）
    - `layerId` 全般 grep（src）: 9 件（`MergeFeatureCommand` shim 2 + `MergeService` API 3 + `EventBus.LayerVisibilityChangedEvent` 1 + `ConflictDetectionService` コメント 1 + migration 拒否ロジック 2）。Phase 4 / Phase 2-D-7 で各々撤去予定
    - `layerId` 全般 grep（tests）: 43 件（dead ヘルパー引数 + `MergeService.test.ts` API テスト + 拒否検証 fixture + コメント）。dead ヘルパー引数撤去は Phase 2-D-7 のクリーンアップに同梱予定
- 教訓反映:
    - 開発ガイド §6.4.15「永続化 shim は read / write / validate / round-trip test を同じ変更単位で同期する」を本サブフェーズで適用。`serialize` / `deserialize` / `migration` を同じ変更単位で撤去し、round-trip test で「再保存ファイルから layerId フィールドが消えていること」+ 「再保存ファイルが validate を通ること」+ 「再読み込みできること」を3点固定
    - 開発ガイド §6.0.1 検出観点1「関数シグネチャ撤去では tests/ にも grep を広げる」を適用。`grep -rn "layerId" tests/` で着手前 140 件 → 完了後 50 件（すべて意図的残置）を確認
    - 開発ガイド §6.3.13「optional フィールド導入は全層を1サブフェーズに詰めない」を適用。スコープを「型 + 永続化 + テスト fixture」に限定し、UI / 描画 / operation UseCase には触らない
    - 新規教訓の追加は不要: 既存教訓（§6.4.15 / §6.0.1 / §6.3.13）で全課題を説明できる
- レビュー指摘対応（コミット内追補）:
    - [Medium] `placement.layerId` 読み捨て path（warning + 継続）を、仕様（要件定義書 §2.5.2 / 現状.md §6.2 = 「旧モデルは互換性なしとして読み込みエラーで拒否、マイグレーションなし」）違反として `SerializationError` 拒否へ修正
    - [Medium] `placement.layerId` だけで旧モデル判定すると 0.x 系で `placement.layerId` を持たないケースが通る抜けがあり、`resolveVersion` から `looksLikeLegacyProject` / `isMigratableLegacyVersion` を撤去して version 欠落 + 0.x 系も拒否対象に組み込み（仕様遵守の完了条件を満たす）
    - [Medium] 現状.md L88 のテスト件数説明「Phase 2-D-6-3c は件数増減なし」を実態（+1 件）に修正。`feedback_verify_test_counts.md` / `feedback_doc_consistency.md` に該当
    - [Low] 現状.md L446 の describe ブロック名を実装に合わせ「永続化の round-trip 整合性」→「互換破棄の検証 (Phase 2-D-6-3c)」へ改名 + 再構成と明示
    - [Low] `normalizePlacement` の `warn → throw` 順序を `'layerId' in source` 先行判定に整理（fallback warning が throw 経路で破棄される意図不明確さを解消）
    - [Low] 旧形式拒否テストのアサーション厚みを増強: `JSONSerializer.test.ts` / `JSONWorldRepository.test.ts` / `jsonSerializerValidation.test.ts` で `SerializationError` クラスアサート + 一意なエラーメッセージ部分マッチ（「placement.layerId を含む形式」「新規プロジェクトを開始してください」「Unsupported version "0.9.0"」「expected "1.0.0"」など）を追加
    - [Low] `resolveVersion(raw, _ctx)` の dead `_ctx` 引数撤去と旧形式 migration ロジック（`normalizeLegacyFeatureAnchor` 等）の dead code sweep は Phase 2-D-7 クリーンアップに同梱として現状.md §6.10 Phase 2-D-7 セクションへ記録

## 2026-05-19 階層モデル新仕様 Phase 2 サブフェーズ D-6-3b: src 層の `placement.layerId` 全 consumer narrow 撤去 + `createAnchorPlacement` シグネチャ変更

現状.md §6.10 Phase 2-D-6-3 の中段。D-6-3a で EdgeSlide 経路の同一レイヤー絞り込みと "SameLayer" 用語を撤去済み。本サブフェーズ 3b は「in-memory ドメイン経路から `placement.layerId` の参照を完全に消し、`createAnchorPlacement` のシグネチャを `(parentId, childIds)` に変える」ことに絞る。JSON 永続化型の `JsonAnchorPlacement.layerId` とテスト fixture の `placement: { layerId: 'l1', ... }` リテラルは D-6-3c に持ち越し。

- 経緯と方針:
    - D-6-3 全体を `feedback_subphase_scope_split.md`「サブフェーズは1経路ずつ」+ 開発ガイド §6.3.13「optional フィールド導入は全層を1サブフェーズに詰めない」の射程で 3a / 3b / 3c に分割済み
    - 3b では `FeatureAnchor.AnchorPlacement.layerId` を required → `readonly layerId?: string` (optional) に格下げ。完全削除は 3c で実施。これにより:
        - src 内 `placement.layerId` 参照を全撤去しても型上は許容される（誰も読まないので undefined）
        - tests fixture の `placement: { layerId: 'l1', parentId: null, childIds: [], isTopLevel: true }` オブジェクトリテラル（24 ファイル / 88 occurrences）が optional プロパティとして touch 不要に
        - `createAnchorPlacement` シグネチャは `(parentId, childIds)` に変更（layerId は出力に含まれない）。テスト側の `createAnchorPlacement('l1', ...)` 形式呼び出し（10 ファイル）は touch 必須
    - JSON 永続化は in-memory 撤去に追随する shim を入れる:
        - `serializeAnchorPlacement(pl)`: `layerId: pl.layerId ?? 'default'` を出力（in-memory placement では layerId が常に undefined になるため、実質 `'default'` 固定値出力）
        - `deserializeAnchorPlacement(json)`: `json.layerId` を読み捨て、in-memory placement に書き込まない
        - これで in-memory 経路から `layerId` が完全に消える一方、JSON 形式は引き続き `layerId: 'default'` フィールドを出力（D-6-3c で JSON 型側からも撤去予定）
- 変更内容（src 17 ファイル + tests 10 ファイル）:
    - **型と生成ヘルパー**: `src/renderer/domain/value-objects/FeatureAnchor.ts` の `AnchorPlacement.layerId` を optional 化 + `createAnchorPlacement(parentId, childIds)` にシグネチャ変更 + JSDoc に「Phase 2-D-6-3b で in-memory ドメイン経路から撤去済み、完全削除は D-6-3c」と明記
    - **AddFeature 経路**: `AddFeatureUseCase.addPoint/addLine/addPolygon/addPolygonFromShape/createFeature` から `layerId: string` 引数を全撤去 / `AddFeatureCommand.AddFeatureParams` の全 variant から `layerId` フィールド撤去 / `AddFeatureUseCase` 内 `createAnchorPlacement(layerId, null, [])` → `createAnchorPlacement(null, [])` / `polygonValidation.ts.createTransientPolygonFeature` から `layerId` 引数撤去 / `appPolygonEditing.ts.validatePendingPolygon` から `layerId: string | null` 引数撤去
    - **階層サービス**: `HierarchyService.buildParentChildLink` / `buildParentChildUnlink` の 4 箇所で `createAnchorPlacement` 呼び出しから layerId 撤去 + JSDoc の「Phase 2-D-6 で消える経路」注釈を削除
    - **その他 UseCase / Command**: `DeleteFeatureUseCase` (1 箇所) / `PrepareFeatureAnchorEditUseCase` (1 箇所) / `UpdateFeatureAnchorUseCase.updatePlacement` (1 箇所) / `ReassignFeatureParentUseCase` (4 箇所 + `samePlacement` 内 `a.layerId === b.layerId` 比較撤去) / `SplitFeatureCommand.addPolygonFromShape` 呼び出し (1 箇所) / `MergeFeatureCommand` の `validationTargets.layerId` を `'default'` 固定値に shim
    - **PropertyProjection**: `layerId: string` フィールドを丸ごと撤去（型 + コンストラクタ params + projectFromAnchor の派生コード）+ 不要 import (`AnchorProperty` / `FeatureShape` / `AnchorPlacement`) を削除
    - **UI**: `parentTransferDialogUtils.ts` の `ParentCandidateItem.layerId` フィールド + `buildParentCandidateItems` / `buildNewFeatureParentCandidateItems` の `layerId: anchor.placement.layerId` 設定を撤去 / `PropertyPanel.svelte` の「レイヤー」欄表示削除 / `App.svelte` の `polygonIndexInLayer` を `polygonIndex` に改名し `anchor.placement.layerId === layerId` フィルタを `anchor?.shape?.type === 'Polygon'` のみに簡素化 + AddFeatureCommand 呼び出し 3 箇所（toolStore コールバック内 / onMapClick の point ツール / `validatePendingPolygon` 2 箇所）から layerId 引数撤去
    - **JSON 永続化 shim**: `jsonSerializerTransforms.ts` の `serializeAnchorPlacement` で `pl.layerId ?? 'default'` を出力 / `deserializeAnchorPlacement` で `json.layerId` を読み捨て（in-memory placement には layerId フィールドを設定しない）
    - **HierarchyService 不要 import**: `FeatureShape` の unused import を削除
    - **tests 修正**: 29 ファイル（当初宣言「10 ファイル」を後述の sweep 追補で実態へ修正）
        - `createAnchorPlacement('l1', parentId, childIds)` → `createAnchorPlacement(parentId, childIds)` の機械置換: `FeatureAnchor.test.ts` / `HierarchyService.test.ts` / `ReassignFeatureParentUseCase.test.ts` / `UpdateFeatureAnchorUseCase.test.ts` / `parentTransferDialogUtils.test.ts`
        - `addPoint(coord, 'l1', time)` / `addPolygon(coords, 'l1', time)` / `addLine(coords, 'l1', time)` / `addPolygonFromShape(shape, 'l1', time)` / `AddFeatureCommand({..., layerId, time})` 形式から layerId 引数を機械置換で撤去（当初コミット案で touch した 9 ファイル）: `AddFeatureUseCase.test.ts` / `UpdateFeatureAnchorUseCase.test.ts` / `CommitFeatureAnchorEditUseCase.test.ts` / `PrepareFeatureAnchorEditUseCase.test.ts` / `ResolveFeatureAnchorConflictsUseCase.test.ts` / `EditFeatureUseCase.test.ts` / `polygonValidation.test.ts` / `commands/AddFeatureCommand.test.ts` / `commands/MergeFeatureCommand.test.ts`
        - **レビュー後 sweep 追補（重大）**: 残 19 ファイルの旧シグネチャ呼び出しを一括 sweep。`addPoint(coord, 'l1', time)` を新シグネチャ `addPoint(coord, time)` で実行すると `currentTime = 'l1' (string)` が `anchor.timeRange.start` に入る壊れた Feature を生成するが、tests/ は `tsconfig.web.json:25-28 include: ["src/renderer/**/*"]` で型チェック対象外、また vitest は実行時型を見ず、各テストが timeRange を参照しない（`features.length === 1` 等で確認するだけ）ため沈黙して通過していた。sweep 対象: `SaveLoadUseCase.test.ts` / `VertexEditUseCase.test.ts`（19 箇所） / `DeleteFeatureUseCase.test.ts` / `commands/SplitFeatureCommand.test.ts`（9 箇所） / `commands/MoveVerticesCommand.test.ts`（全 addPoint/addPolygon） / `commands/MoveVertexCommand.test.ts` / `commands/MoveFeatureCommand.test.ts` / `commands/DeleteVertexCommand.test.ts` / `commands/DeleteFeatureCommand.test.ts`（10 箇所） / `commands/InsertVertexCommand.test.ts` / `presentation/app/appFeatureDragging.test.ts` / `infrastructure/DIContainer.test.ts` / `application/queries/QueryServices.test.ts` / `application/AddFeatureUseCase.test.ts` の `'layer1'` 引数 2 箇所
        - `presentation/app/appPolygonEditing.test.ts`: `validatePendingPolygon(..., 'layer-1', ...)` から layerId 引数を撤去
        - `application/AddFeatureUseCase.test.ts`: 「レイヤーIDが錨のplacementに設定される」テストを撤去（仕様廃止）
        - `application/UpdateFeatureAnchorUseCase.test.ts`: `expect(newAnchor.placement.layerId).toBe(layerId)` アサーションを撤去（テスト件数は維持）+ `const layerId = 'l1'` 定数削除
        - `application/CommitFeatureAnchorEditUseCase.test.ts` / `application/PrepareFeatureAnchorEditUseCase.test.ts`: 「配置の更新を確定/適用できる」テストで `placement: { layerId: 'l2' }` → `placement: { parentId: 'parent-id' }` に置き換え（layerId 更新概念が廃止のため parentId 更新で代替）
        - `domain/value-objects/PropertyProjection.test.ts`: `expect(proj.layerId).toBe('layer1')` アサーションを撤去
        - `domain/value-objects/FeatureAnchor.test.ts`: `withPlacement` テストの `expect(updated.placement.layerId).toBe('l2')` アサーションを撤去（残置レビュー指摘の修正。`withPlacement` 入力リテラルからも `layerId: 'l2'` を撤去して in-memory ドメイン経路の layerId 撤去の本旨に合わせる）
        - `infrastructure/persistence/JSONSerializer.test.ts`: 「旧形式の単一形状地物をanchors形式へ移行できる」の `expect(anchor?.placement.layerId).toBe('l1')` アサーションを撤去（JSON は引き続き layerId フィールドを持つが、in-memory placement には反映されない）
        - `application/commands/MergeFeatureCommand.test.ts`: 「異なるレイヤーのポリゴン結合は拒否」テストを撤去（仕様廃止に伴う。Phase 4 で「結合対象が互いに上位下位関係にないこと」へ改訂予定）
- 振る舞い変更:
    - `PropertyPanel` から「レイヤー」表示行が消える
    - `MergeService.validateMerge` の同一レイヤーチェックが実質無効化（すべての地物が `'default'` 固定値を渡されるため `layerIds.size > 1` チェックは常に通る。仕様としては Phase 2-D-5 で UI から複数レイヤー編集が消えているため、ユーザー観測挙動の変化は無い。Phase 4 で結合事前条件を「結合対象が互いに上位下位関係にないこと」へ正式改訂予定）
    - `.gimoza` 保存時、元の `layerId: 'l1'` 等の値が常に `'default'` に置換される。Phase 2-D-5 で UI から複数レイヤー編集が消えており実質単一レイヤー前提のため、新規プロジェクトのユーザー観測挙動は変わらない（互換破棄方針の射程）
    - **旧 .gimoza ラウンドトリップ警告（レビュー後に解消）**: 当初コミット案では `serializeAnchorPlacement` が `pl.layerId ?? 'default'` を出力する一方、`deserializeAnchorPlacement` は読み捨て、`validateLayerReferences` は引き続き `anchor.placement.layerId` が `world.layers` に存在することを要求する非同期 shim だった。複数 layers を持つ旧 `.gimoza`（例: `layers: [{id: 'l1', ...}, {id: 'l2', ...}]`、`anchors[*].placement.layerId === 'l1'`）を読み込み → 再保存すると、保存側で全 anchor.placement.layerId が `'default'` に置き換わる一方、`world.layers` には `'default'` ID のレイヤーが含まれず、その再保存ファイルを再ロードすると `Feature ... references non-existent layer "default"` でエラーになり開けなくなる経路が残っていた。レビュー High 指摘「保存できるが次に開けないファイルを作る可能性」を受け、本サブフェーズで以下を追加実施:
        - `jsonSerializerValidation.ts` から `validateLayerReferences` 関数本体と `validateJsonWorld` への受け渡しを撤去（D-6-3c スコープから前倒し）
        - `jsonSerializerValidation.test.ts` に **round-trip test を 2 件追加**: (1) 複数 layers を持つ旧形式 → 再保存ファイルが validateJsonWorld を通る、(2) default layer を持たない旧形式でも再保存後に再ロードできる
        - `JSONSerializer.test.ts` の「存在しないレイヤーへの参照でエラー」テスト 1 件を撤去
        - 開発ガイドに **§6.4.15「永続化 shim は read / write / validate / round-trip test を同じ変更単位で同期する」** を新規教訓として追加（経過に本事案を明示）
    - これにより、互換破棄方針（`memory/feedback_break_existing_freely.md`）の射程内であっても「保存できるが次に開けない」状態は排除された
- 検証:
    - `npm run check`: 0 errors / 0 warnings
    - `npm run build`: 通過
    - `npx vitest run`: 1477 passed / 97 ファイル（前回 1478 → -2 件 + レビュー後追補で round-trip test +2 件 / validateLayerReferences テスト -1 件 = 純差 -1 件。-2 件の内訳: 「レイヤーIDが錨のplacementに設定される」-1 / 「異なるレイヤーのポリゴン結合は拒否」-1。「`expect(...placement.layerId).toBe(...)` アサーション撤去」はテスト本体を残したのでファイル件数維持）
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **開発ガイド §6.3.13** 「optional フィールド導入は全層を1サブフェーズに詰めない」: required → 削除のフィールド変更でも構造は同じ。1 サブフェーズ ~15 ファイル / ~300 行を上限に分割。本サブフェーズは src 17 + tests 29 = 46 ファイルでファイル数は大幅超過したが、行数規模は約 ±200 行で、in-memory 経路の同期撤去という単一目的に絞ったためレビュー対概念のリスクは抑えられた（ファイル数超過の原因はレビュー後 sweep で tests/ 19 ファイルの旧シグネチャ呼び出しを追加 touch した分。詳細は本エントリ「重要な是正」参照）
    - **開発ガイド §6.0.1 検出観点1** 「仕様文書での用語撤廃宣言時の同一文書同期」: 撤廃対象 `placement.layerId` / `createAnchorPlacement` 第 1 引数 / `PropertyProjection.layerId` / `ParentCandidateItem.layerId` / `validatePendingPolygon` 第 4 引数 / `AddFeatureUseCase.addPoint(layerId)` を src/ で grep し、撤去対象（D-6-3b）と保留対象（JSON 層 / テスト fixture / `MergeService.validateMerge`）を明示的に分類。レビュー後追補で tests/ への grep 拡張観点を §6.0.1 検出観点1 へ追記
    - **メモリ `feedback_subphase_scope_split.md`** 「サブフェーズは1経路ずつ」: in-memory ドメイン経路のみに絞り、JSON 永続化型・テスト fixture は touch しない（レビュー後追補で「永続化 shim の片側変更」リスクが判明し、validateLayerReferences のみ前倒し撤去）
    - **開発ガイド §6.6.8** 「排他検証は仕様の対象範囲を実装で広げない」+ **§6.6.9** 「派生サービス・ヒットテストは同じ shape 解決経路を共有する」: `HierarchyService.buildParentChildLink/Unlink` で `createAnchorPlacement` の引数を変えるとき、リーフ判定や派生形状の振る舞いは変えず純粋に layerId 撤去のみに留めることを徹底
- **§6 への新規教訓追加**: **§6.4.15「永続化 shim は read / write / validate / round-trip test を同じ変更単位で同期する」を新規追加**（レビュー High 指摘「保存できるが次に開けないファイルを作る可能性」を受けて）。あわせて §6.0.1 検出観点1 に「関数シグネチャ撤去・引数撤去では tests/ にも grep を広げる」を追記（レビュー後 sweep の経緯を反映）
- **次サブフェーズ（D-6-3c）への持ち越し**:
    - `FeatureAnchor.AnchorPlacement.layerId` 型フィールドの完全削除（optional → 削除）
    - `JsonAnchorPlacement.layerId` 型 + `jsonSerializerMigration.ts` の layerId 派生撤去（`jsonSerializerValidation.ts.validateLayerReferences` は D-6-3b で前倒し撤去済み）
    - テスト fixture の `placement: { layerId: 'l1', ... }` リテラル 24 ファイル / 88 occurrences の機械置換
    - `MergeFeatureCommand` の `validationTargets.layerId: 'default'` shim 撤去（Phase 4 で結合事前条件を再設計するタイミング）
    - D-6-3b で追加した round-trip test（`jsonSerializerValidation.test.ts`「永続化 shim の round-trip 整合性」describe）は、D-6-3c で `layerId` フィールド自体が JSON 形式から消えるため、fixture を「layerId フィールドなしでの再保存→再ロード成功」へ更新する必要がある

---

## 2026-05-19 階層モデル新仕様 Phase 2 サブフェーズ D-6-3a 追補: リーフ判定への揃え + 要件定義書の用語先行同期

レビュー High 指摘「`anchor.shape.type === 'Polygon'` のままで移行期間ノード（shape あり + childIds 非空）が障害物に入る。要件定義書 §2.1 line 145-153 / 開発ガイド §6.6.8 のリーフ判定運用に揃え `isLeafPolygonAnchor` にすべき」+ Medium 指摘「要件定義書 line 713 / 721 / 442 / 447 / 564 / 565 が『同一レイヤー』のままで §6.0.1 検出観点1（用語同期）違反」を受けた。

- 変更内容:
    - `src/renderer/presentation/app/edgeSlideConstraintCollectors.ts`: 3 関数すべての narrow を `!anchor || !anchor.shape || anchor.shape.type !== 'Polygon'` → `!anchor || !isLeafPolygonAnchor(anchor)` に統一。`@domain/value-objects/FeatureAnchor` から `isLeafPolygonAnchor` を import。JSDoc を「全ての末端ポリゴン地物の領土リング座標」「移行期間ノード・コンテナを除外」へ改訂。ソース側 (`collectMovingPolygonEdgeConstraints`) も同方針で揃え、障害物側 / ソース側の判定経路を統一
    - `tests/presentation/app/appPolygonEditing.test.ts`: 「移行期間ノード（shape あり + childIds 非空）とコンテナ（shape なし）を EdgeSlide 対象から除外する」回帰テストを 1 件追加。3 関数すべて（`collectPolygonObstacleRings` / `collectPolygonObstacleVertices` / `collectMovingPolygonEdgeConstraints`）で除外を固定。`Feature` / `FeatureAnchor` / `Ring` を直接構築して移行期間ノードとコンテナ fixture を作る形（既存 `makePolygonFeature` ヘルパーは leaf 前提でリファクタ不要）
    - `要件定義書.md`: 「同一レイヤー」旧モデル文言 6 箇所を一括同期
        - line 442: 「空間競合検出（同一レイヤー面情報の重なり検出）」→「空間競合検出（地図全体の末端地物同士の重なり検出）」
        - line 447: 「同一レイヤーに属する複数のポリゴン地物」→「地図全体の複数の末端地物」+ 「§2.1 末端地物の排他性 参照」追記
        - line 564: 「特に同一レイヤーの他面情報との空間的オーバーラップを検出」→「特に地図全体の他の末端地物との空間的オーバーラップを検出」
        - line 565: 「リアルタイムで同一レイヤー同一時間の他の面情報の内部には頂点を設置できない（面情報の排他性制約）」→「リアルタイムで同時刻の他の末端地物の内部には頂点を設置できない（末端地物の排他性制約。§2.1 参照）」
        - line 713: 「同一レイヤーの他面情報との衝突判定」→「地図全体の他の末端地物との衝突判定（末端地物排他は §2.1 で地図全体に適用）」
        - line 721: 「同一レイヤーの他ポリゴンとの衝突判定」→「地図全体の他の末端地物との衝突判定」
    - `要件定義書.md` line 851 の §2.4.4.3 自動配色「同一レイヤー内で共有境界を持つ面」は Phase 6 着手時改訂の射程として明示残置（現状.md §2.2 で残置理由を明示更新）
    - `現状.md` §2.2 機能要件表の「階層モデルの新仕様への置換」備考: Phase 3 / Phase 4 / Phase 6 の改訂対象として「§2.2.3 / §2.3.2 / §2.3.3 内の『同一レイヤー』用語そのものは Phase 2-D-6-3a で先行同期済み」「§2.4.4.3 line 851 は本フェーズで対応」と注記。各 Phase の構造的改訂（バリデーション / 親指定 UI / 操作 UseCase 再設計 / 自動配色実装）は引き続き各 Phase 着手時の射程
    - **開発ガイド.md の用語先行同期（レビュー Medium 追補）**: §6.0.1 検出観点1 の grep で検出された 4 箇所のうち 2 箇所を即時同期、2 箇所を明示残置に分類
        - line 163 §3.4 領域排他性: 「排他性ルール: 同一レイヤー内の面情報は重ならない」→「排他性ルール: 末端地物同士は地図全体で重ならない（要件定義書 §2.1 / §6.6.8）」
        - line 560 §6.6.1 「自己交差、同一レイヤー重複、リング階層破綻」→「自己交差、末端地物の地図全体重複、リング階層破綻」
        - line 568 §6.6.3 結合の事前条件「同一レイヤーに属すること」は Phase 4（操作 UseCase 再設計）着手時改訂として現状.md §2-D-5 の「後続フェーズへ持ち越す改訂」に明示残置追加（結合事前条件は「結合対象が互いに上位下位関係にないこと」へ改訂予定）
        - line 580 §6.6.6 自動配色 line 580「同一レイヤー・現在の表示経度文脈・生値経度設計に揃えて評価」は Phase 6 改訂として既に現状.md §2-D-5 で明示残置済み
- 残置文言と理由:
    - `要件定義書.md` line 851 §2.4.4.3「同一レイヤー内で共有境界を持つ面が同色にならないように色を割り当てる」: Phase 6（自動配色）着手時に §2.2 計画通り改訂。本フェーズで先行同期しない理由は、自動配色の「同 depth 兄弟基準」への変更は仕様判断（4 色定理の適用単位）であり、Phase 6 で実装と同時に改訂する方が整合性が取れるため
    - `開発ガイド.md` line 568 §6.6.3 結合事前条件: Phase 4（操作 UseCase / Command 再設計）着手時に「同一レイヤーに属すること」を「結合対象が互いに上位下位関係にないこと」へ改訂。教訓は実装と同時改訂が筋
    - `開発ガイド.md` line 580 §6.6.6 自動配色: 上記 line 851 と同じく Phase 6 改訂
- 検証:
    - `grep -n "同一レイヤー" 要件定義書.md`: 残置 1 件（§2.4.4.3 line 851 のみ。Phase 6 射程として明示）
    - `grep -n "同一レイヤー" 開発ガイド.md`: 残置 2 件（§6.6.3 line 568 / §6.6.6 line 580。それぞれ Phase 4 / Phase 6 射程として明示）
    - `npx vitest run tests/presentation/app/appPolygonEditing.test.ts`: 20 passed（前回 19 → +1 件: 移行期間ノード / コンテナ除外回帰テスト）
    - `npm run check`: 0 errors / 0 warnings
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **開発ガイド §6.6.8** 「排他検証は仕様の対象範囲を実装で広げない」+「リーフ判定の運用: shape を保持し `childIds.length === 0` を必要十分条件とする。`isLeafPolygonAnchor` のような共通ヘルパーで判定をひとつに集約し、検証経路間でのドリフトを防ぐ」 → EdgeSlide は厳密には排他検証ではなく障害物検出だが、「末端地物排他は地図全体」のドメイン基本ルールから派生する制約として同じヘルパー (`isLeafPolygonAnchor`) を使うのが筋。経路間ドリフトを構造的に防ぐ
    - **開発ガイド §6.0.1 検出観点1** 「仕様文書での用語撤廃宣言時の同一文書同期」: 要件定義書 §2.1 で「末端地物排他は地図全体に適用」と明文化済みなのに §2.2.3 / §2.3.2 / §2.3.3 が「同一レイヤー」のまま残置 = self-contradiction。Phase 計画上は Phase 3 / Phase 4 まで保留予定だったが、本フェーズで実コードを地図全体へ揃えたため用語のみ先行同期。構造的改訂は各 Phase で実施
- **§6 への新規教訓追加要否**: 不要。既存教訓 §6.6.8 リーフ判定運用 / §6.0.1 検出観点1 の射程内で対応完了
- スコープ規模（追補込み）: src 1（書き直し） + tests 1（+1 件追加） + 要件定義書.md 6 箇所 + 現状.md 1 箇所 + 実装済み.md 1 エントリ。当初 3 ファイルから 4 + ドキュメント 3 になったが、§6.10 ルール内

---

## 2026-05-18 階層モデル新仕様 Phase 2 サブフェーズ D-6-3a: EdgeSlide collector の同一レイヤー絞り込み撤去 + 関数リネーム

現状.md §6.10 Phase 2-D-6-3「`placement.layerId` フィールド本体削除」の前段。Phase 2-D-6-1+2 完了後の grep で、`placement.layerId` 参照は src 22 + tests 29 = 計 51 ファイルに散らばっており、§6.10 スコープ管理ルール（~15 ファイル / ~300 行）を大幅に超える。`feedback_subphase_scope_split.md`「サブフェーズは1経路ずつ」に従い、D-6-3 を 3a / 3b / 3c に再分割した。本サブフェーズ 3a は独立した「EdgeSlide 経路の同一レイヤー絞り込み撤去 + 関数リネーム」に絞る。

- 経緯: Phase 2-D-1 で `ConflictDetectionService` が末端地物排他「地図全体」へ移行（要件定義書 §2.1 line 145-153 / 開発ガイド §6.6.8 遵守）した。一方 `edgeSlideConstraintCollectors.ts` の障害物検出は旧モデル『同一レイヤー内のみ障害物』のまま残存し、`collectSourceLayerIds(features, currentTime, sourceFeatureIds)` → `sourceLayerIds.has(anchor.placement.layerId)` で「ソース地物と同一レイヤー」の他ポリゴンだけを障害物として扱っていた。Phase 2-D-5 でユーザーが新規レイヤーを追加・削除できなくなった後は実質単一レイヤー前提のため runtime 観測挙動は変わらないが、§6.6.8 の「ドメイン基本ルールが仕様で対象を限定しているとき実装で対象範囲を広げない」の対概念として「実装で旧モデルの絞り込みを残してはいけない」が成立する。
- 変更内容（src 2 ファイル + tests 1 ファイル / +約 35 -約 35 行）:
    - `src/renderer/presentation/app/edgeSlideConstraintCollectors.ts`: 全書き直し。`collectSourceLayerIds` ヘルパー削除 + `sourceLayerIds` ローカル変数経由のフィルタ削除 + `placement.layerId` 参照削除。関数を `collectSameLayerPolygonObstacleRings` → `collectPolygonObstacleRings` / `collectSameLayerPolygonObstacleVertices` → `collectPolygonObstacleVertices` にリネーム（"SameLayer" 用語撤廃）。JSDoc に「Phase 2-D-1 で末端地物排他が地図全体へ移行したのと整合させて、Phase 2-D-6-3a で旧モデルの『同一レイヤー内のみ障害物として扱う』絞り込みを撤去した」旨を明記
    - `src/renderer/App.svelte`: import 2 箇所 + EdgeSlide 解決経路の呼び出し 2 箇所（`obstacleRings` / `obstaclePoints`）をリネーム後の関数名に更新
    - `tests/presentation/app/appPolygonEditing.test.ts`: import + 2 テストの呼び出し更新。テスト description を「同一レイヤーの他ポリゴンだけを...集める」→「ソース地物を除く全ての面地物の...を集める」へ書き換え + 期待値修正（other-layer の `c1..c4` リングも障害物に含まれる: `rings` は 1 → 2 要素 / `points` は 4 → 8 要素）。コメントで Phase 2-D-6-3a の意図を記録
- 振る舞い変更: 別レイヤーの面地物も EdgeSlide 障害物に含むようになる。Phase 2-D-5 以降は実質単一レイヤー前提のため、runtime 観測挙動の変化は実質ゼロ（dead semantics 撤去）
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **開発ガイド §6.6.8** 「排他検証は仕様の対象範囲を実装で広げない」: 「ドメインの基本ルール（排他性・整合性・階層）が仕様で明示的に対象を限定しているとき、実装で『対称性確保』『自然な拡張』を理由に対象範囲を広げない」+「リーフ判定の運用: shape を保持し `childIds.length === 0` を必要十分条件とする」 → 逆向きの応用。本フェーズは「実装が仕様の対象範囲（地図全体）より狭い旧モデル絞り込みを残していた」事案で、§6.6.8 と同型の整合性ズレ。EdgeSlide も地図全体の障害物を見る形に修正
    - **開発ガイド §6.3.13** 「optional フィールド導入は全層を1サブフェーズに詰めない」+ メモリ `feedback_subphase_scope_split.md`「サブフェーズは1経路ずつ」: D-6-3 全体（51 ファイル）を 1 サブフェーズに詰めず、3a / 3b / 3c に分割。3a はスコープ 3 ファイル / +約 35 -約 35 行で §6.10 上限内
    - **開発ガイド §6.0.1 検出観点1** 「仕様文書での用語撤廃宣言時の同一文書同期」: 撤廃語 `SameLayer` / `sourceLayerIds` を `grep -rn "collectSameLayer\|sourceLayerIds"` で src/tests を一括検索し、漏れがないことを確認（残置 0 件）
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npx vitest run` 1477 passed（97 ファイル、件数増減なし — テスト description / 期待値の変更のみ）
- **§6 への新規教訓追加要否**: 不要。既存教訓 §6.6.8（排他検証は仕様の対象範囲を実装で広げない）の対概念射程内。本フェーズは「実装が仕様より狭い旧モデル絞り込みを残していた」事案で、§6.6.8 の「対称性確保を理由に広げない」の逆向きとして同教訓がそのまま該当
- **次サブフェーズ（D-6-3b）への持ち越し**: `placement.layerId` フィールド本体 / `createAnchorPlacement` シグネチャ / 残る src consumer 12 ファイルの `anchor.placement.layerId` 参照
- **D-6-3c への持ち越し**: JSON 永続化 4 ファイル + テスト fixture 29 ファイルの `layerId:` リテラル

---

## 2026-05-18 階層モデル新仕様 Phase 2 サブフェーズ D-6-1+2 追補: shape あり + childIds 非空（移行期間ノード）でも 4 経路の解決済み座標が一致する構造へ

レビュー指摘「FeatureRenderer.svelte は childIds.length > 0 の Polygon で deriveParentShape() を使って描画するが、hitTestUtils.ts と mapCanvasUtils.ts は anchor.shape をそのまま使うため、shape あり + childIds 非空 の移行期間ノードで描画領域とクリック可能領域 / wrapOffsets がずれる（開発ガイド §6.6.9）」を受けて、sceneEntries に解決済み描画形状 `polygonRings` を持たせ、3 経路（FeatureRenderer / hitTest / wrapOffsets）すべてがその同一座標列を参照する形に統一した。

続くレビュー指摘「ラベルだけ旧 anchor.shape を見ています。FeatureRenderer.svelte は LabelRenderer に anchor だけ渡し、labelRendererUtils.ts は anchor.shape.rings[0] から重心/面積を計算します。shape あり + childIds 非空 で polygonRings が派生形状を採用した場合、描画領域とラベル位置・表示判定がずれます。これは開発ガイド §6.6.9 の『LabelRenderer も同じ派生形状を受け取る』に未対応です」を受けて、LabelRenderer / labelRendererUtils も `polygonRings` を受け取れるようにし、Polygon ラベルの重心・面積判定が描画と同じ座標で行われるよう統一した（4 経路目の対応完了）。

- 変更内容（実装ロジックの主要箇所）:
    - `mapSceneEntries.ts` の `MapSceneEntry` に `polygonRings: ReadonlyArray<RingCoords> | null` を追加。Polygon entry のみ非 null。`collectMapSceneEntries(features, currentTime, vertexCoordinates)` のシグネチャに `vertexCoordinates: ReadonlyMap<string, Coordinate>` を追加し、`resolvePolygonRings()` ヘルパー内で「リーフ（childIds 空）は自身の shape を vertex 解決」「childIds 非空は `deriveParentShape` で派生形状を計算（多 polygon の落とし込みや cycle guard は Phase 2.5-A の正式実装に依存）」「派生空 + shape あり = 移行期間ノードは shape fallback」のルールで rings を解決
    - `hitTestUtils.ts` の `hitTestPolygon(clickLon, clickLat, polygonRings)` シグネチャに変更。`anchor.shape` の vertexIds を内部で解決する経路を撤去し、sceneEntries の解決済み `polygonRings` を直接 evenodd 判定する
    - `mapCanvasUtils.ts` の `computeRenderWrapOffsets` 内 `getAnchorLongitudeBounds` を `getSceneEntryLongitudeBounds(entry, vertices)` に置換。Polygon は `entry.polygonRings` の経度範囲を集計（Point / LineString は引き続き vertex マップ参照）
    - `FeatureRenderer.svelte` の `getPolygonPath(feature, anchor)` を `getPolygonPath(polygonRings)` に変更し、内部で `buildPolygonPathFromCoords(polygonRings)` のみを呼ぶ。`deriveParentShape` の直接呼び出し / `vertexCoordinates` 派生 / `features` props / `currentTime` props を撤去（解決は sceneEntries 経由に一本化）
    - `App.svelte` で `vertexCoordinates` derive を `FeatureRenderer.svelte` から持ち上げ、`collectMapSceneEntries(features, currentTime, vertexCoordinates)` に渡す
    - `MapCanvasSvgLayers.svelte` から `features` / `Feature` import / `<FeatureRenderer {features} {currentTime}>` 引き渡しを撤去（FeatureRenderer が依存しなくなったため）
    - **LabelRenderer 統一**: `labelRendererUtils.ts` の `getFeatureLabelPosition` / `measureFeatureLabelArea` / `shouldRenderFeatureLabel` の Polygon 経路に optional `polygonRings` 引数を追加し、共通ヘルパー `getPolygonOuterCoords` で「`polygonRings` 優先、無ければ `anchor.shape` から vertex 解決」のロジックを 1 箇所に集約。`LabelRenderer.svelte` に `polygonRings` props を追加し、`FeatureRenderer.svelte` の `<LabelRenderer>` 呼び出しで sceneEntries から取得した同一座標列を渡す。これによりラベル位置・面積判定も描画/hitTest/wrapOffsets と同じ座標を参照する（開発ガイド §6.6.9: 「LabelRenderer のラベル位置・面積判定も `containerPolygons` 引数で同じ派生形状を受け取り、描画と一致させる」）
    - **型表記の統一**: `mapSceneEntries.ts` に `export type PolygonRings = ReadonlyArray<RingCoords>` を新設し、`MapSceneEntry.polygonRings` / `hitTestUtils.hitTestPolygon` / `labelRendererUtils.ts` / `LabelRenderer.svelte` / `FeatureRenderer.getPolygonPath` の各引数・props 型を `PolygonRings | null` に統一。レビュー Low 指摘「`ReadonlyArray<RingCoords>` / インライン `ReadonlyArray<readonly { x: number; y: number }[]>` / ローカル `PolygonRings` エイリアスの表記ぶれ」を解消し、将来の責務変更時の対概念リスク（消費者間で型ドリフトが起こる）を抑制
- 回帰テスト:
    - `tests/presentation/components/mapSceneEntriesIntegration.test.ts` に「shape あり + childIds 非空 の移行期間ノード（§6.6.9 射程）」describe を新設し 2 件追加: 「派生が空のとき 3 経路が親自身の shape rings に揃う」「派生が空でない子を持つとき 3 経路が派生形状（子の和）に揃う」。後者は親 shape の広い領域（-50..50）と子の狭い領域（0..10）を区別し、`hitTest(25, 25)` が親 shape 内だが派生形状外でヒットしないこと、`wrapOffsets` の経度範囲も派生形状ベース（基本タイルのみ）であることを固定。**さらに両ケースとも `getFeatureLabelPosition` / `measureFeatureLabelArea` を `polygonRings` 経由で呼び、ラベル重心・面積が描画と同じ座標で算出されることを固定**
    - `tests/presentation/components/labelRendererUtils.test.ts` に「polygonRings を渡すと anchor.shape ではなくその座標で面積を計算する」「polygonRings を渡すと anchor.shape ではなくその座標の重心がラベル位置になる」2 件を追加（labelRendererUtils 単体テスト）
    - `tests/presentation/components/mapSceneEntries.test.ts` に Polygon リーフの `polygonRings` 解決テストと Point/LineString の `polygonRings = null` テストを追加（+2 件）
    - 既存テスト 3 ファイル（hitTestUtils / vertexSelectionContext / mapCanvasUtils）の `collectMapSceneEntries` 呼び出しを新シグネチャ（vertexCoordinates 引数追加）に対応
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **開発ガイド §6.6.9** 「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」: 「判定条件のドリフトがあると、画面では描画されているのに選択できない、ラベル位置が描画と異なる、移行期間ノードの祖先派生が落ちる、といった対概念矛盾が次々と出る」+「検出観点: 『描画される領域 = 選択できる領域 = ラベル位置の基準形状』を回帰テストで固定する。多段階層・離れた孫リーフ・shape あり + childIds 非空 の各ケースで、3経路の結果が一致することを確認する」+「`LabelRenderer` のラベル位置・面積判定も `containerPolygons` 引数で同じ派生形状を受け取り、描画と一致させる」 → 本フェーズの核心。`polygonRings` を 1 箇所で解決し、**4 経路（FeatureRenderer 描画 / hitTest / wrapOffsets / LabelRenderer）すべてがそれを参照する形**にした
    - **開発ガイド §6.1.2** 「非表示要素の選択・操作の混入」: 引き続き本フェーズの設計原則。sceneEntries 共通中間表現は「描画されるもの = クリックできるもの = 頂点所有者 = wrapOffsets 寄与 = ラベル位置基準」を構造で保証
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npx vitest run` 1477 passed（前回 1471 → 今回 1477 で +6 件: mapSceneEntries Polygon リーフテスト +1 / Point の polygonRings null テスト +1 / 移行期間ノード 3 経路一致テスト +2 / labelRendererUtils polygonRings 優先テスト +2）
- **§6 への新規教訓追加要否**: 不要。既存教訓 §6.6.9 の「shape あり + childIds 非空ケースの 4 経路（描画 / hitTest / wrapOffsets / LabelRenderer）一致を回帰テストで固定」がそのまま該当
- **次フェーズ（D-6-3）への持ち越し**（変更なし）:
    - `FeatureAnchor.placement.layerId` フィールド本体削除 + serialize / migration / validation
- **Phase 2-D-7 への持ち越し**（レビュー Low/Info 由来、本フェーズで変更なし）:
    - `<MapCanvas {features}>` props は D-6-1+2 完了時点で `selectionAnchor` derive の `features.find(...)` のみで使用。選択中地物が必ず sceneEntries に含まれる前提が成立すれば `sceneEntries` 経由に置換可能だが、「sceneEntries に入らない時刻外の選択中地物」の取り扱いを D-7 着手時に検証してから撤去する。Phase 2-D-6-1+2 完了時のレビュー Low / Info 由来として記録
- **次々フェーズ（Phase 2.5-A）への持ち越し**（前提依存の明示）:
    - 本フェーズの `polygonRings` 解決は `deriveParentShape` に依存する。現状 `deriveParentShape` は Phase 2-C 時点の暫定実装（`polygonUnion + polygons[0]` で多 polygon 片落とし / 孫の再帰下降なし / cycle guard なし）。`polygonRings` の正確性は Phase 2.5-A での `polygonUnionAll` + 再帰下降 + cycle guard の正式実装で完成する。containers が実データに無い間は問題なし

---

## 2026-05-16 階層モデル新仕様 Phase 2 サブフェーズ D-6-1+2: sceneEntries 共通中間表現の導入 + 描画/hitTest/頂点選択/wrapOffsets の一元化

- 現状.md §6.10 Phase 2-D の 6 番目のサブフェーズ。当初は D-6-1（hitTest + 頂点選択経路の layerId 撤去）と D-6-2（描画経路の layerId 撤去）に分割していたが、D-6-1 着手後のレビュー（FB「入力経路と描画経路の不整合を根本対応する」）で「**hitTest 側はレイヤー可視性で絞らない** が **`FeatureRenderer.svelte` 側は `Layer.visible` で描画対象を絞っている**ため、『画面に描画されないがクリック選択・頂点所有者解析・wrapOffsets に混入する』中間状態が発生する（開発ガイド §6.1.2 / §6.6.9 / §6.0.1 検出観点2 違反）」と指摘され、`sceneEntries` 共通中間表現で 4 経路を一元化する形で D-6-1 と D-6-2 を統合した
- レビュー方針: `features + currentTime + vertices` から、地図上で操作対象となる共通の `sceneEntries` を生成し、描画・ヒットテスト・頂点選択コンテキスト・wrapOffsets が同じ集合を参照する構造へ作り替える。「見えているもの = クリックできるもの = 頂点所有者として扱うもの = wrapOffsets に影響するもの」を同じ中間表現から導く
- スコープ規模（15 ファイル / +661 -670 行 = `git diff --numstat -- src tests` + 削除 + 新規行数集計値。`現状.md` / `実装済み.md` 更新分は除外）:
    - **新規**（3 ファイル / +373 行）:
        - `src/renderer/presentation/components/mapSceneEntries.ts`（+47）: `MapSceneEntry` 型 + `collectMapSceneEntries(features, currentTime)` を定義。現在時刻にアクティブで shape を保持する地物だけを集めて入力順 + 連番 (`featureIndex`) を割り当てる
        - `tests/presentation/components/mapSceneEntries.test.ts`（+131）: ユーティリティの単体テスト（連番割当 / 多レイヤー連番 / コンテナ除外 / 非アクティブ除外 / 空入力 / undefined currentTime / 入力順保持 の 7 件）
        - `tests/presentation/components/mapSceneEntriesIntegration.test.ts`（+195）: **回帰テスト**。同じ sceneEntries から (1) hitTest / (2) `buildSceneVertexOwnerMap` / (3) `computeRenderWrapOffsets` の 3 経路 + 描画対象集合が一致することを 3 ケースで固定（4 経路一致 / 非アクティブ除外連動 / コンテナ除外連動）。FB「sceneEntries へ統合し、描画・入力・頂点選択・wrapOffsets の対象一致をテストで固定できればコミット可能」に対する応答
    - **削除**（2 ファイル / -198 行）:
        - `src/renderer/presentation/components/featureRendererUtils.ts`（-38）: `collectActiveFeatureEntries` を `mapSceneEntries.ts` の `collectMapSceneEntries` に置換したため撤去
        - `tests/presentation/components/featureRendererUtils.test.ts`（-160）: 上記置換に伴い対応テストも `mapSceneEntries.test.ts` に再構成
    - **変更**（10 ファイル / +288 -472 行）:
        - `src/renderer/presentation/components/FeatureRenderer.svelte`（+132 -157）: `layers` props 撤去 + `visibleLayers` / `visibleLayerIds` derive 撤去 + `getLayerFeatures(layerId)` 撤去 + `{#each visibleLayers as layer}` レイヤー単位 `<g opacity={layer.opacity}>` グルーピング撤去。代わりに props で `sceneEntries: readonly MapSceneEntry[]` を受け取り、`{#each sceneEntries as { feature, anchor, featureIndex } (feature.id)}` で直接描画。自動配色は sceneEntries 全体で 1 回算出
        - `src/renderer/infrastructure/rendering/hitTestUtils.ts`（+12 -45）: シグネチャを `hitTest(clickCoord, sceneEntries, vertices, thresholdDeg)` に変更。`Feature` / `Layer` / `TimePoint` import 撤去（`MapSceneEntry` で代替）。ループも `for (const { feature, anchor } of sceneEntries)` に簡素化
        - `src/renderer/infrastructure/rendering/vertexSelectionContext.ts`（+14 -20）: `buildVisibleVertexOwnerMap(features, currentTime)` → `buildSceneVertexOwnerMap(sceneEntries)` に**改名 + シグネチャ変更**（D-6-2 で持ち越し計画だったリネームも今フェーズで完了）。`Feature` / `Layer` / `TimePoint` import 撤去。`isFile.visible` フィルタも撤去
        - `src/renderer/presentation/components/mapCanvasUtils.ts`（+7 -20）: `computeRenderWrapOffsets(viewBox, features, vertices, currentTime, options)` → `computeRenderWrapOffsets(viewBox, sceneEntries, vertices, options)` に変更。`Feature` / `TimePoint` import 撤去、内部の `feature.getActiveAnchor(currentTime)` ループを sceneEntries 走査に簡素化
        - `src/renderer/presentation/components/MapCanvas.svelte`（+7 -20）: props に `sceneEntries: readonly MapSceneEntry[]` を追加し `layers` props を撤去。`visibleLayerIds` derive 撤去。`vertexHandleEntries` は `for (const { feature, anchor } of sceneEntries)` に変更。`computeRenderWrapOffsets` 呼び出しも sceneEntries 渡しに変更
        - `src/renderer/presentation/components/mapCanvas/MapCanvasSvgLayers.svelte`（+4 -4）: props に `sceneEntries` 追加 + `layers` 撤去 + `Layer` import 撤去。`<FeatureRenderer>` 呼び出しも `{sceneEntries}` 渡しに変更
        - `src/renderer/App.svelte`（+15 -12）: `collectMapSceneEntries` import 追加 + `sceneEntries = $derived(collectMapSceneEntries(features, currentTime))` を新設。`buildVisibleVertexOwnerMap` → `buildSceneVertexOwnerMap(sceneEntries)` 改名 + シグネチャ変更。**変数名も `visibleVertexOwnerMap` → `sceneVertexOwnerMap` にリネームし、`selectedVertexOwnerFeatureIds` derive（line 302）/ `collectOwnerFeatureIdsForVertices` 関数内（line 633）/ ボックス選択確定経路（line 1630）の 3 箇所の参照も同期更新**。`hitTest` 呼び出し 2 箇所を sceneEntries ベースに。`<MapCanvas {layers}>` の渡しを撤去し `{sceneEntries}` を渡す
        - `tests/infrastructure/rendering/hitTestUtils.test.ts`（+55 -159）: 全 `hitTest(...)` 呼び出しを `collectMapSceneEntries([...features], time)` 経由の sceneEntries ベースに書き換え。「sceneEntries 経路の制約」describe を新設（描画対象外は hitTest 対象外 / 空 sceneEntries で null）+2 件、旧「レイヤーとフィルタリング」テスト撤去 -2 件 + 旧「時間軸と空集合」を再構成（差し引き 1 件減）
        - `tests/infrastructure/rendering/vertexSelectionContext.test.ts`（+21 -11）: `buildVisibleVertexOwnerMap` → `buildSceneVertexOwnerMap(sceneEntries)` への移行。旧 hidden layer フィルタ用テストを撤去し「空 sceneEntries で空 ownerMap」テストへ置換
        - `tests/presentation/components/mapCanvasUtils.test.ts`（+21 -24）: 全 `computeRenderWrapOffsets(...)` 呼び出しを sceneEntries 経由に書き換え。「sceneEntries に含まれない地物は wrapOffsets 算出に寄与しない」テスト +1 件（対概念整合性固定）
- §6.10 スコープ管理ルールとの関係: ファイル数 15 = 上限ちょうど、行数 +661 / -670 で 300 行を超過。FB「コミット可否: このFB対応前のD-6-1単独では修正が必要。sceneEntries へ統合し対象一致をテストで固定できればコミット可能」の指示により D-6-1 と D-6-2 を統合する判断は確定済み。-670 行のうち -198 は削除のみのファイル（featureRendererUtils 一式）、+373 は新規回帰テスト + 新規ユーティリティ + テスト。実装ロジックの実質変更行数は約 ±300 行で、レビュー往復のリスクは抑えられる範囲
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **開発ガイド §6.1.2** 「非表示要素の選択・操作の混入」: 「ユーザー操作の対象は『現在UI上に表示されている要素』に限定する。データソース全体を直接検索せず、Svelte のリアクティブ状態（`$derived` 等）が管理する表示状態を基準にする」 → 本フェーズの核心。`features` を直接走査する各経路（hitTest / 頂点選択 / wrapOffsets / 描画）を `sceneEntries` 共通中間表現経由に統一することで、「描画されないものが入力対象になる」状態を構造的に排除
    - **開発ガイド §6.6.9** 「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」: 「判定条件のドリフトがあると、画面では描画されているのに選択できない、ラベル位置が描画と異なる、移行期間ノードの祖先派生が落ちる、といった対概念矛盾が次々と出る」 → sceneEntries が「shape を持つ + 現在時刻アクティブ」の判定を 1 箇所に集約し、全 4 経路がこの集合を参照する。回帰テスト（`mapSceneEntriesIntegration.test.ts`）で同じ sceneEntries から各経路の結果が一致することを固定
    - **開発ガイド §6.0.1 検出観点2** 「対概念の同期」: 「描画されるもの ↔ クリックできるもの」「描画されるもの ↔ 頂点所有者として扱われるもの」「描画されるもの ↔ wrapOffsets 算出に寄与するもの」の対概念をすべて sceneEntries で同期。D-6-1 で hitTest 側のみ撤去した結果として生じた中間状態（前回完了報告で§6.1.2 違反として記録した状態）を本フェーズで解消
    - **開発ガイド §6.0.1 検出観点1**（書く前/書いた後の grep による全用例同期）: 本フェーズで `buildVisibleVertexOwnerMap` → `buildSceneVertexOwnerMap` と関数を改名した際、App.svelte 内の **変数名 `visibleVertexOwnerMap` の使用箇所 3 つ**（`selectedVertexOwnerFeatureIds` derive / `collectOwnerFeatureIdsForVertices` 関数 / ボックス選択確定経路）を最初の編集で取りこぼし、`npm run check` 通過のままレビュー指摘で発覚した（複数頂点ドラッグ / 矩形選択確定で実行時 `ReferenceError` になる経路）。Svelte 5 の `$derived` の left-hand identifier 改名は **同名で参照している全箇所** を grep して同期更新する必要がある。`npm run check`（svelte-check）は `.svelte` ファイル内のスクリプトブロックの未定義参照を一部見逃すケースがあるため、識別子改名時は型チェックに頼らず必ず手 grep で網羅する
    - **開発ガイド §6.2.25** 「クリック経路の最終決定は一元化する」採用ポリシー: 「ソートの優先順位は (1) layerOrder 降順、(2) depth 降順（深い子側を優先）、(3) DOM target 一致」 → 本サブフェーズで layerOrder を撤去する段階。Phase 2.5-C で hitTest を最終決定に格上げし、`preferredFeatureId` に DOM target を渡す 3 キー tie-break へ再設計する
    - **開発ガイド §6.3.13** 「optional フィールド導入は全層を1サブフェーズに詰めない」: 本来 D-6-1 / D-6-2 に分割していたが、FB により統合が必要と判明。教訓の「~15 ファイル / ~300 行」を行数面で超過するが、削除（-198）と新規回帰テスト（+195）が大半を占め、実装ロジック実質変更は ±300 行以内。本教訓は「1つの責務を1サブフェーズに集中させる」趣旨であり、本フェーズは「描画/入力/頂点/wrapOffsets を sceneEntries に一元化する」という単一責務に集中している
    - **memory/feedback_break_existing_freely.md** 「既存動作の変更を遠慮しない」 → `<g opacity={layer.opacity}>` レイヤー単位グルーピングを撤去する判断は、Layer 機能撤去の方針上後戻りなし
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npx vitest run` 1471 passed（97 ファイル、件数: 1467 → 1471 で +4 件）。差分内訳: -8 件（`featureRendererUtils.test.ts` 全削除）+ -2 件（hitTestUtils「レイヤー優先」「非表示レイヤー」撤去）+ -1 件（mapCanvasUtils「非表示レイヤー除外」撤去）+ +7 件（mapSceneEntries.test.ts 新規）+ +3 件（mapSceneEntriesIntegration.test.ts 新規）+ +2 件（hitTestUtils「sceneEntries 経路の制約」describe 新設）+ +1 件（vertexSelectionContext「空 sceneEntries」追加 -1+1）+ +1 件（mapCanvasUtils「sceneEntries 外は寄与しない」追加）+ +1 件（hitTestUtils 内分割計上による調整）= 計 -11 + +15 = +4 件
- **§6 への新規教訓追加要否**: 不要。既存教訓 開発ガイド §6.1.2（非表示要素の混入）/ §6.6.9（描画・ヒットテスト・派生サービスの shape 解決経路共有）/ §6.0.1 検出観点2（対概念同期）の射程内。本フェーズは既存教訓に挙げられた問題を、共通中間表現で構造的に解決する応用ケースで、新規教訓化を要する未知の問題はなし
- **手動動作確認手順**:
    1. `npm run dev` で起動済みの dev サーバーに接続（ブラウザでアプリを開く）
    2. 地物を追加して描画されることを確認
    3. 地物の上をクリック → 選択できること（hitTest 経路）
    4. 頂点ハンドルが描画され、頂点クリックで選択できること（頂点選択コンテキスト経路）
    5. 720度を超える経度の地物を追加し、ラップタイルが描画されること（wrapOffsets 経路）
    6. **対概念整合性確認**: 4 経路すべてが同じ集合を扱うこと（描画される地物がすべて選択でき、頂点も操作でき、wrapOffsets にも寄与する）
    7. 時間軸を動かしてアクティブ錨が切り替わる地物を観察 → 非アクティブ時刻では描画も選択も頂点ハンドルも消えること
- **影響**:
    - hitTest はレイヤー可視性で絞らず、最初にヒットした地物を返す。同点複数候補時の上位レイヤー優先 tie-break は消滅（Phase 2.5-C で depth + DOM target ベースに再設計予定）
    - 自動配色は地図全体（sceneEntries 全体）で 1 回算出（旧仕様のレイヤー単位グルーピングは消滅）。`<g opacity={layer.opacity}>` 経由のレイヤー透明度反映機能も撤去（Phase 6 で個別 style ベースに再設計予定）
    - `buildSceneVertexOwnerMap` は Layer.visible フィルタを撤去し、sceneEntries に含まれる全地物を対象とする
    - default 1 レイヤー / 全 visible=true 前提では旧=新で実害なし。互換破棄方針（§6.10 / 現状.md §6.2）の射程
- **次フェーズ（D-6-3）への持ち越し**:
    - `FeatureAnchor.placement.layerId` フィールド本体削除 + `createAnchorPlacement` シグネチャ変更
    - `JsonFeatureAnchor.placement.layerId` + serialize / deserialize / migration / `validateJsonWorld`
    - 全 consumer の `anchor.placement.layerId` 参照撤去（30 ファイル超 → 本フェーズで FeatureRenderer 経路は消えたため減少）
- **Phase 2.5-C への持ち越し**（前回レビュー Info 由来、本フェーズで変更なし）:
    - `App.svelte:1057-1062` の `clickedFeatureId`（DOM 由来 `data-feature-id`）即採用パス → hitTest を最終決定に格上げし、`preferredFeatureId` に DOM target を渡す 3 キー tie-break へ再設計
- **Phase 2-D 完全終了時の持ち越し**（前回レビュー Low 任意由来、本フェーズで変更なし）:
    - コード内 JSDoc / コメントの `Phase 2-D-X` / `Phase 2-C-X` / `Phase 2-B` 等の phase 参照を一括棚卸し

> 本セッションでは当初 D-6-1（hitTest + 頂点選択経路の layerId 撤去）単独として着手したが、
> レビュー FB「入力経路と描画経路の不整合を根本対応する」を受けて D-6-2（描画経路の layerId
> 撤去）と統合し、上記の D-6-1+2 統合エントリへ集約した。D-6-1 単独でのコミットは行っていない。

<!-- D-6-1 単独エントリは D-6-1+2 統合エントリへ吸収済み。記録のため見出しのみ残置:

## 2026-05-16 (skipped) 階層モデル新仕様 Phase 2 サブフェーズ D-6-1（コミットなし、D-6-1+2 統合へ吸収）

- 現状.md §6.10 Phase 2-D の 6 番目のサブフェーズ。Phase 2-D-6 全体（src 30 ファイル / tests 30 ファイル）は §6.10 スコープ管理ルール（~15 ファイル / ~300 行）を超えるため、D-6-1 / D-6-2 / D-6-3 に再分割し、本サブフェーズでは hitTest 経路 + 頂点選択コンテキスト経路の `placement.layerId` 依存のみを撤去する
- スコープ: (1) `hitTestUtils.ts` の `layers` 引数 + `visibleLayerIds` フィルタ + `layerPriority` tie-break + `Layer` import 撤去、(2) `vertexSelectionContext.ts` の `layers` 引数 + `visibleLayerIds` フィルタ + `Layer` import 撤去、(3) `mapCanvasUtils.ts` の `computeRenderWrapOffsets` から `options.visibleLayerIds` 撤去、(4) `MapCanvas.svelte` の `visibleLayerIds = $derived(...)` state + `vertexHandleEntries` 内フィルタ + `computeRenderWrapOffsets` 呼び出し引数の撤去、(5) `App.svelte` の `hitTest` 呼び出し2箇所 + `buildVisibleVertexOwnerMap` 呼び出しの `layers` 引数撤去、(6) 対応する3テストファイルの調整
- 変更内容（8 ファイル: +28 / -143 行 = `git diff --numstat -- src tests` 集計値。`現状.md` / `実装済み.md` 更新分は除外）:
    - `src/renderer/infrastructure/rendering/hitTestUtils.ts`:
        - `Layer` import 撤去（1 行）
        - `hitTest(...)` シグネチャから `layers: readonly Layer[]` 引数を撤去（1 行）
        - 関数本体から `visibleLayerIds` 構築（3 行）+ `layerPriority` 構築（4 行）+ `candidates: Array<...layerOrder>` 配列（5 行）+ `visibleLayerIds.has(...)` フィルタ（1 行）+ `candidates.push(...layerOrder)`（5 行）+ candidates.sort + 戻り値構築（5 行）を撤去
        - 簡素化された hit ループは「最初にヒットした地物を即 return」する形へ。同点複数候補があった場合の上位レイヤー優先 tie-break は消滅（Phase 2.5-C で depth + DOM target ベースに再設計予定）
        - JSDoc に Phase 2-D-6-1 と Phase 2.5-C の説明を追記
    - `src/renderer/infrastructure/rendering/vertexSelectionContext.ts`:
        - `Layer` import 撤去（1 行）
        - `buildVisibleVertexOwnerMap(...)` シグネチャから `layers: readonly Layer[]` 引数を撤去（1 行）
        - 関数本体から `visibleLayerIds` 構築（5 行）+ `!visibleLayerIds.has(...)` フィルタ（4 行 → 3 行）を撤去
        - JSDoc に Phase 2-D-6-1 説明を追記
    - `src/renderer/presentation/components/mapCanvasUtils.ts`:
        - `computeRenderWrapOffsets(...)` の `options` 型から `visibleLayerIds?: ReadonlySet<string>` を撤去（1 行）
        - ループ内の `if (options?.visibleLayerIds && !options.visibleLayerIds.has(...)) continue;` を撤去（3 行）
    - `src/renderer/presentation/components/MapCanvas.svelte`:
        - `visibleLayerIds = $derived(new Set(...))` state を撤去（3 行）
        - `vertexHandleEntries()` 内の `!visibleLayerIds.has(anchor.placement.layerId)` フィルタを撤去し、`!anchor` のみへ簡素化（2 行）
        - `computeRenderWrapOffsets` 呼び出しのオプションから `visibleLayerIds` を撤去（4 行 → 1 行）
    - `src/renderer/App.svelte`:
        - `buildVisibleVertexOwnerMap(features, layers, currentTime)` から `layers` 引数撤去（1 行）
        - `hitTest(...)` 呼び出し2箇所（line 1066-1073 / line 1513-1520）から `layers` 引数撤去（2 行）
    - `tests/infrastructure/rendering/hitTestUtils.test.ts`:
        - `Layer` import 撤去（1 行）
        - 全 `hitTest(...)` 呼び出し16箇所から `layers` 引数撤去
        - `describe('レイヤーとフィルタリング')` を `describe('時間軸と空集合')` にリネームし、「非表示レイヤーの地物はヒットしない」「上位レイヤーの地物が優先される」テスト 2 件を撤去（-2 件）
        - 残テスト「現在時刻でアクティブでない地物はヒットしない」「地物がなければnull」は `time` describe へ移動
    - `tests/infrastructure/rendering/vertexSelectionContext.test.ts`:
        - `Layer` import 撤去（1 行）
        - `visibleLayer` / `hiddenLayer` 定数撤去（2 行）
        - `buildVisibleVertexOwnerMap(...)` 呼び出し全箇所から `layers` 引数撤去（5 箇所）
        - 「現在時刻かつ可視レイヤー上の所有者だけを集計する」テスト本文から `hidden` レイヤー所属 feature と `[visibleLayer, hiddenLayer]` 引数を撤去し、「現在時刻にアクティブな錨の所有者を集計する」へ書き換え（テスト件数増減なし）
    - `tests/presentation/components/mapCanvasUtils.test.ts`:
        - 「720度超の地物でも可視コピーに必要なラップタイルを含める」テストから `{ visibleLayerIds: new Set(['layer-1']) }` オプション撤去（1 行）
        - 「非表示レイヤーの地物は追加ラップタイル算出に含めない」テスト全体を撤去（-1 件、18 行）
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **§6.3.13** 「optional フィールド導入は全層を1サブフェーズに詰めない」: 「1サブフェーズあたりの差分は ~15 ファイル / ~300 行を上限に分割する」「着手前に『触る経路』を最大2層で宣言する」 → Phase 2-D-6 全体は src 30 ファイル / tests 30 ファイルで上限を超えるため、D-6-1 / D-6-2 / D-6-3 に分割。D-6-1 は hitTest + 頂点選択経路のみで合計 8 ファイル / +20 -120 行（実態は -100 行差分）。`FeatureRenderer.svelte` の `visibleLayerIds.has` 描画フィルタ + `{#each visibleLayers}` グルーピング、`featureRendererUtils.ts` の `visibleLayerIds` 引数、`placement.layerId` フィールド本体には触らない（D-6-2 / D-6-3 で対応）
    - **§6.6.8** 「排他検証は仕様の対象範囲を実装で広げない」: 排他検証経路（`ConflictDetectionService` / `PolygonValidationService`）には触らない。Phase 2-D-1 で撤去済みの範囲を維持。hitTest 経路の `visibleLayerIds` フィルタ撤去は排他検証とは独立（hitTest は選択判定のみで排他判定に関与しない）
    - **開発ガイド §6.2.25** 「クリック経路の最終決定は一元化する」採用ポリシー: 「ソートの優先順位は (1) layerOrder 降順、(2) depth 降順（深い子側を優先）、(3) DOM target 一致」 → 本サブフェーズで layerOrder を撤去する段階で、depth / DOM target は Phase 2.5-C で導入する。撤去後の hitTest は「最初にヒットした地物を返す」になるが、`MapCanvas.svelte` の DOM `data-feature-id` 取得経路が clicked id を優先するため、default 1 レイヤー前提では重なりケースの実害なし
    - **開発ガイド §6.0.1 検出観点2** 「対概念の同期」: 「レイヤー可視性フィルタ撤去 ↔ default 1 レイヤー / visible=true 前提では実質 no-op」「layerOrder tie-break 撤去 ↔ Phase 2.5-C で depth + DOM target ベースに再設計予定」と対概念を明示。さらに D-6-1 と D-6-2 の間に「**hitTest 側はレイヤー可視性で絞らない** が **`FeatureRenderer.svelte` 側は `visibleLayerIds.has(...)` で可視レイヤーのみ描画する**（D-6-2 で撤去予定）」という中間状態の対概念矛盾が発生する。`FeatureRenderer.svelte:53-56,71,114` 該当
    - **開発ガイド §6.1.2** 「非表示要素の選択・操作の混入」: 「ユーザー操作の対象は『現在UI上に表示されている要素』に限定する。データソース全体を直接検索せず、Svelte のリアクティブ状態（`$derived` 等）が管理する表示状態を基準にする」 → 上記中間状態は本教訓の射程内（描画されないが hitTest で選択可能になりうる）。本サブフェーズ単独では教訓に違反する状態を残す形になるが、以下の二重防御で**実害は発生しない**: (1) Phase 2-D-3 で `LayerPanel.svelte` 削除、Phase 2-D-5 で `ProjectSettingsDialog` レイヤー編集セクション撤去により、`Layer.visible = false` を作成・編集する UI 入口が消滅済み、(2) default 1 レイヤー / `visible: true` 固定で `App.svelte:841-842` で初期化される、(3) 旧 `.gimoza` で hidden レイヤーを含むケースは互換破棄方針（§6.10 / 現状.md §6.2）の射程。D-6-2 で `FeatureRenderer.svelte` 側の可視性フィルタを撤去すれば対概念矛盾は解消する
    - **memory/feedback_subphase_scope_split.md** 「サブフェーズは1経路ずつ」 → 描画経路（FeatureRenderer / featureRendererUtils）には踏み込まない（D-6-2 のスコープ）。上記 §6.1.2 中間状態を D-6-1 単独で解消しようとすると描画経路まで踏み込む必要があり、本ルールに反するため意図的に「中間状態を作る」設計判断とした
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npx vitest run` 1467 passed（96 ファイル、件数: 1470 → 1467 で hitTest「非表示レイヤー」「上位レイヤー優先」テスト -2 件 + mapCanvasUtils「非表示レイヤーの地物は追加ラップタイル算出に含めない」テスト -1 件）
- **§6 への新規教訓追加要否**: 不要。既存教訓 開発ガイド §6.3.13（サブフェーズ分割）/ §6.6.8（排他検証）/ §6.2.25（クリック経路 tie-break 採用ポリシー）/ §6.0.1（対概念同期）の射程内
- **手動動作確認手順**:
    1. `npm run dev` で起動済みの dev サーバーに接続（ブラウザでアプリを開く）
    2. 地物を 1 つ追加し、その上をクリック → 地物が選択されること（hitTest が機能）
    3. 重なる地物を 2 つ追加（同一レイヤー）し、上の地物をクリック → どちらか一方が選択されること（旧仕様では「上位レイヤー優先」だったが、新仕様では「最初にヒットした地物」。default 1 レイヤー前提では発見順 = features 配列順）
    4. 地物をドラッグして頂点を編集 → 頂点ハンドルが表示・編集できること
    5. 選択中地物の編集モードで頂点選択 → 単一/複数選択コンテキストが正しく解決されること
    6. 720度を超えた経度を持つ地物を追加 → 必要なラップタイルが描画されること
- **影響**:
    - hitTest はレイヤー可視性で絞らず、最初にヒットした地物を返す。同点複数候補があった場合の上位レイヤー優先 tie-break は消滅（Phase 2.5-C で depth + DOM target ベースに再設計予定）。default 1 レイヤー / 全 visible=true 前提では実害なし
    - `buildVisibleVertexOwnerMap` も Layer.visible フィルタを撤去し、現在時刻にアクティブな全錨を対象とする。default 1 レイヤー前提では旧=新
    - `computeRenderWrapOffsets` も `visibleLayerIds` フィルタを撤去し、全地物の経度範囲をラップタイル算出に含める
- **次フェーズ（D-6-2）への持ち越し**:
    - `FeatureRenderer.svelte` の `visibleLayerIds = $derived(new Set(visibleLayers.map(l => l.id)))` + `{#each visibleLayers as layer}` レイヤー単位 `<g opacity={layer.opacity}>` グルーピング + `getLayerFeatures(layerId)` 経路
    - `featureRendererUtils.ts` の `collectActiveFeatureEntries(features, currentTime, visibleLayerIds)` の `visibleLayerIds` 引数
    - `vertexSelectionContext.ts` の `buildVisibleVertexOwnerMap` を実態に合わせて改名（本 D-6-1 で Layer.visible フィルタを撤去したが、§6.10 スコープ管理ルール「サブフェーズは1経路ずつ」に従い改名はスコープ外として持ち越し。`App.svelte` / 関連テストの参照も同期更新する）
    - 上記に伴うテスト調整
- **次々フェーズ（D-6-3）への持ち越し**:
    - `FeatureAnchor.placement.layerId` フィールド本体削除 + `createAnchorPlacement` シグネチャ変更
    - `JsonFeatureAnchor.placement.layerId` + serialize / deserialize / migration / validateJsonWorld
    - 全 consumer の `anchor.placement.layerId` 参照撤去（30 ファイル超）
- **Phase 2.5-C への持ち越し**（レビュー Info 由来）:
    - `App.svelte:1057-1062` の `clickedFeatureId`（DOM 由来 `data-feature-id`）即採用パスは開発ガイド §6.2.25 採用ポリシー（hitTest 優先 + DOM target を第3 tie-break）と逆向き。Phase 2.5-C で hitTest を最終決定に格上げし、`preferredFeatureId` に DOM target を渡す 3 キー tie-break（layerOrder → depth → DOM target）へ再設計する。本 D-6-1 では layerOrder tie-break を撤去する段階で、`clickedFeatureId` 即採用パスは responsibility 外として現状維持（default 1 レイヤー前提では実害なし）
- **Phase 2-D 完全終了時の持ち越し**（レビュー Low 任意由来）:
    - コード内 JSDoc / コメントの `Phase 2-D-X` / `Phase 2-C-X` / `Phase 2-B` 等の phase 参照を一括棚卸しする。Phase 2.5-C で depth tie-break を導入すると本サブフェーズ追加の `hitTestUtils.ts:174-176` JSDoc が stale 化する。grep 対象: `hitTestUtils.ts` / `vertexSelectionContext.ts` / `featureRendererUtils.ts` / `AutoColorService.ts` 等。残す場合も「Phase X 後の挙動」ではなく「現状の挙動」を主語にする
-->

---

## 2026-05-15 階層モデル新仕様 Phase 2 サブフェーズ D-5: ProjectSettingsDialog レイヤー編集セクション撤去 + 自動配色レイヤー単位グルーピング撤去

- 現状.md §6.10 Phase 2-D の 5 番目のサブフェーズ。Phase 2-D-3 でサイドバーのレイヤータブが撤去され、Phase 2-D-4 で `focusedLayerId` 経路が撤去された結果、レイヤーを編集する経路は唯一「プロジェクト設定ダイアログのレイヤー管理セクション」だけが残っていた。本サブフェーズでこの UI 経路を撤去し、レイヤー編集はアプリ起動時の default 1 件作成のみ（`App.svelte` L842 / L2091 の `new Layer('default', 'レイヤー1', 0)`）に固定される
- スコープ: (1) ProjectSettingsDialog から「レイヤー管理」セクション・draftLayers state・add/update/delete 関数・lockedLayerIds prop・layers prop・onSave の layers 引数 を全削除、(2) App.svelte から dialog 用 `lockedLayerIds` 派生と `onSettingsSave` 内の `setLayers(nextLayers)` + `refreshLayerData()` を撤去、(3) FeatureRenderer.svelte の自動配色を「レイヤー単位ループ内で `resolvePolygonAutoColors` 呼び出し」から「コンポーネント先頭で 1 回 derive（表示文脈下の地物のみ）」へ移行（描画自体は引き続き `{#each visibleLayers}` ループ — レイヤー単位の `<g opacity={layer.opacity}>` グルーピングは D-6/D-7 まで保持）、(4) appProjectSettings.ts から `areLayersEqual` と `hasProjectSettingsChanged` の `currentLayers` / `nextLayers` 引数を削除、(5) AutoColorService.ts 冒頭コメントを Phase 2-D-5 後の挙動へ同期
- 変更内容（6 ファイル: +50 / -215 行 = `git diff --numstat` 集計値）:
    - `src/renderer/presentation/components/ProjectSettingsDialog.svelte`:
        - `Layer` import 撤去（1 行）
        - `$props()` から `layers` / `lockedLayerIds` / `onSave` の `layers` 引数を撤去（型定義含め 7 行）
        - `draftLayers` / `newLayerName` state 宣言を撤去（2 行）
        - `$effect` の初期化 4 行 → 2 行（`draftLayers = [...layers]` と `newLayerName = ''` を撤去）
        - 関数 `createDraftLayerId` / `addDraftLayer` / `updateDraftLayerName` / `canDeleteLayer` / `deleteDraftLayer` を全削除（41 行）
        - `save()` の `onSave?.(..., draftLayers, ...)` 引数を撤去（1 行）
        - HTML 「レイヤー管理」セクション 全削除（53 行: 新規レイヤー追加フォーム + draftLayers リスト表示 + 削除ボタン）
        - CSS から `.layer-create-row` / `.layer-list` / `.layer-row` / `.layer-empty` / `.layer-order` セレクタを撤去（11 行）
    - `src/renderer/App.svelte`:
        - `lockedLayerIds = $derived(new Set(features.flatMap(...placement.layerId)))` 派生を撤去（7 行）
        - `onSettingsSave` のシグネチャから `nextLayers: readonly Layer[]` 引数を撤去（1 行）
        - `onSettingsSave` 内の `hasProjectSettingsChanged(...)` 呼び出しから layers 引数を撤去（4 行 → 4 行）
        - `onSettingsSave` 内の `saveLoad.setLayers(nextLayers)` + `refreshLayerData()` を撤去（2 行）
        - `<ProjectSettingsDialog>` への `{layers}` / `{lockedLayerIds}` 渡しを撤去（2 行）
    - `src/renderer/presentation/components/FeatureRenderer.svelte`:
        - `getLayerFeatures(layerId)` のループ内全走査を、`activeFeatureEntries` の `$derived(collectActiveFeatureEntries(...))` でコンポーネント先頭の 1 回算出に置換（地物全体を一度だけ走査）。`getLayerFeatures` は `activeFeatureEntries.filter(...)` に簡素化（22 行 → 8 行）。エントリ収集は新規 `featureRendererUtils.ts` の純粋関数へ抽出して回帰テスト可能化（下記参照）
        - `activeFeatureEntries` 内で `visibleLayerIds.has(anchor.placement.layerId)` による表示文脈フィルタを適用（非表示レイヤー所属の地物は描画対象外であり、自動配色の隣接グラフ・featureIndex 連番に混入させない）。`visibleLayerIds = $derived(new Set(visibleLayers.map(l => l.id)))` を併せて導入
        - `polygonAutoColors = $derived(resolvePolygonAutoColors(entries, vertices, settings))` をコンポーネント先頭に導入（15 行追加）— **表示文脈下の地物のみで 1 回**算出されるためレイヤー単位グルーピングが消滅。入力エントリは `activeFeatureEntries.filter((e) => e.anchor.shape!.type === 'Polygon')` で明示的に Polygon のみへ絞る（`isPolygonAutoColorEnabled` 側でも除外されるが意図を可視化）
        - テンプレート内の `{@const polygonAutoColors = resolvePolygonAutoColors(...)}` を撤去（9 行）。`{#each visibleLayers as layer}` ループとレイヤー単位 `<g opacity={layer.opacity}>` は保持（D-6 で `placement.layerId` 削除と一緒に再設計予定）
        - **featureIndex セマンティクス変化（要注意）**: 旧コードの `result.length` は「レイヤー内連番」を割り当てていたが、新コードの `entries.length` は「表示文脈（可視レイヤー全体）での連番」になる。`getLayerFeatures(layerId)` は filter で抽出するため、抽出後も表示文脈 index が保持される。Point / LineString は `resolvePointStyle(featureIndex, settings)` / `resolveLineStyle(featureIndex, settings)` 経由で getAutoColor(featureIndex) のパレット index に直接使用されるため、**複数の可視レイヤーを含む既存 `.gimoza` を読み込んだ場合に Point / LineString の自動色割当が旧と異なる**。default 1 レイヤーしか存在しない新規プロジェクトでは旧=新（連番一致）で影響なし
    - `src/renderer/presentation/components/featureRendererUtils.ts`（新規 38 行）:
        - 抽出: `collectActiveFeatureEntries(features, currentTime, visibleLayerIds)` 純粋関数。「shape を持つ + 可視レイヤー所属 + 現在時刻でアクティブ錨」の3条件を満たす地物に、入力順を保持しつつ表示文脈連番の `featureIndex` を割り当てる
        - 抽出動機: Svelte コンポーネント内 `$derived.by` の振る舞いを直接ユニットテスト不能だったため、純粋関数化して回帰テストを書けるようにした。Phase 2-D-5 の振る舞い変更（可視/非可視レイヤー混在の除外、多レイヤー featureIndex 地図全体連番化、shape 無しコンテナ除外）をテストで固定する
    - `src/renderer/domain/services/AutoColorService.ts`:
        - 冒頭コメントの「同一レイヤー内で共有境界を持つ面が同色にならないように」を「共有境界を持つ面が同色にならないように」へ修正し、Phase 2-D-5 で地図全体 1 回算出へ移行 + Phase 6 で同 depth 兄弟単位へ再設計予定 + 要件定義書 §2.4.4.3 / 開発ガイド §6.6.6 は旧モデル前提のまま残置という経過を併記（§6.0.1 検出観点1: 実装コメントの同期）
    - `src/renderer/presentation/app/appProjectSettings.ts`:
        - `Layer` import 撤去（1 行）
        - `areLayersEqual` 関数 全削除（14 行）
        - `hasProjectSettingsChanged` のシグネチャから `currentLayers` / `nextLayers` 引数を撤去 + 戻り値式の最後の `!areLayersEqual(...)` を撤去（4 行）
    - `tests/presentation/app/appProjectSettings.test.ts`:
        - `Layer` import + `createLayer()` ヘルパー削除（11 行）
        - `areLayersEqual` import + 「空レイヤー配列同士は等価と判定する」テスト削除（4 行 / 1 件 -1）
        - 残る `hasProjectSettingsChanged` テスト 4 件のシグネチャから layers 引数を全撤去（残存 4 件は不変、ファイル全体は 11 件 → 10 件）
    - `tests/presentation/components/featureRendererUtils.test.ts`（新規 +8 件）:
        - レビュー指摘「複数レイヤー .gimoza での可視/非表示混在・レイヤー跨ぎ隣接・Point/LineString featureIndex 変化の回帰テストが薄い」への対応
        - カバレッジ: (1) 単一レイヤー全地物の連番、(2) 非表示レイヤー所属除外 + featureIndex 詰め、(3) **多レイヤー featureIndex 地図全体連番化**（旧仕様との振る舞い差を明示的に固定）、(4) shape 無し錨（コンテナ）除外、(5) アクティブ錨無し時刻除外、(6) features 空、(7) visibleLayerIds 空（全除外）、(8) 入力順保持（feature 配列順 ≠ レイヤー順でも連番は入力順）
- **触らない（後続サブフェーズで対応）**:
    - `Layer` エンティティ / `Layer.ts`（D-7 で削除予定）
    - `World.layers` フィールド（D-7 で削除予定）
    - `placement.layerId` フィールド自体（D-6 で削除予定）
    - `App.svelte` の `let layers = $state<readonly Layer[]>([])` / `refreshLayerData()` / `getAddTargetLayer()` / `executeAddFeatureCommand({ ... layerId: targetLayer.id ... })` 経路（D-6/D-7 で対応）
    - `MapCanvas.svelte` / `vertexSelectionContext.ts` / `hitTestUtils.ts` の `visibleLayerIds.has(anchor.placement.layerId)` フィルタおよび `layerPriority.get(...)` tie-break（D-6 / Phase 2.5-C で対応）
    - `FeatureRenderer.svelte` の `{#each visibleLayers as layer}` 描画ループとレイヤー単位 `<g opacity={layer.opacity}>` グルーピング（D-6 で `placement.layerId` 削除と同時に再設計）
    - `SaveLoadUseCase.getLayers()` / `setLayers()` 暫定 API（D-7 で `World.layers` 廃止と同時に撤去）
    - EventBus の `layers:changed` / `layer:visibilityChanged` 型定義（D-7）
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **§6.0.1** 「仕様文書での用語撤廃・追加宣言時の同一文書同期」検出観点2「対概念の同期」: 「Layer 編集 UI 撤去 ↔ Layer 作成の唯一経路（App.svelte 初期化）を維持」「ダイアログから draftLayers を撤去 ↔ Layer.order フィールド本体は Layer エンティティ存続のため D-7 まで保持」「自動配色のレイヤー単位グルーピング撤去 ↔ レイヤー単位の `<g>` 描画グルーピングは D-6 まで保持（描画と自動配色は別経路として分離）」と対概念を明示的に同期させた
    - **§6.3.13** 「optional フィールド導入は全層を1サブフェーズに詰めない」: 「1サブフェーズあたりの差分は ~15 ファイル / ~300 行を上限に分割する」「着手前に『触る経路』を最大2層で宣言する」 → 触る経路は Presentation 層（4 ファイル）+ 関連テスト（1 ファイル）+ Domain 層は AutoColorService.ts のコメント同期のみ（1 ファイル / +4 -1）。合計 6 ファイル / +50 -215 行で上限内（Domain 層はコメント変更で挙動には触らないため実質「Presentation 層 + テスト」の 1 経路）
    - **§6.6.8** 「排他検証は仕様の対象範囲を実装で広げない」: 排他検証経路（`ConflictDetectionService` / `PolygonValidationService`）には触らない。Phase 2-D-1 で撤去済みの範囲を維持。自動配色のグローバル化は仕様上「同レイヤー内」制約が消える方向（新モデルではレイヤー概念自体なし）なので拡張ではなく置換
    - **§6.1.6** 「UI文脈を既定値で上書きする追加・編集処理」: ダイアログのレイヤー編集 UI が消えるため、`getAddTargetLayer()` の UI 文脈解決経路は引き続き `saveLoad.getLayers()` のリスト先頭可視レイヤーを返す（変更なし）。default 1 件しかないので結果も不変
    - **memory/feedback_subphase_scope_split.md** 「サブフェーズは1経路ずつ」 → 描画グルーピング・ヒットテストの `placement.layerId` 参照には踏み込まない（D-6 のスコープ）
    - **memory/feedback_break_existing_freely.md** 「既存動作の変更を遠慮しない」 → 「レイヤー単位で別パレットを割り当てる」既存挙動を撤去。新モデルでは Phase 6 の「同 depth 兄弟基準」自動配色へ置換予定だが、Phase 2-D-5 時点では「地図全体で 1 回」のシンプル算出へ寄せる（中間期間の挙動）
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npx vitest run` 1470 passed（96 ファイル、件数: 1463 → 1470 で `areLayersEqual` 単独テスト -1 件 + `collectActiveFeatureEntries` 回帰テスト +8 件）
- **§6 への新規教訓追加要否**: 不要。既存教訓 §6.0.1（対概念の同期） / §6.1.6（UI 文脈の取扱い）/ §6.3.13（サブフェーズ分割）の射程内
- **手動動作確認手順**:
    1. `npm run dev` で起動済みの dev サーバーに接続（ブラウザでアプリを開く）
    2. メニューバーから「ファイル」→「プロジェクト設定」を開く
    3. ダイアログを確認: 「レイヤー管理」セクションが消えていること（基本情報・地図・タイムライン・アプリ設定・惑星パラメータ・表示・グリッド・面スタイル・自動保存 の各セクションは残る）
    4. 「面スタイル」セクションで `defaultPalette` を別のパレットに変更 → 保存 → 地図上の面地物の自動配色が変わることを確認
    5. 地物を複数追加（隣接する面を 3 つ以上）→ 自動配色が「地図全体で 4 色塗り分け」になることを確認（旧仕様ではレイヤーごとに別パレットだったが、新仕様では地図全体で 1 回算出）
    6. プロジェクト設定の「カスタムパレット」セクション動作確認: 追加・編集・削除が機能すること
    7. 既存 `.gimoza` ファイルを読み込み → レイヤー情報がラウンドトリップで保持されること（`SaveLoadUseCase.getLayers/setLayers` は引き続き動作）
    8. 新規プロジェクト → 保存 → 再読み込み → 地物の自動配色が新規時と同じになること
- **影響**:
    - ユーザーが新規レイヤーを追加・削除・改名できなくなる（default の「レイヤー1」のみが固定）。旧仕様では「県」「市町村」のようなレイヤー分けでパレットを変えていたが、新モデルでは Phase 6 の「同 depth 兄弟」基準自動配色へ移行予定なので、Phase 2-D-5 時点では「全地物に同一パレットを適用」となる
    - **Polygon の自動配色**: `resolvePolygonAutoColors` の隣接グラフが「同一レイヤー内」から「表示文脈下の可視レイヤー全体」へ変わるため、複数の可視レイヤーを含む既存 `.gimoza` を読み込むと **`polygonAutoColors.get(feature.id)` の値そのものが旧と異なる**（fallback 経路だけでなく主経路の greedy coloring 結果が変わる）。同色の隣接が増える可能性あり
    - **Point / LineString の自動色**: featureIndex がレイヤー内連番から表示文脈（可視レイヤー全体）連番に変わったため、`resolvePointStyle` / `resolveLineStyle` が getAutoColor(featureIndex) で割り当てるパレット index が旧と異なる。複数の可視レイヤー含む既存 `.gimoza` ではロード後に Point / LineString の色も変わる
    - 非表示レイヤーは表示文脈に含まれないため、その所属地物は自動配色の隣接グラフ・featureIndex 連番に混入せず、可視地物の色は非表示レイヤーの存在に影響されない
    - 上記いずれも default 1 レイヤーのみの新規プロジェクトでは旧=新で影響なし。互換破棄の方針上、保存形式は引き続き Layer を保持し、ロード時の挙動も Layer エンティティ・`placement.layerId` を維持
- **次フェーズへ持ち越す改訂**:
    - 開発ガイド §6.6.6「隣接・自動配色はレイヤーと表示経度の文脈を揃える」（同一レイヤー前提の教訓）は旧モデル前提のまま残存。Phase 2-D 完了時 or Phase 6（自動配色 + 個別スタイル）着手時に「同 depth 兄弟」基準の新教訓へ改訂する
    - 要件定義書 §2.4.4.3「同一レイヤー内で共有境界を持つ面が同色にならないように」/ §2.4.4.6「レイヤー表示とテーマ設定」も旧モデル前提のまま。Phase 6 着手時に改訂する（現状.md §2.2 の Phase 計画と一貫）

---

## 2026-05-15 階層モデル新仕様 Phase 2 サブフェーズ D-4: `focusedLayerId` 残骸の整理 + `unsubLayersChanged` 購読撤去

- 現状.md §6.10 Phase 2-D の 4 番目のサブフェーズ。Phase 2-D-3 で UI 入力経路（Sidebar レイヤータブ + LayerPanel）が消滅し、`focusedLayerId` は実コード上「常に null」のまま伝播する dead state となっていた（D-3 では依存連鎖の都合で残置）。また `unsubLayersChanged` 購読も emit 側（`ManageLayersUseCase`）撤去で永久に発火しない dead listener となっていた。両者を D-4 で完全撤去
- スコープ再定義: 旧計画文の「描画/ヒットテストの layerId 参照撤去」のうち、`focusedLayerId` を介した経路（FeatureRenderer の opacity 計算で焦点外レイヤーを 0.22 倍に薄める処理）は本サブフェーズで撤去。レイヤー単位の `<g>` グルーピング / `visibleLayerIds` フィルタ / `hitTest` の layerOrder tie-break / `lockedLayerIds` / `placement.layerId` を引き渡す経路（`App.svelte` line 209 / 1090 / `MapCanvas.svelte` line 243 / `vertexSelectionContext.ts` / `hitTestUtils.ts`）は Layer エンティティ本体の可視性・順序機能に紐づくため Phase 2-D-5〜D-7（および Phase 2.5-C の hitTest 全面再設計）で順次撤去
- 変更内容（5 ファイル / 修正 -50 行程度）:
    - `src/renderer/App.svelte`:
        - `let focusedLayerId = $state<string | null>(null)` の state 宣言を撤去（旧 line 248-252、コメント含む 5 行）
        - `$effect(() => { if (focusedLayerId && !layers.some(...)) focusedLayerId = null; })` の同期 effect 撤去（5 行）
        - `getAddTargetLayer()` 内の `if (focusedLayerId) { ... }` フォーカス分岐を撤去（7 行 → `layerList.find((layer) => layer.visible) ?? layerList[0] ?? null` のみに簡素化）
        - `const unsubLayersChanged = eventBus.on('layers:changed', () => { refreshLayerData(); markAsDirty(); })` 購読と先行コメント 3 行を撤去（計 7 行）
        - `onDestroy` 内の `unsubLayersChanged()` 呼び出しを撤去（1 行）
        - `MapCanvas` への `{focusedLayerId}` プロップ受け渡しを撤去（1 行）
    - `src/renderer/presentation/components/MapCanvas.svelte`:
        - `$props()` の `focusedLayerId = null as string | null` default と型 `focusedLayerId?: string | null` を撤去（2 行）
        - 内部の `MapCanvasSvgLayers` への `{focusedLayerId}` 渡しを撤去（1 行）
    - `src/renderer/presentation/components/mapCanvas/MapCanvasSvgLayers.svelte`:
        - `$props()` の `focusedLayerId` default と型を撤去（2 行）
        - 内部の `FeatureRenderer` への `{focusedLayerId}` 渡しを撤去（1 行）
    - `src/renderer/presentation/components/FeatureRenderer.svelte`:
        - `$props()` の `focusedLayerId = null` default と型を撤去（2 行）
        - `<g opacity={layer.opacity * (focusedLayerId && layer.id !== focusedLayerId ? 0.22 : 1)}>` を `<g opacity={layer.opacity}>` に簡素化（1 行修正）
- **触らない（後続サブフェーズで対応）**:
    - `Layer` エンティティ / `Layer.ts`（D-7 で削除予定）
    - `placement.layerId` フィールド自体（D-6 で削除予定）
    - `App.svelte` の `lockedLayerIds = $derived(new Set(features.flatMap(...anchor.placement.layerId)))` / `executeAddFeatureCommand({ ... layerId: targetLayer.id ... })` 経路（D-5/D-6 で対応）
    - `MapCanvas.svelte` line 243 の `visibleLayerIds.has(anchor.placement.layerId)` フィルタ（D-5/D-6 で対応）
    - `vertexSelectionContext.ts` / `hitTestUtils.ts` の `visibleLayerIds.has(anchor.placement.layerId)` フィルタおよび `layerPriority.get(anchor.placement.layerId)` tie-break（D-5/D-6 および Phase 2.5-C で対応）
    - `FeatureRenderer.svelte` の `getLayerFeatures(layerId)` 内の `anchor.placement.layerId === layerId` 振り分け + レイヤー単位 `<g>` グルーピング（D-5/D-6 で対応）
    - EventBus の `layers:changed` / `layer:visibilityChanged` 型定義（D-7 で削除予定 — emit / subscribe とも経路消滅したため dead type だが、`SaveLoadUseCase` の `getLayers()` / `setLayers()` 暫定 API と一緒のタイミングで撤去する方が一括 cleanup として整理しやすい）
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **§6.3.13** 「optional フィールド導入は全層を1サブフェーズに詰めない」: 「1サブフェーズあたりの差分は ~15 ファイル / ~300 行を上限に分割する」「着手前に『触る経路』を最大2層で宣言する」 → 触る経路は Presentation 層（App.svelte / MapCanvas / MapCanvasSvgLayers / FeatureRenderer）の 1 層 4 + 1 = 5 ファイルに限定。差分は修正 -50 行程度で上限内
    - **§6.6.8** 「排他検証は仕様の対象範囲を実装で広げない」: 排他検証経路（`ConflictDetectionService` / `PolygonValidationService`）には触らない。Phase 2-D-1 で撤去済みの範囲を維持
    - **§6.1.4** 「コンポーネント間での状態同期漏れ」: emit が消滅した EventBus 購読を残置すると保守時のミスリードになる（「layers:changed が発火する経路がどこかにあるはず」と探してしまう）。dead listener は早期撤去すべき
    - **memory/feedback_subphase_scope_split.md** 「サブフェーズは1経路ずつ」 → 描画グルーピング・ヒットテスト・lockedLayerIds 派生には踏み込まない（D-5/D-6/D-7 のスコープ）
    - **memory/feedback_break_existing_freely.md** 「既存動作の変更を遠慮しない」 → 焦点レイヤー機能（focusedLayerId に応じた opacity 調整）の UI 経路を完全撤去。Phase 5 で深度フォーカス UI として再設計予定だが、ベースは別概念（layerId ベースではなく depth ベース）になるため互換シムは不要
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npx vitest run` 1463 passed（95 ファイル、件数増減なし）
- **§6 への新規教訓追加要否**: 不要。既存教訓 §6.1.4 / §6.3.13 / §6.6.8 の射程内。dead state / dead listener の整理は機械的な cleanup で、新規再発防止観点は発生しなかった
- **手動動作確認手順**:
    1. `npm run dev` でアプリ起動
    2. 地物（点・線・面）を複数追加 → サイドバーの 2 タブ（プロパティ / 地物一覧）で地物が一覧表示されることを確認
    3. 地物を選択 → プロパティパネルで編集 → 描画が更新されることを確認（焦点レイヤー機能消失による副作用がないこと: 全レイヤーが等しい不透明度で描画される、地物選択ハイライトが従来通り機能する）
    4. 既存の `.gimoza` ファイル（旧形式）を読み込み → 地物の描画・選択・編集が正常動作することを確認
    5. 新規プロジェクト → 保存 → 再読み込み → レイヤー情報が保持されていることを確認（`SaveLoadUseCase.getLayers/setLayers` 経由のラウンドトリップは D-7 まで継続）
- **影響**: 焦点レイヤー機能（UI 経路は D-3 で消滅していたが、`focusedLayerId` state の伝播による「焦点レイヤー以外の透明度を 0.22 倍に薄める」描画ロジック）が完全撤去。全レイヤーが `layer.opacity` 通りの不透明度で描画される。Phase 5 で「深度フォーカス UI」として別の仕組み（縦方向領域に基づく強調）が導入されるため、互換シムは不要

---

## 2026-05-03 階層モデル新仕様 Phase 2 サブフェーズ D-3: `ManageLayersUseCase` / `LayerQueryService` 撤去 + `LayerPanel.svelte` 削除

- 現状.md §6.10 Phase 2-D の 3 番目のサブフェーズ。旧計画では「UseCase / Query 撤去」を D-3、「LayerPanel.svelte 削除」を D-4 前半に分けていたが、`LayerPanel.svelte` が `ManageLayersUseCase` / `LayerQueryService` に強く依存しており、UseCase / Query を先に削除すると LayerPanel が型 + 実行時ともに破綻するため、依存上の必然から両者を D-3 に統合。旧 D-4 の「描画/ヒットテストの layerId 参照撤去」は新 D-4 として残す（番号繰り上げ）
- 変更内容（13 ファイル: 削除 4 / 修正 9）:
    - **削除**:
        - `src/renderer/application/ManageLayersUseCase.ts`（105行）
        - `src/renderer/application/queries/LayerQueryService.ts`（17行）
        - `src/renderer/presentation/components/LayerPanel.svelte`（309行）
        - `tests/application/ManageLayersUseCase.test.ts`（210行 / 20件のテスト）
    - **修正（実装側）**:
        - `src/renderer/application/SaveLoadUseCase.ts`: `manageLayers: ManageLayersUseCase` コンストラクタ引数を撤去。`World.layers` のラウンドトリップ保持のため private `layers: readonly Layer[]` フィールドを新設し、`assembleWorld` / `distributeWorld` / `resetProjectState` で操作。Phase 2-D-7 移行までの暫定 API として `getLayers()` / `setLayers(layers)` を公開（コメントで Phase 2-D-7 撤去予定を明示）
        - `src/renderer/infrastructure/DIContainer.ts`: `ManageLayersUseCase` / `LayerQueryService` の import / 型 (`ApplicationCommands.manageLayers` / `ApplicationQueries.layers`) / インスタンス化 / SaveLoadUseCase への引き渡しを撤去
        - `src/renderer/application/index.ts`: `ManageLayersUseCase` / `LayerQueryService` の export を撤去
        - `src/renderer/application/queries/index.ts`: `LayerQueryService` の export を撤去
        - `src/renderer/presentation/components/Sidebar.svelte`: `import LayerPanel` 撤去、`SidebarTab` 型から `'layers'` を削除（`'properties' | 'features'` の 2 タブ構成へ）、初期 `activeTab` を `'properties'` に変更、`focusedLayerId` / `onFocusLayerChange` プロップ削除、レイヤータブのボタン・ラベル・コンテンツ部分を削除
        - `src/renderer/App.svelte`: コンテナ destructuring から `manageLayers` / `layers: layerQueries` を撤去。`refreshLayerData` / `getAddTargetLayer` を `saveLoad.getLayers()` 経由へ書き換え。初期化 (`if (saveLoad.getLayers().length === 0) saveLoad.setLayers([new Layer('default', 'レイヤー1', 0)])`) と新規プロジェクト時のレイヤー初期化を `Layer` 直接生成へ移行（`Layer` を type-only import → 値 import に変更）。`onSettingsSave` の `manageLayers.restore(nextLayers)` を `saveLoad.setLayers(nextLayers)` に置き換え。`Sidebar` への `focusedLayerId` / `onFocusLayerChange` 渡しを撤去（`focusedLayerId` 自体は描画グルーピングに残存し UI 入力経路がないため常に null）
        - `src/renderer/domain/services/HierarchyService.ts`: ヘッダーコメントで `ManageLayersUseCase` を直接参照していた箇所を「Phase 2-D-3 で撤去済み」へ更新
    - **修正（テスト側）**:
        - `tests/application/SaveLoadUseCase.test.ts`: `ManageLayersUseCase` import / インスタンス化 / 引数渡しを撤去。`assembleWorld` テストの `manageLayers.addLayer('l1', 'テスト')` を削除し「layers 空でも組み立てられる」に変更。「読み込んだレイヤーが ManageLayersUseCase に反映される」を「ロード後の `assembleWorld` にラウンドトリップする」に書き換え（SaveLoadUseCase 内部 layers が World に再現することを確認）。SharedVertexGroups テストの `manageLayers.restore(world.layers)` を削除
        - `tests/application/queries/QueryServices.test.ts`: `ManageLayersUseCase` / `LayerQueryService` の import を撤去。`LayerQueryService` 専用テスト 3 件を削除。`SaveLoadUseCase` インスタンス化引数から `manageLayers` を撤去
        - `tests/application/commands/AddFeatureCommand.test.ts`: `ManageLayersUseCase` の import / インスタンス化 / `manageLayers.addLayer(layerId, 'テスト')` を撤去（AddFeatureCommand 自体は manageLayers に依存していなかったため、テストのセットアップ簡素化のみ）
        - `tests/infrastructure/DIContainer.test.ts`: `commands.manageLayers` / `queries.layers` の expect を削除
    - **E2E（追補）**: 自レビュー後の同期（§6.0.1 検出観点1: 撤廃語の grep + UI セレクタ整合性確認で発見）として 3 ファイル整理。`e2e/layer-advanced.spec.ts` 全削除（6 件 — レイヤータブ前提の表示・visibility・追加・フォーカス検証で、UI 経路がなくなったため）。`e2e/edit-mode.spec.ts` から「サイドバーのタブ切替」を 3 タブから 2 タブ構成へ書き換え + 「レイヤータブにデフォルトレイヤーが表示される」「レイヤーの表示/非表示を切り替えられる」の 2 件を削除。`e2e/sidebar-search.spec.ts` の折りたたみ後のタブ表示確認を「レイヤータブ + .layer-panel + .focus-panel」から「地物一覧タブ active」へ書き換え。E2E 合計: 180 → 172 件（-8 件）、22 → 21 ファイル
    - **ドキュメント同期（追補）**:
        - 開発ガイド §1.3 ユースケース一覧から `ManageLayersUseCase` を削除し、`ReassignFeatureParentUseCase` / `SaveLoadUseCase` を明記
        - 実装済み.md ドメインサービステストカバレッジ表で `ManageLayersUseCase` 行を削除、`Query services` の件数を 10 → 7 件へ更新（`LayerQueryService` 削除分を反映）、`SaveLoadUseCase` 行に `getLayers()` / `setLayers()` の暫定 API を補記
        - 実装済み.md コンポーネント表で `LayerPanel.svelte` 行を削除、`Sidebar.svelte` を「3タブ」から「2タブ（プロパティ/地物一覧）」へ更新
        - 現状.md §6.11 旧モデル前提コードリストを「`Layer.ts` / `ProjectSettingsDialog` レイヤー編集セクション / `World.layers` フィールド」に絞り、Phase 2-D-3 で削除済みの識別子（`ManageLayersUseCase` / `LayerQueryService` / `LayerPanel` / Sidebar レイヤータブ）は明示的に削除済みと記載。`SaveLoadUseCase` の暫定 `getLayers()` / `setLayers()` を D-7 撤去予定として追記
- **触らない（後続サブフェーズで対応）**:
    - `Layer` エンティティ / `Layer.ts`（D-7 で削除予定）
    - `placement.layerId` フィールド / 描画グルーピング / ヒットテスト order / 自動配色（D-4 / D-5 / D-6 で順次対応）
    - `World.layers` フィールド（D-7 で削除予定）
    - EventBus の `layers:changed` / `layer:visibilityChanged` イベント型（emit する経路は無くなったが定義は残置 — D-7 で削除予定）
    - `App.svelte` 内の `unsubLayersChanged` 購読（emit されないため無効だが、定義残置に対応した冗長コード）
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **§6.0.1** 「仕様文書での用語撤廃・追加宣言時の同一文書同期」: 「撤廃語をリポジトリ全文 grep し、同期対象のヒット箇所を全部直すか、明示的に『対象外 (理由つき)』と現状.md に記録する」 → `ManageLayersUseCase` / `LayerQueryService` / `LayerPanel` の 3 識別子を `src/` / `tests/` 全文 grep し、実行参照ゼロまで同期。残置している経緯コメントは `SaveLoadUseCase.ts` 3 箇所（暫定 `layers` フィールドの撤去予定説明 / `getLayers()` docstring の `ManageLayersUseCase` 撤去経緯 / `getLayers()` docstring の旧 `LayerQueryService` 防御的コピー契約踏襲）+ `App.svelte` 2 箇所（追補対応で追加した `focusedLayerId` state / `unsubLayersChanged` 購読の dead code 残置理由）= 計 5 箇所。いずれも Phase 2-D-7 削除予定の dead code に対する後続作業者向けガード説明
    - **§6.3.13** 「optional フィールド導入は全層を1サブフェーズに詰めない」: 「~15 ファイル / ~300 行を上限に分割する」「着手前に『触る経路』を最大2層で宣言する」 → 触る経路は UI 層 (LayerPanel.svelte / Sidebar.svelte / App.svelte) + Application 層 (UseCase / Query / DIContainer / SaveLoadUseCase) の 2 経路に限定。13 ファイル / 削除 ~640 行 + 修正 ~80 行（修正分は 300 行以内）
    - **§6.6.8** 「排他検証は仕様の対象範囲を実装で広げない」: 排他検証経路には触らない。今回は UI / UseCase / DI 整理に専念
    - **memory/feedback_subphase_scope_split.md** 「サブフェーズは1経路ずつ」 → 描画・ヒットテスト・自動配色には踏み込まない（D-4 以降のスコープ）
    - **memory/feedback_break_existing_freely.md** 「既存動作の変更を遠慮しない」 → Sidebar からレイヤータブが消え、レイヤー操作 UI（追加・削除・名前変更・visibility・opacity）が一時的に失われる仕様変更を実施。現状.md §6.10 で Phase 4 / Phase 5 で再構築されることが明記されており、計画通りの破壊的変更
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npx vitest run` 1463 passed（95 ファイル、ManageLayersUseCase.test.ts 削除で -20 件 + LayerQueryService 関連 -3 件、計 -23 件 / 1486 → 1463）
- **§6 への新規教訓追加要否**: 不要。既存教訓 §6.0.1 / §6.3.13 / §6.6.8 の射程内。UseCase / Query 撤去という単純な依存ツリー上の整理で、新規再発防止観点は発生しなかった
- **手動動作確認手順**:
    1. `npm run dev` でアプリ起動
    2. サイドバーに「プロパティ」「地物一覧」の 2 タブのみ表示され、「レイヤー」タブが消えていることを確認
    3. 既存の `.gimoza` ファイル（旧形式）を読み込み、地物が正常に描画されることを確認（layers の扱いはラウンドトリップで保持されるため、保存→再読込でも layers が失われないはず）
    4. 新規プロジェクト作成 → 点・線・面の追加が正常動作することを確認（addFeature の layerId 'default' は内部の Layer エンティティ 'default' に対応）
    5. プロジェクト設定ダイアログでレイヤー一覧を編集（ProjectSettingsModal 経由が残っていれば）→ 保存して再読込し、layers が保持されることを確認
- **影響**: サイドバーから「レイヤー」タブが消える。レイヤー名の編集・visibility 切替・opacity 調整は サイドバーから直接操作できなくなる（プロジェクト設定モーダル経由のレイヤー一覧編集は **Phase 2-D-5 のスコープ** のため意図的に残置 — `ProjectSettings` レイヤー順 / 自動配色レイヤー単位グルーピングの新モデル対応とまとめて整理予定）。表示フォーカス機能（focusedLayerId）は UI 入力経路がなくなり常に null（描画は影響を受けるが、`layer.visible` ベースのフィルタは残存）

---

## 2026-05-03 階層モデル新仕様 Phase 2 サブフェーズ D-2: `LayerService` を `HierarchyService` にリネーム

- 現状.md §6.10 Phase 2-D の 2 番目のサブフェーズ。要件定義書 §5.2 で既に `HierarchyService` 命名が確定（Phase 1 改訂時）していたが、コード側のみ `LayerService` のまま乖離していたため、§6.0.1 検出観点1（仕様文書での用語撤廃宣言時の同一文書同期）に従い実装を仕様に揃えた
- スコープ再定義: 旧 D-2 の文言「`Layer` エンティティ / `LayerService` の削除と `getParentFeature` 等のナビゲーションヘルパー再配置」は、`Layer` エンティティの依存先が D-3〜D-6 で順次撤去されないと break するため実現不能だった。`Layer` 削除を Phase 2-D 末尾の D-7 へ繰り下げ、D-2 を「`LayerService` → `HierarchyService` リネーム（純粋なシンボル名変更で挙動変化なし）」に絞り込み
- 変更内容（実装側 6 ファイル: うち 2 ファイルはリネーム / 物理ファイル単位では削除 2 + 新規 2 + 変更 4 = 8 ファイル）:
    - `src/renderer/domain/services/LayerService.ts` → `src/renderer/domain/services/HierarchyService.ts` にリネーム + ヘッダーコメントを「レイヤー階層ドメインサービス」から「階層ナビゲーション・形状導出ドメインサービス」へ更新（旧モデルでの「グローバル `Layer`」廃止と新モデルでの「ツリー位置から派生する depth で階層を表現」を明記）
    - `tests/domain/services/LayerService.test.ts` → `tests/domain/services/HierarchyService.test.ts` にリネーム + import path 更新 + `describe('LayerService')` → `describe('HierarchyService')`
    - `src/renderer/domain/services/index.ts`: バレル export の `from './LayerService'` を `from './HierarchyService'` に更新（メソッド一覧 / 型エクスポートとも）
    - `src/renderer/application/DeleteFeatureUseCase.ts`: import path `@domain/services/LayerService` → `@domain/services/HierarchyService`
    - `src/renderer/application/ReassignFeatureParentUseCase.ts`: 同上
    - `src/renderer/presentation/components/FeatureRenderer.svelte`: 同上（`deriveParentShape` の import）
- ドキュメント同期（§6.0.1 検出観点1: 仕様文書での用語撤廃宣言時の同一文書同期）:
    - 開発ガイド §1.2 ドメインサービス一覧: 「GeometryService, TimeService, LayerService」→「GeometryService, TimeService, HierarchyService」
    - 開発ガイド §6.6.9 派生サービス参照: 「派生サービス (`LayerService.deriveParentShape`)」→「派生サービス (`HierarchyService.deriveParentShape`)」
    - 現状.md §6.10 Phase 2-D-3 以降の見通しを改訂（D-7 として `Layer` エンティティ削除を末尾に追加）
    - 現状.md §6.10 Phase 2-E の説明を「`HierarchyService` (仮) を新設し」→「D-2 でリネームした `HierarchyService` に depth 派生関数を追加」へ更新
    - 現状.md §6.10 Phase 2.5-A の説明を「`LayerService.deriveParentShape`」→「`HierarchyService.deriveParentShape`」へ更新
    - 現状.md §6.11 の再設計対象リストの `Layer` 関連: 「`Layer.ts` / `LayerService.ts` / `ManageLayersUseCase.ts` / Sidebar レイヤータブ」→「`Layer.ts` / `ManageLayersUseCase.ts` / Sidebar レイヤータブ（`LayerService.ts` は Phase 2-D-2 で `HierarchyService.ts` にリネーム済み）」
    - 現状.md §6.10 Phase 2-C-2 の active な既知制約 (line 313) の「`LayerService.deriveParentShape` は…」を `HierarchyService.deriveParentShape` に同期（Phase 2-C-2 完了エントリの歴史記述ではなく現在参照のため）
    - 実装済み.md ドメインサービステストカバレッジ表 (line 710) の `LayerService` を `HierarchyService` に同期（時点未明示の active 参照表のため）
    - `HierarchyService.ts` line 31 のコメント `getParentFeature` の §2.1 引用「同一時点で高々1つの上位レイヤー面情報にのみ属する」を「同一時点で高々1つの上位面情報にのみ属する」へ修正（要件定義書 §2.1 の「グローバルなレイヤー序列という概念は持たず」と整合）
- **触らない**: `Layer` エンティティ / `placement.layerId` フィールド / `HierarchyService.buildParentChildLink` / `buildParentChildUnlink` 内の `parentActive.placement.layerId` 引き渡し（`placement.layerId` 自体は D-6 で削除。コード側にも Phase 2-D-6 で消える経路である旨の注記を追加）
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **§6.0.1** 「仕様文書での用語撤廃・追加宣言時の同一文書同期」: 「仕様文書の特定章で『この用語は使わない』『この用語に置き換える』と用語注記を追加した場合、同一文書内の全用例 + 関連する用語表 + 対応する設計サマリ (`現状.md` §6 / 開発ガイド §6 / `実装済み.md`) を**同じ変更単位で同期する**」 → 仕様（要件定義書 §5.2）が `HierarchyService` 命名を確定済みなので、コード本体・テスト・import 元・開発ガイド §1.2 / §6.6.9・現状.md §6.10 / §6.11 を同じ変更単位で同期。検出観点1 (語彙の grep) で `LayerService` を全文検索し、過去履歴記述（Phase 2-C-1/C-2 当時の状態説明）は時点表現として保持、未来予定・教訓本文は同期した
    - **memory/feedback_subphase_scope_split.md** 「サブフェーズは1経路ずつ」 → リネーム + import 更新の単一経路のみ。`Layer` 削除や `placement.layerId` 撤去には踏み込まない
    - **memory/feedback_break_existing_freely.md** 「既存動作の変更を遠慮しない」 → Phase 2-D の計画文書（現状.md §6.10）を「Layer エンティティ削除を D-7 へ繰り下げ」と再構成。形状の整合性を保つため、D-2 の文言を実態に合わせて改訂
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npx vitest run` 1486 passed（96 ファイル、件数増減なし）。挙動変化ゼロ（純粋なシンボル名変更）
- **§6 への新規教訓追加要否**: 不要。既存教訓 §6.0.1 の射程内。仕様 → 実装の片方向同期（仕様が先行 → 実装が追従）の単純なケース
- **手動動作確認手順**: なし（純粋なシンボル名変更で挙動変化ゼロ。ドメインサービスのリネーム + import path 更新のみ。コード上の動作経路はリネーム前後で完全に同一）
- **影響**: 振る舞い変更なし。`@domain/services/LayerService` import パスを使う外部コードがあれば（現状リポジトリ内では消滅）追従が必要

---

## 2026-05-03 階層モデル新仕様 Phase 2 サブフェーズ D-1: 排他検証から layerId 依存撤去

- 現状.md §6.10 Phase 2-D の最初のサブフェーズ。Phase 2-D 全体は `placement.layerId` 参照 36 ファイル超 / `Layer` 直接参照 13 ファイル超に及ぶため、§6.10 スコープ管理ルール（1サブフェーズ ~15 ファイル / ~300 行）に従い「排他検証経路のみ」に絞って先行実施
- 変更内容（10 ファイル）:
    - `src/renderer/domain/services/ConflictDetectionService.ts`:
        - `SpatialConflict.layerId` フィールド削除
        - `detectSpatialConflicts(features, vertices, time)` から `targetLayerIds` パラメータ撤去
        - `groupPolygonsByLayer` を `collectLeafPolygons`（末端地物のフラットなリスト）に置換
        - `detectConflictsForFeature` のオーバーロードを単一シグネチャ `(target: string | Feature, features, vertices, time)` に整理。`explicitLayerId` パラメータ撤去。`otherAnchor.placement.layerId !== targetLayerId` フィルタを削除して地図全体排他へ移行
        - 関数ヘッダーコメントで Phase 2-D-1 の方針（地図全体排他 + 末端地物のみ対象）を明記
    - `src/renderer/domain/services/PolygonValidationService.ts`: `validatePolygonFeature` の `explicitLayerId` パラメータ撤去
    - `src/renderer/application/polygonValidation.ts`: `validatePolygonOrThrow` の `explicitLayerId` パラメータ撤去。エラーメッセージを「ポリゴンが同一レイヤーの地物 ... と重なっています」→「ポリゴンが他の末端地物 ... と重なっています」へ変更
    - `src/renderer/application/EditFeatureUseCase.ts`: `EditFeatureUseCase.detectConflictsForFeature(featureId, currentTime)` から `layerId` パラメータ削除、内部呼び出しからも削除
    - `src/renderer/application/PrepareFeatureAnchorEditUseCase.ts`: `detectConflicts` 内部の `detectConflictsForFeature` 呼び出しから `activeAnchor.placement.layerId` 引数を削除
    - `src/renderer/App.svelte`: `validatePolygonOrThrow` 呼び出しから `anchor.placement.layerId` 引数削除
    - `src/renderer/application/commands/AddFeatureCommand.ts`: `validatePolygonOrThrow` 呼び出しから `this.params.layerId` 引数削除（`createTransientPolygonFeature` の `layerId` 引数は `placement.layerId` フィールド維持のため残置）
    - `src/renderer/presentation/app/appPolygonEditing.ts`: `validatePolygonOrThrow` 呼び出しから `layerId` 引数削除（同上の理由で `validatePendingPolygon` 自体の `layerId` パラメータは保持）
    - `src/renderer/presentation/components/ConflictResolutionDialog.svelte`: `<p>レイヤー: {currentConflict.layerId}</p>` 行を削除
    - `src/renderer/domain/services/GeometryService.ts`: ヘッダーコメント (line 5) と `ringsOverlap` 関数コメント (line 157) の §2.1 説明文を「同一レイヤー内のポリゴン重なり検出」→「末端地物の排他性 — ポリゴンリング重なり判定の幾何プリミティブ」「末端地物排他検証（地図全体）の幾何プリミティブ」へ追従更新（§6.0.1 検出観点1: 仕様文書での用語撤廃宣言時の同一文書同期。GeometryService 本体は座標計算ヘルパーで layerId に触れていないため挙動影響なし）
- テスト更新（5 ファイル）:
    - `tests/domain/services/ConflictDetectionService.test.ts`:
        - 「同一レイヤーで重なるポリゴンを競合として検出する」→「重なる末端ポリゴンを競合として検出する」（`layerId` フィールドアサーション削除）
        - 「異なるレイヤーのポリゴンは競合しない」→「異なるレイヤーIDの末端ポリゴン同士でも重なれば競合として検出する（地図全体排他）」（振る舞い反転）
        - 「targetLayerIds でフィルタリングできる」テスト削除（パラメータが消えたため）
        - 「異なるレイヤーの地物とは競合しない」→「異なるレイヤーIDの末端地物とも重なれば競合として検出する（地図全体排他）」（振る舞い反転）
        - 「一覧に未登録の仮想ポリゴンでも競合を検出できる」: `'layer-1'` 引数を削除
    - `tests/domain/services/PolygonValidationService.test.ts`: 「同一レイヤー重複を conflicts へ含める」→「末端地物重複を conflicts へ含める」
    - `tests/application/polygonValidation.test.ts`: 「validatePolygonOrThrow は同一レイヤー重複で相手 ID を含むエラーを投げる」→「末端地物重複で相手 ID を含むエラーを投げる」+ エラーメッセージ期待値を新文言に更新
    - `tests/application/EditFeatureUseCase.test.ts`: `edit.detectConflictsForFeature(..., 'l1')` の第3引数を 2 箇所削除
    - `tests/presentation/state/conflictResolutionMachine.test.ts`: `makeConflict` / `makeConflicts` のダミー値が旧形（`featureA: { id, layerId } / featureB: { id, layerId } / overlapRatio` を `as SpatialConflict` でキャスト）だったため、新形（`id / featureIdA / featureIdB / atTime`）へ揃え as キャストも撤廃。xstate actor 側はこれら本体フィールドを参照していないため runtime 振る舞いは不変だが、SpatialConflict 表面が変わったタイミングで型乖離を解消（コミット e2276d7 以来の旧形ダミーを掃除）
- 追加肯定テスト（+2件、§6.0.1 検出観点2「対概念の同期」）:
    - `tests/domain/services/ConflictDetectionService.test.ts` の `detectSpatialConflicts` / `detectConflictsForFeature` 各 describe に「異なるレイヤーIDの末端ポリゴン（地物）が**重ならなければ**競合しない（重なりだけが判定基準）」の積極ケースを追加。地図全体排他のセマンティクスを「異レイヤーでも重なれば検出 ↔ 異レイヤーで重ならなければ非検出」の対で固定し、レイヤーIDが排他判定要因として完全に独立したことを明示する
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **§6.6.8** 「排他検証は仕様の対象範囲を実装で広げない」: 「要件定義書 §2.1 line 145-153 は『**末端地物の排他性は地図全体に適用する**』『**排他検証は末端地物同士のみ**』と明記」 → 今回は「同一レイヤーのみ」から「地図全体」へ広げる方向だが、§2.1 の通りに揃える。`isLeafPolygonAnchor` ヘルパー（Phase 2-C-2 で導入）は維持し、移行期間ノードを排他検証から除外する判定は変えない
    - **§6.3.13** 「optional フィールド導入は全層を1サブフェーズに詰めない」: 「1サブフェーズあたりの差分は ~15 ファイル / ~300 行を上限に分割する」 → Phase 2-D 全体は 36 ファイル超のため、今回は排他検証経路（8 ファイル / 139 行）のみに絞り、`placement.layerId` フィールド削除や `Layer` エンティティ削除は別サブフェーズへ切る
    - **memory/feedback_subphase_scope_split.md** 「サブフェーズは1経路ずつ」 → 排他検証 1 経路のみ。`LayerService` / `LayerPanel` / Sidebar / Auto-coloring / ProjectSettings には触らない
    - **memory/feedback_break_existing_freely.md** 「既存動作の変更を遠慮しない。テストは必要」 → 「同一レイヤー排他」→「地図全体排他」の振る舞い変更を行い、既存テストの期待値を新仕様に合わせて反転または削除
    - **要件定義書 §2.1** (Phase 1 で改訂済み): 「末端地物の排他性は地図全体に適用する」「排他検証は末端地物同士のみ」 — 直接の根拠仕様
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npx vitest run` 1486 passed（96 ファイル）。1485 → 1486 の内訳: `targetLayerIds` フィルタテスト 1 件削除（−1）+ 地図全体排他の対概念肯定テスト 2 件追加（+2）
- **§6 への新規教訓追加要否**: 不要。既存教訓 §6.6.8 / §6.3.13 / 要件定義書 §2.1 の射程内。`SpatialConflict.layerId` フィールド削除と `explicitLayerId` パラメータ撤去はいずれも仕様で既に「地図全体排他」と確定していた振る舞いに実装を合わせるだけの修正
- **手動動作確認手順**:
    1. アプリを起動し、既存 `.gimoza` プロジェクトを開く（または新規プロジェクトで2地物以上を作成）
    2. レイヤーが複数あるプロジェクトで、異なるレイヤーに属する末端ポリゴン地物を空間的に重なるよう編集する
    3. 期待: 編集確定時に競合解決ダイアログが開き、両地物が競合として一覧に表示される（旧挙動: 異レイヤーは検出されなかった）
    4. 競合解決ダイアログにレイヤー欄が表示されないことを確認（時間欄のみ）
    5. ダイアログ内で優先地物を選択して確定 → 通常通りブーリアン差分で解決される
    6. 末端地物 + 集約地物（移行期間ノード = shape あり + childIds 非空）の組み合わせは引き続き競合として検出されない（§6.6.8 末端地物のみ対象）
- **影響**: コード変更のみで仕様変更なし（要件定義書 §2.1 line 145-153 の規定に実装を合わせた）。runtime 振る舞いは「異なるレイヤー間の重なりも competition として検出する」点が変化（地図全体排他）。`SpatialConflict.layerId` フィールドが消えたため、外部から JSON 形式でこの構造を参照する箇所があれば追従が必要（現状は内部利用のみ）。次サブフェーズ D-2 で `Layer` エンティティ削除 / `LayerService` 整理に進む

---

## 2026-05-03 階層モデル新仕様 Phase 2 サブフェーズ C-4: shape 保持規則 validation

- 現状.md §6.10 の正準プラン（C-1〜C-4 + Phase 2.5）に従い C-4 を実施。`jsonSerializerValidation.ts` に `validateShapePresence` を追加し、`validateJsonWorld` に組み込んだ。手編集 JSON / 外部連携 JSON 経路で migration を経由しない場合や、migration を通過してしまう型不整合に対する保険として機能する
- 変更内容（2 ファイル / +66 / -3 行）:
    - `src/renderer/infrastructure/persistence/jsonSerializerValidation.ts`:
        - `validateShapePresence(json)` を新設。検証対象は単一錨内で完結する規則のみ:
            - `shape === undefined` ⟹ `featureType === 'Polygon'` かつ `placement.childIds.length > 0`（コンテナ）
            - `shape !== undefined` ⟹ `featureType` と `shape.type` が整合（Point↔"Point" / Line↔"LineString" / Polygon↔"Polygon"）
        - `expectedShapeTypeFor(featureType)` ヘルパーで Point/Line/Polygon → 期待 shape.type を返す。featureType が Point/Line/Polygon 以外の場合は `null` を返し整合チェックをスキップ（未知 featureType の検出は `transforms.ts` の deserialize 段で `Unknown feature type` として扱う既存責務を維持）
        - `validateJsonWorld` の return リストに `validateShapePresence(json)` を追加。`validatePlacementInvariants` の直後・`validateSharedVertexGroups` の前
        - 関数ヘッダーコメントで Phase 2-C-4 のスコープと Phase 3 へ defer する事項（子参照健全性・親子相互整合・循環検出・リーフ排他・親≡子の和）を明記
    - `tests/infrastructure/persistence/JSONSerializer.test.ts`: 既存「不正な形状タイプでエラー」テスト 1 件の期待メッセージを更新。`featureType: 'Point' + shape: { type: 'Circle' }` 入力は migration `normalizeShape` を通過したのち `validateShapePresence` が `shape.type "Circle" does not match featureType "Point"` を先に検出するため、`Unknown shape type` から新エラー文言へ差し替え。意図（未知 shape タイプの拒否）は新検証で保たれる
- テスト追加（+10件、合計 1475 → 1485件）:
    - `tests/infrastructure/persistence/jsonSerializerValidation.test.ts` 新規 +10件:
        - 合格ケース 4件: Point/Line/Polygon の正常 shape / 集約地物（shape 欠落 + childIds 非空）
        - shape 欠落エラー 3件: Polygon + childIds 空 / Point + shape 欠落 / Line + shape 欠落
        - 整合不一致エラー 3件: Point に LineString shape / Line に Polygon shape / Polygon に Point shape
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **§6.6.8** 「排他検証は仕様の対象範囲を実装で広げない」: 「ドメインの基本ルール（排他性・整合性・階層）が仕様で明示的に対象を限定しているとき、実装で『対称性確保』『自然な拡張』を理由に対象範囲を広げない」 → 現状.md §6.10 Phase 2-C-4 のスコープ外（Phase 3 本体へ）として明記された「子参照健全性 (childIds 存在・Polygon 型)」「親子相互整合 (時間区間カバレッジ)」「循環検出 (時間スライス)」「リーフ排他 (地図全体)」「親 ≡ 子の和」を `validateShapePresence` に絶対に含めない判断に適用。今回は単一錨内 `shape ↔ featureType ↔ childIds` の規則のみ
    - **§6.3.13** 「optional フィールド導入は全層を1サブフェーズに詰めない」: 「4. shape 保持規則 validation（独自規則のみ。子参照健全性・親子整合は別フェーズ）」 → 触る経路を「(1) validation 関数追加 (2) ユニットテスト」の 2 ファイルに固定。1 ファイル改修にとどめると Phase 3 で同じファイルを再度大改修することになるため、テストファイル新設で関心を分離
    - **§6.0.1** 検出観点1「語彙の grep + 修正した表現の他箇所同期」: 既存 validators の表記様式 `Feature "${featureId}" anchor "${anchorId}" ...` および migration `resolveShape` のエラーメッセージ文言（`has no shape but is not a container (childIds is empty)` / `... requires a shape`）と整合させ、両層で同一文言にした。これにより外部レビューや解析時に同じ grep ヒットで両方の発生源を辿れる
    - **要件定義書 §4.1**: 「`shape`?: オブジェクト。地物の形状定義。**末端地物（リーフ）のみが保持する**。集約地物（コンテナ）はこのプロパティ自体を持たず、形状は下位領域の和として実行時に導出する」「点情報の場合: `{ type: "Point", vertexId: "string" }` / 線情報の場合: `{ type: "LineString", vertexIds: ["string"] }` / 面情報（末端地物）の場合: `{ type: "Polygon", rings: [Ring] }`」 → 単一錨内 shape 規則の根拠仕様。`validateShapePresence` はこの規則だけを忠実に検証
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npx vitest run` 1485 passed（96 ファイル）
- **§6 への新規教訓追加要否**: 不要。既存教訓 §6.3.13 / §6.6.8 / §6.0.1 / 要件定義書 §4.1 の射程内で既存 validators の表記様式に揃えるだけの実装のため、新規教訓化するものではない
- **影響**: 手編集 JSON / 外部連携 JSON で `featureType` と `shape.type` の不整合や shape 欠落の規則違反が早期に SerializationError として検出されるようになった。既存の正常 `.gimoza` 互換性に変化なし。Phase 2-C 完了。次は Phase 2-D（`Layer` 削除 + `placement.layerId` 削除）または Phase 2.5（コンテナ UI 基盤）に進む選択肢

---

## 2026-04-30 階層モデル新仕様 Phase 2 サブフェーズ C-3: Consumer narrowing — Infrastructure + Presentation 層

- 現状.md §6.10 の正準プラン（C-1〜C-4 + Phase 2.5）に従い C-3 を実施。C-2 終了時点で Domain + Application 層の narrow 漏れエラーは消滅していたが、`tsc -p tsconfig.web.json` には Infrastructure (`hitTestUtils` / `vertexSelectionContext`) + Presentation (`appFeatureDragging` / `appPolygonEditing` / `appSelection` / `edgeSlideConstraintCollectors` / `labelRendererUtils` / `mapCanvasUtils` / `parentTransferDialogUtils`) 層の shape narrow 漏れが残っていた。今回これらを全消滅させた
- 変更内容（14 ファイル）:
    - `src/renderer/infrastructure/rendering/hitTestUtils.ts`: `for (const feature of features)` ループ入口に `if (!anchor.shape) continue;` を追加。コンテナ錨をヒットテスト対象から除外
    - `src/renderer/infrastructure/rendering/vertexSelectionContext.ts`: `buildVisibleVertexOwnerMap` のループ内に `if (!anchor.shape) continue;` を追加。コンテナ錨を頂点所有マップから除外
    - `src/renderer/infrastructure/rendering/vertexHandleUtils.ts`: `getShapeVertexPositions(shape: FeatureShape | undefined, ...)` / `getShapeEdgePositions(shape: FeatureShape | undefined, ...)` を `FeatureShape | undefined` 受容化し、`shape === undefined` のとき空配列を返す。`VertexHandles.svelte` の `anchor.shape` 直渡しを安全化
    - `src/renderer/presentation/app/appFeatureDragging.ts`: `collectAnchorVertexIds` 入口を `if (!anchor || !anchor.shape) return vertexIds;` に変更
    - `src/renderer/presentation/app/appPolygonEditing.ts`:
        - `isPolygonAnchor(anchor): anchor is PolygonAnchor` 型 predicate を新設（`anchor.shape !== undefined && anchor.shape.type === 'Polygon'`）
        - `getRingDrawingTarget` の narrow を `!anchor || !isPolygonAnchor(anchor)` に置換（`PolygonAnchor` 戻り型に narrow 伝播させるため）
        - `getAnchorReferenceLongitude` 入口に `if (!anchor.shape) return null;`
        - `getSelectedPolygonReferenceLongitude` の判定を `anchor.shape && anchor.shape.type === 'Polygon'` に拡張
    - `src/renderer/presentation/app/appSelection.ts`: `collectAnchorVertexIds` 入口に `if (!anchor.shape) return [];`
    - `src/renderer/presentation/app/edgeSlideConstraintCollectors.ts`: 3 経路 (`collectSameLayerPolygonObstacleRings` / `collectSameLayerPolygonObstacleVertices` / `collectMovingPolygonEdgeConstraints`) の Polygon ガードに `!anchor.shape ||` を前置
    - `src/renderer/presentation/components/labelRendererUtils.ts`: `getFeatureLabelPosition` / `measureFeatureLabelArea` / `measureFeatureLabelLength` / `shouldRenderFeatureLabel` の各入口に `!anchor.shape ||` ガードを追加（4 関数）
    - `src/renderer/presentation/components/mapCanvasUtils.ts`: `getAnchorVertexCount` / `getAnchorLongitudeBounds` の入口に `if (!anchor.shape) return 0/null;`
    - `src/renderer/presentation/components/parentTransferDialogUtils.ts`: `getActivePolygonAnchor` の判定に `!anchor.shape ||` を前置
    - `src/renderer/presentation/components/FeatureRenderer.svelte`: `getLayerFeatures` のフィルタ条件に `&& anchor.shape` を追加（コンテナを描画対象から除外。Phase 2.5-B でコンテナ描画を実装するまでの暫定）。`getPolygonPath` 入口に `!anchor.shape ||` を前置
    - `src/renderer/presentation/components/PropertyPanel.svelte`: 種別フィールドを `anchor.shape?.type ?? '集約地物'`、形状情報フィールドを `anchor.shape?.type === ...` に narrow（コンテナでもクラッシュしない防御的フォールバック）
    - `src/renderer/App.svelte`: 5 経路 (line 211 / 1319 / 1486-1495 / 1695-1711 / 1949) の `anchor.shape.X` 直接参照に narrow を追加
    - `src/renderer/presentation/components/mapCanvas/MapCanvasHud.svelte`: `EditToolbar` 表示条件に `selectionAnchor.shape` ガードを追加（コンテナ選択時にツールバーが crash しないように）
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **§6.3.13** 「optional フィールド導入は全層を1サブフェーズに詰めない」: 1サブフェーズあたりの差分は ~15 ファイル / ~300 行を上限に分割する → 今回は (2) consumer narrow のうち Infrastructure + Presentation 層のみ。新規 validation (C-4) / UI 基盤 (Phase 2.5) には触らない
    - **§6.6.8** 「排他検証は仕様の対象範囲を実装で広げない」（リーフ判定の運用節）: 「shape を保持し `childIds.length === 0` を必要十分条件とする。`isLeafPolygonAnchor` のような共通ヘルパーで判定をひとつに集約」 → 今回は **描画・ヒットテスト・選択・ドラッグ・ラベル** 経路の narrow であって排他検証経路ではないため `isLeafPolygonAnchor` は使わず、`if (!anchor.shape) continue/return;` の単純 narrow にとどめる（C-2 の Domain/Application 排他検証経路と判定基準を分離）
    - **§6.6.9** 「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」: コンテナ描画は Phase 2.5-A〜C で対応する。今回はコンテナを「描画されない・選択不能・ラベルなし」の skip 動作で揃える（実データに containers が無いため runtime UX に影響なし）
    - **memory/feedback_subphase_scope_split.md**「サブフェーズは1経路ずつ」: Infrastructure + Presentation 層の narrow のみに絞り、validation や UI 基盤・コンテナ描画には踏み込まない
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npm test` 1475 passed（テスト数変化なし — runtime 不変のため新規テスト不要）
- **`tsc -p tsconfig.web.json` の状態変化**: C-2 終了時点で Infrastructure + Presentation 層に残っていた shape narrow 漏れエラー（hitTestUtils 6件 / vertexSelectionContext 1件 / appFeatureDragging 7件 / appPolygonEditing 7件（うち TS2322 が PolygonAnchor 戻り型 narrow を含む）/ appSelection 7件 / edgeSlideConstraintCollectors 9件 / labelRendererUtils 16件 / mapCanvasUtils 10件 / parentTransferDialogUtils 1件）はすべて消滅。残るのは shape と無関係な pre-existing 型エラーのみ（`Window.api` 型未宣言 / lib mismatch / `toSorted` ES2023 など）で C-3 のスコープ外
- **§6 への新規教訓追加要否**: 不要。既存教訓 §6.3.13 / §6.6.8 / §6.6.9 の射程内。`getShapeVertexPositions` の signature 変更（`FeatureShape` → `FeatureShape | undefined`）は §6.6.9 の「3経路で同じ shape 解決経路を共有する」原則の前段で「コンテナを skip する経路を統一する」だけのつなぎ実装で、新規教訓化するものではない（Phase 2.5-B で正式な派生形状描画に置き換わる）
- **影響**: コード反映のみで仕様変更なし。runtime 挙動は完全に不変（実データに undefined shape が無いため narrow パスは runtime で発生しない）。次サブフェーズ C-4 で `jsonSerializerValidation.ts` への `validateShapePresence` 追加に進む

---

## 2026-04-30 階層モデル新仕様 Phase 2 サブフェーズ C-2: Consumer narrowing — Domain + Application 層

- 現状.md §6.10 の正準プラン（C-1〜C-4 + Phase 2.5）に従い C-2 を実施。C-1 で `FeatureAnchor.shape` を `FeatureShape | undefined` 化したことで `tsc -p tsconfig.web.json` に噴出していた consumer の narrow 漏れエラーのうち、Domain + Application 層のものを全消滅させた。Infrastructure / Presentation 層は C-3 のスコープなので今回は触らない
- **自レビュー後の修正**: 当初は `if (!anchor.shape) continue;` の単純 narrow を排他検証経路にも適用したが、§6.6.8「リーフ判定: shape を保持し `childIds.length === 0` を必要十分条件」「移行期間ノード（shape あり + childIds 非空）も `ConflictDetectionService` に拡張禁止」を満たさない（移行期間ノードが排他検証に含まれてしまう）。レビューを受けて `isLeafPolygonAnchor` 共通ヘルパーを `FeatureAnchor.ts` に新設し、`ConflictDetectionService` / `PolygonValidationService` の3経路はリーフ判定ベースへ集約した
- 変更内容（18 ファイル / +60 / -24 行）:
    - `domain/value-objects/FeatureAnchor.ts`:
        - `LeafPolygonAnchor` 型 predicate 用の絞り込み型を新設（`FeatureAnchor & { shape: Polygon shape }`）
        - `isLeafPolygonAnchor(anchor): anchor is LeafPolygonAnchor` ヘルパーを新設。判定式は `anchor.shape?.type === 'Polygon' && anchor.placement.childIds.length === 0`。要件定義書 §2.1 / §6.6.8 / 現状.md §6.4 に従い、移行期間ノードを排他検証から除外する判定の共通化点
    - `domain/services/`:
        - `SharedVertexService.ts`: `anchorContainsVertex` / `getAdjacentVertexIdsInAnchor` / `getRestrictedShapeComponentKeys` の各 switch 直前に `if (!anchor.shape) [return false / return [] / continue]` を追加（コンテナ錨は共有頂点判定の対象外）
        - `ConflictDetectionService.ts`: `detectConflictsForFeature` (line 132 / 146) と `groupPolygonsByLayer` (line 178) を `!isLeafPolygonAnchor(anchor)` ベースの絞り込みに変更。**§6.6.8 厳密遵守**: 集約地物・移行期間ノードを排他検証経路から完全に除外
        - `PolygonValidationService.ts`: `validatePolygonFeature` の早期 return 条件を `!isLeafPolygonAnchor(activeAnchor)` に変更。type predicate により後続の `activeAnchor.shape.rings` 参照は narrow 済みで型エラーなし
        - `LayerService.ts`: `deriveParentShape` の子錨ループ内ガードと `resolvePolygonRings` の入口に `!anchor.shape ||` を前置。**Phase 2.5-A defer**: 子コンテナの孫リーフ再帰取り込み（§6.6.9）/ `polygonUnionAll` 多 polygon 保持（§6.6.5）/ visited cycle guard（§6.4.14）は未実装でコメントに明示
    - `application/`:
        - `polygonValidation.ts`: `anchorContainsAnyVertex` switch 直前に `if (!anchor.shape) return false;`
        - `AddFeatureUseCase.ts`: `computeNextRingNum` で `a.shape.type === 'Polygon'` を `a.shape && a.shape.type === 'Polygon'` に
        - `DeleteFeatureUseCase.ts`: `collectVertexIds` switch 直前に `if (!anchor.shape) continue;`
        - `EditFeatureUseCase.ts`: `addHoleRing` / `addExclaveRing` / `deleteRing` / `validateRingPlacement` の Polygon ガードに `!anchor.shape ||` を前置（4 箇所）
        - `VertexEditUseCase.ts`: `insertVertexOnLine` / `insertVertexOnPolygon` / `deleteVertexFromLine` / `deleteVertexFromPolygon` の shape 型チェックに `!anchor.shape ||` を前置（4 箇所）
        - `ReassignFeatureParentUseCase.ts`: `collectFeatureVertexIds` switch 直前に `if (!anchor.shape) continue;`
        - `ResolveFeatureAnchorConflictsUseCase.ts`: 競合解決ループ内 (line 95 / 100) と `resolveRings` (line 176) のガードに `!anchor.shape ||` を前置（差分演算は移行期間ノードに対しても適用しうるため、リーフ判定ではなく shape 存在 narrow にとどめる）
        - `PrepareFeatureAnchorEditUseCase.ts`: `detectConflicts` の Polygon ガードに `!activeAnchor.shape ||` を前置
    - `application/commands/`:
        - `DeleteVertexCommand.ts`: `collectAllUsedVertexIds` switch 直前に `if (!anchor.shape) continue;`
        - `MergeFeatureCommand.ts`: 結合対象アクティブ錨ガードに `!anchor.shape ||` を前置
        - `MoveFeatureCommand.ts`: `collectActiveVertexIds` 入口を `!anchor || !anchor.shape` ガードに変更
        - `SplitFeatureCommand.ts`: 分割対象アクティブ錨ガードに `!anchor.shape ||` を前置
        - `splitFeatureSharedEdgeInsertion.ts`: `insertSharedEdgeVerticesIntoFeature` 入口を `!anchor || !anchor.shape` ガードに変更（runtime で undefined shape が `insertSharedEdgeVerticesIntoShape` に渡らないようにする防御）
- テスト追加（+4件、合計 1471 → 1475件）:
    - `tests/domain/value-objects/FeatureAnchor.test.ts` +4件: `isLeafPolygonAnchor` がリーフ（shape あり + childIds 空）で true / 移行期間ノード（shape あり + childIds 非空）で false / コンテナ（shape なし + childIds 非空）で false / Point shape（shape あり + childIds 空だが Point）で false
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **§6.3.13** 「optional フィールド導入は全層を1サブフェーズに詰めない」: 1サブフェーズあたりの差分は ~15 ファイル / ~300 行を上限に分割する → 今回は (2) consumer narrow のうち Domain + Application 層のみ。Infrastructure / Presentation は C-3 へ、新規 validation は C-4 へ、UI 基盤は Phase 2.5 へ切り出した
    - **§6.6.8** 「排他検証は仕様の対象範囲を実装で広げない」: 「リーフ判定の運用: shape を保持し `childIds.length === 0` を必要十分条件とする。`isLeafPolygonAnchor` のような共通ヘルパーで判定をひとつに集約し、検証経路間でのドリフトを防ぐ」 → `isLeafPolygonAnchor` を `FeatureAnchor.ts` に集約し、`ConflictDetectionService` / `PolygonValidationService` の3経路で使用。要件定義書 §2.1 line 150-153 の「集約地物には直接の排他制約を課さない」を厳密に遵守
    - **§6.6.9** 「描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有する」: 「子地物が集約地物（`shape === undefined`）であれば孫の和を再帰呼び出しで取り込む（多段階層）」 → 今回は **未実装として Phase 2.5-A に defer**。`LayerService.deriveParentShape` のコード冒頭コメントで既知制約を明示。Phase 2-C 時点では containers が実データに無いため runtime 影響なし
    - **memory/feedback_subphase_scope_split.md**「サブフェーズは1経路ずつ」: スコープを Domain + Application 層のみに限定し、UI / 描画 / 派生 / validation には手を出さない
    - **memory/feedback_no_speculative_helpers.md**: `isLeafPolygonAnchor` は §6.6.8 が明示的に推奨する共通ヘルパー（「`isLeafPolygonAnchor` のような共通ヘルパーで判定をひとつに集約し、検証経路間でのドリフトを防ぐ」）であり、本サブフェーズで `ConflictDetectionService` / `PolygonValidationService` の3経路で実際に使用するため speculative ではない。C-1 で同梱した `requireLeafShape` は今回適用箇所が発生せず保留（リーフ前提関数の本格使用は Phase 2.5 / Phase 4 で発生する見込み）
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npm test` 1475 passed
- **`tsc -p tsconfig.web.json` の状態変化**: C-1 終了時点で Domain + Application + Infrastructure + Presentation 全層の narrow 漏れエラーが噴出していたが、C-2 終了時点で Domain + Application 層のものは全消滅。残るのは Infrastructure (`hitTestUtils`) + Presentation (`appFeatureDragging` / `appPolygonEditing` / `appSelection` / `edgeSlideConstraintCollectors` / `labelRendererUtils` / `mapCanvasUtils` / `parentTransferDialogUtils`) のみ（これらは C-3 のスコープ）
- **§6 への新規教訓追加要否**: 不要。既存教訓 §6.3.13 / §6.6.8 / §6.6.9 の射程内。`isLeafPolygonAnchor` の集約は §6.6.8 末尾で明示推奨されているパターンの実装で、新規教訓ではない。`deriveParentShape` の defer は §6.6.9 / §6.6.5 / §6.4.14 の Phase 2.5-A 着手時に解決する既知制約で、新規教訓化するものではない
- **自レビュー失敗の所感**: 当初実装（`if (!anchor.shape) continue;` のみ）は §6.6.8 末尾の「リーフ判定の運用: shape を保持し `childIds.length === 0` を必要十分条件とする」を読み損ねていた。`memory/feedback_read_dev_guide.md` / `memory/feedback_read_spec_before_change.md` の射程内だが、§6.6.8 は今回の作業で AI 作業開始ゲートで引用した教訓そのものであり、引用後の理解の浅さが問題。次回からは「教訓の本文末尾の運用節」までスコープに入れて引用する
- **影響**: コード反映のみで仕様変更なし。runtime 挙動は完全に不変（実データに undefined shape および移行期間ノードが無いため narrow / リーフ判定パスはいずれも runtime で発生しない）。次サブフェーズ C-3 で Infrastructure + Presentation 層の consumer narrow（~12 ファイル）に進む

---

## 2026-04-29 階層モデル新仕様 Phase 2 サブフェーズ C-1: ドメイン型 + 永続化

- 2026-04-29 の一括実装試行（10往復ロールバック）を受けて、現状.md §6.10 の正準プラン（C-1〜C-4 + Phase 2.5）に従い C-1 のみのスコープで再着手。要件定義書 §4.1「`shape`?: ... 末端地物（リーフ）のみが保持する。集約地物（コンテナ）はこのプロパティ自体を持たず、形状は下位領域の和として実行時に導出する」をコードに反映する第1段
- 変更内容（7 ファイル）:
    - `src/renderer/domain/value-objects/FeatureAnchor.ts`:
        - `FeatureAnchor.shape` を `FeatureShape | undefined` へ変更（コンストラクタ・`withShape` 引数も `undefined` 受容）
        - `requireLeafShape(anchor)` ヘルパーを新設（リーフ前提の関数で使う取り出しヘルパー。`shape === undefined` のとき Error を投げる）
            - **C-2 の narrow 適用に向けたヘルパーを C-1 に同梱**: 現状.md §6.10 Phase 2-C-2 の適用パターン「リーフ前提の関数: `const shape = requireLeafShape(anchor);` で取り出し」で利用予定。`memory/feedback_no_speculative_helpers.md` の射程と接するが、「実際に使う段（C-2）の直前で導入」+「リーフ取得 / コンテナ Error の単体テスト 2 件同梱」で API として成立しており、speculative helper ではないと判定
    - `src/renderer/infrastructure/persistence/jsonSerializerTypes.ts`:
        - `JsonFeatureAnchor.shape?: JsonFeatureShape` （optional 化）
    - `src/renderer/infrastructure/persistence/jsonSerializerTransforms.ts`:
        - `serializeAnchor`: `shape === undefined` のとき shape フィールド自体を JSON へ出力しない
        - `deserializeAnchor`: `json.shape === undefined` のとき `FeatureAnchor.shape` を undefined とする
    - `src/renderer/infrastructure/persistence/jsonSerializerMigration.ts`:
        - `normalizeAnchor` / `normalizeLegacyFeatureAnchor` で `placement` を先に正規化し、`resolveShape(record, anchorId, featureType, placement, featureIds, ctx)` で shape の有無を判定する経路へ集約
        - `migrateJsonWorld` 入口で `collectFeatureIds(featureSources)` により全 feature の id セットを抽出し、normalize 経路へ伝播
        - `resolveShape` の判定:
            - shape または fallback ソース（`record.vertexId` / `vertexIds` / `rings`）あり → 既存 `normalizeShape` を呼ぶ
            - shape 欠落 + `featureType === 'Polygon'` + `placement.childIds.length > 0` + 全 childIds が `featureIds` に実在 → undefined（コンテナ）+ 警告
            - shape 欠落 + `featureType === 'Polygon'` + childIds 非空だが不在 child を含む → `SerializationError`（要件定義書 §2.5.2「子の親側参照欠落」検出の最低限）
            - shape 欠落 + Polygon + childIds 空 → `SerializationError`
            - shape 欠落 + Point/Line → `SerializationError`
    - `src/renderer/infrastructure/persistence/jsonSerializerValidation.ts`:
        - `validateOrphanedVertices` と `validatePolygons` の入口で `if (!anchor.shape) continue;` を追加し、コンテナ錨をスキップ
- テスト追加（+11件、合計 1460 → 1471件）:
    - `tests/domain/value-objects/FeatureAnchor.test.ts` +5件: shape を undefined としてコンテナ錨生成 / `withShape(undefined)` でリーフ→コンテナ遷移 / `withShape(shape)` でコンテナ→リーフ復帰 / `requireLeafShape` がリーフ shape を返す / `requireLeafShape` がコンテナで Error を投げる
    - `tests/infrastructure/persistence/JSONSerializer.test.ts` +6件: shape 未定義のコンテナ錨ラウンドトリップで shape フィールド非出力 / 旧形式（shape なし + Polygon + childIds 非空）はコンテナ undefined 化 + 警告 / 旧形式（shape なし + Point）は `SerializationError` / 旧形式（shape なし + Line）は `SerializationError` / 旧形式（shape なし + Polygon + childIds 空）は `SerializationError` / 旧形式（shape なし + Polygon + childIds に存在しない feature ID）は `SerializationError`
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **§6.3.13** 「optional フィールド導入は全層を1サブフェーズに詰めない」: 1サブフェーズあたりの差分は ~15 ファイル / ~300 行を上限に分割する → 今回は (1) 型定義 + (3) JSON 永続化 + 既存 validator スキップのみで 7 ファイル。consumer narrow / UI / 描画 / 新規 validation 規則は C-2〜C-4 へ切り出した
    - **§6.6.8** 「排他検証は仕様の対象範囲を実装で広げない」: `ConflictDetectionService` を集約地物・移行期間ノードに拡張するのは禁止 → C-1 では排他検証ロジックには触らない（C-2 でループ内 `if (!anchor.shape) continue;` の narrow 適用にとどめる）
    - **§6.3.6** 「データ構造と描画ロジックの不整合」: 配置情報の追加でシリアライズ・操作・テストの全箇所網羅 → 型 / serialize / deserialize / migration / validation スキップ / テストを同一変更単位で揃えた
    - **§6.3.5** 「Undo/Redoのデータ復元」: 既存コマンドは `shape` 全体を保存・復元するため自動的に undefined を保持。runtime に未だ undefined shape が現れない範囲なので Undo/Redo 経路の追加対応は不要
    - **§6.0.1** 「仕様文書での用語撤廃・追加宣言時の同一文書同期」: `shape` の optional 化に伴い grep で `anchor.shape` の参照箇所を確認し、C-2 のスコープ把握に使用
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npm test` 1471 passed
- **`npm run check` と consumer narrow の関係**: `tsc -p tsconfig.web.json` を直接走らせると src/renderer 配下の `anchor.shape.type` 直接アクセスが narrow 漏れエラーになるが、本リポジトリの `npm run check` (svelte-check) は .svelte 中心の検査のため通る。runtime も実データに undefined shape が現れないため不変。現状.md §6.10 の方針通り「型エラーを残したまま runtime green」で C-2 へ進む
- **§6 への新規教訓追加要否**: 不要。既存教訓 §6.3.13 / §6.6.8 / §6.3.6 / §6.3.5 / §6.0.1 の射程内
- **影響**: シリアライズ・migration の3つの「shape 欠落」分岐が増えた以外、UI / 描画 / 操作・コマンドの runtime 挙動は不変。次サブフェーズ C-2 で Domain + Application 層の consumer narrow（~15 ファイル）に進む

---

## 2026-04-29 階層モデル新仕様 Phase 2 サブフェーズ C: 一括実装試行 → ロールバック

サブフェーズ C「`FeatureAnchor.shape` optional 化」を一括実装で試みたが、自レビュー10往復が必要となり全撤回した。`git restore` で b77f7ce（サブフェーズ B 完了時点）まで戻し、教訓を恒久化した。

- **失敗の内訳**:
    - 1サブフェーズで「ドメイン型変更 + 全 consumer narrow + JSON 永続化 + migration + validation + 描画 + ヒットテスト + PropertyPanel/LabelRenderer/parentTransfer の UI 対応 + LayerService.deriveParentShape の正式化」まで詰め込んだため、対概念の同期漏れが続発（描画とヒットテストの判定ドリフト、描画とラベル/PropertyPanel の表示条件不一致、migration とドメインサービスの再帰の循環ガード位置不一致）
    - 追補8 で `ConflictDetectionService` に集約地物を追加したが、要件定義書 §2.1 line 150-153「排他検証は末端地物同士のみ」「集約地物ペアに対して直接の非重複検証を実装してはいけない」と逆向きだった（着手前の §2.1 精読不足）
- **教訓の恒久化**:
    - memory（cross-session）: `feedback_subphase_scope_split.md`（サブフェーズは1経路ずつ） / `feedback_read_spec_before_change.md`（ドメイン基本ルール変更前の §2.1 / §2.2 / §6 精読）
    - 開発ガイド §6.2.25（クリック決定一元化） / §6.3.13（optional 全層を1サブフェーズに詰めない） / §6.4.14（親子グラフ cycle guard） / §6.6.8（排他検証の対象範囲を実装で広げない） / §6.6.9（描画・ヒットテスト・派生サービスは同じ shape 解決経路を共有）
- **再スコープ**: 現状.md §6.10 にて Phase 2-C を C-1〜C-4 に再分割、Phase 2.5（コンテナ UI 基盤）を Phase 3 の前に挿入する正準プランへ統合
- **影響**: コード変更ゼロ、テスト総数 1460 件のまま不変

---

## 2026-04-28 階層モデル新仕様 Phase 2 サブフェーズ B: `FeatureAnchor.placement.isTopLevel` 追加

- 要件定義書 §4.1 / 現状.md §6.2 で確定済みの「最上位フラグを錨側に持つ（時間依存スナップショット）」設計をコードに反映する第2段。サブフェーズ A（`property.kind`）は表示専用ラベルで整合性に関与しなかったが、本サブフェーズは「同一錨内で `placement.isTopLevel === (placement.parentId === null)`」の不変条件をコマンド層で維持する点が中核
- 変更内容:
    - `AnchorPlacement` に `isTopLevel: boolean` を required で追加（`src/renderer/domain/value-objects/FeatureAnchor.ts`）。同ファイルに `createAnchorPlacement(layerId, parentId, childIds)` ヘルパーを新設し、`isTopLevel` を `parentId === null` から派生させて呼び出し側が直接フィールドを指定する経路を排除
    - `JsonAnchorPlacement.isTopLevel: boolean` を追加し、シリアライザは常に書き出す（`src/renderer/infrastructure/persistence/jsonSerializerTypes.ts` / `jsonSerializerTransforms.ts`）
    - マイグレーションは `normalizeIsTopLevel` で旧形式（`isTopLevel` フィールドなし）を `parentId === null` から派生し警告を出す。さらに `isTopLevel` と `parentId` の不変条件が崩れた読み込みデータも `parentId` から再派生する（`src/renderer/infrastructure/persistence/jsonSerializerMigration.ts`）
    - 永続化バリデーションに `validatePlacementInvariants` を追加し、シリアライズ層の入口で不変条件を検証（`src/renderer/infrastructure/persistence/jsonSerializerValidation.ts`）
    - 主要 placement 生成箇所を `createAnchorPlacement` 経由に統一（部分更新時も `parentId` から `isTopLevel` を再派生）:
        - `AddFeatureUseCase.createFeature`（新規地物の最上位錨生成）
        - `polygonValidation.createTransientPolygonFeature`（バリデーション用一時地物）
        - `LayerService.buildParentChildLink` / `buildParentChildUnlink`（親子リンク／解除）
        - `DeleteFeatureUseCase.deleteFeature`（子の `parentId: null` 化に連動して `isTopLevel: true`）
        - `ReassignFeatureParentUseCase`：`parentId` 更新 / 旧親 `childIds` 整理 / `buildParentPlacementForSegment`（新親の子追加）すべてヘルパー経由に変更し、`samePlacement` の比較に `isTopLevel` を追加
        - `PrepareFeatureAnchorEditUseCase` の `patch.placement` 部分マージ：マージ後に `createAnchorPlacement` で再構築し不変条件を維持
        - `UpdateFeatureAnchorUseCase.updatePlacement`：渡された `AnchorPlacement` の `isTopLevel` は信頼せず `createAnchorPlacement` で `parentId` から再派生する。アプリケーション層の境界で不変条件違反を遮断する防御
    - `PropertyProjection` に `isTopLevel: boolean` を追加し `projectFromAnchor` で `placement.isTopLevel` を反映（UI 投影でも錨側情報を表現可能に）
- テスト追加（+10件、合計 1450 → 1460件）:
    - `tests/domain/value-objects/FeatureAnchor.test.ts` +3件: `createAnchorPlacement` が parentId === null から isTopLevel=true を派生 / parentId 指定で isTopLevel=false / `withPlacement` で最上位フラグも反映
    - `tests/infrastructure/persistence/JSONSerializer.test.ts` +4件: isTopLevel ラウンドトリップ保持 / 旧形式（フィールドなし）から派生 / parentId 指定旧形式は false 派生 / 不変条件違反データの再派生
    - `tests/application/ReassignFeatureParentUseCase.test.ts` +2件: parentId 更新と同時に isTopLevel を派生 / newParentId=null の所属解除で isTopLevel=true 復帰
    - `tests/application/UpdateFeatureAnchorUseCase.test.ts` +1件: `updatePlacement` が不変条件違反の placement を渡されても `parentId` から再派生する境界防御
    - 既存テスト全量で placement リテラル（24ファイル相当）に `isTopLevel` を追加し、不変条件 `isTopLevel === (parentId === null)` を全テストデータで満たす形に更新。`tests/domain/services/LayerService.test.ts` / `tests/presentation/components/parentTransferDialogUtils.test.ts` / `tests/application/ReassignFeatureParentUseCase.test.ts` のフィクスチャは `createAnchorPlacement` ヘルパー経由に集約し、フィールド追加漏れを構造的に防止
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **§6.3.5** 「Undo/Redoのデータ復元」: placement に required field を追加するとスナップショットの完全性に影響 → 既存コマンドは `placement` 全体を保存・復元するため自動的に `isTopLevel` を保持
    - **§6.3.8** 「イミュータブルエンティティのAPI不備」: withXxx 系メソッド導入時の網羅 → `withPlacement(placement: AnchorPlacement)` は `placement` 全体を差し替える既存仕様で、`createAnchorPlacement` ヘルパーを介した呼び出しに統一することで部分更新時の取りこぼしを防止
    - **§6.3.6** 「データ構造と描画ロジックの不整合」: 配置情報の追加でシリアライズ・操作・テストの全箇所網羅 → JSON 型 / serialize / deserialize / migration / validation / `PropertyProjection` / 全テストデータを同一変更単位で揃えた
    - **§6.0.1** 「仕様文書での用語撤廃・追加宣言時の同一文書同期」: 用語追加（`isTopLevel`）の影響範囲を grep で確認しながら同期 → 全 placement 生成・読み取り箇所を `placement:`、`{ layerId:.*parentId:.*childIds:` で網羅探索
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npm test` 1459 passed、`npm run test:e2e` 180 passed
- **§6 への新規教訓追加要否**: 不要。既存教訓 §6.3.5 / §6.3.6 / §6.3.8 / §6.0.1 の射程内。`createAnchorPlacement` のような派生フィールドを集中ヘルパーに閉じ込めるパターンは §6.3.8 の派生として読める（withXxx の網羅性問題と同型）
- **影響**: コード反映のみで仕様変更なし。新規保存時は自動的に `isTopLevel` フィールドが含まれる。コマンド層がヘルパー経由になったことで、Phase 2 残作業（C: shape optional 化、D: Layer 削除、E: depth 派生サービス）の前提が整った
- **`.gimoza` 互換性の段階整理**: 現状.md §6.2「既存 `.gimoza` 互換性は破棄する」は Phase 2 全体完了後（特にサブフェーズ D で `Layer` / `placement.layerId` を削除した時点）の最終形を指す方針宣言である。サブフェーズ B 単体ではまだ `Layer` / `layerId` が残っているため、`isTopLevel` フィールドだけは旧形式（フィールドなし）に対して migration で `parentId === null` から派生補完する。`Layer` を残したまま `isTopLevel` 追加だけで読込不能にすると、サブフェーズ間で動作するアプリが消える期間が生まれるためのつなぎ実装。サブフェーズ D で `Layer` / `placement.layerId` を削除する時点で、旧形式（`Layer` 配列前提）は本質的に読み込み不能となり、§6.2 の互換破棄が完成する

---

## 2026-04-28 階層モデル新仕様 Phase 2 サブフェーズ A: `FeatureAnchor.property.kind` 追加

- 要件定義書 §4.1 / 現状.md §6.2 で確定済みの「種別ラベルを錨側に持つ（時間依存）」設計をコードに反映する第1段。Phase 2 全体は B（`placement.isTopLevel` 追加）/ C（`shape` optional 化）/ D（`Layer` 削除）/ E（depth 派生サービス追加）が後続するが、`kind` は表示専用 optional ラベルで整合性検証に関与しないため、他サブフェーズと独立して着手・反映できる
- 変更内容:
    - `AnchorProperty` に `kind?: string` を追加（`src/renderer/domain/value-objects/FeatureAnchor.ts`）。錨ごとに保持し、同一錨内の `withProperty` 呼び出しで階級変動（例: 「植民地 → 州」、「国 → 帝国 → 共和国」）を表現可能
    - `JsonAnchorProperty.kind?: string` を追加し、シリアライザは `kind !== undefined && kind !== ''` のみ JSON へ書き出す（`src/renderer/infrastructure/persistence/jsonSerializerTypes.ts` / `jsonSerializerTransforms.ts`）
    - デシリアライズ時は空文字列を `undefined` に正規化（`deserializeAnchorProperty`）
    - マイグレーションは旧形式（`kind` フィールドなし）を `undefined` のまま読み込ませる（`normalizeAnchorProperty` in `jsonSerializerMigration.ts`）。読み込み警告は不要
    - `PropertyProjection` に `kind: string | undefined` を追加し、`projectFromAnchor` で錨の `property.kind` を反映
- テスト追加（+9件、合計 1441 → 1450件）:
    - `tests/domain/value-objects/FeatureAnchor.test.ts` +4件: kind 省略時 undefined / kind 指定保持 / withProperty で階級変動 / withProperty で kind を未指定に戻せる
    - `tests/domain/value-objects/PropertyProjection.test.ts` +1件: kind が投影へ反映される
    - `tests/infrastructure/persistence/JSONSerializer.test.ts` +4件: kind ラウンドトリップ / kind 未設定時に JSON へ書き出さない / 旧形式（kind なし）から読み込み時 undefined / 空文字列 kind を undefined へ正規化
- AI作業開始ゲート（開発ガイド §5.1.2）で参照した教訓:
    - **§6.3.6** 「データ構造と描画ロジックの不整合」: kind を追加したらシリアライズ・操作・テストの全箇所を網羅 → エンティティ / Json型 / serialize / deserialize / migration / PropertyProjection / テストを同一変更単位で揃えた
    - **§6.0.1** 「仕様文書での用語撤廃・追加宣言時の同一文書同期」: 用語追加（`kind`）の影響範囲を grep で確認しながら同期 → AnchorProperty 生成箇所を `new PropertyProjection(` / `PropertyProjection(` で確認しファクトリ関数のみと特定
    - **§6.3.5** 「Undo/Redoのデータ復元」: kind は AnchorProperty 全体としてコマンドの property スナップショットに含まれるため、property を保存する既存コマンド（UpdateFeatureAnchor 系）は自動的に kind を保持する。専用テストは Phase 7 の編集 UI 実装と同じ変更単位で追加予定
- ビルド・型・テスト: `npm run check` 0 errors / 0 warnings、`npm run build` 通過、`npm test` 1450 passed
- **§6 への新規教訓追加要否**: 不要。既存教訓 §6.3.6 / §6.0.1 / §6.3.5 の射程内
- **影響**: kind を編集する UI（PropertyPanel / ParentTransferDialog）は本サブフェーズの対象外。Phase 7（Sidebar UI 整理）と同じ変更単位で導入する。本サブフェーズはデータモデル + 永続化のみ反映するため、ユーザー視点では `.gimoza` ファイルに `kind` を手書きしない限り挙動変化なし

---

## 2026-04-28 階層モデル新仕様 Phase 2 prep 追補 第4回: 外部レビュー反映（§2.5 保存内容の per-anchor 表現 + 履歴セクションの修正済み注記）

- 【中】要件定義書 §2.5「保存内容」の「各地物の最上位フラグ・種別ラベル・ツリー階層（親子関係）」が Feature レベル保持として読める表現だった。今回の主変更（要件定義書 §4.1: `FeatureAnchor.placement.isTopLevel` / `FeatureAnchor.property.kind` を錨側に持つ）と整合させるため、「各歴史の錨における最上位フラグ・親子関係（`placement`）および種別ラベル（`property.kind`）（時間依存スナップショット）」に修正
- 【低】実装済み.md の Phase 2 prep 初版セクション（line 73 周辺）が、後続追補で覆された `Feature.isTopLevel` / `Feature.kind` 前提を肯定文として残しており、grep 時に旧設計として誤参照されるリスクがあった。セクション冒頭に「⚠ 初版時点の記述。後続追補（第1回・第2回・第3回）で `Feature.isTopLevel` / `Feature.kind` を `FeatureAnchor.placement.isTopLevel` / `FeatureAnchor.property.kind` に移動するなど複数の設計判断が修正されている」注記を追加。最新仕様の参照先も明示
- **教訓反映**: 開発ガイド §6.0.1 検出観点2（対概念の同期）の射程内。「Feature レベル保持に読める表現」と「FeatureAnchor 側保持の主変更」の対概念が §2.5 で同期されていなかった事例。grep 観点としては「各地物の最上位フラグ」「各地物の種別ラベル」「地物自身が isTopLevel」「地物自身が kind」を撤廃語クラスに加える運用で次回以降カバーされる
- **影響**: ドキュメント整合性の改善のみ。仕様内容そのものは不変
- **§6 への新規教訓追加要否**: 不要（§6.0.1 の射程内）

---

## 2026-04-28 階層モデル新仕様 Phase 2 prep 追補 第3回: 外部レビュー反映（§4.2 JSON 例の縮退性注記）

- 【軽微】§4.2 JSON 例の合衆国 `childIds` が `["poly-state-virginia"]` のみのため、「親 ≡ 子の和」整合性ルールの観点では本例が「合衆国形状＝ヴァージニア形状」として読める縮退例である。形式的には妥当だが、読者の誤読を防ぐため JSON ブロック直前に「例の縮退性に関する注記」を追加。実際の歴史では13州が並ぶが本例ではヴァージニア州のみを記載していること、実データでは13州の和が合衆国の形状を構成することを明記
- **影響**: ドキュメントへの説明追記のみ。例の構造そのものは不変
- **§6 への新規教訓追加要否**: 不要（軽微な可読性改善）

---

## 2026-04-28 階層モデル新仕様 Phase 2 prep 追補 第2回: 外部レビュー反映（kind の時間依存性 + JSON例検証可能化 + 旧表現整理）

- 【高】**kind の時間依存性問題**: 第1回追補で `kind` を `Feature` 直下のままにしていたが、現状.md §6.9 D「階級変動」は `kind` 更新で扱う前提であり、§4.2 JSON 例の Virginia は kind: "州" 固定のまま pre-1776 anchor の description に「植民地」と記述する矛盾があった。Issue 1 (isTopLevel) と同型の対概念矛盾を踏んでおり外部レビューで指摘された。設計選択として `kind` を `FeatureAnchor.property` 配下へ移動:
    - §4.1 Feature から `kind` を削除し、`FeatureAnchor.property` 配下に追加。「錨ごとに保持することで時間軸上の階級変動（『植民地』→『州』、『国』→『帝国』→『共和国』）を表現できる」設計意図を本文に明記
    - §4.2 JSON 例の各 Feature から top-level `kind` を削除し、各 anchor の `property.kind` 配下へ移動。古王国 A は「国」→「帝国」（territorial_expansion 時に階級変動）を示す例に変更。Virginia は pre-1776 で `kind: "植民地"` / 1776+ で `kind: "州"` を併記し時間依存性を可視化
    - §5.2 Feature 説明を「`kind?`」削除に追従。「最上位フラグ・親子関係・種別ラベルは FeatureAnchor 配下の時間依存情報」と再表現
    - 現状.md §6.2 を「`FeatureAnchor.property` に `kind` を追加する」に修正。階級変動の例も明記
    - 現状.md §6.4 直轄領継承文言を「`kind` は元リーフの遷移時刻における該当錨の `property.kind` を継承」に修正
    - 現状.md §6.8 「`kind` は表示用ラベルとして錨側 (`FeatureAnchor.property.kind`) に持つ」に修正
    - 現状.md §6.12 識別子表を `FeatureAnchor.property.kind`? に修正、補足を「錨側に保持（時間依存）」に更新
- 【中】**isTopLevel 旧表現の残存**: 第1回追補で見落とした2箇所（Phase 2 実装時に Feature 直下へ戻る誘導になる可能性）を修正:
    - 現状.md §2.2 機能要件: 「地物自身が `isTopLevel` / `kind` を持つ」を「`FeatureAnchor.placement.isTopLevel` / `FeatureAnchor.property.kind` を錨側に持つ（時間依存スナップショット）」に修正
    - 現状.md §6.10 Phase 概略 step 2: 「`Feature` 構造変更 = `isTopLevel` / `kind` / ...」を「`FeatureAnchor` 構造変更 = `placement.isTopLevel` / `property.kind` / `shape` optional / `placement.layerId` 削除」に修正
- 【中】**§4.2 JSON 例の共有頂点検証不能性**: 共有頂点グループ `[v3, v20]`（代表座標 (20,10)）に対し v20 が (50,50) で定義され、共有頂点制約を破った状態だった。さらに第1回追補で Virginia ring に v20, v21, v22 を流用したため例として読むと共有頂点検証に落ちる。修正:
    - v20 の座標を (20, 10) に変更し v3 と整合（共有頂点グループの代表座標と一致）
    - Virginia 用に独立した頂点 v30, v31, v32 を vertices 配列に追加し ring を v30/v31/v32 で構成
    - 既存例の参照欠落（新王国 territory ring が v25 を参照するが定義なし）も併せて修正し、v25 を vertices 配列に追加
- **教訓反映**: 開発ガイド §6.0.1 の既存教訓（検出観点2: 対概念の同期）の射程内のため、新規教訓追加は不要。ただし第1回追補で `isTopLevel` を per-anchor に倒したのに `kind` には同じ判断を適用しなかったのは「対概念の同期」の発動ミスに該当。**§4.2 JSON 例は仕様文書のテストデータであり、実データとして検証可能であるべき**という観点を本作業内で意識化（§6.0.1 の射程内で運用される）
- **影響**: コード変更なし、ドキュメント変更のみ。テスト総数・ビルド状態は不変
- **§6 への新規教訓追加要否**: 不要（§6.0.1 検出観点2 の射程内）

---

## 2026-04-28 階層モデル新仕様 Phase 2 prep 追補: 外部レビュー反映（isTopLevel の時間依存性 + 表記/JSON例修正）

- 【高】**Issue 1 (substantive)**: §4.1 で `Feature.isTopLevel` を Feature 直下に置いた初版設計が、`placement.parentId` の時間依存性（FeatureAnchor 配下）と矛盾。連邦化シナリオ（現状.md §6.5: 13国 → 合衆国成立で同一性継続したまま最上位フラグが変わる）と「独立・帰属」の時間軸位相変化を表現できないことを外部レビューで指摘された。設計選択 (A) を採用し `isTopLevel` を `FeatureAnchor.placement` 配下へ移動:
    - §4.1 Feature から `isTopLevel` を削除し、placement 配下に移動。「最上位フラグを錨側に置くことで時間軸上の位相変化を表現できる」設計意図と13国帰属の例を本文に追記
    - §4.1 階層深度の説明を時間依存版に修正（「対象時刻における当該地物の有効錨のツリー位置から派生」「親地物の同時刻有効錨から導出した depth + 1」）
    - §4.2 JSON 例の各 placement に `isTopLevel` を追加。Virginia エントリに pre-1776 錨（isTopLevel: true / parentId: null）と 1776以降錨（isTopLevel: false / parentId: 合衆国）を併設して時間依存を可視化
    - §5.2 Feature 説明から `isTopLevel` を削除し「最上位フラグと親子関係は FeatureAnchor.placement 配下の時間依存情報」と再表現
    - §5.3 ReassignFeatureParentUseCase の不変条件文言を「同一錨内」に修正
    - 現状.md §6.2 の「`Feature` に `isTopLevel` を追加する」を「`FeatureAnchor.placement` に追加する」に変更
    - 現状.md §6.3 の depth 派生・不変条件・データ構造説明を時間依存版に修正
    - 現状.md §6.5 アメリカ合衆国爆誕シナリオを per-anchor モデルに書き換え（時刻 T で新錨追加・既存錨は継続を明示）
    - 現状.md §6.12 識別子表を `FeatureAnchor.placement.isTopLevel` / 「同一錨内」不変条件に修正
- 【中】**Issue 2 (型パス表記の不正)**: `Feature.placement.parentId` という表記は型上不正（placement は FeatureAnchor 配下）。3箇所を修正:
    - §4.1 階層深度節「Feature.placement.parentId を辿る」を「対象時刻における当該地物の有効錨の `placement.parentId` を辿る」に修正
    - §5.2 Feature 説明「`isTopLevel === (parent === null)` 不変条件」を口語表現「最上位フラグの真と親領域なしは同値」に統一
    - §5.3 ReassignFeatureParentUseCase の不変条件表記も同様に統一
- 【中】**Issue 3 (§4.2 JSON 例の参照不整合)**: poly-federation-usa の childIds に poly-state-massachusetts が含まれるが実体定義がなかった。childIds から削除して Virginia 1件のみとし、「親 ≡ 子の和」整合性検証の例示として閉じた構造に修正
- 【中】**Issue 4 (§2.5 整合性チェック項目の混在)**: 「ツリー階層構造の不整合」括弧書きに含まれていた「集約地物が形状を保持／末端地物が形状を欠落」（形状保持規則違反）をツリー位相不整合から分離。「末端／集約地物の形状保持規則違反」を独立項目化し、修復UI設計時の診断単位を明瞭化
- **教訓反映**: 開発ガイド §6.0.1 関連経過に今回事例（Issue 1: Feature 直下フィールド追加時の時間依存対概念との不変条件整合確認漏れ）を追記。新規教訓追加は不要（§6.0.1 検出観点2「対概念の同期」の射程内）。「Feature 直下に新フィールドを置く案を採るときは、対概念（既存の時間依存項目との不変条件）が成立可能か必ず確認する」を関連経過の検出指針として記録
- **影響**: コード変更なし、ドキュメント変更のみ。テスト総数・ビルド状態は不変
- **§6 への新規教訓追加要否**: 不要（外部レビュー時にも明示確認）

---

## 2026-04-28 階層モデル新仕様 Phase 2 prep: 要件定義書 §4.1 / §4.2 / §5 / §2.5 改訂

> ⚠ **初版時点の記述。後続追補（第1回・第2回・第3回）で `Feature.isTopLevel` / `Feature.kind` を `FeatureAnchor.placement.isTopLevel` / `FeatureAnchor.property.kind` に移動するなど複数の設計判断が修正されている**。本セクションは作業初版の履歴記録であり、最新仕様は要件定義書 §4.1 / §4.2 と現状.md §6 を参照。後続追補の修正内容は本セクション直前の追補第1回・第2回・第3回エントリを参照。

- 現状.md §2.2 の「**Phase 2 着手時** に §4.1 データモデル / §5 アーキテクチャ / §2.5 保存（データモデル全面置換の前提）」スコープを実施。Phase 2 のコード置換着手前に、データモデル仕様本文を新階層モデル前提へ整える前提作業を完了
- **改訂内容**:
    - **§4.1 基本データ構造**:
        - `FeatureAnchor.shape` を optional 化。「末端地物（リーフ）のみが保持する。集約地物（コンテナ）はこのプロパティ自体を持たず、形状は下位領域の和として実行時に導出する」と明記。点情報・線情報は常に末端地物として `shape` を保持する旨を併記
        - `FeatureAnchor.placement.layerId` を削除。`parentId` を「文字列または null（最上位フラグが真の地物では null）」に整理し、`childIds` を「下位領域を持たない場合は空配列」に整理
        - `Feature` に `isTopLevel: 真偽値`（最上位フラグ。`isTopLevel === (parent === null)` 不変条件）と `kind?: 文字列`（種別ラベル、表示専用）を追加
        - `Layer` エンティティ定義を削除
        - 「階層深度（派生値、データに保持しない）」セクションを追加。算出ルールと永続化対象外であることを明記
    - **§4.2 保存ファイル形式**:
        - JSON 例から `layers` 配列を削除
        - 各 `placement` から `layerId` を削除
        - 各 `Feature` に `isTopLevel` / `kind` フィールドを追加
        - 連邦化シナリオの例（合衆国コンテナ + ヴァージニア州子地物）を追加。集約地物が `shape` を持たないこと、子地物が `parentId` で親を参照することを明示
    - **§5 アーキテクチャ**:
        - エンティティ一覧から `Layer` を削除
        - ドメインサービス一覧から `LayerService` を削除し、`HierarchyService`（ツリー階層管理、親子参照整合性、最上位フラグ判定、階層深度派生、末端/集約地物判定）を追加
        - ユースケース一覧から `ManageLayersUseCase` を削除し、`ReassignFeatureParentUseCase`（割譲・連邦化・独立・帰属・直轄化・自治化・解体を統合した所属変更フロー、新規上位領域作成サブフロー含む）を追記。新規追加ではなく既存実装の再設計対象である旨を明確化
        - `Sidebar` 説明から「レイヤー一覧」を削除し、「具体的なタブ構成は Phase 7 で確定」と注記
        - `Feature` エンティティ説明に `isTopLevel` / `kind` プロパティと不変条件を追記
    - **§2.5 データ保存・読み込み**:
        - 「保存内容」: 「レイヤー構造と所属関係」を「各地物の最上位フラグ・種別ラベル・ツリー階層（親子関係）」に置換
        - 「データ読み込み時の詳細な挙動」: 旧モデル（レイヤー概念を含む形式バージョン）のファイルを互換性なしとして読み込みエラーで拒否する旨を明記。マイグレーション提供しない方針（現状.md §6.2「既存 `.gimoza` 互換性は破棄する」と整合）
        - 整合性チェック項目を新モデル準拠に更新: 「レイヤーの階層構造の不整合」を「ツリー階層構造の不整合（最上位フラグと親領域の不一致、循環参照、子の親側参照欠落、親の子側参照欠落、集約地物が形状を保持／末端地物が形状を欠落 など）」に置換し、「末端地物排他の違反」「親領域の形状と下位領域の和の不一致」を新規追加
- **現状.md 同期**:
    - §6.12 用語と内部実装識別子の対応表を更新:
        - `Feature.isTopLevel` を「Phase 2 で命名予定。仮称」から「要件定義書 §4.1 で識別子確定。Phase 2 でコード実装予定」に変更
        - `Feature.kind` を同様に変更
        - `FeatureAnchor.shape` の optional 化を「Phase 2 で optional 化予定」から「要件定義書 §4.1 で optional 化確定。Phase 2 でコード反映予定」に変更
        - 不変条件「最上位フラグの真と親領域なしは同値」を `Feature.isTopLevel === (Feature.anchors[*].placement.parentId === null)` に具体化
    - §2.2 機能要件: 「**Phase 2 prep（要件定義書 §4.1 / §4.2 / §5 / §2.5 改訂）は 2026-04-28 完了**」を追記。Phase 3-7 のスコープ振り分けは維持
- **スコープ外**: 旧モデル「レイヤー」言及が残る §2.2.3（Phase 3）/ §2.3.2 / §2.3.3（Phase 4）/ §2.4.4 / §2.4.4.6（Phase 6）/ §2.7 UI（Phase 5/7）は当該 Phase 着手と同じ変更単位で改訂する。今回 grep 検証で残存箇所を確認済み（§4.1 / §4.2 / §5 / §2.5 内に旧用語なし、残存は全て後続 Phase スコープ）
- **影響**: コード変更なし、ドキュメント変更のみ。テスト総数・ビルド状態は不変
- **参照教訓**: 開発ガイド §6.0.1「仕様文書での用語撤廃・追加宣言時の同一文書同期」（検出観点1: grep による撤廃語確認、検出観点2: 対概念の同期、検出観点3: 実装状況主張の事実確認）。今回は §4.1 / §4.2 / §5 / §2.5 内の `レイヤー` / `layerId` / `Layer` / `ManageLayers` / `LayerService` を grep で確認し全箇所修正。`isTopLevel` / `kind` / `HierarchyService` の主張に「Phase 2 でコード実装予定」「実コード未実装」を併記して観点3（既存実装の主張回避）を遵守
- **§6 への新規教訓追加要否**: 不要（§6.0.1 の既存教訓で防止済みのケースに該当）

---

## 2026-04-28 階層モデル新仕様 Phase 1: 要件定義書 §2.1 改訂

- 現状.md §6 で確定した新階層モデル仕様（Layer 廃止 + ツリー型 + 派生階層深度 + 末端地物排他全体適用 + 新規親作成サブフロー + 階層深度フォーカス + 政治的特殊形態）に基づき、要件定義書 §2.1 を全面改訂。§6.10 Phase 1「仕様確定が以降の前提作業」を完了
- **改訂内容**:
    - 「面情報の排他性」: 同一レイヤー内の重複禁止 → 末端地物の排他性を地図全体に適用。集約地物の排他性は派生的に成立すると明記
    - 「穴・飛び地の編集」内の「同レイヤーの他のポリゴン」を「地図全体の他の末端地物」に更新
    - 「共有頂点システム」内の「同一レイヤー内の面重複」「異なるレイヤーに属する共有ポリゴン」を末端地物排他＋集約/末端の評価方針に書き換え
    - 「多層レイヤー構造」セクションを削除し、新セクション「階層構造（ツリー型階層モデル）」と「階層深度フォーカス（表示制御）」に置換。最上位フラグと親不在の同値、親≡子の和の厳密一致、無主地許容、種別ラベル（整合性非関与）を明記
    - 「分裂と割譲/合邦機能」を「分裂と所属変更機能」に改題し、所属変更を「割譲・連邦化・独立・帰属・直轄化・自治化・解体」を表現する単一フローに統合。新規上位領域作成サブフロー、親候補制約、典型シナリオを定義
    - 「合体（結合）機能」を末端地物前提に変更し、結合後の上位領域選択肢に新規上位領域作成サブフロー再利用を追加
    - 新セクション「政治的特殊形態の表現」を追加。連邦・二重君主国・同君連合・共同統治領・占領地・係争地の表現方針を明文化
- **スコープ外**: 要件定義書の他セクション（§2.2.3 整合性維持プロセス／§2.3.2 追加モード／§2.3.3 編集ツール／§2.4.4 自動配色／§2.4.4.6 レイヤー表示とテーマ設定／§2.5 保存／§2.7 UI／§4.1 データモデル／§5 アーキテクチャ）には旧「レイヤー」概念の言及が残存。Phase 5–7（表示フォーカスUI／自動配色／Sidebar UI 整理）の実装と同タイミングで併せて改訂する。本件は現状.md §2.2 に追記済み
- **影響**: コード変更なし、ドキュメント変更のみ。テスト総数・ビルド状態は不変
- 参照教訓: 開発ガイド §6.0「教訓の使い方と更新ルール」、§重要な注意事項「要件定義書は仕様本文と内部実装補遺を分離」、現状.md §6.12「用語と内部実装識別子の対応」（仕様本文では英語識別子を使わず「最上位フラグ」「種別ラベル」「集約地物」「末端地物」等の和訳語のみで記述）
- **追補（同日のフィードバック反映）**: ※ 以下は**初版時点の記述**。後続の追補（外部レビュー反映 第2回・第3回・第5回）で複数項目が修正されている。最終仕様は要件定義書 §2.1 と現状.md §6 を参照
    - 集約地物への遷移時の差分処理を §2.1 階層構造に追記。元の末端地物形状と新下位領域の和の差分は**直轄領（末端地物）として自動生成**する。デフォルト名「`<元名> 直轄領`」、種別ラベル継承、複数領域に分かれる場合は飛び地化ロジックで1つの末端地物として表現
    - **下位領域の獲得経路**を明記: 既存地物が下位領域を獲得するのは「所属変更フロー」または「末端地物と集約地物の混在結合」のみ。分裂・末端地物のみの結合・集約地物のみの結合は新地物の生成または既存地物の形状変化を扱う ⚠ **後続追補で修正済み**: 第2回追補で「混在結合は終了+新規生成に統一」と整理した結果、最終仕様では**所属変更フローのみ**が下位領域獲得経路となる
    - **二分割機能**を「元の末端地物が形状変化として継続、もう片方が新規末端地物として生成」に変更。穴あけと同型化（元地物の同一性を保持）
    - **親候補制約**から「祖先」用語を削除し「対象地物自身および対象地物の上位領域すべて（直接の親、その親、…と最上位まで連鎖して辿った上位領域の集合）」に置換。階層深度フォーカスからも「祖先・子孫・系譜」用語を削除し「上位領域すべて・自身・下位領域すべて（縦方向に繋がる領域）」の説明的表現に置換（時間軸の祖先・子孫と概念混同を避けるため）
    - **結合機能を全面改訂**: 末端地物のみ／集約地物のみ／混在の3パターンに対応
        - 末端地物のみ: 形状の論理和（既存仕様）
        - 集約地物のみ: 平坦化結合（中間階層は保持しない。階層保持は所属変更フローを使う設計分離）
        - 混在（自動帰属）: 末端地物が集約地物の下位領域として加わる。末端地物は存在を継続 ⚠ **後続追補で同一性整理**: 第2回追補で「結合の3パターン全てを終了+新規生成で統一」と整理した結果、混在パターンでは元の集約地物は終了し新統合集約地物が生成される（混在末端地物は存在継続して新統合集約地物の下位領域へ取り込まれる）
    - **結合と所属変更の使い分け**を §2.1 結合機能に明記（結合=地物数を減らす／所属変更=ツリー位相を変える） ⚠ **後続追補で表現修正**: 第3回・第5回追補で「混在結合では末端地物が存在継続するため選択地物総数は減らない場合がある。減るのは結合の主体となる地物の数」と明確化
    - 現状.md §2.2 マニュアル整備の備考に「結合と所属変更の使い分けはマニュアルで重点解説」を追記

---

## 2026-04-28 階層モデル新仕様 Phase 1 追補: 外部レビュー反映 第1回（祖先用語の取りこぼし + §6 同期）

- 【高】要件定義書 §2.1 内「直轄化」説明文に残っていた「祖先」用語を「上位領域（直接の親の親、その親、…と最上位まで連鎖した中の任意の上位領域）」に置換。§2.1 内の用語注記との self-contradiction を解消
- 【中】現状.md §6 (canonical 設計) を Phase 2-4 の前提として §2.1 改訂に同期:
    - §6.4 整合性ルールに「リーフ → コンテナ遷移時の差分自動処理」「下位領域獲得経路」を追記
    - §6.9 B 数量変化テーブルに分裂の動作変更（元リーフ継続+片方新規）と結合の3パターン（リーフのみ/コンテナのみ平坦化/混在自動帰属）を追記
    - §6.9 D の「直轄化」から「祖先」を削除
- 【中】現状.md §6 全体から「祖先・子孫・系譜」用語を撤廃し、説明的表現に統一:
    - §6.5 親候補制約の表現を更新
    - §6.7 表示フォーカス内「個別フォーカス」「縦方向領域の絞り込み」に書き換え
    - §6.8 Sidebar タブ案を「縦方向領域ナビゲーション」に書き換え
    - §6.12 表示テーブルの仕様用語列を「個別フォーカス」「縦方向領域」「縦方向領域の絞り込み」に書き換え（内部実装識別子の継承表記は後の追補で訂正）
- 【低】開発ガイド §6 に新教訓 §6.0.1「仕様文書での用語撤廃・追加宣言時の同一文書同期」を追加。同一文書内 self-contradiction の早期検出（grep 確認）と Phase 完了基準を明記
- **教訓反映**: 開発ガイド §6.0.1 を新規追加。今回の取りこぼし（§2.1 内「直轄化」+ §6 全体の用語表）が「用語撤廃宣言時の同一文書同期不足」という根本原因に該当するため、再発防止教訓として恒久化

---

## 2026-04-28 階層モデル新仕様 Phase 1 追補: 外部レビュー反映 第2回（混在結合の同一性統一 + 用語集同期）

- 【高】混在結合の地物同一性の矛盾を解消。要件定義書 §2.1 集約地物の項「下位領域の獲得経路」「既存集約地物への下位領域追加」と現状.md §6.4「既存コンテナへの子追加」「下位領域獲得経路」が、結合機能本体（line 336）の「元集約地物終了+新集約地物生成」と矛盾していた。**結合機能の3パターン全てを「終了+新規生成」で統一する設計判断**を反映し、混在結合は「既存への子追加」ではなく「結果コンテナ新規生成 + 混在リーフが新コンテナの子として帰属」と整理。「下位領域の獲得経路」は所属変更フロー一本に統一。Undo/Redo・履歴の錨・ID継続の実装方針が結合機能内で一致する
- 【中】要件定義書 §2.1 line 127 の旧セクション参照名「2.1 分裂と割譲/合邦機能」「穴あけ分割」を新名「2.1 分裂と所属変更機能」「穴あけによる新地物生成」に追従修正
- 【中】要件定義書 §6 用語集を新モデルに同期（§6.0.1 教訓を適用）:
    - 旧モデル用語を削除: `レイヤー` / `上位レイヤー` / `下位レイヤー` / `合邦`
    - 既存用語を更新: `上位領域` / `下位領域` / `プロパティ` / `合体（結合）` から「レイヤー」言及を削除
    - 新モデル用語を追加: `末端地物` / `集約地物` / `階層深度` / `最上位フラグ` / `種別ラベル` / `直轄領` / `所属変更フロー` / `新規上位領域作成サブフロー` / `縦方向領域`
- **教訓反映2**: 今回のレビュー指摘は §6.0.1 の事例そのもの（同一文書内 self-contradiction の取りこぼし）。新教訓は機能している（grep 検証で残存箇所を特定し全部修正できた）。ただし **§6.0.1 の検出観点に「対概念の他一方も同期確認」を追加すべき** ことが今回の Issue 1 で判明（混在結合と既存への子追加は対概念で、片方を変えたら他方も同期する必要がある）。教訓追記は次回作業時に検討（追補3 で実施）

---

## 2026-04-28 階層モデル新仕様 Phase 1 追補: 外部レビュー反映 第3回（結合対象選択集合制約 + Phase スコープ細分化）

- 【高】§2.1 結合の制約に「**選択集合内の地物が互いに上位・下位関係にないこと**」を追加。集約地物 A と A の下位領域 X を同時選択する不正ケース（重複 child / 不正なツリー更新）を検出・拒否する仕様を明記。現状.md §6.4 にも「結合対象の選択集合制約」として canonical 設計を追加
- 【中】§2.1 結合の説明文「複数地物の数を減らして1つに統合する操作（地物の数が減る）」を、混在結合と矛盾しない形に修正:「選択された結合対象を1つの結果地物（統合地物）にまとめる操作。結果地物は新規生成され、選択対象のうち**結合の主体**（リーフのみでは選択リーフ全体、集約のみでは選択集約全体、混在では選択集約のみ）は同一性が終了する。混在パターンでは選択された末端地物は存在継続し新統合集約地物の下位領域へ取り込まれる」と明確化。現状.md §6.4 にも「減るのは結合の主体となる地物の数」を追記
- 【中】現状.md §2.2 の Phase スコープ振り分けを細分化。要件定義書の他セクションを Phase 5-7 一括ではなく、各 Phase の前提に応じて Phase 2 (§4.1 / §5 / §2.5) / Phase 3 (§2.2.3) / Phase 4 (§2.3.2 / §2.3.3) / Phase 5 (§2.7 レイヤースライダー部分) / Phase 6 (§2.4.4 / §2.4.4.6) / Phase 7 (§2.7 サイドバー構成) へ割り振り。Phase 2-4 実装時に古い仕様参照する危険を回避
- 【低】開発ガイド §6.0.1 を即時拡張。検出観点を2分類化:
    - 検出観点1（語彙の grep）: 既存内容
    - 検出観点2（対概念の同期）: ある概念に仕様判断を加えたら、対概念にも同じ判断を反映する。grep だけでなく「対概念の説明文がこの判断と整合するか」を能動的に確認
    - **レビュー指摘の即時反映ルール**: レビューで「次回検討」と書きたくなる再発防止観点があれば本節の趣旨に反する。判明した観点は同一作業内で本節へ追記し、先送りしない（追補2 で「次回検討」と書いた反省を反映）
    - 関連する経過に「2026-04-28 §2.1 改訂第2回で対概念同期不足が発見された事例」を追記
- **教訓反映3**: §6.0.1 を更に強化（検出観点2の追加 + 「次回検討」放置禁止ルールの明文化）。今回の取りこぼしは「対概念の同期」という grep では発見できないクラスの問題で、新観点として明文化する価値があった

---

## 2026-04-28 階層モデル新仕様 Phase 1 追補: 外部レビュー反映 第4回（lineage 識別子の事実確認 + 用語集スタブ + 履歴構造再編）

- 【中】現状.md §6.12 の Phase 5 関連 4 行（`global depth slider` / `individual focus` / `lineage` / `lineage filter`）を src/ で grep 検証した結果、いずれもコード未実装と判明。前回追補2 で「内部実装識別子のみ既存の `lineage` を継承」と書いたのは事実誤認。識別子列を「(Phase 5 で命名予定。仮称 `<旧記述>`)」に修正し、備考に「実コード未実装」を追記。Phase 5 着手時に正式な内部識別子を確定する
- 【低】要件定義書 §6 用語集に所属変更フローのバリエーション7語のスタブを追加: `割譲` / `帰属` / `連邦化` / `独立` / `直轄化` / `自治化` / `解体`。各エントリは簡易定義 + 「2.1節参照」型で検索性を向上
- 【低】実装済み.md の構造を再編成。これまで「Phase 1 完了」セクションにぶら下がっていた追補2/3/4（外部レビュー反映 第1〜3回）と本追補（第4回）を、独立した top-level セクションに分離。後から時系列で各レビュー反映を独立して追跡できるようにした
- **教訓反映4**: 用語表の「既存の」「継承」記述は、書く前に必ず src/ で grep 確認する（feedback_completion_definition.md の「UI存在だけで完了にしない」と同じ趣旨で、ドキュメントの「実装済み」記述も実物確認が必要）。今回の事実誤認は前回追補2 時点で grep していれば防げた。**本セッション内で即時実行**: 開発ガイド §6.0.1 に検出観点3「実装状況主張の事実確認」を追加。「既存」「継承」「実装済み」「現行」「採用済み」など実装状況を主張する記述を書くときは、対象識別子を src/ で grep してから書く。書いた後のレビューでも同キーワードで検索し対象実在を確認する。これは前回追補3 で確立した「次回検討放置禁止」ルールに従い、判明した観点を同一作業内で §6.0.1 へ即時追記したもの

---

## 2026-04-28 階層モデル新仕様 Phase 1 追補: 外部レビュー反映 第5回（混在結合の表現同期 + 集約地物排他の安全化 + Phase 4 識別子事実確認 + 履歴版指示）

- 【高】「結合は地物数を減らす」表記が混在結合と矛盾。前回追補3 で §2.1 本体は「結合の主体が同一性を終了、混在末端は存在継続」と修正したが、用語集（要件定義書 §6 line 1575）と現状.md §2.2 マニュアル備考（line 57）と現状.md §6.9 連邦化エントリ（line 212）に古い「地物数を減らす」表現が残存。grep 検証で全箇所検出して修正。**§6.0.1 検出観点1 を私自身が発動しなかった取りこぼし**（修正した表現の他箇所同期確認の不足）
- 【中】要件定義書 §2.1 line 150「集約地物の排他性は派生的に成立する。集約地物同士の関係も自動的に整合する」は、上位-下位関係の集約地物同士は当然包含関係にあるという事実を見落とした危険表現。実装側が集約地物ペアの非重複検証を入れると正しいツリーを拒否する。「**排他検証は末端地物同士のみ**。集約地物には直接の排他制約を課さない。上位-下位関係の集約地物同士の包含は正常な状態。兄弟/無関係集約の非重複は末端地物排他から派生的に導出される副次的性質」と書き換え、実装時の検証アルゴリズムは「末端地物同士のみペア検証」を採用と明記
- 【中】実装済み.md Phase 1 サマリ（同日のフィードバック反映）の line 27（混在結合を下位領域獲得経路に含める旧記述）と line 34（結合=地物数を減らす旧記述）に「⚠ 後続追補で修正済み」マークと最終仕様への参照を追記。履歴は残しつつ最終状態へのリンクを明示
- 【低】§6.12 conquest / federation を Phase 4 で命名予定に修正。**前回追補4 で lineage 系を直したのに同じ表内の他 identifier を確認しなかった取りこぼし**（§6.0.1 検出観点3 を §6.12 全体に発動しなかった）
- 【拡大対応】§6.12 全体を grep 検証し、未実装識別子を一括修正:
    - `Feature.isTopLevel`, `Feature.kind` → Phase 2 で命名予定に修正
    - `FeatureAnchor.shape` → Phase 2 で optional 化予定と注記
    - `Feature.style` → Phase 6 で命名予定に修正
    - leaf, container, depth → 「概念名」と注記し Phase 3 で派生サービス実装予定と明記
    - 不変条件テーブルの `isTopLevel === (parent === null)` → Phase 2 で実装予定と注記
- 【追加教訓】開発ガイド §6.0.1 検出観点1 と検出観点3 を強化:
    - 検出観点1 拡張: 「修正した表現の他箇所同期確認」（同じ説明表現を別箇所で再利用している場合（用語集、マニュアル備考、他章のサマリ等）も grep で検出して同期する。本文を修正したら同じ表現の他出現を必ず grep する）
    - 検出観点3 拡張: 「同じ表内の同類対象一括確認」（ある表内の1行を修正したら、同じ表内の同類エントリにも同じ問題が含まれている可能性が高いため、表全体の identifier を src/ で grep してまとめて修正する）
- **教訓反映5**: 私自身が §6.0.1 を発動しなかった反省として、検出観点1 と検出観点3 にそれぞれ「他箇所同期」「表内一括確認」を追加。今回の取りこぼし（「地物数を減らす」表現の他箇所、§6.12 表内の他 identifier）はいずれも観点拡張の事例として恒久化。前回追補4 で lineage 1行のみ直した直後に conquest/federation という同表内の問題を見落としたのは、「同じ表内の同類対象一括確認」観点が無かったため。今回の追加で再発を防ぐ

---

## 2026-04-28 不具合修正

- 既存バグ修正: `FeatureRenderer.svelte` の `getPolygonPath` が `vertexCoordinates()` を関数として呼び出していたため、`$derived.by` の戻り値（Map）に対して `TypeError: $.get(...) is not a function` を投げ、子地物を持つ面情報を描画する経路でフラッシュ中エラー（§6.2.9）を起こしていた。`vertexCoordinates` への参照に直して解消
- 開発ガイド§6.2.24に、`$derived` / `$derived.by` の戻り値を関数として呼ばないこと、条件付きで通る派生計算は E2E の `pageerror` 監視で押さえる教訓を追記。§6.7 索引にも本件を反映
- 影響: 子地物を持つ既存ポリゴンの描画経路で発生する潜在的クラッシュを解消。同日の所属変更系新規実装は仕様再検討のため一旦廃棄したが、本バグ修正は独立して有効

---

## 2026-04-26 追加実装

- 面追加フローに既存親指定の導線を接続。描画中HUDに、現在時刻から新規面の存在期間を覆える面情報だけを親候補として表示し、「親なし」または既存面を選択できるようにした。
- `App.svelte` が選択中の親候補を `AddFeatureCommand.parentId` へ渡すようにし、既存のUndo/Redo対応済みの親子関係更新をそのまま利用する構成にした。候補が時間変更や読み込み、描画キャンセル、確定成功、ツール切替で無効になった場合は選択を破棄する。プロパティパネルにも面情報の親と下位領域を名称（ID）形式で表示し、作成後の所属関係を確認できるようにした。
- 要件定義書§2.3.2の面情報追加ツール仕様へ、追加時の親指定と親子関係更新を追記した。
- 開発ガイド§6.2.8に、XState外の補助的な一時状態を確定・キャンセル・ツール切替・モード切替の全経路で破棄する教訓を追記した。
- 回帰防止として、新規面の親候補が永続する面情報だけに絞られること、候補が名称順に並ぶことを固定する単体テスト2件と、面追加時に既存親を指定して作成後の親名（ID）を確認するE2E、親選択がキャンセル後と確定後にリセットされるE2Eの計2件を追加。

---

## 2026-04-22 追加実装

- 要件定義書§2.1の割譲/合邦/親指定UIを接続。`ReassignFeatureParentUseCase` と `ReassignFeatureParentCommand` を追加し、指定時刻以降の対象地物 `parentId`、旧親・新親の `childIds` を同一操作として更新するようにした。途中時刻での変更は錨を分割し、Undo/Redoでは変更前後の錨状態を復元する
- 編集モードのコンテキストメニューに「所属変更」を追加。ダイアログで「選択地物のみ」または「下位領域すべて」を選び、既存の面情報または「親なし（最上位）」へ所属変更できるようにした。選択地物が子を持つ場合の直接変更、自己参照、循環参照、非ポリゴン親は確定処理側でも拒否する
- `AddFeatureCommand` に `parentId` 指定を追加し、新規面を親の下位領域として作成する処理をUndo/Redo込みで固定した。既存の面追加フローを阻害しない作成時親指定UIの導線は当時 `現状.md` に残課題として維持し、2026-04-26に接続済み
- 追補修正として、所属変更で途中時刻の錨を分割する際は既存全錨IDを走査して未使用IDを発行し、復元済みデータとのID衝突を防ぐようにした。ダイアログの可否判定も確定側と同じ「指定時刻以降に下位領域を持たない」条件に揃えた。`AddFeatureCommand` の redo は所属変更ユースケースを再実行せず、初回実行時の変更後スナップショットだけを復元する形に統一した
- 下位領域をすべて失った旧親は指定時刻以降の空錨を削除/終端し、全錨を失う場合はFeature自体を削除するようにした。所属変更コマンドのUndo/Redoも削除Featureを復元/再削除できるスナップショット方式へ拡張した
- 新親が対象地物の将来存在期間を覆わない所属変更を拒否するようにした。親指定失敗後に `AddFeatureCommand` が追加済み地物をロールバックする場合は `feature:removed` を通知する
- 追補として、新親の `childIds` は移動対象の存在期間だけに付与するよう錨分割を追加し、下位領域一括変更で旧親が消えた場合の選択状態も移動先へ寄せるようにした。親候補UIも確定側と同じ将来存在期間検証で絞り込むようにした
- 回帰防止として、所属変更ユースケース/コマンド13件、地物追加時の親指定3件、コンテキストメニュー1件、親候補/可否判定7件の単体テストを追加・更新

---

## 2026-04-22 不具合修正

- B25: 分割ツールが既存共有頂点を含む頂点を新規頂点へ置き換えた際、共有先地物との `SharedVertexGroup` が旧頂点を参照したままになり、分割片同士の共有グループが別途作られる問題を修正。分割前の共有グループから対象地物の旧頂点を外し、同座標に生成された分割後の新頂点へ共有関係を引き継ぐようにした
- B25追補: 分断線が共有辺の途中を切る場合は、共有先地物Bの該当辺にも対応頂点を挿入し、A/A+側の切断点とB側の新頂点を同一 `SharedVertexGroup` に入れるよう修正。複数切断点も共有辺の順序でB側へ挿入し、共有先B側だけが中間頂点を持つ場合も共有端点間の直線チェーンとして扱う。共有辺上の既存中間頂点は切断線が通らない限りB側へ増やさず、切断点と同座標の既存B頂点は新規作成せず共有グループへ入れる。既存B頂点が別の共有グループに所属済みの場合は、新規グループを重複作成せず既存グループへA/A+側頂点を統合し、1頂点1共有グループを維持する。Undo/RedoではB側の形状更新と追加頂点も同じIDで復元する
- 要件定義書§2.3.3.2 に、分割時の共有頂点引き継ぎ仕様を追記
- 開発ガイド§6.4.13 に、共有辺を切る分割では隣接地物にも対応頂点を挿入する教訓を追加
- 回帰防止として、既存共有頂点2点を跨ぐ分割で共有先地物の頂点と分割後の両地物の新頂点が同一共有グループへ入ること、共有辺途中を切る分割で共有先地物Bに対応頂点を挿入してUndo/Redoでも同じIDで復元すること、同一共有辺に複数切断点がある場合もB側へ同順で頂点挿入すること、共有辺上の既存中間頂点を非切断時にB側へ挿入しないこと、B側だけに中間頂点がある共有境界チェーンへ切断点を挿入すること、切断点と同座標の既存B頂点を共有グループへ入れること、既存B頂点がB-C共有グループ所属済みなら同グループへ分割側頂点を統合することを固定する単体テスト7件を追加

---

## 2026-04-21 追加実装・配布準備

- 旧形式マイグレーションとユーザー向け差分警告を実装。`JSONSerializer` に `deserializeWithReport()` とマイグレーション前処理を追加し、バージョンなし/0.x系の構造互換データ、`anchors` なしの単一形状地物、`sharedVertexGroups` / `timelineMarkers` / `baseMap` 欠損などを現在形式へ補完して読み込めるようにした
- `WorldRepository.loadWithReport()` / `SaveLoadUseCase` / `world:loaded` イベントを通じて互換性警告をUIへ伝播し、読み込み成功時は変換差分をalert表示、読み込み失敗時は `SerializationError` の具体的な互換性エラーを表示するようにした
- レビュー追補: `.gimoza` に `assets/base-map.svg` がなくても `project.json` 側に `metadata.settings.baseMap.svgText` が埋め込まれていれば、実際には地図を表示できるため「プリセット地図設定」警告を出さないよう修正
- 回帰防止として、旧形式JSON・`.gimoza`・SaveLoadUseCaseの警告伝播を固定する単体テスト8件を追加
- B21: App初期化時に地物・頂点・共有頂点の表示用stateを同期していなかったため、HMR/自動リロード後にモード切替まで地物表示がずれる問題を修正。初期化時に `refreshFeatureData()` を実行し、読み込み/新規プロジェクト時には選択・ドラッグ・モーダル・Undo/Redo履歴をクリアして前プロジェクトの一時状態を持ち越さないようにした
- B21追補: プロジェクト読み込み/新規作成時に `toolStore` の描画中座標が残り、旧プロジェクト由来の未確定プレビューが新プロジェクトで確定され得る問題を修正。`RESET_INTERACTION` イベントを追加し、`resetInteractionState()` から toolStore と Svelte state の両方を同期リセットするようにした
- 回帰防止として、描画中/パン中の `RESET_INTERACTION` で `drawingCoords` が空になること、リセットが図形確定コールバックを呼ばないことを固定する単体テスト3件と、新規プロジェクト時に未確定図形が破棄されるE2E 1件を追加
- `electron-builder` を導入し、`pack` / `dist` / `dist:win` スクリプト、Windows x64 NSIS/portable 配布設定、`build/icon.ico` のアプリアイコン参照、`.gimoza` ファイル関連付け、repository/bugs/homepage メタデータを `package.json` に追加
- OS関連付けで起動した `.gimoza` を実際に開けるよう、main process で起動引数・二重起動・macOS `open-file` からプロジェクトパスを拾い、preload IPC 経由で renderer の `loadProjectFromPath()` へ接続
- 回帰防止として、起動引数からのプロジェクトパス抽出、読み込み中windowへの遅延通知、二重起動時の前面復帰、preloadの `onOpenProjectPath` 購読を固定する単体テスト11件を追加
- READMEを拡充し、概要・主要機能・必要環境・開発コマンド・配布ビルド・保存形式・関連ドキュメントを整理

---

## 2026-04-21 不具合修正

- B22: 分割ツールの完了時に、piece A/B の切断境界で同一座標になった頂点を `SharedVertexGroup` として登録するよう修正。Undo 時は分割前の共有頂点グループを復元し、既存グループを保持したまま分割由来グループだけを取り消せるようにした
- 回帰防止として、開線分割で切断辺の2端点が共有頂点グループ化されること、Undoで分割前の共有頂点グループ状態へ戻ることを固定する単体テスト2件を追加
- B23: `SplitFeatureCommand` の redo が生成IDを採番し直して後続コマンドの参照を壊す問題を修正。初回分割後の更新元地物・新地物・追加頂点・共有頂点グループをスナップショット化し、redoでは同じIDを復元するようにした
- 同じ §6.4.12 の観点で、`AddFeatureCommand` と `MergeFeatureCommand` も redo 時に初回生成IDを復元する方式へ修正。追加→移動、分割→新地物移動、結合→頂点移動の Undo×2→Redo×2 操作列を固定する単体テスト3件を追加
- B24: 分割ツールが開線の端点外へ補助的に延長した半平面で、同一ポリゴン内の未横断部分まで分断していた問題を修正。`KnifeService` で人工延長線上だけに生じた左右片を検出し、分割結果へ返す前に再結合するようにした
- B24追補: 開線端点がリング境界上に触れる場合も人工延長区間として扱うよう修正し、境界接触だけの未横断部分を分断しないようにした
- B24追補: 人工延長で再結合したcomponentの所属先を面積ではなく、実際の分断線に接している半平面片の接触長で決めるよう修正。非対称な凹ポリゴンで有効な分割が片側へ寄って失敗扱いになる回帰を防いだ
- B24追補: 開線端点がポリゴン内部にある場合でも、端点外へ追加された補助延長区間は常に人工延長として再結合対象にするよう修正。端点の先にある未横断部分を分断しないようにした
- 回帰防止として、C字形ポリゴンの下腕だけを横断する開線、終点が上腕境界に触れる開線、終点が上腕内部で止まる開線、右側に細い接続部を持つC字形ポリゴンの下腕を切る開線の単体テスト4件を追加

---

## 2026-04-19 配布準備

- Windows用アプリアイコン `build/icon.ico` を生成（16/24/32/48/64/96/128/256 の 8 解像度）。`build/icon.svg` からロゴ Latin 書体の "g" 字形を抽出し、ダーク背景・層状のサブモチーフ付き正方形アイコンとして再構成。ImageMagick でサイズ毎にラスタライズして結合

---

## 2026-04-19 不具合修正

- B20: 線上の点追加と頂点削除を `InsertVertexCommand` / `DeleteVertexCommand` としてUndo/Redo対象に接続。App側の直接 `VertexEditUseCase` 呼び出しをコマンド経由へ変更し、線上に追加した頂点を削除した直後のUndoで復元できるようにした
- B19: 分割ツールでブーリアン演算結果の複数片を最大片だけに潰していた問題を修正。`KnifeService` は分割片ごとのMultiPolygonを保持し、`SplitFeatureCommand` は複数の離れた片を複数territory ringとして地物に反映するようにした
- 回帰防止として、頂点挿入/削除Undoの単体テスト5件、凹ポリゴン分割のMultiPolygon保持テスト2件、線上頂点削除後UndoのE2E 1件を追加
- レビュー追補: 複数territory ringを持つ地物の再分割で2個目以降のterritoryをhole扱いしないよう、分割前の形状をterritory単位のMultiPolygonへ再構成してから `KnifeService` に渡すように修正。共有頂点を含む頂点削除では未使用頂点と `SharedVertexGroup` を前進方向でも整理し、複数選択頂点のDeleteを `DeleteVerticesCommand` による1つのUndo履歴へまとめた
- 回帰防止として、複数territory再分割、共有頂点削除時のグループ整理、複数頂点削除のUndo粒度を固定する単体テスト3件を追加
- P1追補: 複数territory ringを持つ分割後地物で、2個目以降のterritoryをUIのナイフ確定判定がhole扱いして確定不可にする問題を修正。`resolvePolygonShapePolygons` を共通化し、`App.svelte` と `SplitFeatureCommand` が同じ「territory + 直下のholes」単位を `KnifeService` に渡すようにした
- 回帰防止として、複数territoryの2個目を横断する開線が `canConfirmKnifeForPolygons` で確定可能になる単体テスト1件を追加
- 追加修正: `InsertVertexCommand` のredoで新しい頂点IDを採番し直していた問題を修正。初回挿入後のFeature/Vertexスナップショットを保持し、redoでは同じ頂点IDを復元することで、後続の `DeleteVertexCommand` が固定IDを削除できるようにした
- 回帰防止として、線に頂点を挿入→削除→Undo×2→Redo×2しても削除対象IDがずれない単体テスト1件を追加
- P2追補: 複数territoryの開線バリデーションで交差回数を全territory合算していた問題を修正。少なくとも1つのterritory外周が2回以上交差する場合だけ確定可能にした
- P2追補: 分割結果をRingへ戻す際のterritory/hole階層再構築を `PolygonShapeService` に共通化し、`SplitFeatureCommand` と `MergeFeatureCommand` の両方で使用するようにした。穴内島のterritoryを分割後もhole配下へ再接続する
- 回帰防止として、複数territoryで1回ずつ交差する開線を拒否する単体テスト1件と、穴内島を含む面の分割後リング階層を固定する単体テスト1件を追加
- 追加精査: 開線が横断していない離れたterritoryを、延長半平面で分断しないようにした。非横断territoryは形状を丸ごと維持し、半平面との重なり面積が大きい側へ所属させることで、穴内島も親hole側へ残す
- 回帰防止として、非横断territoryが延長半平面で割れないこと、`PolygonShapeService` の多段穴内島再接続、親付きterritoryのpolygon解決を固定する単体テスト3件を追加

---

## 2026-04-18 不具合修正

- EdgeSlideの辺-点滑りで、面内部を通る候補を捨てた後に直前座標へ固着していた高速ドラッグを修正。障害物を外周線だけでなく面として判定し、移動辺が面内部を通らない境界候補へ滑らせるようにした
- 回帰防止として、辺-点滑りの面侵入回避と高速ドラッグ時の境界退避を固定する単体テスト2件を追加

---

## 2026-04-17 追加実装

- JSON読み込み時のデータ整合性検証を拡充し、共有頂点グループの存在頂点参照・2点以上・重複/多重所属・代表座標一致と、ポリゴンリングの3点以上・ringType・トップレベル領土・自己交差・リング親子包含を検証するようにした
- 回帰防止としてJSON読み込み検証の単体テスト6件を追加し、壊れた共有頂点グループと壊れたポリゴンを拒否すること、`sharedVertexGroups` 欠損の旧データを空グループとして読み込めることを固定
- 地図アセットの canonical path を `src/renderer/public/assets/maps/base-map.svg` に一本化し、未参照だったルート直下の `assets/maps/base-map.svg` と古い `assets/maps/base-map-1.svg`、空になった `assets/` フォルダも削除
- 任意SVGをプロジェクト設定ダイアログから選択できるようにし、`viewBox` または `width` / `height` を持つSVGだけを受け付けるようにした。選択したSVGはプロジェクト設定として即時反映され、プリセット地図へ戻す操作も追加
- `.gimoza` 保存時は設定済みベースマップを `assets/base-map.svg` へコピーし、`project.json` からは巨大なSVG本文を除外。`.gimoza` 読み込み時はアーカイブ内の `assets/base-map.svg` を復元し、地図表示へ渡すように変更
- JSON保存時はカスタムベースマップ設定を保持し、読み込み時の旧形式欠損はプリセット地図へフォールバック
- 回帰防止として単体テスト8件を追加し、任意SVG設定の正規化・差分判定・JSONラウンドトリップ・`.gimoza` アセットコピー/復元を固定

---

## 2026-04-17 不具合修正

- 共有頂点グループの一部だけを含む複数頂点移動で、未選択の共有メンバーが移動せず代表座標だけ更新される不整合を修正。複数頂点移動時は選択頂点から共有グループ全メンバーへ移動対象を展開し、Undoでも全メンバーと共有グループ状態を復元するようにした
- 回帰防止として共有頂点グループ一部選択の複数頂点移動テスト1件を追加
- レイヤーフォーカス中のレイヤーを追加先として使うようにし、点/線/面の追加先が常に先頭レイヤーになる不具合を修正
- 結合ツールで同一地物・異レイヤー・非ポリゴンなどの不正対象を拒否し、離れたポリゴン結合では複数の領土リングを保持するように修正。複数territory ringを持つ地物の再マージでも2つ目以降をhole扱いせず、穴内の島を結合した場合もhole配下のterritoryとしてリング階層を再構築するように修正
- 地物移動時に共有頂点グループのリンク先頂点と代表座標を一緒に更新し、Undo時も共有グループ状態を復元するように修正。存在しない地物のno-op実行をUndoしても共有頂点グループを消さないように保護。地物ドラッグのプレビュー復元を開始時スナップショット方式へ変更し、緯度クランプ後も元座標へ戻すように修正
- EdgeSlideで移動線分が障害物を横切る場合はドラッグを拒否し、内部へ入る場合は最初に交差した境界で止めるように修正。境界上の始点から内部へ向かう移動も反対側エッジへ飛ばさず拒否し、移動先が境界上の合法接触は許可
- EdgeSlideで境界接触後に同じ辺へ沿って滑る再ドラッグを許可し、最初の衝突位置に固着する回帰を修正。あわせて移動頂点に接続された面の辺が他地物の頂点を掃過する場合、最初に接触する位置で止める制約を追加
- EdgeSlideの衝突応答を接触位置停止から、接触後の残り移動を接触辺/接触線へ射影する方式へ拡張。障害物を横切る頂点ドラッグは最初の接触辺に沿って滑らせ、移動辺が他地物の頂点へ当たる場合も固定点-障害物点の接触線へ滑らせるようにした
- 辺-点滑りの結果として移動辺が障害物面の内部を通る候補はそのまま採用せず、安全な境界候補へ退避させるようにし、障害物頂点を線分だけでなく面の境界として扱うように補強
- 表示モードでは編集HUD・頂点ハンドル・コンテキスト編集操作・Ctrl+A頂点選択を無効化し、選択表示のみ残すように修正
- 頂点ドラッグ中にブラウザ既定のSVGフォーカスアウトラインが大きな橙色リングとして表示される問題を抑制し、ドラッグ中の頂点ハンドルを通常選択時より小さく表示するように修正。マーカーサイズ指定方式は今後整理対象としてコードコメントに残した
- 回帰防止として頂点ドラッグ中ハンドルのサイズとアウトライン抑制をE2Eで固定
- 回帰防止として EdgeSlide境界滑り・移動辺-頂点衝突の単体テスト12件を追加
- 回帰防止として単体テスト15件とレイヤー追加先E2E 1件を追加し、Ctrl+A E2Eを編集モード前提に更新

---

## 2026-04-16 追加実装

- 地図ベースSVG読み込みを `viewBox` / `width` / `height` ベースの汎用正規化へ変更し、固定の出界図スケール依存を解消
- 測量レイヤーを編集ハンドルより背面に移動し、測量マーカー表示中の頂点ドラッグ視覚衝突を修正。測量パネルにクリアボタンを追加し、E2Eを追加
- `EdgeSlideService` を頂点ドラッグへ接続し、同一レイヤーの他ポリゴン内部へ入る単一頂点ドラッグを境界へ滑らせる処理を追加
- Electron main 側に未保存変更の close guard を追加し、renderer dirty state を preload IPC 経由で同期
- `.gimoza` 無圧縮ZIP形式の読み書きを実装。`project.json` と `assets/base-map.svg` を格納し、ダイアログとD&Dで `.gimoza` を扱えるようにした
- レイヤー不透明度UIをアイコン・数値付きに改善。ファイル操作エラー文面をユーザー向けに調整
- `package.json` / AboutDialog を v1.0.0 に更新し、MIT LICENSE とエンドユーザー向け README を追加
- 結合確定後に選択対象が必ず解除されていた不具合を修正
- 回帰防止として単体テスト17件と測量クリアE2E 1件を追加

---

## 2026-04-15 追加実装

- Electron メインウィンドウ生成を `src/main/window.ts` へ分離し、`Menu.setApplicationMenu(null)` と `autoHideMenuBar` / `setMenuBarVisibility(false)` を適用して OS デフォルトメニューバーを無効化
- `MenuBar.svelte` の全画面バックドロップを透明な `div` に変更し、メニューバーとドロップダウンの重なり順を明示して、プルダウン展開中の白転を修正
- 回帰防止として Electron ウィンドウ設定の単体テスト 3 件と、メニューバックドロップ透明性・メニュー切替の E2E 1 件を追加

---

## 2026-04-09 追加実装

- `MapCanvas.svelte` / `VertexHandles.svelte` の残存 a11y 改善を完了。地図キャンバスにキーボードパン・ズーム導線を追加し、頂点/エッジハンドルをフォーカス可能な操作要素として接続
- `ResolveFeatureAnchorConflictsUseCase` を改修し、競合解決用IDを決定的生成へ変更。`resolve()` では頂点ストアを直接変更せず `createdVertices` として返し、`CommitFeatureAnchorEditUseCase` で適用
- 回帰防止として `ResolveFeatureAnchorConflictsUseCase` / `CommitFeatureAnchorEditUseCase` にテストを3件追加

---

## 1. ドメイン層

すべてイミュータブル設計（readonly + withXxx メソッド）。

> **件数の更新方針**: 以下の per-module 件数列は歴史記録として残しているが、新規エントリで件数を更新しない（`memory/feedback_no_test_count_in_narrative.md`）。テスト総数は概要セクションが pre-commit hook で自動更新する。本表は引き続き「どのモジュールにどのようなテストがあるか」の質的索引として機能する。

| クラス | テスト | 備考 |
| --- | --- | --- |
| `Coordinate` | 10件 | x=経度, y=緯度, `equals()`, `normalize()`, `clampLatitude()` |
| `TimePoint` | 22件 | year必須/month・day任意, `compareTo()`, `isBefore()`, `isAtOrAfter()` |
| `Ring` | 7件 | id, vertexIds, ringType(territory/hole), parentId |
| `FeatureAnchor` | 10件 | TimeRange, AnchorProperty, FeatureShape, AnchorPlacement |
| `PropertyProjection` | 10件 | §4.1: FeatureAnchorからの読み取り専用投影 |
| `Vertex` | 6件 | id, coordinate, `withCoordinate()` |
| `Feature` | 10件 | id, featureType, anchors, `getActiveAnchor()`, `existsAt()` |
| `SharedVertexGroup` | 7件 | id, vertexIds, representativeCoordinate |
| `TimelineMarker` | 7件 | §4.1: タイムラインマーカー |
| `World` | 9件 | 集約ルート。WorldSettings/WorldMetadata |

### ドメインサービス

| サービス | テスト | 備考 |
| --- | --- | --- |
| `GeometryService` | 35件 | セグメント交差、ray casting、自己交差、面積・距離 |
| `EdgeSlideService` | 25件 | エッジ沿い移動 |
| `SharedVertexService` | 40件 | スナップ候補、リング/線形単位の共有化制約、マージ/アンマージ、連動移動 |
| `RingEditService` | 41件 | 穴/飛び地リング追加・削除・階層再編、開始位置からの親リング/モード自動判定、リング階層検証 |
| `ConflictDetectionService` | 19件 | ポリゴン重なり検出 |
| `PolygonValidationService` | 6件 | 自己交差・リング階層・同レイヤー重複の統合検証 |
| `BooleanOperationService` | 17件 | polygon-clipping差分/交差/和 |
| `TimeService` | 49件 | 錨検索、時間範囲バリデーション、ステップ操作 |
| `HierarchyService` | 41件 | 親子ナビゲーション、形状導出、階層バリデーション |
| `KnifeService` | 21件 | 二分割/閉線分割 |
| `PolygonShapeService` | 2件 | Polygon shapeのterritory/hole単位解決、リング階層再構築 |
| `MergeService` | 19件 | 結合/割譲/合邦 |
| `SurveyService` | 25件 | 大円距離、球面面積、座標表示 |
| `AutoColorService` | 26件 | 四色定理ベース自動配色 |

## 2. アプリケーション層

| クラス | テスト | 備考 |
| --- | --- | --- |
| `EventBus` | 8件 | 型安全Pub/Sub |
| `NavigateTimeUseCase` | 8件 | 現在時刻管理、ステップ操作 |
| `AddFeatureUseCase` | 27件 | 点・線・面の追加、restore() |
| `SaveLoadUseCase` | 22件 | World組立/分配、save/open。Phase 2-D-7c-2a で in-memory Layer 残骸、Phase 2-D-7c-2b で `World.layers` 自体を撤去（暫定 `getLayers()` / `setLayers()` も同時撤去） |
| `VertexEditUseCase` | 21件 | 頂点移動・挿入・削除 |
| `UpdateFeatureAnchorUseCase` | 20件 | 錨の追加・編集・削除 |
| `EditFeatureUseCase` | 17件 | 地物編集ファサード |
| `DeleteFeatureUseCase` | 10件 | 地物削除、頂点クリーンアップ |
| `PrepareFeatureAnchorEditUseCase` | 13件 | 錨編集準備、競合検出 |
| `ResolveFeatureAnchorConflictsUseCase` | 7件 | 競合解決（ブーリアン差分） |
| `CommitFeatureAnchorEditUseCase` | 9件 | 編集確定 |
| `UndoRedoManager` | 22件 | コマンドパターン、二重スタック（最大100） |
| `Query services` | 7件 | Feature/Timeline/Project の読み取り専用クエリ。CQRS の読み取り経路を分離（`LayerQueryService` は Phase 2-D-3 で撤去） |
| `polygonValidation` | 10件 | 一時ポリゴン生成、頂点逆引き、ポリゴン検証の例外化 |
| 具象コマンド | 89件 | Add(16)/DeleteFeature(11)/DeleteVertex(6)/InsertVertex(2)/MoveVertex(13)/MoveVertices(6)/MoveFeature(10)/Split(13)/Merge(12) |

## 3. インフラストラクチャ層

| クラス | テスト | 備考 |
| --- | --- | --- |
| `ViewportManager` | 30件 | viewBox管理、zoom/pan、座標変換、動的wrapタイル算出、中心経度管理 |
| `featureRenderingUtils` | 33件 | 座標変換、SVGパス生成、生値経度の描画補助、主表示帯への折り返し補助 |
| `hitTestUtils` | 26件 | 地物選択判定（点/線/面、隣接ラップ対応） |
| `vertexHandleUtils` | 19件 | 頂点ハンドル・エッジハンドル表示用、生値経度座標の解決 |
| `vertexDragManager` | 9件 | 頂点ドラッグ状態管理 |
| `featureDragManager` | 10件 | 地物ドラッグ状態管理 |
| `knifeDrawingManager` | 21件 | ナイフツール描画状態管理 |
| `snapIndicatorUtils` | 11件 | スナップインジケーター |
| `ringDrawingManager` | 9件 | リング描画状態管理 |
| `JSONSerializer` | 35件 | World↔JSONシリアライズ |
| `JSONWorldRepository` | 11件 | FileSystemPort経由でIPC抽象化、.gimozaベースマップアセット |
| `StyleResolver` | 28件 | パレット管理��自動配色、カスタムパレット定義・解決 |
| `ConfigManager` | 14件 | プロジェクト設定/アプリ設定、ローカル保存 |
| `Logger` | 5件 | 構造化ログ、ログレベル、JSONL永続化、簡易実行時間計測 |
| `DIContainer` | 7件 | Pure DI。コマンド/クエリ/インフラを単一入口で構成 |
| `surveyModeManager` | 17件 | 測量モード状態管理 |
| `boxSelectManager` | 16件 | 矩形選択状態管理 |
| `editInteractionUtils` | 8件 | 編集サブツール判定、頂点選択維持判定 |
| `contextMenuBuilder` | 9件 | コンテキストメニュー項目構築 |
| `dirtyTracker` | 7件 | 未保存変更トラッカー |
| `autoBackupManager` | 15件 | バックアップ管理ロジック |
| `keyboardShortcutManager` | 12件 | ショートカット一元管理 |
| `vertexSelectionContext` | 7件 | 可視レイヤー上の頂点所有者解析、単一/複数/不明コンテキスト判定 |

## 4. プレゼンテーション層

### Svelteコンポーネント

| コンポーネント | 備考 |
| --- | --- |
| `App.svelte` | ルート。全UseCase統合管理 |
| `MapCanvas.svelte` | ビューポート制御、マウス入力、子コンポーネント配線 |
| `mapCanvas/MapCanvasSvgLayers.svelte` | SVG描画レイヤー（背景/地物/編集/測量） |
| `mapCanvas/MapCanvasHud.svelte` | キャンバスHUD（グリッドラベル、経度コンパス、ツールバー、測量情報） |
| `VertexHandles.svelte` | 頂点/エッジハンドル表示 |
| `EditToolbar.svelte` | 編集ツールバー |
| `SplitConfirmModal.svelte` | 分割確認モーダル |
| `MergeConfirmModal.svelte` | 結合確認モーダル |
| `ConflictResolutionDialog.svelte` | 競合解決ダイアログ |
| `MeasurementOverlay.svelte` | 測量オーバーレイ |
| `ContextMenu.svelte` | コンテキストメニュー |
| `PropertyPanel.svelte` | プロパティパネル |
| `ProjectSettingsDialog.svelte` | プロジェクト設定ダイアログ |
| `MenuBar.svelte` | メニューバー |
| `DrawingPreview.svelte` | 描画プレビュー |
| `GridRenderer.svelte` | グリッド表示 |
| `Toolbar.svelte` | 4モード＋設定ボタン |
| `Sidebar.svelte` | 2タブ（プロパティ/地物一覧）。Phase 2-D-3 でレイヤータブを撤去 |
| `TimelinePanel.svelte` | タイムライン操作 |
| `FeatureRenderer.svelte` | 地物描画、レイヤーフォーカス減衰、下位領域の和描画 |
| `StatusBar.svelte` | 座標・ズーム表示 |
| `LongitudeCompass.svelte` | 経度コンパスUI（目盛り・ナッジ・ホイール・キーボード） |
| `GridLabelsOverlay.svelte` | 緯度経度ラベルオーバーレイ |

### プレゼンテーションユーティリティ

| ユーティリティ | テスト | 備考 |
| --- | --- | --- |
| `labelRendererUtils` | 9件 | ラベル重心・表示判定 |
| `longitudeCompassUtils` | 4件 | 可視スパン・目盛り刻み・ジャンプ幅・nice step |
| `gridLabelsOverlayUtils` | 6件 | 経度/緯度ラベル位置計算 |
| `featureSearchUtils` | 7件 | 地物検索・クエリ分割・ハイライト区間生成 |
| `propertyPanelHistoryUtils` | 5件 | アンカー時間バーセグメント・現在時刻位置 |
| `timelinePanelUtils` | 8件 | タイムラインステップ/ティック/クランプ |
| `mapCanvasUtils` | 15件 | MapCanvas 分割後の共通補助（頂点数、FPS、ズーム範囲、距離表示、動的wrapタイル算出、SVG地図正規化） |
| `appProjectSettings` | 11件 | プロジェクト設定の正規化・差分判定、ベースマップ設定 |
| `appSelection` | 8件 | プロパティ選択状態の整形、アンカー全頂点抽出 |
| `appPolygonEditing` | 19件 | `App.svelte` から抽出したポリゴン追加/穴・飛び地追加/エッジ滑り接続の補助ロジック |
| `appFeatureDragging` | 1件 | 地物ドラッグのプレビュー移動・開始時スナップショット復元 |

### 状態機械

| 機械 | テスト | 備考 |
| --- | --- | --- |
| `toolMachine` | — | 表示/追加/編集/測量の4モード |
| `timelineMachine` | 41件 | 再生/停止/ステップ/速度変更 |
| `conflictResolutionMachine` | 23件 | 競合解決ダイアログ状態遷移 |

## 5. E2Eテスト

21ファイル、172テスト。devサーバー(localhost:5173)ベースのブラウザE2E。

| ファイル | テスト数 | 内容 |
| --- | --- | --- |
| `app-launch.spec.ts` | 7 | ページ表示、主要UI要素存在 |
| `tool-modes.spec.ts` | 7 | V/A/E/Mキーモード切替 |
| `feature-add.spec.ts` | 2 | ポイント/ポリゴン追加 |
| `menu-and-shortcuts.spec.ts` | 6 | メニュー開閉、バックドロップ透明性、ショートカット、Ctrl+A全頂点選択 |
| `map-display.spec.ts` | 10 | SVG表示、ズーム/パン、グリッド |
| `measurement-mode.spec.ts` | 5 | 測量モード基本操作 |
| `timeline.spec.ts` | 7 | タイムライン基本操作 |
| `add-mode.spec.ts` | 12 | 追加ツール動作、東西端またぎプレビュー |
| `edit-mode.spec.ts` | 5 | 編集モード選択/プロパティ（Phase 2-D-3 でレイヤータブ前提テスト 2 件削除 + タブ切替テストを 2 タブ構成へ書き換え） |
| `undo-redo.spec.ts` | 4 | Undo/Redo基本操作、線上頂点削除後Undo |
| `project-settings.spec.ts` | 10 | 設定ダイアログ、アプリ設定UI/ローカル保持 |
| `context-menu.spec.ts` | 3 | コンテキストメニュー |
| `map-display-advanced.spec.ts` | 27 | 無限スクロール、グリッド詳細、グリッドラベル、経度コンパス |
| `measurement-advanced.spec.ts` | 11 | 測量詳細、東西端またぎ測量、測量クリア |
| `timeline-advanced.spec.ts` | 12 | タイムライン詳細 |
| `edit-mode-advanced.spec.ts` | 12 | 編集モード詳細 |
| `edit-mode-regression.spec.ts` | 9 | 編集サブツール独立化、複数頂点選択維持、複数頂点同時移動、頂点選択コンテキスト、穴内飛び地追加、穴内への別地物追加、穴リングと飛び地リングの共有頂点化 |
| `edit-mode-selection.spec.ts` | 1 | 頂点ドラッグ後の地物選択維持 |
| `property-save-settings.spec.ts` | 18 | プロパティ/保存/設定詳細、カスタムパレット |
| `save-load-regression.spec.ts` | 2 | 新規プロジェクト後の保存先・メタデータのリセット回帰、描画中の新規プロジェクトで未確定図形を破棄 |
| `sidebar-search.spec.ts` | 2 | 地物検索、サイドバー/タイムライン折りたたみ（Phase 2-D-3 で折りたたみ展開後のレイヤータブ確認を地物一覧タブ確認へ書き換え） |

E2E拡充フェーズ: Phase A〜E完了（詳細テスト73件追加）。

## 6. フェーズ履歴

| フェーズ | 内容 | 状態 |
| --- | --- | --- |
| 1 | 基盤接続（EventBus、StatusBar配線、IPC） | 完了 |
| 2 | 最小編集サイクル（レイヤー、描画、追加、toolMachine、選択） | 完了 |
| 3 | 永続化と編集（JSONSerializer、頂点編集、錨更新、Undo/Redo） | 完了 |
| 4 | 高度な編集（排他性、共有頂点、穴/飛び地、競合検出） | 完了 |
| 5 | 残ロジック完成（ドメイン/アプリ/インフラ/プレゼン層補完、ナイフ/結合/測量/自動配色） | 完了 |
| 6 | UI接続 — 編集操作（頂点ハンドル、ドラッグ、スナップ、リング、ナイフ、結合、測量、範囲選択、コンテキストメニュー） | 完了 |
| 7 | UI接続 — パネル・ダイアログ（プロパティ、地物一覧、タイムライン拡張、設定、メニューバー、ラベル、面スタイル、保存警告/バックアップ） | 完了 |
| 8 | 品質・仕上げ（E2E 150テスト、無限スクロール、ショートカット） | 進行中 |

### 不具合修正

| # | 修正内容 | 日付 |
| --- | --- | --- |
| #7 | 地物削除をDeleteFeatureUseCase経由に統一。DeleteFeatureCommand(Undo対応)新設、feature:removedイベント購読、markAsDirty連動 | 2026-03-25 |
| #20 | プロパティタブが選択地物変更で更新されない。PropertyPanelの$effectにfeature.id/anchor.idの明示的追跡を追加 | 2026-03-25 |
| #8 | 自動バックアップのAPI名不一致。window.fileAPI→window.apiに修正（3箇所） | 2026-03-25 |
| #9 | プロジェクト設定の永続化・反映。SaveLoadUseCaseにmetadata getter/setter追加、assembleWorldでユーザー設定を使用、読込時にmetadata復元 | 2026-03-25 |
| #13 | Dirty状態の一元化。undoRedo.subscribe()で全コマンド操作をカバー、頂点削除・設定変更にもmarkAsDirty追加 | 2026-03-25 |
| #3 | 面追加連続時の頂点残留。toolMachineのCONFIRM/MAP_DOUBLE_CLICKにclearDrawingアクション追加。toolStoreの確定検出をlastEventベースに変更 | 2026-03-25 |
| #4 | 描画プレビュー非表示 — 手動確認で修正済みと判明（#3修正の副作用と推定） | 2026-03-25 |
| #6 | 頂点ハンドル非表示 — 手動確認で修正済みと判明 | 2026-03-25 |
| #21 | サイドバー選択で更新が止まる問題を修正。根本原因だった `MapCanvas.svelte` の `canConfirmKnife` 未定義参照を `knifeCanConfirm` 配線へ修正し、Sidebar の自動切替を新規選択時のみに限定。E2Eで自動切替・選択ハイライト・pageerrorなしを回帰固定 | 2026-03-27 |
| #1/#5 | 横方向無限スクロールの複製描画と測量線非表示を修正。`MapCanvas.svelte` の `wrapOffsets` グループに `VertexHandles` / `DrawingPreview` / `MeasurementOverlay` を統合し、複製地図上でも編集ハンドル・描画プレビュー・測量線が追随するよう修正。E2Eで wrap 中の複製描画を回帰固定 | 2026-03-27 |
| #2 | 東西端またぎと無限スクロール本体を修正。`ViewportManager` の `wrapOffsets` を viewBox 連動の動的生成に変更し、`featureRenderingUtils` / `DrawingPreview` / `MeasurementOverlay` / `hitTestUtils` で経度アンラップを導入して `179° → -179°` を短い経路として扱うよう統一。単体9件・E2E4件を追加 | 2026-03-27 |
| #12 | プロジェクト設定のグリッド値が地図描画に反映されない問題を修正。`App.svelte` から `MapCanvas.svelte` へ `gridInterval` / `gridColor` / `gridOpacity` を配線し、`GridRenderer.svelte` に渡すよう統一。E2Eで保存直後の線本数・色・不透明度を回帰固定 | 2026-03-27 |
| #26 | 自動バックアップ初回ローテーションの `ENOENT` ログを修正。`main/index.ts` / `preload` に `file:exists` を追加し、`autoBackupManager` の `rotateBackupFiles()` が存在確認後のみ世代シフトするよう変更。未生成世代への `readFile` を止め、単体2件を追加 | 2026-03-27 |
| #22 | 東西境界で地物が切れる問題を修正。`FeatureRenderer.svelte` に `data-feature-id` を付与し、`MapCanvas.svelte` / `App.svelte` で描画済みSVG要素の地物IDを優先して選択判定に使うよう変更。`GridRenderer.svelte` と選択ハイライトの `pointer-events` も整理し、絶対経度クリックの回帰テストを追加 | 2026-03-28 |
| #23 | ズーム時に大地物が消える問題を修正。`ViewportManager.getWrapOffsets()` に左右1タイル分の描画余白を追加し、横スクロール境界付近でも複製地物が切れにくいよう調整。関連単体テストを更新 | 2026-03-28 |
| #24 | 超長距離地物のファントムエッジを修正。`vertexHandleUtils` にアンラップ済み頂点・エッジ位置計算を追加し、閉じリング終端のファントムエッジを解消。`VertexHandles.svelte` のスナップ表示も同一ラップ基準へ統一 | 2026-03-28 |
| #25 | hitTestPolygonのeven-oddルール破損を修正。`featureRenderingUtils` / `hitTestUtils` でリング経度列を同じ参照ラップへ揃え、`evenodd` の穴判定を安定化。単体4件を追加 | 2026-03-28 |
| #27 | 共有頂点ドラッグUIを修正。`App.svelte` の地物更新をスナップショット再代入へ変更し、頂点ドラッグ中は共有グループ全体をプレビュー移動、ドロップ時は `MoveVertexCommand` 経由で共有頂点化まで確定するよう修正。`snapIndicatorUtils` / `VertexHandles.svelte` / `MapCanvas.svelte` を複数候補表示対応に拡張し、関連単体4件を追加 | 2026-03-28 |
| #10/#11 | 自己交差と同一レイヤー重複の検証を UI/コマンドへ接続。`PolygonValidationService` / `polygonValidation` を追加し、面追加・頂点移動・地物移動・リング追加の確定前に共通検証を通すよう修正。`MapCanvas.svelte` に拒否理由バナーを追加し、関連単体5件を追加 | 2026-03-28 |
| U4 | 同一地物内の自己共有を禁止。`MoveVertexCommand` で同一地物内の共有頂点化を拒否し、さらに同一地物の隣接頂点が同じ共有先グループへ入るケースも禁止。`App.svelte` でも同一地物の頂点と禁止対象の共有候補をスナップ候補から除外。回帰テスト2件を追加 | 2026-03-28 |
| #15/#17/#18/#19 | E2E fixme 4件を解消。`Sidebar.svelte` の線地物バッジ判定を `Line` に修正し、`MapCanvas.svelte` の描画確定可否を線/面で分岐、`App.svelte` + `surveyModeManager` で完了済み測量履歴を保持してモード変更後も表示を維持、`Escape` のみ明示クリアに変更。関連単体2件追加、149 E2E 全件通過 | 2026-03-28 |
| U5 | `svelte.config.js` を `svelte.config.mjs` へリネームし、`npm run check` 時の ESM/CJS 読み込みエラーを解消 | 2026-03-28 |
| U6 | 変形拒否メッセージがクリックで即消える回帰を修正。`App.svelte` の `onMapClick()` から `validationMessage` 初期化を除去し、拒否理由が残るようにした。可視ラップ上の頂点ハンドルを使う E2E 1件を追加して回帰固定 | 2026-03-28 |
| U7 | タイムラインの月日ステップ・再生進行を修正。`TimelinePanel.svelte` が年のみ送っていた処理を `TimePoint` ベースに変更し、28日固定の日送りや再生がステップ単位に引きずられる問題を解消。`sliderMin`/`sliderMax` を `App.svelte` から注入しプロジェクト設定に追随。ロジックを `timelinePanelUtils.ts` に抽出し単体8件を追加 | 2026-03-29 |
| #29/#30 | 編集サブツールを整理。`EditToolbar.svelte` に独立した `地物移動` トグルを追加し、`App.svelte` / `MapCanvas.svelte` / `editInteractionUtils` で地物ドラッグを編集モードかつ明示有効時のみに制限。穴追加/分割開始時は地物移動を解除し、複数選択済み頂点のドラッグ開始でも選択集合を維持するよう修正。さらに頂点/地物ドラッグ完了後の後続 `click` を抑止して地物選択が外れないようにし、関連単体8件・E2E4件を追加 | 2026-03-29 |
| #31 | 複数地物をまたぐ頂点選択コンテキストを実装。`vertexSelectionContext` で可視レイヤー上の頂点所有者を解析し、`App.svelte` / `MapCanvas.svelte` / `FeatureRenderer.svelte` / `PropertyPanel.svelte` / `Sidebar.svelte` をアクティブ地物と頂点選択コンテキストの二系統に分離。矩形選択を全可視頂点対象へ拡張し、単一コンテキストではプロパティと編集ツールを維持、複数所有者では警告メッセージを表示。関連単体7件・E2E2件を追加 | 2026-03-31 |
| #32 | 複数選択頂点の同時移動を実装。`要件定義書.md` に複数頂点同時移動の要件を追記し、`MoveVerticesCommand` を新設。`App.svelte` で選択集合ベースの頂点ドラッグ文脈を持ち、複数選択時は同一ベクトルで全対象頂点と共有頂点代表座標をプレビュー・確定するよう修正。関連単体4件・E2E1件を追加 | 2026-03-31 |
| #28 | 穴/飛び地追加ツールを仕様準拠化。`EditToolbar.svelte` / `MapCanvas.svelte` / `contextMenuBuilder.ts` を単一の「穴/飛び地追加」導線へ統合し、`RingEditService` で初回クリック位置から穴/飛び地モードと親リングを自動判定、描画中の各頂点制約とリング階層検証を共通化した。さらに `PolygonValidationService` を介して頂点移動時の包含関係破綻も拒否するよう接続し、関連単体13件・E2E1件を追加 | 2026-03-31 |
| #33 | 穴を持つポリゴンの重複判定を修正。`ConflictDetectionService` が領土リング単体ではなく「領土リング + 直下の穴リング」を占有ポリゴンとして評価し、穴内の別地物追加は許可しつつ、穴内飛び地との重なりは引き続き検出するよう修正。関連単体4件・E2E1件を追加 | 2026-03-31 |
| #34 | 穴リングとその中の飛び地リングの共有頂点化を修正。`SharedVertexService` の参加制限を地物単位からリング/線形単位へ是正し、`App.svelte` の同一地物一括除外も撤廃して、UI から同一地物内の別リング頂点を共有候補に出せるよう修正。関連単体5件・E2E1件を追加 | 2026-03-31 |
| U8 | 新規プロジェクト後も保存先パスとメタデータが引き継がれる問題を修正。`SaveLoadUseCase` に `resetProjectState()` を追加し、`App.svelte` の `newProject()` から保存コンテキストを初期化するよう変更。単体1件・E2E1件を追加し、共有頂点の保存再読込も回帰確認した | 2026-03-31 |
| #35 | 自動バックアップを要件定義書§2.5.4準拠へ修正。`autoBackupManager` を `savebackup/<保存ファイル名>/<timestamp>.json` 方式へ置き換え、`preload` / `main` にバックアップ一覧・削除・保存先解決APIを追加。保持上限を10世代へ拡張し、`App.svelte` は `autoSaveInterval` に追随して同方式で保存するよう修正。関連単体2件を追加 | 2026-03-31 |
| #36 | 横方向ラップ上の地物移動で保存座標が範囲外へ逸脱する問題を修正。`MoveFeatureCommand` の検証/確定処理でのみ平行移動後の座標を `normalize()` し、保存時に `-180〜180` を維持するようにした。`App.svelte` の地物移動プレビューは巻き戻し前提の中間状態なので非正規化のまま保持する。関連単体1件を追加 | 2026-03-31 |
| #37 | 地物ドラッグプレビューの180°子午線付近の描画崩壊を修正。`featureRenderingUtils` と `vertexHandleUtils` に主表示帯への表示寄せを追加し、`FeatureRenderer.svelte` の点・ラベル描画も同じ基準へ統一した。非正規化プレビュー座標は保持したまま、SVG化直前だけラップを揃える。関連単体6件を追加 | 2026-04-01 |
| #38 | タイムライン再生速度の二重加速を修正。`TimelinePanel.svelte` の再生タイマーを `TICK_INTERVAL_MS` 固定へ戻し、速度は `applyTimelineTick()` の日数増分だけで表現するよう統一。`timeline-advanced.spec.ts` に 10x 再生の回帰テスト1件を追加 | 2026-04-01 |
| #39 | ラベル描画を `LabelRenderer.svelte` と `labelRendererUtils.ts` に分離し、`labelAreaThreshold` と `labelVisibility.minZoom/minDisplayLength` を実レンダリングへ反映。あわせて `ProjectSettingsDialog` にズーム下限/上限と自動バックアップ間隔の入力欄を追加し、`MapCanvas.svelte` が `zoomMin/zoomMax` を実際のビューポート制限へ適用するよう修正。関連単体8件・E2E1件を追加 | 2026-04-01 |
| #40 | レイヤー管理UI・パレットテーマ・自動配色UI接続を一括実装。`ProjectSettingsDialog` にレイヤー追加/名前変更/削除UIを追加し、`StyleResolver` に組み込みパレット（クラシック/パステル/ハイコントラスト）と `createDefaultPolygonStyle` を追加。`FeatureRenderer` を `resolveStyle` ベースに移行し、`PropertyPanel` にパレット選択UIを追加。`EventBus` に `layers:changed` イベントを追加しレイヤー変更の一元通知化。関連単体7件・E2E1件を追加 | 2026-04-02 |
| #41 | 隣接辺ベースの自動配色を描画へ接続。`StyleResolver` に共有辺による隣接判定（`buildSharedBoundaryAdjacencies`）と `resolvePolygonAutoColors` を追加し、`FeatureRenderer` がレイヤーごとに隣接グラフを構築して `AutoColorService.greedyColoring` で色を割り当てるよう変更。`autoColor=true` の面は保存済み色より再解決色を優先する。180°子午線またぎの共有辺も正規化で対応。関連単体4件を追加 | 2026-04-02 |
| U9 | アプリ設定（共有頂点スナップ距離・描画更新頻度・全頂点マーカー表示上限）を `ProjectSettingsDialog` / `ConfigManager` / `App.svelte` / `MapCanvas.svelte` に接続。`ConfigManager` にローカル保存を追加し、保存後はスナップ距離・描画更新頻度・頂点マーカー抑制へ即時反映するよう修正。関連単体3件・E2E2件を追加 | 2026-04-02 |
| #42 | 180°子午線をまたぐポリゴンの描画崩壊を修正。`MapCanvas.svelte` を「背景→ベースマップ→地物→グリッド→編集オーバーレイ→測量オーバーレイ」のレイヤー単位描画へ再構成し、隣接 `wrapOffset` タイルの背景やグリッドがはみ出した地物を上書きしないようにした。`map-display-advanced.spec.ts` にレイヤー分離構造を確認する E2E 1件を追加 | 2026-04-02 |
| #43 | 東西端またぎの頂点ドラッグで周回情報が潰れる問題を修正。`VertexHandles.svelte` がハンドル表示に使ったアンラップ済み座標を `App.svelte` へ渡し、`vertexDragManager` でその周回に追従するドラッグ座標を生成するよう変更。さらに `VertexEditUseCase` / `MoveVerticesCommand` / `SharedVertexService` から頂点移動時の経度 `normalize()` を外し、緯度のみクランプして 180°超の経度を保持するよう修正。関連単体3件を追加 | 2026-04-02 |
| B1/B3 | 開発ガイド§6.2.16に合わせて経度の生値設計へ移行。`featureRenderingUtils` / `vertexHandleUtils` / `hitTestUtils` / `ViewportManager` / `labelRendererUtils` から unwrap 前提を外し、描画・ラベル・頂点ハンドル・ヒットテスト・クリック座標整合を「生値保存 + 表示時のみ折り返し」に統一。`AddFeatureUseCase` / `VertexEditUseCase` / `MoveFeatureCommand` などの保存経路も経度 `normalize()` を廃止し、関連単体テストを更新 | 2026-04-05 |
| #44 | 未接続の表示・設定残件を一括実装。`StyleResolver` にカスタムパレットの定義/解決を追加し、`ProjectSettingsDialog.svelte` で登録・編集・既定パレット選択を可能化、`PropertyPanel.svelte` に時間バー/リスト切替とカスタムパレット反映を接続した。あわせて `LayerPanel.svelte` + `FeatureRenderer.svelte` にレイヤーフォーカススライダーと非フォーカス減衰表示、`MapCanvas.svelte` + `ViewportManager` に経度シフトUI（`LongitudeCompass`）、`GridLabelsOverlay` に緯度経度ラベル、`FeatureRenderer.svelte` に `LayerService.deriveParentShape()` の描画接続を追加。関連単体24件・E2E3件を追加 | 2026-04-05 |
| #45 | `Sidebar.svelte` を `FeatureListPanel.svelte` へ分割し、`featureSearchUtils.ts` を追加して地物一覧の検索（ID/名前/説明/属性、ハイライト、Enterジャンプ）を実装。あわせて `Sidebar.svelte` と `TimelinePanel.svelte` に折りたたみトグルを追加し、関連単体4件・E2E2件を追加 | 2026-04-07 |
| U10 | 外部レビュー追補。`featureSearchUtils` に currentTime 未指定・空文字ハイライト・属性値文字列化の単体テスト3件を追加し、`sidebar-search.spec.ts` から `.layer-title` 不在への実装依存アサーションを除去。要件定義書§5.5 に `FeatureListPanel` の責務を追記 | 2026-04-08 |
| S2/CQRS | `DIContainer` を renderer の単一入口へ拡張し、`appState.ts` を廃止。`Feature/Layer/Timeline/ProjectQueryService` を追加して UI の読み取り経路をコマンド側 UseCase から分離し、`App.svelte` / `LayerPanel.svelte` / `TimelinePanel.svelte` を新経路へ移行。関連単体4件を追加 | 2026-04-08 |
| S3/U11 | `JSONSerializer.ts` を facade + `jsonSerializerTypes` / `jsonSerializerTransforms` / `jsonSerializerValidation` / `jsonSerializerErrors` へ分割して 44行まで縮小。あわせて `main/ipcHandlers.ts` と `preload/api.ts` を切り出して IPC/preload を直接テスト可能にし、関連単体13件を追加。`MenuBar` / `ContextMenu` / 各種ダイアログは `button` ベースへ置き換えて a11y セマンティクスを改善し、`menu-and-shortcuts.spec.ts` を再確認 | 2026-04-08 |
| S1/U12 | `MapCanvas.svelte` をビューポート制御専用へ寄せ、描画レイヤーを `mapCanvas/MapCanvasSvgLayers.svelte`、HUD を `mapCanvas/MapCanvasHud.svelte` へ分割。あわせて `appProjectSettings` / `appSelection` / `mapCanvasUtils` を追加して `App.svelte` の一部ロジックを抽出し、未接続だった `Ctrl+A` / 編集メニューの「全頂点選択」を実装。`VertexHandles.svelte` に選択クラスを追加し、関連単体8件・E2E1件を追加 | 2026-04-08 |
| T1/S1 | `テスト改善計画.md` フェーズ1〜3を完了。`PolygonValidationService` / `polygonValidation` / `appPolygonEditing` など計53件の単体テストを追加し、`App.svelte` のポリゴン編集系補助を `appPolygonEditing.ts` へ抽出して 1809行まで縮小。あわせて `RingEditService` のリング階層エラーへリングID付きメッセージを付与し、`add-mode.spec.ts` / `edit-mode-regression.spec.ts` 計19件を再確認 | 2026-04-09 |
| #46/U13 | 低優先残件だった「2周超（720°超）地物の表示」と「排他判定の経度範囲拡張」を実装。`mapCanvasUtils` に動的wrapタイル算出を追加して 720°超の地物や描画中プレビューでも必要な周回タイルを描画し、`BooleanOperationService` / `ConflictDetectionService` / `ResolveFeatureAnchorConflictsUseCase` で 360°シフト付きの重なり判定・差分適用を導入。あわせて競合解決結果が頂点・リングへ反映されず複数片が落ちる不具合を修正し、関連単体7件を追加。さらに `vite` を direct dependency へ追加して `npm run check` 時の依存解決ノイズを解消 | 2026-04-09 |
| U14 | `Logger` を実装。`ConfigManager` と `ProjectSettingsDialog.svelte` にログレベル設定を追加し、`DIContainer` から構造化ロガーを供給、`main/ipcHandlers.ts` / `preload/api.ts` に JSONL 追記用 IPC を追加した。`App.svelte` は自動バックアップ失敗・未処理例外・未処理 Promise rejection・保存/読込失敗をロギングし、保存系の未処理 rejection をユーザー通知付きラッパーへ統一。関連単体5件・IPC/preload テスト10件を追加 | 2026-04-09 |

## 7. アセット

| 項目 | 備考 |
| --- | --- |
| ベースマップSVG | `src/renderer/public/assets/maps/base-map.svg`。SVGの viewBox / width / height を 360x180 へ正規化。プロジェクト設定から任意SVGへ差し替え可能 |

## 8. 技術メモ

- **Svelte 5 + XState v5**: `@xstate/svelte` はSvelte 3/4向けのため不使用。`createActor` ベースで手動統合。
- **electron-vite 5**: vite 6 + @sveltejs/vite-plugin-svelte 5 との互換性のために v5 を使用。
- **ベースマップのスケーリング**: base-map.svg またはプロジェクト設定の任意SVGの viewBox / width / height を読み取り、SVG 上で 360x180 に正規化。
- **E2E**: devサーバー(localhost:5173)ベースのブラウザE2E。Electron直接起動はChromium 136+ CLI引数制限のため断念。
