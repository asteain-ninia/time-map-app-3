# 提案 — gimoza 次の一手メモ（修正優先順位の再評価、2026-07-07）

> **位置づけ**: 未確定の提案。現状.md §5「修正優先順位」（2026-04-28確定）を、その後に出た3つの材料で再評価した短いメモです。§5 の書き換えは行っていません（採用時にUSERが反映）。
> **作成経緯**: Fable 5 サブスク枠最終日の追加作業。`提案-B34仕様確定資料-2026-07-07.md` と同日のセット。

---

## 1. §5確定（04-28）以降に卓上へ載った材料

1. **ファイル図鑑屋レビュー（06-14、お手紙）**: 🔴4件 — S1 filesystem IPCが任意絶対パス受理（ipcHandlers.ts:57-104）/ S2 SVG `{@html}`注入＋正規表現サニタイザすり抜け（mapCanvasUtils.ts:122-127, MapCanvasSvgLayers.svelte:122）/ S3 `shell.openExternal`スキーム検証なし（window.ts:85）/ S4 CSP皆無。**S1×S2の連鎖でローカル任意ファイルR/W/削除に到達しうる**。ドロップイン検証済みパッチ2本が `file-zukan\_workshop-2026-06-14\engineering\patches\`（timemap-svg-xss / timemap-ipc-hardening）に既製。
2. **ヌケモレ点検（07-07）**: お手紙以後このリポにコード変更コミットは皆無＝上記4件はすべて未対応のまま。同時に「パッチが前提とするコードから乖離していない」ことも意味する（適用成功の見込みが高い状態が保たれている）。
3. **B34判断資料（07-07）**: 第4案「ロード時正規化」を推奨。採用なら undo 改修・経路別ガードが不要になり、**結合再設計（Phase 4-6/4-7）から独立に前倒し導入可能**（判断資料§4）。前提はUSERの問1回答のみ。

## 2. 提案する改訂優先順位（v1.0.0配布に向けて）

```
0.【新設・最優先/並行可】セキュリティ4点セット（S1+S2+S4+S3）
1. 階層モデルの新仕様への置換（従来1位を維持 — Phase 4-6残作業から再開）
1.5.【新設・任意タイミング】B34ロード時正規化（問1回答後、独立に挿し込み可）
2. 要件定義書の仕様補完（B15）  ※B34採用時はロード時正規化の明文化を同梱
3. テスト品質改善（テスト改善計画 フェーズ2〜3）
4. 構造改善（S1: App.svelte分割）
5. E2E拡充 + Electron実機統合E2E + パフォーマンス
```

**0 を新設して先頭に置く理由（3点）**:
- **配布ブロッカー性**: §5は「v1.0.0配布に向けて」のリストだが、確定日（04-28）がレビュー（06-14）より前のため、配布前必須のセキュリティ4件が構造的に入っていない。外部`.gimoza`や外部SVGを読む配布アプリとして、S1×S2連鎖は配布前に閉じるべき筆頭。
- **独立性**: 4件の修正箇所は main プロセスと presentation 層（IPC・CSP・サニタイザ・openExternal）で、階層モデル置換の主戦場（domain/application 層）と**ファイルがほぼ重ならない**。Phase 4-6 の進行と衝突せず並行できる。
- **既製度**: 中核2件はパッチ済み（検証込み）。手紙以後コード無変更なのでそのまま当たる公算が高い。着手時は念のためパッチ前提コードとの diff 照合から。

**1.5 の挿し込みについて**: B34は severity「中」なので従来は結合再設計待ちだったが、ロード時正規化案なら実装が小さく（正規化パス1本＋テスト）、いつでも入れられる。**ただしロード時正規化はインポート由来の移行期間ノードを消すだけで、アプリ内で移行期間ノードを一時生成しうる経路の規律は従来どおり必要** — 上位領域決定ダイアログ着手時の遷移解決義務（現状.md:508）と、EditFeature系3経路の配線時義務（現状.md:529）は**そのまま残る**。B34④を「これらの義務が消える」と読まないこと。

## 3. ついでの整合メモ

- 図鑑屋の🟡のうち **C3（.gimoza読込時CRC32未検証）** は、B34ロード時正規化と同じ「ロード経路」を触るため、正規化パス実装と同じ変更単位でやると安い（zipArchive.ts:175-186）。
- StepPet で同型だった「配布不能設定」問題（テストAdMob ID等）の教訓どおり、0番の完了条件は「コードが直った」ではなく「**配布物を生成して初回起動まで通した**」に置くのを推奨（§2.2の「初回起動体験」項と接続）。

---

*作成: Claude Fable 5、2026-07-07。本ファイル新規作成以外の変更なし。根拠の file:line はヌケモレ点検報告（file-zukan\_nukemore-audit-2026-07-07\）とB34判断資料に依拠。*
