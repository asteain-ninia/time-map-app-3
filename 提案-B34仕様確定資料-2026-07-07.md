# 提案 — B34「移行期間ノードの shape 取り扱いポリシー」仕様確定資料（2026-07-07）

> **位置づけ**: 未確定の**判断資料**です。B34（現状.md §1.2）は「ユーザー判断で確定」とされているため、本書は選択肢の分析と推奨案を提示するに留めます。採用時は要件定義書・現状.md への反映を別変更単位で行ってください。実装は行っていません。
> **作成経緯**: Fable 5 サブスク枠最終日の「計画とヒントを残す」作業。材料収集はOpus調査エージェント（READ-ONLY、全主張 file:line 裏取り済み）、設計判断はFable 5。
> **訂正1件**: B34本文が引く「§6.4.15」は要件定義書ではなく**開発ガイド.md の教訓 §6.4.15**（round-trip同期、開発ガイド.md:553-558）です。

---

## 1. 問題の要約

移行期間ノード＝**あるスライスで shape(Polygon) と childIds非空を同時に持つ錨**（worldValidation.ts:71-73）。アプリ内操作では定常生成されない（末端→集約遷移は shape を即破棄、現状.md:482）が、外部生成の `.gimoza` はロード検証を通って持ち込める。この状態で以下の3系統の操作をすると「親≡子の和」が崩れ、保存はできるが `validateParentChildUnion`（worldValidation.ts:52-89、呼出は JSONSerializer.ts:57-59）が再ロードを拒否する:

1. **削除系**: `planFeatureRemoval`（HierarchyService.ts:622-652）は shape を再計算・破棄しない
2. **所属変更系**: `removeEmptyParentRangesFromTime`（ReassignFeatureParentUseCase.ts:1106-1137）は childIds 空化区間しか剪定しない
3. **shape編集系**: `VertexEditUseCase` の insert/move/delete 頂点・`UpdateFeatureAnchorUseCase.updateShape`（:133-142）にガードなし — なお `isShapeEditable`（HierarchyService.ts:554-559）は**定義済みだが本番未使用**（consumer ゼロ）

将来 UI 配線されると第4群（`updatePlacement` / PrepareFeatureAnchorEdit / CommitFeatureAnchorEdit、現状.md:529）も同型リスクを持つ。

---

## 2. 判断を規定する5つの事実（今回の調査で確定）

**事実A — 正当なデータでは保持shapeは冗長。**
`validateParentChildUnion` はロード時に全スライスで「親≡子の和（厳密一致）」を強制する。したがって**ロードに成功した移行期間ノードの shape は、その瞬間、常に子の和と等しい**。破棄しても情報は失われない（入れ子でも推移的に成立、worldValidation.ts:42-50）。

**事実B — 保持shapeは画面に出ていない。**
描画は派生形状（子の和）を優先し、保持 shape は派生が空のときの fallback のみ（mapSceneEntries.ts:58-59,167-172）。つまり「縮退」と「破棄」は**描画結果が同一**。差が出るのはロード検証とリーフ分類だけ。

**事実C — 新モデルの定常状態は「shape XOR children」。**
shape は末端のみ保持、コンテナは shape プロパティ自体を持たない、リーフ判定＝「shape保持かつ子なし」（現状.md:121,141,583）。移行期間ノードは**定常概念として想定されていない**。残す設計は、childIds-only のリーフ判定が依存する不変条件「shapeなし⟹childIds非空」（開発ガイド§6.6.8:651）の誤分類リスクを永続化する。

**事実D — undo 構造の非対称。**
単一地物スナップショット型（InsertVertexCommand:33-96）と全マップスナップショット型（DeleteVerticesCommand:31-94）が併存。「縮退」「操作時の破棄＋直轄領生成」は編集対象以外を触るため**全マップ型への移行が必要**になる。「操作拒否」と「ロード時正規化」は undo 構造に触れない。

**事実E — 経路ゲートは漏れる（実証済み）。**
B34 の3系統目（shape編集）は当初追跡に無く後から発見された（現状.md:32）。教訓 §6.1.8「状態遷移の検出は入力経路でゲートせず結果状態で判定する」はこの問題の一般解を既に示している。

---

## 3. 選択肢の比較

B34 の3候補に加え、事実Aから導かれる第4案「**ロード時正規化**」を含めて比較します。

| 観点 | ①操作時にshape破棄(＋直轄領) | ②操作拒否 | ③子の和へshape縮退 | **④ロード時正規化（破棄）** |
|---|---|---|---|---|
| 3(+4)経路への一律適用 | 経路ごと or 結果状態スキャン | **全経路の入口にガード必要**（漏れ実績あり: 事実E） | 経路ごと or 結果状態スキャン | **経路対応そのものが不要**（入口で存在を消す） |
| undo/redo への影響 | 全マップ型へ移行必要（事実D） | なし（ガードのみ） | 全マップ型へ移行必要 | **なし**（ロードは undo 起点の外） |
| 新モデル「shape XOR children」との整合 | 高 | 低（状態温存） | 低（廃止したい状態を毎回再計算して維持） | **最高**（概念を実質廃止） |
| 情報損失 | なし（操作前は親≡子の和: 事実A） | なし | なし | **なし**（事実A） |
| 描画変化 | なし（事実B） | なし | なし | なし |
| UX | 透明 | **行き止まり**（削除も編集もできず、解除手段が別途必要） | 透明 | 透明 |
| 実装コスト | 中〜高（幾何演算＋Command改修） | 低〜中（ただしガード網の保守が永続コスト） | 高（結合と同じ幾何演算を操作毎に） | **低**（ロード後1パス＋剪定1箇所） |

**③縮退は明確に劣後**: 定義上「常に子の和と等しい shape」を毎回再計算して持ち続ける＝冗長データの維持にコストとバグ面積を払う。描画は変わらないのに（事実B）。
**②拒否は次点にもならない**: 経路ゲート方式の脆さは B34 自身が実証しており（事実E）、外部データを読んだユーザーには「編集も削除もできない地物」という行き止まりだけが残る。

---

## 4. 推奨案 — ④ロード時正規化（破棄系の徹底形）

**内容**: デシリアライズ→構造検証→`validateParentChildUnion`（既存、ここで親≡子の和を確認＝**正規化の無損失性を保証する安全条件**）→ **正規化パス: 移行期間ノードの shape を破棄して純コンテナ化**。以後アプリ内に移行期間ノードは存在しないため、3経路（将来の第4群も）の操作時対応・ガード・再計算はすべて不要になる。

**なぜこれが3候補より強いか**: B34 の3候補はいずれも「移行期間ノードが存在する世界で操作をどう守るか」を問うている。事実A（正当データでは shape が冗長）により、**存在自体を入口で解消しても何も失わない**ことが保証されるため、問題を守るのではなく消すことができる。§6.1.8 の精神（経路でなく状態で扱う）の最徹底形でもある。

**認識すべき挙動差（唯一の実質的トレードオフ）**:
現行の削除経路は「shape を保持する錨は childIds が空になっても残る＝末端地物へ戻る」（HierarchyService.ts:614、教訓§6.4.17:571）。ロード時に shape を破棄すると、**移行期間ノード由来のコンテナは最後の子を削除したとき末端として生き残らず、空コンテナとして剪定＝消滅する**。
- 新モデルの精神（コンテナは子で定義される存在）ではこちらが自然で、所属変更経路の剪定挙動とも揃う。
- ただし外部データ作者が「子を全部消しても親が自分の形で残る」ことを期待していた場合は挙動が変わる。→ **ここだけがユーザー判断の核**です（§6 確認事項1）。

**副次効果**:
- `validateParentChildUnion` は「ロード時の門番」から「正規化の前提確認」へ役割が明確化。round-trip は自然に安定（純コンテナは shape 欠落で保存される — フォーマットは対応済み、jsonSerializerTypes.ts:42,85）。§6.4.15 の「保存できるが開けない」は構造的に再発不能になる。
- `ConflictDetectionService` の移行期間ノード拡張禁止（§6.6.8:648）に触れる機会自体が消える。
- Phase 4-6/4-7（結合再設計）から**独立に導入可能**。結合は Phase 4-6a で「存在終了+新規生成」型に再設計済みで移行期間ノードを生まない。Phase 4-6 残の「上位領域決定ダイアログ」による末端→集約遷移の再発対策（`resolveLeafToContainerTransition` 共有、現状.md:508）は**アプリ内遷移の話であり B34（外部データ由来）とは別課題**として分離できる — B34 を結合再設計の完了待ちにする必要はない。

---

## 5. 実装ヒント（採用時のロードマップ）

1. **正規化パス**: JSONSerializer.ts:57-59 の `validateParentChildUnion` 成功直後に `normalizeTransitionalNodes(features)` を1つ追加。各錨で `shape?.type==='Polygon' && placement.childIds.length>0` なら `withShape(undefined)` 相当へ。入れ子は一括破棄で可（推移的等値は検証済み: worldValidation.ts:42-50）。
2. **仕様の明文化**: 要件定義書のロード/検証の節へ「移行期間ノードはロード時に純コンテナへ正規化される（無損失: 検証済み等値が前提）」を追記。B15（仕様文書化不足）の一部返済になる。
3. **テスト**:
   - 移行期間ノード入り `.gimoza` フィクスチャ → ロード後に shape が無いこと・描画派生が不変なこと・3経路の操作後も保存→再ロードが通ること（経路ごとに1本、§6.1.8:300 の流儀）
   - 最後の子削除でコンテナが消滅する新挙動の明示テスト
   - 計画中の `TEST-savedata.gimoza`（現状.md §2.1）に移行期間ノードケースを必ず含める
4. **後片付け（任意）**: 本番未使用の `isShapeEditable`（HierarchyService.ts:554-559）は、正規化導入後は存在理由が消えるため削除候補。既存12テストファイルの移行期間ノード関連テストは「ロード後には存在しない」前提への書き換えが必要なものを棚卸しする。
5. **教訓の還流**: 確定後、開発ガイド§6へ「検証が等値を保証するデータは、入口で正規形へ畳み込むことで以後の全経路対応を消せる」を追記。

---

## 6. ユーザーに確認したいこと（これだけ決めれば確定できます）

1. **最後の子を削除したとき、移行期間ノード由来の親は消滅してよいか？**（§4 の挙動差。YES なら④で確定。NO＝「親は末端として生き残ってほしい」なら、④の変形として「正規化時に shape を破棄する代わりに、子の和と等しい shape を持つことをメタデータで記録し、最後の子削除時のみ末端化を許す」案が考えられるが、複雑化に見合うかは疑問）
2. 移行期間ノードを意図的に作る外部ツール・ワークフローは存在するか？（存在するなら、そのツールの期待仕様を先に確認）
3. 導入時期: 結合再設計と独立に先行導入してよいか？（§4 のとおり技術的には独立）

---

*作成: Claude Fable 5（分析・判断）＋ Opus 調査エージェント（材料収集）、2026-07-07。本ファイルの新規作成以外、リポジトリへの変更はありません。*
