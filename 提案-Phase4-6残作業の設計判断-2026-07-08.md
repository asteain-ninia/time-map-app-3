# 提案 — Phase 4-6 残作業の設計判断メモ（2026-07-08）

> **位置づけ**: 未確定の設計判断メモ。現状.md §6.10「Phase 4-6 残作業」および §6 の 2026-06-10 申し送り（EditFeature 系配線義務）が「着手時に決める」「設計時の論点」として明示的に先送りしている判断について、選択肢と推奨をFableがまとめたものです。仕様・実装ではありません。
> **作成経緯**: Fable 5 サブスク枠最終日（延長）の作業。`提案-B34仕様確定資料` `提案-次の一手` と同系列。コードは読取のみ（ReassignFeatureParentUseCase.ts / 要件定義書.md §2.1 を精読）。
> **前提の確認**: §5 修正優先順位1位「階層モデル新仕様への置換」の残りは Phase 4-6残作業 → Phase 4-7。ここの設計判断を先に固めておくと着手が速い。

---

## 論点マップ（現状.mdが先送りしている4件）

| # | 論点 | 出典 | 種類 |
|---|---|---|---|
| A | 上位領域決定ダイアログで既存末端を親に選んだ時の遷移解決方式 | §6.10 Phase 4-6残・:508 | **着手時必須** |
| B | 将来史破棄警告の要否 | :510 | 設計時の論点 |
| C | プロパティ決定ダイアログの設計 | :509 / 要件§2.1 line 346-348 | UX設計 |
| D | EditFeature系3経路の配線時遷移解決義務 | :529 | 配線着手前 |

結論を先に: **AとDは同じ解（既存 `resolveLeafToContainerTransition` の共有）で閉じる。既に実装の受け皿がある。** BとCはUXの小論点で、Aが決まれば従属的に決まる。

---

## A. 上位領域決定ダイアログの遷移解決 — 「共有」を推奨（結果状態スキャンではなく）

### 現状の構造（コード実測）
- `resolveLeafToContainerTransition`（ReassignFeatureParentUseCase.ts:1174）は**既に存在し**、所属変更の征服経路（newParentId）・自治化経路（createNewParent.parentId）から呼ばれている（:303-308）。移行期間ノード（shape あり Polygon + childIds 非空、:1201-1205）を検出し、子の和との差分を直轄領リーフとして生成、差分が空なら shape のみ破棄（:1231-1234）する。
- `override?: DirectlyGovernedOverrideSpec`（:1180）で**ダイアログからの名称・種別上書きを受ける口が既に空いている**（:1243-1254）。ダイアログ非経由の直接呼び出しでは「`<元名> 直轄領`」＋種別継承にフォールバック。
- 結合経路 `mergeLeafFeatures`（:434）は現状これを呼ばない。理由は証明コメント（:427-432）に明記: 「結合で childIds を獲得するのは結果地物の親のみ、その親は effectiveTime で結合対象を子に持つため変異前から末端でない → 移行期間ノード化は構造上生じない（§6.1.8 結果状態による証明、経路ゲートではない）」。そして**「上位領域決定ダイアログで既存末端を結果地物の親に選べるようになる後続サブフェーズで遷移解決の共有が必要になる」**と自分で予告している。

### なぜこの予告どおり「共有」なのか
ダイアログで既存末端 P を結果地物の新親に選ぶと、P は「変異前は末端（shape あり・子なし）」「変異後は shape を保ったまま結合結果を子に持つ＝移行期間ノード」に変わる。証明コメントの前提（childIds を得るのは"元から末端でない親"だけ）がまさに崩れる。ここで取れる道は2つ:

| 方式 | 内容 | 評価 |
|---|---|---|
| **①共有（推奨）** | ダイアログで既存末端が親に選ばれた分岐で、結合の変異確定後に `resolveLeafToContainerTransition(staged, …, transitionFeatureId=P, override=プロパティダイアログの入力)` を呼ぶ | 征服・自治化と**同一コード・同一 undo 同梱・同一テスト観点**。§6.6.9「同じ判定は同じ実装」に合致。override 口も既にある |
| ②結果状態スキャン | 変異後の staged 全体を走査し「shape あり＋childIds 非空」を検出して一括正規化 | §6.1.8 の一般解ではあるが、結合の局所変異に対して全体スキャンは過剰。既存の局所呼び出し資産を捨てることになる |

**①を推奨する決め手**: 遷移が生じる地物が呼び出し時点で**特定済み**（＝ダイアログでユーザーが選んだ P そのもの）だから、全体スキャン（②）の探索は要らない。②が正当化されるのは「どの地物が遷移したか変異後にしか分からない」場合だが、ここは違う。§6.1.8 の「経路でゲートするな」という戒めは「入力経路の種類で if 分岐して検出漏れするな」の意味であり、①は入力経路で分岐しているのではなく**結果として移行期間ノードになる地物を名指しで正規化**しているので戒めに反しない（征服・自治化が既にこの形）。

**B34（`提案-B34仕様確定資料`）との関係**: B34で「ロード時正規化」を採ると、外部由来の移行期間ノードは入口で消える。しかし**アプリ内でダイアログ操作が一時生成する移行期間ノードは別物**で、この A の遷移解決が引き続き担う。B34④とAは守備範囲が違う（B34判断資料§2でも明記した）。両方要る。

### 実装ヒント
- 結合フローに「resultParentId が既存末端か？」の分岐を1つ足し、真なら変異確定後に `resolveLeafToContainerTransition` を同一 undo 単位内で呼ぶ。
- `mergeLeafFeatures` の証明コメント（:427-432）は「ダイアログ導入で前提が崩れる」旨を既に書いているので、実装時はコメントを「崩れた→共有で解決済み」に更新。
- 回帰テストは §6.1.8:300 の流儀で「既存末端を親に選ぶ結合」1本＋「新規親/最上位を選ぶ結合（遷移が起きない）」1本を対にする。

---

## D. EditFeature系3経路の配線時義務 — Aと同じ「共有」で閉じる

現状.md:529 が挙げる `UpdateFeatureAnchorUseCase.updatePlacement` / `PrepareFeatureAnchorEditUseCase.applyCandidatePatch`（placement分岐）/ `CommitFeatureAnchorEditUseCase.applyAnchors` は、いずれも placement.childIds を直接書けて shape を残したまま childIds 非空化できる（＝移行期間ノードを作る口）。現状 App.svelte 未配線で実害なし。

**推奨**: これら3経路を UI 配線する時も、**A と同じ `resolveLeafToContainerTransition` の共有**を義務化する。個別に防御コードを書くと B31/B33 で踏んだ dangling リスクの再来。現状.md:529 が挙げる2案（各経路にNOTE追加 / ReassignFeatureParentUseCase 経路への統合）のうち、**後者（統合）を推奨** — A で結合フローが同じ関数を共有する前例を作るなら、EditFeature系もそこに合流させるのが一貫する。3経路それぞれにNOTEを書く案は、書き忘れが1箇所でもあれば穴が開くので、構造的統合に劣る。

---

## B. 将来史破棄警告 — 「出す」を推奨（ただし条件付き）

結合は対象の effectiveTime 以降の将来錨（後日の所属変更・形状変化）を存在終了で破棄する（要件§2.1 line 329 の帰結、4-6a/4-6b で確定済み・データ整合上は問題なし）。論点は「破棄する将来錨を持つ対象を結合する時、確認警告を出すか」。

**推奨: 出す。ただし"将来錨が実在する対象を含む時だけ"。**
- 理由: データ整合は保たれるが、**ユーザーの意図しない歴史の消失**は起こりうる（「200年後にこの国が分裂する」という既入力の未来が、現在時刻での結合で黙って消える）。B34資料で扱った「保存できるが開けない」とは別種の、UX上のサイレント損失。
- 条件付きにする理由: 将来錨を持たない対象（大多数のケース）で毎回警告を出すと狼少年化する。結合対象の全錨を走査し `timeRange.start.isAfter(effectiveTime)` な錨が1つでもある時だけモーダルに「この結合は N 件の将来の変更履歴を破棄します」と列挙する。
- 文面には破棄される錨の**種類と時刻**（例: 「1850年の所属変更、1920年の形状変化」）を出すと、ユーザーが「それは消していい未来か」を判断できる。破棄そのものを止める必要はない（設計上正しい挙動なので）、あくまで告知。

この判定ロジックは Phase 4-6b の `resolveContiguousLeafShapeEnd`（同一形状連続区間の終端解決）が既に将来錨を走査しているので、その走査結果を警告要否の判定に**再利用できる**（二重走査を避ける）。

---

## C. プロパティ決定ダイアログ — 最小構成で十分

要件§2.1 line 346-348 の要求は「結合対象のいずれか1つのプロパティ（名称・種別・属性）を引き継ぐ／新規入力」。4-6a の暫定実装は「最初の対象から継承＋名称のみ MergeConfirmModal で上書き」。

**推奨: リスト選択＋新規入力の2モードに素直に拡張、属性は継承のみ先行。**
- 名称・種別: 対象リストから1つ選ぶラジオ ＋「新規入力」欄。これは A の `override`（DirectlyGovernedOverrideSpec、name/kind を持つ）に**そのまま流し込める** — ダイアログの出力型を override 型に合わせておけば、遷移解決とプロパティ決定が同じ型で繋がる。
- 属性（任意プロパティ群）: 初期は「選んだ1つから丸ごと継承」だけ実装し、フィールド単位のマージは将来。理由は、属性のフィールド別マージ UI はコストが高く、結合の主目的（形状統合）に対して優先度が低い。§2.1 の要求は「いずれか1つを引き継ぐ」なので丸ごと継承で要件は満たす。
- **A との結線**: 上位領域決定（A）→ プロパティ決定（C）の2段ダイアログは、要件 line 342-348 の順序どおり。C の出力を A の `resolveLeafToContainerTransition(override=…)` に渡す一本道にすると、直轄領が生成される場合（既存末端を親に選び差分が残る場合）でもその直轄領の名称・種別をユーザー入力で決められる。

---

## まとめ — 着手順の推奨

1. **A を先に決める**（既存末端を親に選ぶ分岐で `resolveLeafToContainerTransition` 共有）。これが Phase 4-6残の骨。
2. C のダイアログ出力型を A の `override` 型（DirectlyGovernedOverrideSpec）に合わせる。2段ダイアログが一本道になる。
3. B の警告は 4-6b の将来錨走査を再利用して条件付きで足す。
4. D（EditFeature系）は Phase 4-6 で作った A の共有関数に、配線時に合流させる（個別NOTE方式は非推奨）。
5. これらが済めば Phase 4-7（集約結合）は「派生形状の再計算」が主で、遷移解決の枠組みは A で完成済みのものを流用できる。

---

*作成: Claude Fable 5、2026-07-08。ReassignFeatureParentUseCase.ts・要件定義書.md §2.1 は読取のみ。本ファイル新規作成以外の変更なし。*
